# RELAY · v1.2 next-step ordering — confirm the build sequence
<!--
  Single source of truth for this two-agent relay.
  Read this ENTIRE file before doing anything. Act only on your turn.
-->

NEXT: Reviewer
STATUS: Open
ROUND: 2 / 5

## ▶ TAKE YOUR TURN — read this first (works for ANY agent: Claude, Codex, Gemini)
The operator just said "take your turn on this file." Everything you need is **in this file** — don't wait for pasted instructions.
1. **Read this whole file** (header, Setup, Ground rules, every turn in the Log).
2. **Check it's your turn:** `NEXT` (top) names the role to act. Confirm you are the agent bound to it (see Setup) **and** the last Log block isn't already yours. If not → STOP and reply "wrong window — nudge the <other> window."
3. **Do your role's work** on the artifact named in Setup (read the real files / the latest `git show <last commit>` diff; cite `file:line`):
   - **Reviewer:** review vs the Definition of Done → graded findings (`[Blocker]`/`[Should]`/`[Nit]`/`[Pass]`), each with a concrete proposed fix → set a **Verdict** (Approved | Changes requested | Blocked). Do **not** edit the artifact; you only append findings here.
   - **Producer:** for every open finding log a disposition (Implemented / Modified / Declined + why), make the change, then add new work.
4. **Append ONE block** at the very bottom, directly **above** the marker line (`<!-- ↓↓↓ NEXT TURN ... -->`). Never edit earlier turns. Header it `### Round N · <Role> · <your-label> · <date time>`; a Reviewer block carries `**Verdict:**` + `**Findings & proposals:**` (graded bullets) + `**Commit:**`; a Producer block carries `**Decisions on proposals:**` + `**Did:**` + `**Re-review this:**` + `**Commit:**`. (Need the exact shape? Mirror the most recent block of the other role above.)
5. **Update the header:** flip `NEXT` to the other role; set `STATUS` (`Approved` closes the relay — Reviewer only; else leave `Open`); the Producer bumps `ROUND` when opening a new cycle.
6. **Commit only the files you touched** (artifact + this log): `git commit -m "relay(<slug>): <your-label> r<N>"`, then put the short hash in your block's `Commit:` line and `git commit --amend --no-edit`. Push if the team shares a remote.
7. **Stop.** Tell the operator your one-line result (e.g. "Changes requested, 1 Blocker — Producer's turn").

## Setup
- Artifact under review: this relay file (a DECISION, not code) — the recommended v1.2 next-step ordering in Round 1 below. Source docs to check it against: `PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md` (§1 critical path, §3a build status, §4 open items, §7 Klaviyo/P21 enabler, §8 audit decision) and its `companion_to` `PROJECT/2-WORKING/v1.2/V1.2-BUILD-PLAN-TEMPLATE-2026-06-08.md` (Sam's phase contracts).
- Definition of Done: the proposed next-step ordering is **correct** (faithful to the docs — no misread status/blocker), **complete** (no unmet prereq omitted), and **safely sequenced** (nothing scheduled ahead of a hard gate). Reviewer confirms or names the specific correction.
- Producer: Claude (Opus 4.8)   ·   Reviewer: Codex
- Handoff: manual nudge
- Started: 2026-06-15

## Ground rules
1. This file is the single source of truth. If it isn't written here, assume the other agent doesn't know it. The two agents may be different tools (e.g. Claude and Codex) and never share memory.
2. Read the whole file. Take a turn only if `NEXT` names your role — otherwise reply "not my turn" and stop.
3. One turn = one block appended at the very bottom, above the marker. Never edit earlier turns. Then update `NEXT`, `STATUS`, `ROUND` at the top. (Only exception: right after committing, fill the hash into your own just-written turn's `Commit:` line.)
4. Stay tight. Requests and findings are bullets, not essays.
5. **The Reviewer never edits the artifact.** It proposes graded findings, each with a concrete suggested fix where possible. The Producer (the original author), with the operator, decides each proposal and implements the approved ones — logging a disposition (Implemented / Modified / Declined + reason) for every one.
6. Grade every finding:  `[Blocker]` must fix to ship · `[Should]` strong recommendation · `[Nit]` optional · `[Pass]` checked and sound (records what was verified, not assumed). Answer the Producer's "Re-review this" questions in an `Answers:` block.
7. The Reviewer posts a Verdict every turn. The relay ends on **Approved** — so to get proposals actioned in-thread the Reviewer sets `Changes requested`, not `Approved`; a `[Nit]` left on an `Approved` verdict is the author's discretion, handled out-of-band. If the max `ROUND` ends without `Approved`, set `STATUS: Escalated` and hand back to the human.
8. End your turn by committing it: `relay(<slug>): <role> r<N>`, then fill the hash into your `Commit:` line — so the other agent can `git diff` exactly what changed. If your turn touched no tracked files (comments-only, or this log is gitignored), write `Commit: none (comments only)`.
9. **One window at a time, clean tree at every handoff.** Both agents share one working tree; the `NEXT` pointer is honor-system, not a lock. Never start a turn while the other window may still be editing, and never flip `NEXT` with uncommitted changes left in the tree — commit or stash first, so the next agent never inherits half-finished state.
10. **Evidence contract — state your proof every turn.** The Producer logs a one-line `Verification:` (what it ran / skipped / couldn't run); the Reviewer logs a verdict `Basis:` — `behaviorally proven` (ran/observed) or `textual only` (read, not run) — and classes any prior fix `textually fixed` vs `behaviorally proven`. An `Approved` can't silently mean "looked fine on read": if correctness rides on runtime behavior and nothing ran, `textual only` is the strongest honest verdict. Scales to the artifact — a pure doc answers `N/A — non-executable artifact`.

## Roles
- **Producer** — the only writer of the artifact: builds it, requests review, decides and implements proposals (with the operator), updates.
- **Reviewer** — reviews against the DoD, proposes graded findings with suggested fixes, sets a verdict. Never edits the artifact.

---
## Log

### Round 1 · Producer · Claude (Opus 4.8) · 2026-06-15
**Did:** Derived the v1.2 next-step ordering from the build docs (citations are `V1.2-BUILD-SWE-INTERNAL.md` line numbers). Submitting it for an independent correctness/completeness/sequencing check.

**Established state (claims to verify against the repo):**
- Branch `build/v1.2-p22-scorer` == `origin` == `202561b` (PR #21 merge); PR #21 was **docs-only** — `git diff --stat eb69dfc..202561b` = 4 doc files only; `eb69dfc` ("feat(p22)…") is a clean ancestor of `202561b`. No app code changed, nothing lost.
- Critical path (§1.3:68): `P20 → P22(scorer) → P22.5(activate · needs P21) → P26`. P25 is OFF the critical path (§1.2:61).
- **P20** — done + DEPLOYED to prod; prod `alembic current = c4d5e6f7a8b9 (head)` (§7.1:624). Backbone freshness resolved 2026-06-11 (§3a:263).
- **P22** — COMPLETE, `landed (not deployed)` 2026-06-12; 382 passed; all 3 seams filled; deploys BUNDLED with P21 at validation time (§3a:302, :305).
- **P21** — code-complete but only against a MOCKED write client; lives on `build/v1.2-p20`, NOT on `development`/prod (§7.1:592, :626).
- Unblock gate for P22.5 + P26 (§7.3:689): ≥1 write-scoped Klaviyo connection with a VERIFIED test-profile write+revert. Write scope today (§7.1:606): Binoid ✓, Bloomz ✓, Bounce read-only (re-key in progress).
- §8 must ride INTO v1.2: (a) §8.1 CI is the precondition for ANY refactor (no CI today); (b) §8.3 D7 — add `app/db/repository.py` thin tenant-scoped helper NOW and have P22 use it, hard gate = one green FULL suite when the file lands (§8.3:764-771); (c) §8.2 v1.2-surface P0s: CC1/CC3 job race, S2 membership on the write path, S1/S4/S5 prod posture before real-tenant go-live (§8.2:728-735).

**Proposed next-step ordering (the thing to confirm):**
1. **CI first** (§8.1) — GitHub Actions pytest + ruff/mypy. Few hours; it IS the automated green-suite net the D7 gate (step 2) needs.
2. **D7 thin helper** — add `app/db/repository.py`, wire P22 to use it, run one green full suite as the §8.3 gate.
3. **§8.2 write-path P0s** — CC1/CC3 job-race FSM + in-flight guard; S2 membership check on the write path. Do S1/S4/S5 prod posture here too (go-live blockers before any real-tenant write).
4. **Unblock the critical path** — deploy P21+P22 bundle (§7.2a recipe; migration `a7b8c9d0e1f2` applies linearly on prod's `c4d5e6f7a8b9`), then run test-profile write+revert on Binoid or Bloomz (already write-scoped) → satisfies §7.3.
5. **P22.5 activate → P26 measure.** P25 (posture) lands late, off critical path.

**Key judgment to stress-test:** the deepest critical-path item (P22.5/P26) is BLOCKED on the §7.3 Klaviyo gate, but steps 1–3 are unblocked pure-code/infra work that §8 says must land before real-tenant go-live anyway — so "keep building" should start at CI+D7, NOT wait on the Klaviyo gate. The P21 deploy/validation (step 4, owner Noel, partly external) runs in parallel.

**Review this:**
- Any MISREAD status or blocker vs the docs (esp. P21 deploy state, P22 deployed-vs-landed, write-scope table)?
- Is anything sequenced AHEAD of a hard gate (e.g. wiring P22 to D7 before CI exists; activating before S2/S1/S4/S5)?
- Any unmet prereq I OMITTED — a P22.6 contract item, the manual Klaviyo flow wiring (§4), or a Sam-owned `<<SAM-MUST-OWN>>` confirmation that gates go-live?
- Is "CI before D7" the right call, or is D7 cheap enough to land first? Does the §8.3 gate truly require a live Postgres run that CI can satisfy?

**Verification:** N/A — non-executable artifact (a decision/sequence). Underlying git facts were observed first-hand earlier this session (fetch + diff-stat + merge-base); doc citations are line-referenced for your independent check.
**Open questions:** §8.5 leaves "does v1.2 go live to real merchant tenants?" to Sam/Noel — if yes, step 3's S-items are hard go-live blockers, not "later." Does that change your recommended order?
**Commit:** e7e1685

### Round 1 · Reviewer · Codex · 2026-06-15 09:58 PDT
**Verdict:** Changes requested.
**Basis:** textual only — non-executable artifact, verified against `PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md` and the referenced git metadata.

**Findings & proposals:**
- `[Blocker]` Step 4 collapses the Klaviyo gate too aggressively. Your sequence treats "deploy P21+P22 + verified test-profile write/revert" as the full unblock, then moves straight to `P22.5 activate -> P26 measure`; but the docs still put the **one-time manual flow wiring** on the critical path (`PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:74-78`) and keep it open in both tracked dependencies (`PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:487-489`) and the §7.3 checklist (`PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:699`). Proposed fix: split step 4 into `4a. deploy + test-profile write/revert` and `4b. wire the validation/measurement tenant's Klaviyo flow`, or state explicitly that `P22.5` dogfood activation can start after 4a but `P26` waits for 4b.
- `[Should]` The ordering compresses the **measurement-tenant / holdout-design** decision too much. The docs still carry an explicit Sam decision on how to treat Black Crow before the June 30 measurement clock (`PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:141-167`), and Bloomz is still pending the predictive-vendor probe before it can be called the cleaner measurement tenant (`PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:680-682`, `PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:695`). Proposed fix: add a parallel track under step 4 like `4c. settle measurement tenant + holdout treatment (Bounce incremental-over-Black-Crow vs Bloomz clean-room, with Sam sign-off)` so step 5 does not imply `P26` can begin with that unresolved.
- `[Pass]` `CI before D7` is the right call. §8.1 makes CI the precondition for any refactor (`PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:717-721`), and the D7 spike says the behavioral confirm owed when `app/db/repository.py` lands is one green full-suite run (`PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:762-766`).
- `[Pass]` The core status calls are accurate: P20 is deployed on prod and linear-migration-safe to build on (`PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:623-629`), P21 is landed-not-deployed on `build/v1.2-p20` (`PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:621-637`), P22 is landed-not-deployed and bundled with P21 at validation time (`PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:302-306`), and Binoid/Bloomz are write-scoped while Bounce is still read-only (`PROJECT/2-WORKING/v1.2/V1.2-BUILD-SWE-INTERNAL.md:601-610`).

**Answers:**
- `MISREAD status/blocker?` None found in the P20/P21/P22 or write-scope claims.
- `Anything sequenced ahead of a hard gate?` Only on the measurement side: `P26 measure` is still ahead of the manual flow-wiring and measurement-design prerequisites unless you make those explicit.
- `Any omitted prereq?` Manual flow wiring is the concrete omission. The Bloomz predictive-vendor probe / Sam measurement call is not a code-build blocker for steps 1-3, but it is a planning prerequisite for a clean `P26` start.
- `Does §8.5 change the order?` It strengthens step 3 rather than reshuffling it. If v1.2 goes live to real merchants, S1/S4/S5 stay ahead of any real-tenant writes exactly as you proposed.

**Commit:** 6b8d971

### Round 2 · Producer · Claude (Opus 4.8) · 2026-06-15
**Decisions on proposals:** (verified every cited line against `V1.2-BUILD-SWE-INTERNAL.md` before deciding)
- `[Blocker]` Step 4 collapses the Klaviyo gate (flow-wiring omitted) — **Implemented.** Confirmed: §7.3 prose says write+revert "unblocks P22.5 and P26," but its own checklist keeps `{{ person.ltvera_companion_rec_1 }}` flow-wiring OPEN (`:699`), and §1.4 (`:74-78`) + §4 (`:487-489`) both put it on the critical path. My "satisfies §7.3" elided it. Fix: split step 4 into 4a/4b and gate P26 on 4b. · Proof: read `:687-699` this turn (checklist shows write-verify and flow-wiring as separate open items).
- `[Should]` Measurement-tenant / holdout (Black Crow) design compressed — **Implemented** as new track 4c. Confirmed §2.3 (`:141`) is a Sam-owned measurement decision and Bloomz is pending the §7.2 predictive-vendor probe (`:680-682`, `:695`). · Proof: §2.3 header + §7.3:695 read.

**Did:** Corrected ordering — build core (1–3) unchanged (both `[Pass]`); measurement tail re-sequenced:
1. **CI** (§8.1) — GitHub Actions pytest + ruff/mypy.
2. **D7 thin helper** — `app/db/repository.py`, wire P22 to use it, one green full suite as the §8.3 gate (CI from step 1 satisfies it).
3. **§8.2 write-path P0s** — CC1/CC3 job-race FSM + in-flight guard; S2 membership on write path; S1/S4/S5 prod posture (go-live blockers before any real-tenant write).
4. **Unblock the critical path — now split:**
   - **4a. Deploy P21+P22 bundle (§7.2a) + verified test-profile write+revert** on Binoid/Bloomz → satisfies the §7.3 *write-verify* check. Unblocks **P22.5 dogfood activation**.
   - **4b. Wire the validation/measurement tenant's Klaviyo flow** (`{{ person.ltvera_companion_rec_1 }}` block — one-time UI, §1.4/§4/§7.3:699). **Hard prereq for P26** (no wired flow → the rec never reaches the customer → nothing to measure).
   - **4c. Settle measurement tenant + holdout treatment with Sam** — Black Crow confound (§2.3) + Bloomz predictive-vendor probe (§7.2:680). Planning prereq for a clean P26; runs in parallel with 4a/4b.
   - (Bounce re-key, §7.3:697, only needed if Bounce is the activation tenant — not a gate, since Binoid/Bloomz are write-scoped.)
5. **P22.5 activate** (after 4a) **→ P26 measure** (after 4b *and* 4c). P25 (posture) lands late, off critical path.

**Re-review this:** the split step 4 (4a unblocks P22.5; 4b+4c gate P26) and track 4c's placement as parallel-not-serial. If that matches the docs, this is Approved-ready.
**Verification:** N/A — non-executable artifact. Findings verified by reading `:687-699` and §2.3/§7.2 citations this turn.
**Commit:** 771f61a

<!-- ↓↓↓  NEXT TURN GOES ABOVE THIS LINE — keep this marker last  ↓↓↓ -->
