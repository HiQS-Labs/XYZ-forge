# Qwen3.8 27B local replication — frozen before inference

Exact loaded key qwen3.8-27b-mlx; LM Studio reports Qwen3.8 27B, MLX, 8-bit, context 107264. Reuse existing single instance without reload or context override. Inventory does not expose a reasoning option; omit reasoning exactly as for Gemma 12B, record actual reasoning-token usage, and do not assume non-reasoning behavior. This differs from the explicit reasoning=off used for Bonsai and Gemma31.

Two fresh sequential requests, same original 16 synthetic cases, six questions, full source packet, original grader/key and system prompt as Gemma 12B. Only candidate names change in questions; adapter changes only model key and receipt schema. Verify payload parity against Gemma after model-name substitutions, and byte-identical payloads between this candidate's runs. No prior answers, grades, key, or parent analysis in input. Temperature 0, max_output_tokens 6000, integrations=[], store=false, stream=true, no previous response. Same 840-second HTTP read timeout, 900-second harness cap; no automatic retries, post-answer prompt revisions or repair of output.

Use shipped consult.sh with experiment-local CODEX_BIN override: local LM Studio inference, not Codex inference or sandboxing. Save pending receipt/request before inference, durable SSE and timestamped events as emitted, final response, answer and hashes. Require chat.end, no server error and instance-to-model identity for transport success. Grade syntax/decisions and technical reasoning separately. Retain all failures. Original grader negative controls run before inference; empty/missing-end/missing-delta stream controls must reject after completion. If reasoning is emitted, retain it separately and compare only message deltas with final message text.

Same cases do not isolate engine, quantization, tokenizer, context, reasoning or cache/hardware differences. Two temperature-zero runs are repeatability checks, not independent held-out data or p95. No actual 44-label next-action benchmark, private corpus, live queue writes, product runtime changes or full runtime suite. Publish full results and a qualitative post-hoc overall grade only to Needle #13; append this model to the existing grading comparison. Other candidates' grades remain unchanged unless new evidence directly warrants revision.
