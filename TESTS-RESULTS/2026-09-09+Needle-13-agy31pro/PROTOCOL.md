# Antigravity Gemini 3.1 Pro (High) CLI replication

Two fresh sequential real agy CLI calls, explicitly --model gemini-3.1-pro-high --effort high, current CLI catalog verified. Source packet unchanged111980bytes/hash in reference,16cases/sixquestions. Same advisor instruction is prepended to USER prompt: CLI does not expose overriding system prompt, temperature or maxoutput6000. Those controls are not claimed. CLI adds its own system/context, making this an agent-runtime task replication, not a strict model-only comparison.

Existing consult CODEX_BIN override captures transcripts and isolation; adapter invokes real agy --sandbox --mode plan --disable-slash-commands --output-format json --print-timeout14m with unique log. Never dangerously-skip-permissions. No continue/conversation flag; fresh calls. Overall harness900seconds, CLI840seconds, subprocess870seconds; Qwen exception not used. Tool use prohibited by prompt; CLI runtime still exposes capabilities, so observed tool calls must be recorded and count as protocol deviations, not claimed impossible. Do not supply previousanswers/key in prompt. Runtime's incidental context and tool exposure are limitations.

Questions change model names and minimally replace HTTP/disabled-tools wording with actual CLI/sandbox-plan/prohibited-tools wording before inference. No repairs or retries. Preserve rawstdout JSON/stderr/runtime log, extracted answer, usage and monotonic duration. CLI selected-model configuration can support requested selection, not independent backend identity attestation. Model self-identification is not evidence. Do not infer subscription dollar cost from tokens.

Freeze inputs in parent commit beforecalls. Original grader/key and controls; validate nonempty JSON/exact extraction, distinct conversations, source/hash/parity, selected-model log, credentialabsence. No product runtime changes/full suite. Source-review grade remains separate from16caseaccuracy. No productionprivacy/race/p95/nextactionaccuracy qualification.
