# Bonsai 27B local replication — frozen before inference

Repeat Gemma's two fresh sequential requests on the unchanged original 16 cases, six technical questions, complete source packet, grader and expected key. Only candidate model names change in questions. No previous outputs, grading key or parent analysis are sent. Temperature 0 repetitions are repeatability checks, not independent held-out samples.

Model key prism-ml/bonsai-27b; inventory reports Bonsai 27B, MLX, 2-bit, preloaded context 228096. Reuse this loaded instance by omitting context_length. Unlike Gemma, this model explicitly exposes reasoning on/off with default on; set reasoning=off to align with Gemma's observed zero-reasoning-token condition. Record this deliberate difference and actual server usage. Other request fields and system prompt match Gemma: max_output_tokens=6000, temperature=0, integrations=[], store=false, stream=true, no previous response. Preserve the same SSE adapter, changing only model key, receipt schema and explicit reasoning field.

Use shipped consult.sh via experiment-local CODEX_BIN override, not Codex inference or sandboxing. Save request/pending receipt before inference, durable incremental SSE and timestamped events, final API JSON, answer and hashes. Require chat.end, no server error and instance-to-model identity verification. Same 840-second HTTP read timeout and 900-second harness cap. No automatic model retries, post-answer prompt tuning or repair of invalid output. Keep all failures; schema/decision and technical-prose assessment are distinct.

Run original grader controls before inference. Compare source/request parity against Gemma, excluding the declared model-name/reasoning differences. Two identical Bonsai request payloads required. Validate complete stream equals final answer; empty, missing-end and missing-delta negative controls must fail. Full product test suites are not applicable to experiment-only artifacts; no product runtime changes.

Differences still prevent a causal model leaderboard: model family, quantization, tokenizer, preloaded context and warm/cache state differ; Luna additionally had source-reading tools and reasoning medium. The 16 explicit cases cannot establish production next-action accuracy, tool authority, independent corpus support, p95 or cost. Publish reviewed results and raw snapshot only to Needle #13. No additional XYZ #522 result comment.
