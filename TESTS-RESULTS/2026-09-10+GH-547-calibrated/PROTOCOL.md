# Needle #31 — operator-calibrated real activity round

Use the operator's eight-purpose taxonomy from Needle #29 and independent primary/optional-secondary component areas. Only CI/CD and Skills area semantics were operator-confirmed; the additional 10 component categories in TAXONOMY.md are provisional annotations. Rebalance's cached HiQS-Labs issues/PRs supply title plus the first1800 characters of description. Description truncation is recorded; no claim that later text was read. Input also includes repository name for project context. No state/merge metadata or expected labels go into features.

collect.py defines family grouping, temporal65/15/20 pools, deterministic sampling and eight-bucket candidate enrichment. Labels come from annotators, not enrichment keywords. Prepared sample128train/32validation/40holdout, with previously tested exact titles excluded from holdout. Same project may appear across time splits. Explicit title references/duplicate titles are grouped, implicit links may remain. Creation-time splitting does not reconstruct historical title/description versions. Data has been sampled using only source contents before label assignment.

Two independent model annotators label disjoint80-record train/validation packets with descriptions. A third model annotator receives only taxonomy and holdout40, blinded to training labels and predictions. Their annotations are NOT human gold labels; operator calibration does not imply human approval of each record. Require ID/schema consistency and nonempty inputs before training. Preserve null uncertain primaries, secondary labels and rationale locally. Freeze all input/code/taxonomy hashes and labels before fitting. Do not alter holdout labels after seeing predictions.

Compare training-majority primary-label baselines, word1/2gram TF-IDF logistic heads, and frozen ModernBERT attention-mask-aware mean-pooled L2-normalized vectors with logistic heads. Each method gets IDENTICAL examples/inputs and separate purpose/area primary heads. Fit on non-null training labels. Vocabulary fitted on train only. Validation selects C from[0.1,1,10,100] and class_weight from[None,balanced], maximizing fixed training-class macro-F1 on labeled validation rows, tie-break first grid entry. Do not refit using validation. No encoder weight updates.

Uncertainty: for each method/axis choose the lowest validation max-probability threshold among0.0,0.05,...,0.95 that yields >=80% correct over at least5 accepted validation rows, counting accepted null-truth rows as wrong. If none qualifies, reject all. This is an empirically selected rejection policy, NOT probability calibration or a guaranteed error bound. Apply it unchanged to holdout. Report raw primary accuracy/macro-F1, acceptance coverage, accepted correct/accepted total, unknown false accepts, labeled correct retained and per-class support/confusion. A reject-all policy has zero coverage and undefined accepted accuracy, not perfect accuracy.

Secondary heads: per label binary logistic regression using the selected primary C/class_weight, same features; train only on axis-known rows. Fit a secondary label only when there are >=3 positive and >=3 negative training examples. Predict at0.5. Report micro precision/recall/F1 and unsupported labels; no claim sparse secondary categories are qualified. All secondary labels are provisional per-record model annotation under the operator's rule.

CPU4threads, eagerattention, batch4. Assert token count <=8192; source descriptions already truncated by the collector. Record load, encoding, fit/validation and two repeated holdout prediction timings; same weights/inputs should yield identical labels. Two passes are repeatability, not independent accuracy samples. Save fitted artifacts locally only. Witness controls for empty split, cross-split exact duplicate, missing label ID and invalid purpose. Verify held-out input hashes unchanged. No public private text or row-level labels/predictions; publish aggregate metrics, scripts and hashes. Network inference/operational actions by classifiers prohibited.

Decision: compare on the newly frozen40-case holdout, report failures/low coverage even if worse. Changed taxonomy, data, descriptions, normalization and validation search prevent causal comparison with earlier runs. Fresh human review, more per-class examples and an independent production holdout remain prerequisites for deployment. No A–F generative grade, p95, privacy export or autonomous permission claim.

Before fitting, reviewed labels exposed only1 planning training example. Eight additional older real candidates were manually reviewed, yielding7 planning and1 closeout; final training136, validation32, holdout40. No held-out title or label changed. One extra belongs to an already represented TRAIN-only explicit family; no family crossed into validation/holdout. Final training supports are recorded at run time. The eight candidates were chosen by missing TRAIN support, not holdout scores. collect.py reproduces initial sampling; the locally hashed planning-candidates and labels-extra files record the subsequent enrichment.
