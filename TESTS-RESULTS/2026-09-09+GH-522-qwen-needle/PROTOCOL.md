# Qwen local replication — frozen before inference

Repeat the original 16 synthetic cases, same expected key and unchanged grade.py, and six technical questions twice. Adapt only model name and API/tool context; source files are supplied inline because the API has no filesystem access. Preserve all failed calls. Do not include the answer key, previous model outputs, or parent analysis in model input.

Use the exact user-requested model qwen/qwen2.5-coder-32b at localhost:1234/api/v1/chat. Model inventory reports MLX 4-bit. Request context_length=32768, max_output_tokens=6000, temperature=0, integrations=[], store=false, stream=false. No reasoning option for this non-reasoning model. Two fresh sequential requests, no previous_response_id, no prompt changes after answers. Repeated temperature-zero samples measure repeatability, not independent statistical evidence. If the API reports context overflow, preserve the failure and report the protocol limitation before any revised trial. No automatic retry for model/content failures.

Use shipped consult.sh via CODEX_BIN pointing at the experiment-local LM Studio adapter; the codex-named harness slot/filename is transport plumbing, not a claim of Codex/OpenAI inference or Codex sandboxing. Raw response JSON and monotonic full-request time are saved in addition to relay transcripts. No full suite, queue writes, live hooks, training, private histories or server network exposure.

Strict grader controls must pass before inference. Schema failure remains a failure; do not silently repair malformed answers. Review prose and evidence separately. Same cases do not make this an isolated model comparison: Luna had adaptive source-reading tools and a different prompt, runtime and reasoning setting. Neither comparison qualifies production accuracy, p95, instruction-following under tool access or actual operating cost. Publish raw snapshots/analysis to both issues under the user's existing instruction once reviewed.
