# Invalid transport pilot

Two fresh temperature-zero runs completed, but Q4 and Q6 in the wrapper still named Gemma. Both byte-identical outputs failed the original grader at decision count (six question IDs instead of sixteen case IDs; four answers instead of six). These results are retained for audit but excluded from the official MiniCPM comparison. The corrected wrapper changes only those two candidate-name references.
