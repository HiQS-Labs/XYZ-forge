# GH-424 focused negative controls — 2026-09-08

Status: writer and rollback checks pass; dashboard acceptance remains **unmet**.
These are focused builder observations, not full-gate or merge-readiness evidence.

## Reproduction

`bash test/gh424-roadmap-status-marker.sh` runs five Python unittest cases against
real temporary SQLite ledgers. It exercises the CLI parser, handlers, writer lock,
receipt chain, dump/generated-view writer and `releases check`. It creates a private
`.git` directory solely for lock sidecars; it runs no Git or network commands.
Reconciler subprocesses are intercepted to execute real CLI writes and inject a
failure after the second write. Therefore this is not a full reconciler process test.

During this relay, `TMPDIR` was pinned to the pre-created `.relay-scratch/` directory.
All temporary fixtures, saved source copies and raw logs stayed there. The harness
must run its independent gate after handoff.

Optional `GH424_APP` and `GH424_WAVE` variables select saved source files for replay.
The source directory remains on the Python import path for unchanged dependencies.
The saved pre-change source SHA-256 values were:

- `releases_app.py`: `2b754101314e32495cb4c9e97110c3ae40bfa55dcded78c5c590b0f569520586`
- `wave_reconcile.py`: `fc671eef1f5d21ac31de85c95a63c0e8cb5d9bef4fcbf972ed6bd447d7ec3d9b`

## Witnessed results

| Configuration | Result | What failed |
| --- | --- | --- |
| Saved pre-change app and reconciler | 5 tests, 4 failures; exit 1 | Missing marker option; selector/dry-run marker tests cannot execute; both rollback cases fail |
| Current app, snapshot delayed until downstream orchestration | 5 tests, 2 failures; exit 1 | DB/dump retain the first write; existing generated view also retains that generation |
| Current app, `RELEASES.generated.md` omitted from snapshot set | 5 tests, 2 failures; exit 1 | Existing generated view stays ahead; originally absent generated view remains present |
| Final writer and reconciler | 5 tests pass; exit 0 | All scoped writer/rollback assertions hold |

Pre-change CLI output:

```text
releases: error: unrecognized arguments: --status-marker ✅
```

Pre-change rollback output (first write already snapshotted too late, second write
left the generated view ahead):

```text
OK: generation trio consistent at 4 (DB <-> dump)
FAIL: rule=generation-mismatch: RELEASES.generated.md carries generation 5 but the DB is at 4
```

The generated-view-only mutation separately produced generation 5 versus restored
DB generation 3, and the byte comparison named only `RELEASES.generated.md`.
The tests assert nonempty initial DB/dump/generated bytes before comparing them.

The legacy-verb control passes on both versions: `roadmap update --raw-text`, `move`,
`repoint`, `rate`, and releases-mode `sync` leave the original marker at `🆕`, even
when raw text contains `✅`. The new writer deliberately keeps that independence.
Bulk correction is a loop of named `roadmap update --issue-num N --status-marker ✅`
operations, with one receipt per row; no bulk SQL or inference from prose is needed.
Greens also cover all three enum values, rejected selectors/unknown values, dry-run,
unchanged raw text plus explicit marker, `--gid`, explicit section updates, unrelated
column preservation, `updated_at`, receipt operation/target/timestamp, byte-for-byte
rollback and clean `releases check` afterward.

## Unresolved dashboard acceptance — do not approve this lane as complete

A separate isolated probe invoked the real `utils/roadmap-dashboard.sh` after a
marker-only CLI write. The write preserved `raw_text`, as required. The actual result:

```text
DB status_marker: ✅
Regenerated dashboard: | GH-424 · fixture 424 | — | ... |
AssertionError: dashboard still ignores the explicit marker
```

The renderer's DB adapter uses only `raw_text`; its status scanner also omits `🆕`
and `🚧`. Its source is outside this turn's explicit edit allowlist. The stored row,
lossless `roadmap list --json` API and generated dashboard must not be hand-altered
to hide this gap. Scope expansion for `utils/roadmap-dashboard.sh` was requested and
remains pending. The follow-up must consume the explicit DB marker at rendering,
pin regeneration with a failing control, and arrange marker-write refresh behavior.
`ROADMAP-DASHBOARD.md` was left untouched because regenerating it cannot fix this
source defect. No live ledger status transitions were performed.
