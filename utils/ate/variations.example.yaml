model: openrouter/z-ai/glm-5.2
edit_formats: [diff, whole, udiff]
map_tokens: [0, 1024, 4096]
auto_commits: [true, false]
message: "Add a one-line docstring to the function `foo` in sample.py"
per_variation_timeout_seconds: 180

# The stock AIDER preset's explicit opt-back-in (#141 Phase 5): the rollup-issue label default
# is neutral ('bug') for every grid; only this preset declares aider-pipeline.
issue_labels: [bug, aider-pipeline]
