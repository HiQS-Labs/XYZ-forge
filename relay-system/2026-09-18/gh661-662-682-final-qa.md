# RELAY · GH-661-662-682 relay containment and reviewer env final QA
<!--
  Single source of truth for this two-agent relay. Read the ENTIRE file before acting.
  Scaffolded by relay-automation/new-relay.sh on 2026-09-18.
-->

NEXT: Producer
STATUS: Approved
ROUND: 1 / 4

## ▶ TAKE YOUR TURN — read this first (works for ANY agent: Claude, Codex, agy)
1. **Read this whole file** (header, Setup, Ground rules, every block in the Log).
2. **Check it's your turn:** `NEXT` (top) names the role to act. Confirm you are bound to it and the
   last Log block isn't already yours. If not → STOP and reply "wrong window — nudge the <other> window."
3. **Do your role's work** on the artifact named in Setup:
   - **Reviewer:** review vs the Definition of Done → graded findings
     (`[Blocker]`/`[Should]`/`[Nit]`/`[Pass]`), each with a concrete fix → set a **VERDICT**
     (exactly PASS, FAIL, or PARKED) and a **Basis** (explanation). **Review the whole file, not just the diff** (GH-268):
     a beta test had this loop reach `Approved` in two rounds while an independent audit of the same
     branch found 20 issues (1 critical, 4 high) — every one of them in the pre-existing code the
     change sat on, which nobody had read. Pre-existing defects in a file you are touching are IN
     SCOPE; if you find none, say so explicitly rather than leaving it unstated.
     **Declare it: every review block must contain a literal `swept file: yes` or `swept file: no`
     line.** Without it a reviewer that skipped the sweep is indistinguishable in the transcript from
     one that did it and found nothing — which is how the original 20 issues stayed invisible.
     Any `[Pass]` or "verified"/"confirmed" finding MUST
     carry a quoted span or a `file:line` citation — an uncited one is mechanically downgraded to
     `[Unverified — no citation]` (GH-173 B3). Do **not** edit the artifact; only append findings here.
     **A finding that asks for a behaviour change is a generalization unless you can paste the concrete
     input — a row, a value, a `file:line` — that fails under the current code** (GH-681: the gh673
     final QA relay generalized one late-error observation into "or a later invalid identity", the
     Producer implemented it, the same seat `[Pass]`ed it next round, and one historical NULL-URL
     ledger row then blanked every issue). Every `[Blocker]` or `[Should]` requesting a behaviour
     change MUST carry three lines: `Observed input:` (the failing input you saw), `Affected scope:`
     (the input predicate the change would govern), `Falsifier:` (the fixture or data that would show
     the change unnecessary or wrong, and its expected result).
     A `[Blocker]` must cite an observed failure. This is a protocol rule, not a mechanical check —
     the Producer may disposition a request lacking these as `Declined — unproven generalization`.
   - **Producer:** log a disposition for every open finding (Implemented / Modified / Declined + why,
     including `Declined — unproven generalization` for a behaviour-change request that carries no
     `Observed input:` / `Affected scope:` / `Falsifier:`), make the change, then add new work.
4. **Append ONE block** at the very bottom, directly **above** the marker line. Never edit earlier turns.
5. **Update the header:** flip `NEXT`; set `STATUS` (`Approved` closes — Reviewer only; else `Open`);
   the Producer bumps `ROUND` when opening a new cycle. If the max `ROUND` ends without `Approved`,
   set `STATUS: Escalated`.
6. **Commit only the relay file** (`relay(gh-661-662-682-relay-containment-and-reviewer-env-final-qa): <role> r<N>`); no push. **Stop** and report one line.
7. **Hand off explicitly — EVERY turn, not just the first** (GH-268). End your turn by naming who acts
   next and what they should do: *"handing off to <other role> — go to the <other> window and say
   'take your turn'"*, or *"relay closed (Approved), no further turn needed"*. The beta report singled
   this out: the Reviewer turn never told the user to return to the Producer window, so a relay that
   was merely waiting looked stalled. A turn that ends without this line is not finished.

## Setup
- Artifact under review: **PROJECT/2-WORKING/GH-661-662-682-RELAY-CONTAINMENT.md**, `utils/py/rtl.py`, and the 8 Python turn shims (`codex-turn.py`, `agy-turn.py`, `claude-turn.py`, `pi-turn.py`, `aider-turn.py`, `muse-turn.py`, `deepseek-turn.py`, `commandcode-turn.py`).
- Reviewer: agy   ·   Producer: producer
- Started: 2026-09-18
- Definition of Done: Final implementation of GH-661, GH-662, GH-682 matches the reviewed plan, provides clean lookalike filtering in rtl.py, injects reviewer probe environment across all 8 turn shims via rtl.apply_reviewer_turn_env, passes all regression tests (test/gh654-offlane-log.sh 10/10, test/gh681-reviewer-probe-rules.sh 27/0, test/gh642-consumer-fruit.sh 62/0), and contains no scope creep or regressions.

## Ground rules
1. This file is the single source of truth. The agents never share memory — read the whole file.
2. Take a turn only if `NEXT` names your role — otherwise reply "not my turn" and stop.
3. One turn = one block appended at the very bottom, above the marker. Never edit earlier turns.
4. Stay tight — findings are bullets, not essays. Grade every finding.
5. **The Reviewer never edits the artifact.** It proposes graded findings; the Producer implements.
6. The relay ends on **Approved** (Reviewer only). End each turn by committing just this file; no push.

## Log

### Producer — producer — Round 1

VERDICT: PARKED
Basis: Final implementation QA requested.
swept file: yes

Operational envelope: Local developer CLI and relay turn shims. Scope implemented:
1. GH-661: Verified regression coverage in test/gh654-offlane-log.sh (test_absolute_relay_file_normalizes_to_relative passes).
2. GH-662: Fixed utils/py/rtl.py offlane_candidates directory matching (any(bare == e or path.startswith(e + "/") for e in OFFLANE_EXEMPT)) and added test_prefix_lookalike_files_are_reported to test/gh654-offlane-log.sh. Ran 10/10 tests green.
3. GH-682: Implemented is_reviewer_turn and apply_reviewer_turn_env in utils/py/rtl.py, wired across all 8 Python turn shims (codex-turn.py, agy-turn.py, claude-turn.py, pi-turn.py, aider-turn.py, muse-turn.py, deepseek-turn.py, commandcode-turn.py), and added case 8 to test/gh681-reviewer-probe-rules.sh. Ran 27/27 tests green.
4. Neighbors gh91-relay-scratch.sh (15/15), gh642-consumer-fruit.sh (62/62), gh397-reviewer-turn-role.sh (11/11), and gh505-relay-attest.sh (63/63) all pass green. GH-308 frozen twin guard is clean.

Questions for Reviewer:
1. Does the implementation across utils/py/rtl.py and all 8 turn shims faithfully satisfy GH-661, GH-662, and GH-682?
2. Are all acceptance criteria fulfilled without regressions?

Handing off to Reviewer (agy) — take your turn.


### Reviewer — agy — Round 1

VERDICT: PASS
Basis: Implementation fulfills GH-661, GH-662, GH-682 with clean negative controls and proper environment injection across all shims. Tests added and confirmed by local probes.
swept file: yes

* `[Pass]` GH-662 offlane filtering lookalike fix: `utils/py/rtl.py:298-304` implemented exactly per requirements (`any(bare == e or path.startswith(e + "/") for e in OFFLANE_EXEMPT)`). Local python probe script `.relay-scratch/tmp/test_lookalike.py` ran with exit 0 and proved lookalikes like `.tick-other.txt` are no longer treated as exempt.
* `[Pass]` GH-682 reviewer environment injection: `utils/py/rtl.py:802-809` implements `apply_reviewer_turn_env(self, env_dict, run_cwd, agent="")` which successfully sets `PYTHONDONTWRITEBYTECODE=1` and `TMPDIR` upon `self.is_reviewer_turn`.
* `[Pass]` Turn shims updated: Verified all 8 python turn shims (e.g., `utils/py/codex-turn.py:96`, `utils/py/agy-turn.py:436`) call `rtl.apply_reviewer_turn_env(env_dict, run_cwd, me)` prior to executing the agent CLI.
* `[Pass]` Test regressions verified: `test/gh681-reviewer-probe-rules.sh:176-209` includes Case 8 which fully asserts the environment dictionary injection correctness for both reviewer and producer roles.
* `[Pass]` Swept `utils/py/rtl.py` and all 8 turn shims in full; no other pre-existing defects found in the touched files.

relay closed (Approved), no further turn needed.

<!-- ↓↓↓ NEXT TURN goes here (append above nothing — this marker stays last) ↓↓↓ -->
