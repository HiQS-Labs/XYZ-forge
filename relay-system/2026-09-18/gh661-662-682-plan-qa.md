# RELAY · GH-661-662-682 relay containment and reviewer env plan QA
<!--
  Single source of truth for this two-agent relay. Read the ENTIRE file before acting.
  Scaffolded by relay-automation/new-relay.sh on 2026-09-18.
-->

NEXT: Producer
STATUS: Approved
ROUND: 2 / 4

## ▶ TAKE YOUR TURN — read this first (works for ANY agent: Claude, Codex, agy)
1. **Read this whole file** (header, Setup, Ground rules, every block in the Log).
2. **Check it's your turn:** `NEXT` (top) names the role to act. Confirm you are bound to it and the
   last Log block isn't already yours. If not → STOP and reply "wrong window — nudge the <other> window."
3. **Do your role's work** on the artifact named in Setup:
   - **Reviewer:** review vs the Definition of Done → graded findings
     (`[Blocker]`/`[Should]`/`[Nit]`/`[Pass]`), each with a concrete fix → set a **VERDICT**
     (exactly PASS, FAIL, or PARKED) and a **Basis** (explanation). **Review the whole file, not just the diff** (GH-268):
     a beta test had this loop reach `Approved` in two rounds while an independent audit of the same
     branch found 20 issues (1 critical, 4 high) — every one of them in the pre-existing code the
     change sat on, which nobody had read. Pre-existing defects in a file you are touching are IN
     SCOPE; if you find none, say so explicitly rather than leaving it unstated.
     **Declare it: every review block must contain a literal `swept file: yes` or `swept file: no`
     line.** Without it a reviewer that skipped the sweep is indistinguishable in the transcript from
     one that did it and found nothing — which is how the original 20 issues stayed invisible.
     Any `[Pass]` or "verified"/"confirmed" finding MUST
     carry a quoted span or a `file:line` citation — an uncited one is mechanically downgraded to
     `[Unverified — no citation]` (GH-173 B3). Do **not** edit the artifact; only append findings here.
     **A finding that asks for a behaviour change is a generalization unless you can paste the concrete
     input — a row, a value, a `file:line` — that fails under the current code** (GH-681: the gh673
     final QA relay generalized one late-error observation into "or a later invalid identity", the
     Producer implemented it, the same seat `[Pass]`ed it next round, and one historical NULL-URL
     ledger row then blanked every issue). Every `[Blocker]` or `[Should]` requesting a behaviour
     change MUST carry three lines: `Observed input:` (the failing input you saw), `Affected scope:`
     (the input predicate the change would govern), `Falsifier:` (the fixture or data that would show
     the change unnecessary or wrong, and its expected result).
     A `[Blocker]` must cite an observed failure. This is a protocol rule, not a mechanical check —
     the Producer may disposition a request lacking these as `Declined — unproven generalization`.
   - **Producer:** log a disposition for every open finding (Implemented / Modified / Declined + why,
     including `Declined — unproven generalization` for a behaviour-change request that carries no
     `Observed input:` / `Affected scope:` / `Falsifier:`), make the change, then add new work.
4. **Append ONE block** at the very bottom, directly **above** the marker line. Never edit earlier turns.
5. **Update the header:** flip `NEXT`; set `STATUS` (`Approved` closes — Reviewer only; else `Open`);
   the Producer bumps `ROUND` when opening a new cycle. If the max `ROUND` ends without `Approved`,
   set `STATUS: Escalated`.
6. **Commit only the relay file** (`relay(gh-661-662-682-relay-containment-and-reviewer-env-plan-qa): <role> r<N>`); no push. **Stop** and report one line.
7. **Hand off explicitly — EVERY turn, not just the first** (GH-268). End your turn by naming who acts
   next and what they should do: *"handing off to <other role> — go to the <other> window and say
   'take your turn'"*, or *"relay closed (Approved), no further turn needed"*. The beta report singled
   this out: the Reviewer turn never told the user to return to the Producer window, so a relay that
   was merely waiting looked stalled. A turn that ends without this line is not finished.

## Setup
- Artifact under review: **PROJECT/2-WORKING/GH-661-662-682-RELAY-CONTAINMENT.md** (and implementation plan for GH-661, GH-662, GH-682).
- Reviewer: agy   ·   Producer: producer
- Started: 2026-09-18
- Definition of Done: Unified plan for GH-661, GH-662, and GH-682 is grounded, DRY, commensurate with the small scale of changes, has falsifiable acceptance criteria with negative/mutation controls, properly scopes blast radius, and preserves repo containment rails.

## Ground rules
1. This file is the single source of truth. The agents never share memory — read the whole file.
2. Take a turn only if `NEXT` names your role — otherwise reply "not my turn" and stop.
3. One turn = one block appended at the very bottom, above the marker. Never edit earlier turns.
4. Stay tight — findings are bullets, not essays. Grade every finding.
5. **The Reviewer never edits the artifact.** It proposes graded findings; the Producer implements.
6. The relay ends on **Approved** (Reviewer only). End each turn by committing just this file; no push.

## Log

### Producer — producer — Round 1

VERDICT: PARKED
Basis: Independent plan review requested.
swept file: yes

Operational envelope: Local developer CLI and relay turn shims. Scope is surgical and commensurate:
1. GH-661: Verify regression coverage in test/gh654-offlane-log.sh for relative relay-file initialization (runtime fix landed in ac9fb274e #658) and document closeout.
2. GH-662: Fix utils/py/rtl.py offlane_candidates directory matching (check exact equality or slash-delimited child) so prefix lookalikes (.tick-other.txt, relay-system-other.txt) are reported as off-lane while preserving legitimate exemptions.
3. GH-682: In utils/py/rtl.py, provide apply_reviewer_turn_env helper to inject PYTHONDONTWRITEBYTECODE=1 and TMPDIR=<run_cwd>/.relay-scratch/tmp across 5 Python turn shims (codex-turn.py, agy-turn.py, claude-turn.py, pi-turn.py, aider-turn.py). Extend test/gh681-reviewer-probe-rules.sh.

Questions for Reviewer:
1. Is the directory boundary matching in rtl.py surgical, correct, and free of false positives/negatives?
2. Does the apply_reviewer_turn_env helper correctly encapsulate the reviewer environment injection across the 5 turn shims without changing builder turn semantics?
3. Are the acceptance criteria and test verification (with negative lookalike controls and process env assertions) falsifiable and sufficient?
4. Are per-task RELEASES ratings (GH-661: 20/20/50/95, GH-662: 65/50/50/90, GH-682: 75/60/50/85) grounded and appropriate?

Handing off to Reviewer (agy) — take your turn.

### Reviewer — agy — Round 1

VERDICT: FAIL
Basis: The plan scopes GH-682 to 5 turn shims, but there are 8 python turn shims in `utils/py/`. Missing `muse-turn.py`, `deepseek-turn.py`, and `commandcode-turn.py` will leak bytecode and temp files on reviewer turns.
swept file: yes

Findings:
- `[Blocker]`: GH-682 must update all 8 Python turn shims, not just 5. Update `muse-turn.py`, `deepseek-turn.py`, and `commandcode-turn.py` in addition to the 5 listed, and fix the "5 turn shims" language in the `Status`, `Context & Diagnosis`, and `Acceptance Criteria`.
  - Observed input: The plan states `update 5 turn shims` and lists `codex-turn.py, agy-turn.py, claude-turn.py, pi-turn.py, aider-turn.py`.
  - Affected scope: The turn shims receiving the `apply_reviewer_turn_env` injection.
  - Falsifier: A reviewer turn using `muse` or `deepseek` that writes `.pyc` files would fail containment unless these shims are also updated.
- `[Should]`: The new `apply_reviewer_turn_env` helper MUST use the existing Bash `rtl_is_reviewer_turn "$relay_file" "$agent"` (via `self._run_rtl`) for role detection. Do not duplicate `NEXT:` or driver directive parsing in Python.
  - Observed input: Python-side role parsing that ignores driver directives (like `RELAY_ROLE`).
  - Affected scope: The `apply_reviewer_turn_env` logic in `rtl.py` determining whether to inject `TMPDIR`.
  - Falsifier: A run under a driver where `RELAY_ROLE=builder` but the relay file says `NEXT: Reviewer` (or vice versa); naive Python parsing would get it wrong, but `rtl_is_reviewer_turn` is robust.
- `[Pass]`: GH-661 verification plan is covered by existing test `test/gh654-offlane-log.sh:58` (`test_absolute_relay_file_normalizes_to_relative`).
- `[Pass]`: GH-662 `rtl.py` exact-equality / slash-separated child logic correctly fixes the lookalike problem. Verified via probe output: `old=True new=False` for `.tick-other.txt` and `.relay-scratch-other.txt`.
- `[Pass]`: Ratings for GH-661, GH-662, GH-682 (GH-661: 20/20/50/95, GH-662: 65/50/50/90, GH-682: 75/60/50/85) are commensurate with the tasks' scale and risk.

Handing off to Producer — go to the producer window and say "take your turn".

### Producer — producer — Round 2

VERDICT: PARKED
Basis: Implemented all reviewer findings in PROJECT/2-WORKING/GH-661-662-682-RELAY-CONTAINMENT.md.
swept file: yes

Dispositions:
- `[Blocker]` (8 Python turn shims): Implemented. Updated `PROJECT/2-WORKING/GH-661-662-682-RELAY-CONTAINMENT.md` and implementation plan to explicitly cover all 8 Python turn shims (`codex-turn.py`, `agy-turn.py`, `claude-turn.py`, `pi-turn.py`, `aider-turn.py`, `muse-turn.py`, `deepseek-turn.py`, and `commandcode-turn.py`).
- `[Should]` (`apply_reviewer_turn_env` uses `rtl_is_reviewer_turn`): Implemented. `is_reviewer_turn()` delegates directly to `rtl_is_reviewer_turn {shlex.quote(self.relay_file)} {shlex.quote(agent)}` via `self._run_rtl` to reuse canonical driver-aware role detection in `relay-turn-lib.sh`.

Handing off to Reviewer (agy) — take your turn for Round 2.

### Reviewer — agy — Round 2

VERDICT: PASS
Basis: Producer addressed all findings. The plan correctly scopes all 8 Python turn shims and leverages canonical role detection (`rtl_is_reviewer_turn`), satisfying the Definition of Done.
swept file: yes

Findings:
- `[Pass]`: `[Blocker]` resolved. The plan encompasses all 8 turn shims (`PROJECT/2-WORKING/GH-661-662-682-RELAY-CONTAINMENT.md:47`: `all 8 Python turn shims: codex-turn.py, agy-turn.py, claude-turn.py, pi-turn.py, aider-turn.py, muse-turn.py, deepseek-turn.py, commandcode-turn.py`).
- `[Pass]`: `[Should]` resolved. The plan correctly mandates `apply_reviewer_turn_env` to use canonical role detection (`PROJECT/2-WORKING/GH-661-662-682-RELAY-CONTAINMENT.md:46`: `Detection uses rtl_is_reviewer_turn via _run_rtl to reuse the canonical driver-aware role detection in relay-turn-lib.sh`).

Relay closed (Approved), no further turn needed.

<!-- ↓↓↓ NEXT TURN goes here (append above nothing — this marker stays last) ↓↓↓ -->
