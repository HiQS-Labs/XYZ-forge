
consult: advisor was IDLE for >=90s (no CPU, no transcript growth) and was killed before the 735s wall cap [timeout-idle-no-progress: no CPU and no file progress — the agent was blocked waiting on something external (a lock, a prompt, or a hung network call) rather than working. [cpu=0.00s/s, samples=30, worktree-progress=no]]. This is an EXTERNAL condition consult detected and contained, not one it prevented.

consult: advisor failed or exceeded the 900s cap
