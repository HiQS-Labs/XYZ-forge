---
Goal: QA the GH-690 clone-retirement pointers (4 files, reminder-line only)
Date: 2026-09-17
NEXT: codex
STATUS: Approved
---

# Context

Issue #690: task clones were never retired after landing. This diff adds a retirement
pointer (one print/one doc line per surface) at four natural end-of-run points. Review
commit HEAD (the diff is small): `git show HEAD` plus these files:

- `utils/py/express.py` — pointer after the successful land closeout print
- `utils/py/marathon_drive.py` — pointer inside the end-of-run cost-summary method (fires on success AND halt)
- `skills/start-task/SKILL.md` — PR-ready step surfaces the pointer
- `skills/jog/SKILL.md` — Teardown Cleanliness item extended

# Questions

1. Is any reminder in a scope that fires at the wrong time (e.g. import time, refusals, dry-runs)?
2. Does the wording correctly route to /merge-cleanup (the existing sanctioned tool) rather than
   implying auto-deletion? Auto-delete was deliberately rejected: task clones carry unpushed
   bookkeeping commits (relay-turn evidence).
3. Any consumer (suite, doc, script) that pins the exact pre-change output and would now break?

# Verdict

**Verdict:** Approved

**Findings:**
1. **No.** The `express.py` reminder is safely placed inside `closeout()`, which is entirely bypassed during dry-runs and only fires after a successful landing. The `marathon_drive.py` reminder is safely placed inside `xyz_marathon_cost_summary()`, which explicitly aborts for `--help/usage/lock-contention/lane-parked/--dry-run` early exits and only fires for driven runs (on success or halt). 
2. **Yes.** The wording clearly reads "can be retired via /merge-cleanup" instead of implying auto-deletion.
3. **No.** Tests use substring matching (`grep -Eq` or `grep -qF`) for their assertions instead of strict multi-line exact block matching. The newly appended prints do not break the presence checks. The documentation baselines (like `GH-402-negative-control.md`) do not cause test failures since they are just references.

<!-- ▽ RELAY AUTOMATION: DO NOT MODIFY THIS BLOCK ▽ -->
▶ TAKE YOUR TURN (codex)
<!-- △ RELAY AUTOMATION: DO NOT MODIFY THIS BLOCK △ -->

### System · relay-drive — 2026-09-18T05:38:45Z
terminal STATUS Approved written by builder-role turn (agy) — reverted
