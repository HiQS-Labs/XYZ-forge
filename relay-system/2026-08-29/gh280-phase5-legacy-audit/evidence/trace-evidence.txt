# GH-280 Phase 5 legacy-audit — runtime trace evidence (stdlib trace, line-level)
# Fixture: disposable full clone /tmp/gh280-p5-fixture @ fa85a955 + three scratch git repos
# (git init + releases init + one jog row GH-9901). No relay-automation present in the
# scratch roots, so the legacy real body stops at its own not-found guard — the exact
# boundary this probe exists to observe. Generated 2026-08-29T06:17:12Z.

## Run A — pre-flip default (relay), real body head
$ python3 -m trace --count --coverdir=... releases_app.py --root FIXTURE jog run --auto-merge --max-tasks 1
rc=0 (NOTE: the legacy drive-failure path exits 0 — jog_run_main breaks on failure without
      a nonzero exit; observed quirk, pre-existing, out of scope for this lane)
    jog: runner scripts not found or not executable (drive=None, shim=None)
    
    ============================================================
    jog: processing GH-9901 (attempt 1) at position 1
    ============================================================
    jog: drive execution failed for GH-9901 (exit 2).
    jog: stopping queue execution on task failure.

## Run B — --simulate --auto-merge --max-tasks 1 (the shape jog-queue.sh §13/§15 and adapter §I use)
$ python3 -m trace --count --coverdir=... releases_app.py --root FIXTURE jog run --simulate --auto-merge --max-tasks 1
rc=0
    
    ============================================================
    jog: processing GH-9901 (attempt 1) at position 1
    ============================================================
    jog: [simulate] simulated single-phase drive on GH-9901 with builder=agy
    jog: task GH-9901 passed; auto-merging into development...
    jog: reached max-tasks limit (1); pausing queue.

## Run C — --executor marathon --builder codex, no reviewer (post-flip default shape)
$ python3 -m trace --count ... ; then un-traced re-run for the true exit code
rc=2 (refusal BEFORE lock/lease/loop; under trace the exit prints 0 — trace-module artifact,
      the un-traced re-run in a second fixture gives rc=2)
    jog: --reviewer <agent> is required with --executor marathon — Jog does not select a reviewer by default (GH-280: no silent cost-bearing or same-agent route)

## Executed-line extraction from the three jog_run.cover files (legacy + dispatch ranges)
# (def lines count as executed at import; body lines are the signal)
range                                                relay  simulate  marathon-ref
run_single_phase_drive body (1268-1378, def excl.)      18         3             0
  simulate arm (1269-1271)                               1         3             0
  real body: script discovery (1273-1299)               17         0             0
  real body: relay scaffold+dispatch (1300-1378)         0         0             0
_verify_legacy_pr_before_merge body (1382-1395)          0         0             0
handle_landing_boundary body (1399-1459)                 0        12             0
jog_run_main legacy arm (1616-1660)                     12        12             0
jog_run_main marathon arm (1600-1614)                    1         1             0
jog_run_main validation arm (1486-1490)                  1         1             5

Read: the legacy real body's dispatch half (1300-1378) executed in NO configuration —
no committed test drives it (jog-queue §13/§15 and adapter §I are simulate-only; the
adapter's relay-drive stubs serve marathon-drive, not jog's legacy dispatch).
The simulate run DOES execute handle_landing_boundary's auto-merge arm (1404-1428)
and the legacy call sites (1639-1657): --simulate is coupled to the legacy skeleton.
  simulate handle_landing_boundary executed lines: [1404, 1405, 1407, 1408, 1409, 1410, 1411, 1413, 1414, 1426, 1427, 1428]
