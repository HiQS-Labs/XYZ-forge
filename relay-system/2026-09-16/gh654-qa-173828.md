---
Goal: QA the GH-654/658/659/660 containment + drift-guard hotfix chain
Date: 2026-09-16
NEXT: orchestrator
STATUS: Open
---

# Context

Adversarial QA of five express-landed commits that fix a containment blockade (marathon builder
turns destroyed by a false off-lane verdict) and add a vendored-skill drift guard. Review the
CURRENT state of these files (all at HEAD):

- `utils/py/rtl.py` — `offlane_candidates()`, `normalized_allow_csv()`, `OFFLANE_EXEMPT`, the
  `worktree_end()` diagnostic, and the `_run_rtl` bridge's `rtl_init` invocation
- `utils/py/skill_drift_check.py` — new drift guard
- `test/gh654-offlane-log.sh`, `test/gh660-skill-drift.sh` — regression suites (registered in `validate.sh`)

The commits under review: 8ee53a1e (#654 logging), 7d7d60e5 + ac9fb274 (#658 absolute-relay-file
fix then its reviewer-scoping regression fix), 0ec05b28 (#659 csv trim), 99b376d9 (#660 drift
guard). `git show <sha>` works in the worktree.

Known context you must weigh, not re-discover: the frozen bash twin `relay-turn-lib.sh` was NOT
touched (GH-308), so under `XYZ_PYTHON=0` the csv-trim and relay-file defects remain — accepted,
Python-first direction. The mirror (`offlane_candidates`) deliberately over-reports vs the bash
sweep (documented in its docstring).

# Questions — answer each with a number, cite file:line


1. **Reviewer-scoping regression is really fixed?** 
   Yes. `self.relay_file` is passed verbatim as an absolute path to `rtl_init` (`utils/py/rtl.py:750`) and `rtl_turn_prompt` (`utils/py/rtl.py:787`). No Python call site normalizes it to a relative path before handing it to bash, meaning it safely resolves from any CWD.

2. **`normalized_allow_csv` placement**
   There is a leak! `turn_prompt` still uses the raw `self.allow_paths` instead of `normalized_allow_csv(self.allow_paths)`. `utils/py/rtl.py:787` passes the untrimmed string directly into the `rtl_turn_prompt` bash command.

3. **`offlane_candidates` fidelity**
   If the relay file lives outside the worktree, `os.path.relpath` yields a path starting with `..`, so the ternary leaves it absolute (`utils/py/rtl.py:276`). It will then fail to match any worktree-relative path from porcelain. This results in an **over-report (safe)**, as it will fail to silence paths it otherwise might have.

4. **`OFFLANE_EXEMPT` divergence**
   I agree with the documented divergence. Because `offlane_candidates` is used exclusively for pre-verdict logging (`utils/py/rtl.py:826-829`), over-reporting prints false alarms but NEVER creates false confidence.

5. **`skill_drift_check.py`**
   - **CLI Ambiguity**: The help text says `--canonical` is the "canonical skills/ directory" (`utils/py/skill_drift_check.py:58`), but `scan(canonical / "skills", ...)` (`utils/py/skill_drift_check.py:68`) means it expects the repository root. Passing the `skills/` dir fails with `EXIT_USAGE`.
   - **Byte-compare Semantics**: `_digest` (`utils/py/skill_drift_check.py:28`) uses raw `read_bytes()`. It will falsely report drift if the vendor cloned on Windows (CRLF) or if an editor trimmed trailing whitespace.

6. **Suite quality**
   The mutation proofs satisfy the bar.
   Untestable-by-fixture branches:
   - `except ValueError:` in `os.path.relpath` (`utils/py/rtl.py:274`) (only throws on Windows cross-drive paths).
   - `except Exception:` wrapping `git status` (`utils/py/rtl.py:257`) (cannot easily trigger a subprocess launch failure without mocks).

7. **Anything missed**
   - **Containment mirror edge**: In `offlane_candidates`, `i += 1` consumes the rename/copy source path (`utils/py/rtl.py:287`) and discards it without checking it against `allow_paths`. This stays silent on an unallowed deletion/move, contradicting the "resolve toward REPORTING" comment.

# Verdict

**Verdict:** Changes requested

- Leak in `turn_prompt` allowlist trimming
- CLI help text vs implementation mismatch in `skill_drift_check.py`
- Cross-platform byte-compare false positives in `skill_drift_check.py`
- `offlane_candidates` rename source path omission



### Round 2 · Producer (orchestrator) — all four findings fixed, landed 4481ab48

1. turn_prompt csv leak → normalized_allow_csv at the rtl_turn_prompt bridge (utils/py/rtl.py)
2. --canonical ambiguity → accepts repo root OR skills/ dir directly; help corrected (utils/py/skill_drift_check.py)
3. CRLF false positives → _digest normalizes CRLF→LF before hashing
4. rename-source omission → offlane_candidates checks the R/C second NUL field against the allowlist; suite case added (test/gh654-offlane-log.sh, 9 cases green)

Re-review requested against HEAD (4481ab48). Same questions, plus: confirm each of your four findings is resolved.

### Round 3 · Reviewer (agy) — verification

1. **turn_prompt csv leak**: Confirmed fixed. `normalized_allow_csv(self.allow_paths)` is now used in `turn_prompt` (`utils/py/rtl.py:793`).
2. **--canonical ambiguity**: Confirmed fixed. The CLI accepts both the repo root and the `skills/` directory itself (`utils/py/skill_drift_check.py:70`). Help text is also updated.
3. **CRLF false positives**: Confirmed fixed. `_digest` normalizes CRLF to LF using `.replace(b"\r\n", b"\n")` before hashing (`utils/py/skill_drift_check.py:31`).
4. **rename-source omission**: Confirmed fixed. `offlane_candidates` now correctly appends the rename/copy source path (second NUL field) to the paths list (`utils/py/rtl.py:291`).

**Verdict**: Approved. All four findings are successfully resolved and verified against HEAD.

<!-- ▽ RELAY AUTOMATION: DO NOT MODIFY THIS BLOCK ▽ -->
▶ TAKE YOUR TURN (agy)
<!-- △ RELAY AUTOMATION: DO NOT MODIFY THIS BLOCK △ -->
