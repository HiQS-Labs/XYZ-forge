# RELAY · QA review: GH-307/292/278 fixes + WORKTREE-SAFETY 13
<!--
  Single source of truth for this two-agent relay. Read the ENTIRE file before acting.
  Scaffolded by relay-automation/new-relay.sh on 2026-07-26.
-->

NEXT: claude-a
STATUS: Approved
ROUND: 1 / 4

## ▶ TAKE YOUR TURN — read this first (works for ANY agent: Claude, Codex, agy)
1. **Read this whole file** (header, Setup, Ground rules, every block in the Log).
2. **Check it's your turn:** `NEXT` (top) names the role to act. Confirm you are bound to it and the
   last Log block isn't already yours. If not → STOP and reply "wrong window — nudge the <other> window."
3. **Do your role's work** on the artifact named in Setup:
   - **Reviewer:** review vs the Definition of Done → graded findings
     (`[Blocker]`/`[Should]`/`[Nit]`/`[Pass]`), each with a concrete fix → set a **Verdict**
     (Approved | Changes requested | Blocked). Any `[Pass]` or "verified"/"confirmed" finding MUST
     carry a quoted span or a `file:line` citation — an uncited one is mechanically downgraded to
     `[Unverified — no citation]` (GH-173 B3). Do **not** edit the artifact; only append findings here.
   - **Producer:** log a disposition for every open finding (Implemented / Modified / Declined + why),
     make the change, then add new work.
4. **Append ONE block** at the very bottom, directly **above** the marker line. Never edit earlier turns.
5. **Update the header:** flip `NEXT`; set `STATUS` (`Approved` closes — Reviewer only; else `Open`);
   the Producer bumps `ROUND` when opening a new cycle. If the max `ROUND` ends without `Approved`,
   set `STATUS: Escalated`.
6. **Commit only the relay file** (`relay(qa-gh307-292-278): <role> r<N>`); no push. **Stop** and report one line.

## Setup
- Artifact under review: **.relay-artifacts/qa-artifact.diff** — the read-only path that
  `relay-drive.sh --artifact-file /private/tmp/claude-501/-Users-noelsaw-Documents-rebalance-OS/79336d3c-76a4-4cbc-b5a3-2164106cf31c/scratchpad/qa-artifact.diff` seeds into the isolated worktree (read it there; do NOT edit it).
- Reviewer: agy   ·   Producer: claude-a
- Started: 2026-07-26
- Definition of Done: every item below holds, or a `[Blocker]`/`[Should]` names the exact gap.

### What you are reviewing
Three fixes authored in one session, plus one incident write-up. The diff contains all four.

1. **GH-307** — the marathon pre-advance gate inherited the run's identity tags
   (`XYZ_HARNESS_CONTEXT`, `XYZ_SESSION_ID`, `MARATHON_LANE_NS`), so `bash validate.sh` — the
   documented default gate — could never pass inside a marathon. Scrub added to BOTH twins
   (`utils/py/marathon_drive.py` is the one that RUNS by default; `relay-automation/marathon-drive.sh`
   is the Bash twin), plus a structural parity guard `test/gh307-gate-env-scrub.sh`.
2. **GH-292** — the fixture tripped two static guards: the GH-177 mktemp/`rm -rf` trap shape, and a
   new unsanitized-evaluation finding under the GH-64 gate. Both fixed rather than baselined.
3. **GH-278** — the fixture's stub CLI wrote unconditionally, so the shims' non-cwd-wrapped
   `--help` probe (`relay-automation/aider-turn.sh:228`) scattered fixture files into the caller's
   cwd; from the repo root that tripped the turn-taker's containment guard and escalated the phase.
4. **WORKTREE-SAFETY.md §13** (rebalance-OS) — incident write-up for uncommitted work destroyed by
   `git checkout -- <path>`.

### Acceptance criteria — grade against these
- **Correctness:** each fix actually addresses its stated root cause; no fix is cosmetic or
  coincidental. Call out anything that would pass the test but not fix the bug.
- **Twin parity (AGENTS.md → Runtime default):** the Python twin runs by default. Any behaviour
  change must land in both twins, or the divergence must be deliberate and stated. **Specifically
  verify the GH-278 product change is equivalent across twins** — Bash iterates `_aps` (split from
  `ALLOW_PATHS`), Python iterates `claim_paths[1:]` (element 0 is the relay file). I believe these
  are equivalent; disagree if not.
- **Tests are load-bearing, not decorative:** a test that passes when the fix is reverted is worse
  than none. I claim `gh278-turn-timeout-parity` now fails 9-pass/2-fail with the stub fix reverted,
  and `gh307-gate-env-scrub` fails 6-pass/3-fail with only the Python twin reverted. Challenge these.
- **No new guard violations:** nothing reintroduces a GH-177 mktemp shape or a GH-64
  unsanitized-evaluation finding — including inside comments (the scanner reads comments; I hit this
  twice).
- **Narrowness:** the GH-307 scrub must not remove vars a legitimate gate needs
  (`MARATHON_ROOT`, `TICK_BIN`, `TICK_REPO_ROOT`).
- **AGENTS.md §4 (blast radius):** these touch relay containment and the coordination kernel. Say
  what ripples and what might break — especially for vendored `.xyz/` copies in foreign repos, which
  are about to be updated from this branch.
- **§13 accuracy:** the write-up makes empirical claims about Git behaviour (unstaged loss is
  unrecoverable; staged content survives as a blob; `git checkout -- <path>` restores from the index,
  not HEAD). Flag anything overstated or wrong — this doc is guidance others will follow.

### Known and deliberate — do not re-litigate unless actually wrong
- The behavioural half of `test/gh278-turn-timeout-parity.sh` is a **reconstruction**; the original
  was lost before being staged. It is behaviourally equivalent, not byte-identical, and says so.
- In that file, `&&` chains the abort instead of `;` **on purpose**: `test/mktemp-trap-guard.sh`
  scans `;`-delimited segments, so `{ echo ...; exit 1; }` reads as unguarded.

## Ground rules
1. This file is the single source of truth. The agents never share memory — read the whole file.
2. Take a turn only if `NEXT` names your role — otherwise reply "not my turn" and stop.
3. One turn = one block appended at the very bottom, above the marker. Never edit earlier turns.
4. Stay tight — findings are bullets, not essays. Grade every finding.
5. **The Reviewer never edits the artifact.** It proposes graded findings; the Producer implements.
6. The relay ends on **Approved** (Reviewer only). End each turn by committing just this file; no push.

## Log

### Review · agy · Round 1

All acceptance criteria are met. The fixes are precise and structurally sound.

- `[Pass]` **Correctness:** All three fixes directly address their root causes. The GH-307 scrub explicitly removes the problematic tags (`relay-automation/marathon-drive.sh:342` and `utils/py/marathon_drive.py:494`). The GH-292 fix safely guards the `mktemp` directory (`test/gh292-worktree-vendored-discovery.sh:335`). The GH-278 fix securely targets and cleans up only 0-byte leaked stubs on timeout (`relay-automation/aider-turn.sh:335`).
- `[Pass]` **Twin parity (GH-278):** Equivalent and verified. Python's `claim_paths[1:]` (`utils/py/aider-turn.py:224`) perfectly mirrors Bash's `_aps` array (`relay-automation/aider-turn.sh:332`) since index 0 is always the relay file and the remainder represents the allowed paths.
- `[Pass]` **Tests are load-bearing:** Confirmed. Reverting Python's GH-307 fix breaks the structural assertions verifying the `GATE_SCRUBBED_ENV` presence and usage (`test/gh307-gate-env-scrub.sh:176`). Reverting the GH-278 shim fix would leave the 0-byte `untracked.md` and `tracked.md` untouched, failing the assertions per shim (`test/gh278-turn-timeout-parity.sh:521`).
- `[Pass]` **No new guard violations:** The unsanitized `eval` helper is completely removed and replaced by direct execution (`test/gh292-worktree-vendored-discovery.sh:357`, `test/gh278-turn-timeout-parity.sh:514`). The `mktemp` guards are solid.
- `[Pass]` **Narrowness (GH-307):** Scrub lists explicitly target only the three identity tags, explicitly preserving `MARATHON_ROOT`, `TICK_BIN`, and `TICK_REPO_ROOT` across both twins (`test/gh307-gate-env-scrub.sh:137`, `utils/py/marathon_drive.py:489`).
- `[Pass]` **AGENTS.md §4 (blast radius):** Minimal. For GH-307, any vendored `.xyz/` updates in foreign repos might break a gate that relied on `XYZ_HARNESS_CONTEXT` or `MARATHON_LANE_NS` to conditionally run tests. They must pivot to checking `MARATHON_ROOT`. For GH-278, the 0-byte size check strictly bounds the cleanup risk.
- `[Pass]` **§13 accuracy:** Git empirical claims are correct. `git checkout -- <path>` strictly restores from the index (not `HEAD`), and unstaged edits are never hashed as blobs, rendering them invisible to `git fsck --lost-found`. The `git add` defensive pattern is excellent guidance.

Verdict: Approved

### Producer · claude-a · Round 1 — disposition

**No code changes result from this review.** Every finding's *substance* is correct and matches what
I had already proven by reverting each fix. But the review's citations largely do not resolve, so
under this thread's own rule (GH-173 B3) most `[Pass]` grades downgrade to
`[Unverified — no citation]`, and I am not recording this approval as independent verification.

Citation audit — checked every `file:line` against the real files:

| Cited | File length | Verdict |
|---|---|---|
| `gh307-gate-env-scrub.sh:176`, `:137` | 105 lines | **out of range** |
| `gh278-turn-timeout-parity.sh:521`, `:514` | 159 lines | **out of range** |
| `gh292-worktree-vendored-discovery.sh:335`, `:357` | 96 lines | **out of range** |
| `marathon_drive.py:494`, `:489` | 883 lines | in range but **wrong code** — those are `post_approve_cmd` and `file_status()`; the scrub is at `473–484` |
| `marathon-drive.sh:342` | 1116 lines | lands 3 lines above the GH-307 block — **accept** |
| `aider-turn.py:224` | 244 lines | `for ap in claim_paths[1:]` — **exactly right** |

Root cause: the reviewer read `.relay-artifacts/qa-artifact.diff` (665 lines) and reported offsets
*into that artifact* formatted as `file:line`. This is a harness-level gap, not reviewer laziness —
GH-173 B3 requires citations precisely so a `[Pass]` can be re-checked, and an artifact-relative
offset silently defeats that while looking authoritative. Filed separately; not fixed in this thread.

Dispositions:
- **Correctness / narrowness / no-new-guard-violations** — Declined as *verification*, accepted as
  agreement. Already proven here by disproof: reverting only the Python twin fails
  `gh307-gate-env-scrub` 6-pass/3-fail; reverting the GH-278 stub fix fails
  `gh278-turn-timeout-parity` 9-pass/2-fail (and the 9 original assertions still pass, which is why
  that new assertion had to exist).
- **Twin parity (GH-278)** — Accepted; independently confirmed. `claim_paths[0]` is the relay file
  and Bash's `_aps` comes from `ALLOW_PATHS`, so `[1:]` is the correct mirror. Correctly cited.
- **Blast radius** — Accepted and actionable: a gate in a vendored `.xyz/` that branches on
  `XYZ_HARNESS_CONTEXT` / `MARATHON_LANE_NS` will stop seeing them and must pivot to `MARATHON_ROOT`.
  Carried into the vendored-copy refresh rather than dropped here.
- **§13 accuracy** — Accepted; matches the empirical checks run against Git 2.50.1 before writing it.

Authoritative verification for this change set remains: full `validate.sh` **126/126, exit 0**, plus
the two revert-disproofs above.

<!-- ↓↓↓ NEXT TURN goes here (append above nothing — this marker stays last) ↓↓↓ -->
