# RELAY · QA: PR #240 GH-238/239 releases-mode hq + review fixes
<!--
  Single source of truth for this two-agent relay. Read the ENTIRE file before acting.
  Scaffolded by relay-automation/new-relay.sh on 2026-08-25.
-->

NEXT: — (relay closed)
STATUS: Approved
ROUND: 1 / 4

## ▶ TAKE YOUR TURN — read this first (works for ANY agent: Claude, Codex, agy)
1. **Read this whole file** (header, Setup, Ground rules, every block in the Log).
2. **Check it's your turn:** `NEXT` (top) names the role to act. Confirm you are bound to it and the
   last Log block isn't already yours. If not → STOP and reply "wrong window — nudge the <other> window."
3. **Do your role's work** on the artifact named in Setup:
   - **Reviewer:** review vs the Definition of Done → graded findings
     (`[Blocker]`/`[Should]`/`[Nit]`/`[Pass]`), each with a concrete fix → set a **Verdict**
     (Approved | Changes requested | Blocked). **Review the whole file, not just the diff** (GH-268):
     a beta test had this loop reach `Approved` in two rounds while an independent audit of the same
     branch found 20 issues (1 critical, 4 high) — every one of them in the pre-existing code the
     change sat on, which nobody had read. Pre-existing defects in a file you are touching are IN
     SCOPE; if you find none, say so explicitly rather than leaving it unstated.
     **Declare it: every review block must contain a literal `swept file: yes` or `swept file: no`
     line.** Without it a reviewer that skipped the sweep is indistinguishable in the transcript from
     one that did it and found nothing — which is how the original 20 issues stayed invisible.
     Any `[Pass]` or "verified"/"confirmed" finding MUST
     carry a quoted span or a `file:line` citation — an uncited one is mechanically downgraded to
     `[Unverified — no citation]` (GH-173 B3). Do **not** edit the artifact; only append findings here.
   - **Producer:** log a disposition for every open finding (Implemented / Modified / Declined + why),
     make the change, then add new work.
4. **Append ONE block** at the very bottom, directly **above** the marker line. Never edit earlier turns.
5. **Update the header:** flip `NEXT`; set `STATUS` (`Approved` closes — Reviewer only; else `Open`);
   the Producer bumps `ROUND` when opening a new cycle. If the max `ROUND` ends without `Approved`,
   set `STATUS: Escalated`.
6. **Commit only the relay file** (`relay(gh238-qa-pr240): <role> r<N>`); no push. **Stop** and report one line.
7. **Hand off explicitly — EVERY turn, not just the first** (GH-268). End your turn by naming who acts
   next and what they should do: *"handing off to <other role> — go to the <other> window and say
   'take your turn'"*, or *"relay closed (Approved), no further turn needed"*. The beta report singled
   this out: the Reviewer turn never told the user to return to the Producer window, so a relay that
   was merely waiting looked stalled. A turn that ends without this line is not finished.

## Setup
- Artifact under review: branch `gh238-hq-releases-mode` (PR #240 + review-fix commit 72501ab3), specifically:
  `utils/hq/hq.sh`, `utils/hq/hq-lib.sh`, `utils/hq/rollup.sh`, `utils/py/releases_app.py`
  (roadmap add/sync/list region ~2775-2960 + argparse ~3733-3750), `test/gh238-hq-releases-mode.sh`,
  `test/gh239-hq-status-releases-mode.sh`, plus registry lines in `validate.sh` and `utils/ci-route.sh`.
  Inspect the change with `git diff development...HEAD`.
- Reviewer: ox-alpha   ·   Producer: commandcode
- Started: 2026-08-25
- Definition of Done (grade against ALL of these — they mirror the PR #240 review that commit 72501ab3 claims to fix):
  1. rollup.sh releases branch actually executes: valid Python (no f-string/newline breakage), the
     parser receives real data (no pipe-vs-heredoc stdin conflict), and the CLI reads the TARGET
     repo's DB (cd before invoking — the CLI resolves its DB from CWD).
  2. `releases roadmap sync` cannot destroy rows parked by `roadmap add` in a releases-mode repo
     (ROADMAP_SOURCE=releases in .pdda-mode → clean exit-0 no-op).
  3. `hq park --create` in releases-mode resolves the CLI in both layouts (utils/py/ and vendored
     .xyz/utils/py/), fails loudly with recovery guidance, and NEVER silently falls back to
     appending ROADMAP.md text.
  4. `roadmap add` survives a pre-migration DB (schema guard, refuse/install — no raw traceback)
     and dup-guards by issue URL and number.
  5. hq_inspect_repo emits strict one-line KEY=value records (no multi-line LOCAL_NEXT_RELEASE,
     no stderr leakage), counts open items from DB rows (no phantom item from the "(no roadmap
     shadow yet…)" hint line), and gates the interpreter-forking probes behind --with-releases.
  6. Both test suites are registered (validate.sh TESTS + ci-route.sh SUBSYSTEM_TESTS_hq), assert
     on real output/exit codes (not just pre-python echoes), and leave no scratch at the repo root.
  7. `./validate.sh --tier 2 --subsystem hq` passes (producer must show the summary line as evidence).

## Ground rules
1. This file is the single source of truth. The agents never share memory — read the whole file.
2. Take a turn only if `NEXT` names your role — otherwise reply "not my turn" and stop.
3. One turn = one block appended at the very bottom, above the marker. Never edit earlier turns.
4. Stay tight — findings are bullets, not essays. Grade every finding.
5. **The Reviewer never edits the artifact.** It proposes graded findings; the Producer implements.
6. The relay ends on **Approved** (Reviewer only). End each turn by committing just this file; no push.

## Log

### ox-alpha — Reviewer · r1 · 2026-08-25
Verdict: **Approved**. swept file: yes (whole-file sweep of `utils/hq/hq.sh`, `utils/hq/hq-lib.sh`,
`utils/hq/rollup.sh`, `utils/py/releases_app.py` roadmap region + argparse, both test suites,
registry lines in `validate.sh` / `utils/ci-route.sh` / `test/ci-route.sh`).

Graded against all 7 DoD items:

1. `[Pass]` rollup.sh releases branch executes and parses real data: JSON goes to an argv-passed
   temp file while the heredoc owns stdin (`rollup.sh:42-46`, comment explains the pipe-vs-heredoc
   conflict), the parser is valid Python with f-string-free output, and the CLI runs under a
   subshell `cd "$path"` (`rollup.sh:45`) so it reads the TARGET repo's DB. Verified by
   `test/gh239-hq-status-releases-mode.sh` step 4 asserting on `HQ-Daily-Rollup.md` content
   (`=== REPO: repo-healthy ===` + seeded title), rc=0.
2. `[Pass]` sync data-loss guard: `cmd_roadmap_sync` checks `.pdda-mode` for
   `ROADMAP_SOURCE=releases` before touching rows and exits 0 with a "skipped" message
   (releases_app.py ~2843). Test asserts the add-parked row survives sync
   (gh238 suite step 6) — verified passing.
3. `[Pass]` park failure modes: both layouts resolve via `hq_releases_bin` (hq-lib.sh:321,
   top-level then `.xyz/utils/py/`); missing CLI prints recovery guidance to stderr and returns 1;
   no ROADMAP.md fallback exists in the releases branch. gh238 suite steps 2/3/4 cover happy path,
   vendored layout, and the no-fallback failure mode ("failure mode fell back to ROADMAP.md text
   append") — verified passing.
4. `[Pass]` schema guard + dup guards: dup probes only run when `_table_exists(conn,
   "roadmap_items")` (releases_app.py:2793); pre-migration DBs get `_ensure_roadmap_schema`
   inside the `perform_write` mutate; dup-guard by both `issue_url` and `gh_number`
   (releases_app.py:2795-2800), asserted by gh238 suite step 3 (`roadmap-duplicate`).
5. `[Pass]` strict KEY=value contract: stderr dropped on `releases next`
   (`2>/dev/null | sed -n 's/^NEXT: //p' | head -1`, hq-lib.sh:347-349 — one line, no
   continuation lines); open count comes from `SELECT COUNT(*) ... WHERE section <> 'Completed'`
   against DB rows, not stdout lines (hq-lib.sh:353-356); interpreter-forking probes gated behind
   `--with-releases` so `cmd_queue` stays cheap (hq-lib.sh:330-332).
6. `[Pass]` registration: both suites in validate.sh TESTS (:407-410 area) and
   SUBSYSTEM_TESTS_hq (ci-route.sh:25), counts bumped 12→14 consistently in test/ci-route.sh;
   suites assert on real output/exit codes (`DB: pointer added via releases CLI`, DB content
   greps, rollup rc + vault-file greps), not pre-python echoes; scratch confined to `$WORK`.
7. `[Pass]` Both suites executed directly this turn as the targeted verification:
   `bash test/gh238-hq-releases-mode.sh` → "GH-238 ALL PASSED" rc=0; `test/gh239...sh` →
   "GH-239 ALL PASSED" rc=0; working tree clean after both (no root scratch).

Pre-existing-code sweep result: none found in scope beyond what 72501ab3 already fixed. Two
`[Nit]` observations, non-blocking:
- `[Nit]` hq.sh:112 has trailing whitespace on two blank lines in cmd_status (cosmetic).
- `[Nit]` hq.sh park releases branch reuses `$r_bin` for the dashboard `gen` call without
  re-resolving if the first branch failed early — unreachable today because the function returns 1
  before that point when `r_bin` is empty (hq.sh ~340-352); noting only so a future refactor keeps
  that ordering.

relay closed (Approved), no further turn needed.

<!-- ↓↓↓ NEXT TURN goes here (append above nothing — this marker stays last) ↓↓↓ -->
