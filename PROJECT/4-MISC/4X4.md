---
client: Hypercart / Neochrome (internal R&D)
repo: https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm
last_edit: 2026-06-15
week_of: 2026-06-15
source_pr_number: n/a (spike on main)
sprint: relay-automation Phases 1–5 SHIPPED + cross-model/Option-A

---

# 4x4 Dashboard - Strategic Goals & Weekly Checklist
A simple, actionable framework to prioritize and track engineering tasks. Focus on alignment, transparency, continuous improvement, and increasing clarity for everyone. This document does not replace your project management tools. It is meant to be a simple, actionable checklist to help you focus on the most important tasks for the week.

# About the Current Project
**Trinity / xyz 3-agent coordination swarm.** A spike for letting Claude Code, Codex, and Gemini work the same codebase concurrently without colliding. The core is `tick` — a small Node CLI backed by a disjoint-files-per-event log under `.tick/` that gives agents soft path-scoped claims, a 2-claim cap, atomic next+claim (`tick take`), heartbeats (`tick ping`), and a read-only auditor (`tick analyze`) that scores per-agent compliance from git history.

Two projects rode on `tick`: **Project 1 — the swarm** (proven; graduated) and **Project 2 — relay-automation**, now the focus and **shipped end-to-end**.

**Project 1 (swarm):** mechanically proven, then the load-balance question resolved. Run 3 = 40% concurrency (static split idled the fast agent); **Run 4 = 72%** on a balanced fixture (cleared the ≥50% bar) → graduated. Run 5 (balanced 2-lane build) re-hit 39% purely from **start-skew** — the lesson being *simultaneous start dominates the metric, not lane design*.

**Project 2 (relay-automation):** the daily-use tool — a tick-backed, self-healing automation of the portable `/relay` review loop. **Phases 1–5 all shipped**, `validate.sh` **20/20**: handoff-exclusive turn-token (Phase 1), liveness watchdog (2), verdict gating (3), hands-free `/loop` poll + tick-native relay turns + self-expiring loops (4), and a packaged sibling skill (5). Beyond the plan: a **cross-model Claude↔Codex relay** with **Option-A headless Codex turns** (`codex exec` behind a path-allowlist shim) — live-proven. XYZ-swarm further work is deferred; relay-automation is the higher daily-use payoff.

---

## 1. Strategic Backlog
**Maximum of 4 items. Focus on long-term goals and impactful improvements.**
**Update/Reminder:** If these are new projects without a clear scope, consider using the [Project Scope Outline](#bonus-project-scope-outline) below.

1. - [ ] **Commercial-viability hardening** — see [ROADMAP.md](../../ROADMAP.md): adversarial-proof gaps G1–G5 (mid-turn kill, dup token, **stale-writer fencing**, concurrent pollers, cross-repo/model) + mechanisms R1–R4. **R1 epoch fencing (G3) is the keystone.** Subsumes the older polish items (race hammer-test = G4, auto-reap = R2, fresh-clone E2E = G5, unattended runner). *(Graduate-to-Phase-2 ✅ done — Run 4 cleared the bar.)*
2. - [ ] **Enforcement layer** — `tick` does not enforce that edits stay inside declared paths. Add a pre-commit hook or file watcher so drift is caught, not just measured after the fact. *(Note: `codex-turn.sh` now enforces a path-allowlist for headless Codex turns — a first instance of this.)*
3. - [ ] **Kill worktree/branch friction** — replace the per-clone workaround with a shared `.tick/`-only ref or an out-of-band sync daemon so agents can stay on their own branches.
4. - [ ] **`tick claim --confirm`** — a second-phase mutex that re-projects before gating irreversible edits, closing the projection-after-push race (one-shot claim is a soft claim, not a true lock).
5. - [ ] **Onboarding front-door polish** *(from 2026-06-17 front-door audit — exceeds the 4-item cap; merge or demote when planning the week)* — two doc lifts, ~an afternoon: **(a)** thin the repo root — move non-onboarding `.md` files (`4X4.md`, `snapshot.md`, `SKILL-BUILD-PLAN.md`, `TODO_DEFER.md`) into `docs/` or `PROJECT/` so the front door isn't visually buried under ~12 root markdown files; **(b)** add an explicit "Access you'll need: a ChatGPT login (Codex) + GCA auth (Gemini)" callout in [relay-automation/README.md](../../relay-automation/README.md) before the operator-usage section, so the two transitive auth walls are named up front. *(Verdict was ✅ Smooth — the kernel path is auth-free at 23/23; these are polish, not blockers.)*

---

## 2. Current Week
**Active tasks for the week. Maximum of 4 items.**

> **Tip:** If your team frequently handles urgent issues, consider reserving 1-2 slots for hotfixes. Otherwise, use all 4 slots for planned work.

- [x] **relay-automation Phases 1–5 shipped** — packaged as a sibling skill (`skill/relay-automation/`), `validate.sh` **20/20**.
- [x] **Cross-model Claude↔Codex + Option-A headless** — `codex-turn.sh` drives a Codex turn via `codex exec` behind a path-allowlist (no push); live-proven.
- [x] **First hands-free dogfood + self-expiring loops** — all-Claude relay closed in 2 rounds, 0 turn-nudges; `poll.sh --deadline` + self-delete documented.
- [x] **Manual relay round with Gemini** — done; the portable `/relay` worked with a *third* model (embedded instructions + nudge). Gemini hardened `codex-turn.sh`: found + cleared 2 real bypasses (commit-bypass, quoted-path), Approved r3. Containment recorded in [decisions/2026-06-15-unattended-agent-containment.md](../../decisions/2026-06-15-unattended-agent-containment.md).
- [x] **`gemini-turn.sh` + shared safety core** — Gemini CLI installed/authed (0.46.0, GCA); refactored the containment into a shared `relay-turn-lib.sh` sourced by both `codex-turn.sh` + `gemini-turn.sh`. **Live-validated**: a real `gemini -p` turn reviewed a seeded artifact, stayed in lane, committed file-scoped, no push. `validate.sh` **21/21**. **Reconciled** with Gemini's own standalone draft (`fe0bd61`): kept its name/convention + the shared-core architecture, and fixed its `gemini exec` invocation (no such subcommand — headless is `-p`). *(Two model-CLIs now take headless turns behind one boundary.)*

---

## 3. Previous Week
**Review completed, deferred, or blocked tasks from the prior week.**

- [x] **Run 4 (meta-exercise)** — balanced 2-lane swarm built the relay-automation Phase-1 slice; **72% work-bounded concurrency** (cleared ≥50%) → **graduated to Phase 2**. Decision recorded.
- [x] **Run 5 (Phase-2 build)** — watchdog ‖ runner built in parallel; 39% concurrency, root-caused to **start-skew** (not lane design); operator accepted the datapoint.
- [x] **relay-automation Phases 1–5 + (a) tick-native relay turns** — full build, Codex-relay-reviewed at each step; `validate.sh` 12 → **20**.
- [x] **Cross-model + Option-A** — Codex CLI installed → headless spike passed → `codex-turn.sh` shim + live Claude↔Codex relay turn.

---

## 4. Recent Lessons Learned
**Capture insights to improve processes and avoid repeating mistakes.**

1. **Simultaneous start dominates the concurrency metric, not lane design.** Same balanced design hit 72% (Run 4, ~2s start-skew) and 39% (Run 5, ~116s skew). Balance is necessary but not sufficient — enforce start-together (launch-sync / automated launch) before reading the number.
2. **Relay reviews catch real bugs — including the reviewer's own.** Codex caught a `relay-drive.sh` close-mismatch (Approved-without-`done` leaked a live token); the live dogfood caught a `poll.sh` empty-`--claude-agents` crash tests never hit. Independent + live passes find what unit tests miss.
3. **`codex exec` makes Option A real — but cap it.** Headless Codex turns work (authed, parseable verdict), but a trivial turn is ~11k tokens and an autonomous agent needs a hard **path-allowlist + no-push** boundary (the `codex-turn.sh` shim) and a round/budget cap.
4. **Self-closing automation > housekeeping.** Per-session cron loops can't be stopped cross-window — give every loop a `--deadline` + self-delete, or they linger. And don't embed base64 in markdown (linters corrupt it) — ship a tarball sidecar.

---

## Bonus: Project Scope Outline

> **Note:** This section is a work-in-progress and is being developed as a natural extension of the 4x4 methodology. It is not yet a core part of the framework but is included here as a supplementary tool for teams who want to add more context to their planning.

### 1. Goals
Prove that 3 heterogeneous AI agents (Claude Code, Codex, Gemini) can work one codebase concurrently with zero merge conflicts, zero file collisions, and measurable, sustained parallelism (≥50% concurrent-claim time). Mechanical correctness is achieved; the remaining goal is demonstrated load-balanced concurrency, then a Phase 2 graduation into a real integration.

### 2. Assumptions
- All coordinating agents share one branch (`.tick/` is branch-scoped; cross-branch is Phase 2).
- Agents honestly declare paths — `tick` measures drift but does not yet enforce it.
- Node standard library only; `package.json` is shared and out of scope for every task.
- Time estimate: Run 4 (design + implement + retest + write-up) ≈ 1 focused session (low) to 1 week (high) depending on whether work-stealing or a new fixture is chosen.

### 3. Potential Risks
- **Work-stealing complexity** could reintroduce claim races the 2-claim cap currently prevents.
- **No enforcement** means a real run could drift outside declared paths and only be caught post-hoc.
- **Worktree/branch friction** remains an adoption risk if Phase 2 needs agents on separate branches.
- **Metric chasing** — over-tuning the fixture to clear ≥50% without proving generalizable coordination.

### 4. Long-Term Maintainability
CLI is ~600 lines of JS; correctness concentrates in the projection logic, which is the easy place to break silently — keep the bash test suite (~400 lines) green on every change. Auto-push/local-transport contract and the disjoint-files-per-event model are the load-bearing invariants; document any change to them in RECAP.md. The auditor (`tick analyze`) is the durable artifact reusable across all future runs.

---

## How to Use This Template

For detailed guidance on the 4x4 framework, see the [README](../../README.md).

**Quick tips:**
- Update this document weekly (move "Current Week" to "Previous Week" and plan your new week)
- Limit each section to 4 items maximum to maintain focus
- Capture lessons learned immediately while they're fresh
- Link to detailed tasks in your project management tools (GitHub, Jira, etc.)

---

## License
This work is licensed under the Creative Commons Attribution 4.0 International (CC BY 4.0) License. To view a copy of this license, visit [https://creativecommons.org/licenses/by/4.0/](https://creativecommons.org/licenses/by/4.0/).

Copyright © 2026 Hypercart DBA Neochrome, Inc. | 4x4Clarity.com
