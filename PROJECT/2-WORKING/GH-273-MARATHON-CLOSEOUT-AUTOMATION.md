---
gh_issue: 273
source: https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/273
title: "Automate the marathon pre-flight/post-flight ceremony — /pre-marathon, /post-marathon + post-approve-cmd hook"
status: "Phases 0-1 SHIPPED 2026-07-21 (skill-nudge hook, /pre-marathon + /post-marathon); Phases 2-4 not yet contracted"
created: 2026-07-21
updated: 2026-07-21
owner: noel
doc_type: project
reversibility: "Easy — Phases 1-3 are new command/manifest/script files, additive and independently revertable. Phase 4 touches marathon_drive.py to add one new optional flag (--post-approve-cmd, symmetric to the existing --pre-advance-cmd) — no existing behavior changes unless the flag is passed."
complexity: 3
risk: 2
effort: 5
phases: 5
ratings_provisional: true
related:
  - "PROJECT/1-INBOX/LESS-TYPING.md — source analysis (Fable 5 web app) this doc formalizes"
  - "skills/marathon-triage/SKILL.md — pre-marathon reconciliation/ranking/wave-forming; /pre-marathon wraps it, does not replace it"
  - "skills/marathon-cleanup/SKILL.md — post-hoc archive-only sweep; complementary, not overlapping (runs after /post-marathon, not instead of it)"
  - "skills/loose-ends/SKILL.md — generic commit/push/doc-sync; /post-marathon's final step delegates to it via a per-repo manifest (Phase 2)"
  - "utils/py/marathon_drive.py — already exposes --pre-advance-cmd; Phase 4 adds the symmetric --post-approve-cmd"
  - "relay-automation/hooks/xyz-vendor-reminder.sh — the fail-open, non-blocking hook template Phase 0's skill-nudge hook follows (stdin-discard/opt-out-env/exit-0-always pattern)"
  - ".claude/settings.json — already wires 2 PreToolUse + 1 SessionStart hook to relay-automation/hooks/*.sh; Phase 0 adds a UserPromptSubmit entry to the same file"
non_goals:
  - Rebuilding marathon-triage's reconciliation/ranking/preflight logic inside a new /pre-marathon
    command — that skill already exists and works; /pre-marathon calls it.
  - Bypassing marathon-triage's explicit no-fire safety boundary. /pre-marathon still requires an
    operator-confirmed dry-run before firing.
  - Shipping the Phase 4 harness flag before Phases 1-3 have proven the sequence out manually —
    kernel-adjacent (marathon_drive.py) changes come last, after lower-risk validation.
  - Making the Phase 0 nudge hook global (~/.claude/settings.json) on day one — scoped to this
    repo's project-local .claude/settings.json first, matching Phase 1's .claude/commands/ scope;
    promoting it globally is a deliberate future call, not a default.
  - A SessionStart-style once-per-session nudge — considered and rejected in favor of a
    UserPromptSubmit per-prompt nudge, which is contextual to what the operator is about to type
    rather than a generic reminder that goes stale after the first screen of a session.
goal: >
  Formalize a Fable 5 analysis of ~583 logged Claude Code prompts, corroborated against the raw
  corpus, into a 5-phase plan that kills two heavily repeated manual prompt sequences: the
  pre-marathon ceremony (capture → add to plan → preflight sweep → cleanup → dry-run → fire) and
  the post-marathon ceremony (commit/push → PR → merge → pull/switch → close issues → move docs →
  PDDA sweep → optional Slack blurb) — plus a Phase 0 prompt-time nudge so the operator notices
  these skills exist before hand-typing what they already automate.
---

# GH-273 · Automate the marathon pre-flight/post-flight ceremony

## Status

| What was just completed | What's next |
|---|---|
| Plan drafted 2026-07-21: reviewed the Fable 5 web app's analysis of `0. Claude Prompts.md` (583 logged prompts across 14 repos, generated by CLIO), verified its repetition claims directly against the corpus (`/usr/bin/grep -ic`: 78 commit+push mentions, 12+ explicit "add to marathon" phrasings, 8 "move to 3-COMPLETED", 6 "close issue", 5 "PDDA sweep", 5 "dry-run", 22 "slack" — out of 583 entries), and cross-checked the recommendation against this repo's existing skill inventory (`marathon-triage`, `marathon-cleanup`, `loose-ends`, `marathon_drive.py --pre-advance-cmd`) to find real overlap before proposing new surface. Filed [#273](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/273). **2026-07-21, same day:** added Phase 0 (a `UserPromptSubmit` skill-nudge hook) ahead of the original 4 phases, so the plan itself gets surfaced by the mechanism it's building, not just documented; redrafted the preflight contract to target Phase 0. **Phase 0 SHIPPED same day** — fired via `swarm-preflight → marathon-drive` (Codex builder, agy reviewer): `relay-automation/hooks/skill-nudge.sh` + 14 new `test/xyz-harness-hooks.sh` cases, all passing, `.claude/settings.json` wired. The `bash validate.sh` pre-advance gate false-failed on an unrelated pre-existing flaky assertion (`test/xyz-harness-hooks.sh`'s "relay green count", untouched by this build); confirmed flaky not regressive by an isolated re-run (61/61 pass) and a full clean `validate.sh` re-run (exit 0). Re-firing `marathon-drive` for the same phase hit a known limitation (the `MARATHON-P1-TURN` tick token was already `done`, so it couldn't reopen — see `phases/p1/ESCALATION.md` § Resolution); confirmed and closed out manually instead of re-driving. **Phase 1 SHIPPED same day** — `--phase-id gh273-phase1` (a distinct id per fire, learned from Phase 0's collision — see [#274](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/274)): `.claude/commands/pre-marathon.md` + `.claude/commands/post-marathon.md`, Codex builder/agy reviewer Approved. Same flaky pre-advance gate false-failure as Phase 0 (`test/xyz-harness-hooks.sh`, confirmed unrelated and clean on isolated + full re-runs); closed out manually rather than risk a third `marathon-drive` invocation against the now-`done` token. Full account: `phases/gh273-phase1/ESCALATION.md`. | Redraft the preflight contract for Phase 2 (`loose-ends-sequence.md` manifest) and fire it with its own `--phase-id`. |

## Table of contents

- [Background](#background)
- [Verdict on the source analysis](#verdict-on-the-source-analysis)
- [Phase 0 — prompt-time skill-nudge hook](#phase-0--prompt-time-skill-nudge-hook)
- [Phase 1 — `/pre-marathon` and `/post-marathon` slash commands](#phase-1--pre-marathon-and-post-marathon-slash-commands)
- [Phase 2 — per-repo `loose-ends-sequence.md` manifest](#phase-2--per-repo-loose-ends-sequencemd-manifest)
- [Phase 3 — extract `marathon-closeout.sh`](#phase-3--extract-marathon-closeoutsh)
- [Phase 4 — `--post-approve-cmd` harness hook](#phase-4----post-approve-cmd-harness-hook)
- [Swarm Preflight Contract](#swarm-preflight-contract)

## Background

`PROJECT/1-INBOX/LESS-TYPING.md` captured a Fable 5 web app's read of the operator's Claude Code
prompt log (`~/Documents/Noel Saw/0. Claude Prompts.md`, a CLIO-generated log, 583 `##` entries
across 14 repos as of 2026-07-21) and proposed a 4-step plan to eliminate two repeated manual
ceremonies. This doc verifies that analysis against the raw corpus and this repo's actual tooling,
then turns the surviving recommendation into an executable PDDA plan per the operator's request.

## Verdict on the source analysis

**Concur, with two corrections.**

The repetition claim is real, not sampling noise. Direct corpus counts (583 total `##` entries):
78 lines mention commit+push together, 12+ prompts explicitly say "add ... to marathon" in
distinct phrasings ("add to an upcoming marathon", "add to a marathon file", "add this as a
project to available marathon file", "add the plan into a marathon plan to fire later"), 8 mention
moving docs to `3-COMPLETED`, 6 mention closing GH issues, 5 mention a PDDA sweep, 5 mention
`dry-run`, 22 mention Slack. The staged effort ordering (slash commands today → per-repo manifest
this week → script extraction next → harness integration later) is sound: it validates the
sequence cheaply before touching `marathon_drive.py`.

**Correction 1 — the pre-marathon half overlaps with `marathon-triage`, don't rebuild it.**
`skills/marathon-triage/SKILL.md` already reconciles capture docs against live issue state,
validates preflight contracts, ranks candidates, and forms collision-safe waves — the bulk of the
"pre-marathon" sequence in the log. It also *explicitly refuses to fire a marathon* ("Never execute
the marathon from this skill") — a deliberate safety boundary, not a gap to route around. So
`/pre-marathon` should be a thin wrapper: invoke `marathon-triage`, add the one thing it doesn't do
(sweep stale phase dirs / `ESCALATION.md` leftovers), then a separate confirmed dry-run + fire step.
A from-scratch `/pre-marathon` would duplicate real, working logic.

**Correction 2 — the post-marathon half is a genuine, unclaimed gap.** `skills/marathon-cleanup/SKILL.md`
is archive-only: report-before-mutation, and its scope is reconciling/archiving already-verified-complete
marathon docs — it does not commit, push, open a PR, merge, or close issues. `skills/loose-ends/SKILL.md`
covers generic commit/push/doc-sync but not the marathon-specific PR→merge→pull→close-issues→PDDA-sweep
chain. Nothing in this repo currently automates that chain. This is the higher-value half of the
recommendation and the one to prioritize.

The phased plan below keeps the Fable analysis's structure but reassigns Phase 1's `/pre-marathon`
to wrap existing tooling instead of reimplementing it, and keeps Phase 4 (the harness flag) last,
consistent with the analysis's own effort ordering and this repo's precedent of not touching
`marathon_drive.py` before an idea is validated at the command/script layer.

## Phase 0 — prompt-time skill-nudge hook

**Origin:** raised in-session (2026-07-21) after this doc's own verdict — the operator hand-types
marathon-lifecycle ceremony because they forget `marathon-triage`/`marathon-cleanup`/`loose-ends`
already exist. A nudge at the moment the operator is about to type the ceremony closes that gap
directly, ahead of building the ceremony automation itself.

**Mechanism — `UserPromptSubmit`, not `SessionStart`.** This repo's `.claude/settings.json` already
wires `SessionStart` → `relay-automation/hooks/xyz-vendor-reminder.sh` (a once-per-session reminder)
and two `PreToolUse` guards. A once-per-session nudge goes stale after the first screen of a long
session; a `UserPromptSubmit` hook fires on every prompt and can match the *specific* thing the
operator is about to ask for, so it's the better fit here (see `non_goals`).

**Design, following the `xyz-vendor-reminder.sh` template (same fail-open contract):**

- New `relay-automation/hooks/skill-nudge.sh` — reads the `UserPromptSubmit` event JSON on stdin
  (unlike `xyz-vendor-reminder.sh`, this hook needs the `prompt` field, not just a discard), matches
  the prompt text against a small, explicit keyword → skill table scoped **only** to marathon
  lifecycle phrasing (to avoid nagging on unrelated prompts):
  - "add ... to marathon" / "add to the marathon" / "fire the marathon" → `marathon-triage`
  - "preflight sweep" / "preflight all" / "dry-run each plan" → `marathon-triage`
  - "commit and push" + ("close ... issues" / "move ... to 3-completed" / "PDDA sweep") in the same
    prompt → `loose-ends`, `marathon-cleanup`
  - no match → silent, exit 0 (never nag on an unrelated prompt)
- On a match: exit 0, emit `UserPromptSubmit` JSON with `hookSpecificOutput.additionalContext` set to
  one line — `"BTW: <skill> already does this — see skills/<skill>/SKILL.md."` Never blocks the
  prompt, never asks-then-acts (same non-blocking contract as `xyz-vendor-reminder.sh`).
  Opt out with `XYZ_NO_SKILL_NUDGE=1`, same pattern as `XYZ_NO_VENDOR_REMINDER=1`.
- Wire it into `.claude/settings.json`'s existing `hooks` object as a new `UserPromptSubmit` array
  entry pointing at `relay-automation/hooks/skill-nudge.sh` — additive, does not touch the existing
  `PreToolUse`/`SessionStart` entries.
- Keyword table and match rationale live **in this file** (above) and in the script's header comment
  — searchable in both places, not just tribal knowledge in a shell script.

**QA gate:** `bash -n` clean; a manual replay of ~10 real prompts from `0. Claude Prompts.md` (5
marathon-lifecycle, 5 unrelated) fires the nudge only on the 5 matching ones; `test/xyz-harness-hooks.sh`
gains a case for it; `XYZ_NO_SKILL_NUDGE=1` suppresses it; the hook never blocks prompt submission
(fail-open on any parse error, same as the template).

## Phase 1 — `/pre-marathon` and `/post-marathon` slash commands

- `.claude/commands/pre-marathon.md`: invoke `marathon-triage` for the ranked/preflight-checked
  candidate list, then sweep `PROJECT/2-WORKING` for stale phase dirs and `ESCALATION.md` leftovers
  from prior runs, then `--dry-run` each ready plan and report execution order. Fire only after
  explicit operator confirmation — do not change `marathon-triage`'s no-fire boundary.
- `.claude/commands/post-marathon.md`: commit and push all changed files including manual edits →
  create a PR with notes → merge if green → pull and switch back to `development` → close every GH
  issue resolved by the run → move completed docs to `PROJECT/3-COMPLETED` → run a full PDDA sweep
  → end with `/loose-ends`.

**QA gate:** both command files exist under `.claude/commands/`, dry-run against a real recent
marathon plan and a real recent closeout produces the same sequence of actions the operator
previously typed by hand (spot-check against 3 log entries), and `/pre-marathon` visibly delegates
to `marathon-triage` rather than reimplementing its reconciliation logic.

## Phase 2 — per-repo `loose-ends-sequence.md` manifest

Encode the repo-specific tail (PDDA sweep command, doc folders, gitignore rules) so
`/post-marathon`'s final `/loose-ends` call runs the correct sequence per repo instead of the
operator re-specifying it. Start with this repo (`xyz-3-agents-swarm`) as the reference manifest.

**QA gate:** a `loose-ends-sequence.md` exists for this repo, `/post-marathon` reads it instead of a
hardcoded sequence, and running it against a real closeout matches what the operator's manual
`/loose-ends` invocation previously did.

## Phase 3 — extract `marathon-closeout.sh`

Pull the zero-judgment git ceremony (commit → push → PR → merge → pull → switch) out of the
`/post-marathon` prompt into `marathon-closeout.sh` beside the harness (`utils/` or
`relay-automation/`, matching existing script placement conventions), so it is one command and
usable headless. `/post-marathon` shrinks to "run `marathon-closeout.sh`, then close issues / file
docs / PDDA sweep via the Phase 2 manifest."

**QA gate:** `marathon-closeout.sh` runs standalone (no Claude session) against a real merged PR and
produces the same end state (pulled, on `development`, PR merged) as the manual sequence; a
regression test covers its exit codes.

## Phase 4 — `--post-approve-cmd` harness hook

Wire `marathon-closeout.sh` as an optional `--post-approve-cmd` in `utils/py/marathon_drive.py`,
symmetric to the existing `--pre-advance-cmd` (`marathon_drive.py:58`). When set, a driven marathon
runs the closeout script automatically after the final phase's gate passes, so an interactive
session can start at "review the PR" instead of "do the ceremony."

**QA gate:** `--post-approve-cmd` is documented alongside `--pre-advance-cmd` in
`marathon_drive.py --help`, defaults to unset (no behavior change for existing callers), and a
`test/marathon-drive.sh` case exercises it end-to-end against a stub closeout script.

## Swarm Preflight Contract

Scoped to Phase 1 only (two new `.claude/commands/*.md` prompt files; no scripts or harness code
touched — building the commands is just authoring prompts, not executing their ceremony). Phase 0
SHIPPED 2026-07-21 (see Status table + CHANGELOG). Phases 2-4 each need their own contract redraft
once the phase before them ships — same pattern as `GH-268`.

```json
{
  "target": { "repo": ".", "ref": "development" },
  "gate": "bash validate.sh",
  "fix_probes": [
    { "type": "path_absent", "path": ".claude/commands/pre-marathon.md" },
    { "type": "path_absent", "path": ".claude/commands/post-marathon.md" }
  ],
  "artifacts": [ ".claude/commands/pre-marathon.md", ".claude/commands/post-marathon.md" ],
  "artifacts_new": [ ".claude/commands/pre-marathon.md", ".claude/commands/post-marathon.md" ],
  "remediation": {
    "source": "issue#273",
    "criteria": "Phase 1 only: .claude/commands/pre-marathon.md invokes the marathon-triage skill for reconciliation/ranking/waves rather than reimplementing it, additionally sweeps PROJECT/2-WORKING and phases/*/ for stale phase dirs and orphaned ESCALATION.md files, dry-runs each ready plan, and requires explicit operator confirmation before firing (never bypasses marathon-triage's no-fire boundary). .claude/commands/post-marathon.md sequences: commit+push all changed files including manual edits -> open a PR with notes -> merge if green -> pull and switch back to development -> close every GH issue resolved by the run -> move completed docs to PROJECT/3-COMPLETED -> run a full PDDA sweep (utils/pdda/pdda.sh run) -> end by invoking /loose-ends. Both files are plain markdown prompt bodies (no new scripts); this doc's Phase 1 section is the source of truth for their exact step sequence."
  },
  "lanes": { "agy_safe": [ ".claude/commands/pre-marathon.md", ".claude/commands/post-marathon.md" ], "orchestrator_only": [] }
}
```
