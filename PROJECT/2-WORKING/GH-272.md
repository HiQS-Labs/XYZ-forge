Reported from: sleuth-app (vendored `.xyz/` install, same-repo review lane, no `--target-root`)
Harness commit: 70640ca

Drove a headless Codex review (`relay-drive.sh --review-once`) of a local diff. Twice in a row (first with the shim's default 300s turn timeout, then again with `RELAY_TURN_TIMEOUT_S=900`), Codex's own transcript shows it completing a full, real review — findings written, verdict set, then executing `tick release <task> --agent codex --to claude-a` inside its isolated worktree, followed by "handing the token to the producer window."

But outside the worktree, the real relay file stayed at its empty scaffold and `tick info <task>` still showed `status: open, handoff-to: codex` — neither attempt's work ever landed. The worktree's own `tick release` invocation logged `TICK_REPO_ROOT="<repo>/.xyz"`, but `find-harness.sh --env` (run from the caller) exports `TICK_REPO_ROOT=<repo>` (no `.xyz` suffix) — two different tick event-log namespaces, so the token state the turn updates is never the one being watched.

**Expected:** the turn's `tick release`/commit should land in the same namespace/branch the driving session is watching.
**Observed:** exit 7 (timeout) then exit 3 ("genuine stall — reviewer took no action"), despite both attempts producing real, substantive review content that simply never committed.
**Frequency:** every time so far (2/2), for this specific same-repo vendored-`.xyz` lane; not yet tested against a centralized-harness or `--target-root` setup.
**Blocking:** no — recovered by reading the transcript log directly instead of re-driving a third time.