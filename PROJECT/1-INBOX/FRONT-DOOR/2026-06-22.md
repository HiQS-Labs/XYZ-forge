---
title: Front-door remediation + relay-xyz adherence — unified plan (2026-06-22)
slug: front-door-remediation
status: Proposed (1-INBOX — not yet active)
created: 2026-06-22
updated: 2026-06-22
owner: Noel (operator) · Claude (producer)
related:
  - FRONTDOOR.md                                       # the live deterministic dashboard this plan drives to green
  - PROJECT/2-WORKING/GH-11-CROSS-REPO-TARGETING.md    # Asks 2–5 overlap Phase 3
  - PROJECT/1-INBOX/FEEDBACK/FEEDBACK-OPUS-MAX.md       # parallel Opus-Max session: PDDA hardening (@2610e45) + relay-xyz adherence (folded into Phase 4)
  - skills/relay-xyz/SKILL.md                          # Phase 4 hoists find-harness --check to the first body line
  - ROUTER.md
non_goals:
  - Not restructuring docs/ navigation — the structure is sound; this is drift, not architecture.
  - Not (re)building any relay-xyz infra — `find-harness.sh`, `--target-root`, `CONSULT_ROOT`, `install.sh` already EXIST (confirmed by both sessions); only docs + adherence remain.
  - No code/kernel changes — every item here is a doc edit.
goal: >
  Drive FRONTDOOR.md to all-green AND close the relay-xyz agent-adherence gap a parallel Opus-Max
  session surfaced: fix the onboarding drift (stale test counts, dead README links, the CLAUDE.md
  phantom-path block), surface the shipped cross-repo features, and make the relay-xyz harness
  un-missable (hoist `find-harness.sh --check` to the top of SKILL.md) with correct-tool guidance
  (`consult.sh` for one-shot reads vs `--target-root` for turn-based relays). Doc-only; a cold clone
  (human or agent) reaches working AND uses the harness as designed.
---

## Status

| What was just completed | What's next |
|---|---|
| **Audit + unification done** — front-door walk → [FRONTDOOR.md](../../../FRONTDOOR.md) (⚠️ Bumpy; secrets clean; ROUTER is the one canonical entry), now **folded with a parallel Opus-Max session** (relay-xyz infra is complete → the friction is agent-adherence; its PDDA acting-layer hardening already shipped @ `2610e45`). 12 findings, each with a deterministic check. | **Phase 1 — quick-win drift fixes** (stale test counts in 3 docs + 2 dead README links). |

> **Header note:** this table now uses the canonical `What was just completed | What's next`. Folding in
> the parallel session means honoring its PDDA hardening (`2610e45`), which **deleted the "Most recently
> completed phase" alias** — so the earlier-requested header would now `error` on promotion to `2-WORKING`.

## Table of Contents

- [Audit summary (both sessions)](#audit-summary-both-sessions)
- [Phase 1 — Quick-win drift fixes](#phase-1--quick-win-drift-fixes)
- [Phase 2 — Repair the agent front door (CLAUDE.md)](#phase-2--repair-the-agent-front-door-claudemd)
- [Phase 3 — Surface the recent features](#phase-3--surface-the-recent-features)
- [Phase 4 — relay-xyz adherence (parallel Opus-Max session)](#phase-4--relay-xyz-adherence-parallel-opus-max-session)

## Audit summary (both sessions)

A cold clone of HEAD **does** reach working (clone → `ROUTER.md` → `./validate.sh` green, no accounts or
keys); the residual friction is cheap doc drift — no architecture, no secrets (scan clean). A **parallel
Opus-Max session** on the same repo independently confirmed the relay-xyz half: the infra is **complete**
— `find-harness.sh` (device-agnostic locator: `--root`/`--env`/`--check`, symlink-safe), `--target-root`
(in `relay-drive.sh`), `CONSULT_ROOT` (in `consult.sh`), and `install.sh` all exist, and SKILL.md *does*
reference the locator. So **nothing is missing; the friction is agent-adherence** — agents skim SKILL.md
and skip the "run `find-harness.sh` first" step. That session also shipped PDDA acting-layer hardening
(`2610e45`: the only destructive stale-move → flag-only; LLM findings clamped `warn`-max; status-header
alias removed). Phases 1–3 fix the front-door drift; **Phase 4 folds in that session's relay-xyz
adherence fixes.** Live status + re-runnable checks: [FRONTDOOR.md](../../../FRONTDOOR.md). Phases ordered
quick-wins-first.

## Phase 1 — Quick-win drift fixes

*Minutes; highest friction-removed-per-effort. Doc edits only.*

- [ ] **FD-02** — `README.md` test count `28` → `36` (two spots: the "👉 New here?" callout and "Current status"). *Observable:* `grep -cE '28 ?/ ?28' README.md` returns `0`.
- [ ] **FD-03** — `AGENTS.md` test count `12/12` → `36` (the "`validate.sh` is the gate" bullet). *Observable:* `grep -c '12/12' AGENTS.md` returns `0`.
- [ ] **FD-04** — `ROADMAP.md` status-table `33/33` → `36/36`. *Observable:* `grep -c '33/33' ROADMAP.md` returns `0`.
- [ ] **FD-05** — `README.md:28` link: repoint `PROPOSAL-AUTOMATION.md` to `PROJECT/1-INBOX/EXP-AUTOMATION/…` (its real home), or move the file. *Observable:* the linked path passes `test -e`.
- [ ] **FD-06** — `README.md:33` `skill/relay-automation/` → `skills/relay-automation/` (plural). *Observable:* `grep -c 'skill/relay-automation' README.md` returns `0`.

### QA checklist — Phase 1

- [ ] No stale count remains anywhere on the front door: `grep -rE '12/12|28 ?/ ?28|33/33' README.md AGENTS.md ROADMAP.md` prints nothing.
- [ ] Every relative link in `README.md` resolves (no 404) — re-run the FD-05 check in FRONTDOOR.md.
- [ ] `FRONTDOOR.md` rows FD-02…FD-06 flipped ⬜ → ✅ after re-running their checks.
- [ ] Doc-only: `git diff --stat` touches no file under `src/`, `bin/`, `relay-automation/`, or `test/`; `./validate.sh` still `36/36`.

## Phase 2 — Repair the agent front door (CLAUDE.md)

*The single worst step for the ~75% who clone with an agent. One decision gates it.*

- [ ] **Decide (operator):** is root `CLAUDE.md` meant for THIS repo, or a different-repo orchestrator file that leaked in? It instructs a fresh agent to `cat experiments/coordination-layer/*` and `git pull origin experiment/coordination-layer` — neither exists here.
- [ ] **FD-01 (if for this repo)** — replace the five phantom-path `cat`s + the phantom-branch `git pull` with "read `ROUTER.md` first." *Observable:* every path `CLAUDE.md` tells an agent to read passes `test -e`.
- [ ] **FD-01 (if NOT for this repo)** — gate or remove it so a cold agent isn't dropped into phantom paths (e.g. move the orchestrator content out of the auto-loaded root file).

### QA checklist — Phase 2

- [ ] Every `cat ./…` / required-reading path in `CLAUDE.md` exists (`test -e` on each → all pass).
- [ ] No instruction references a branch the repo doesn't have (no `experiment/coordination-layer`).
- [ ] A dry read-through of `CLAUDE.md` lands a fresh agent on `ROUTER.md` with zero failed `cat`s.
- [ ] `FRONTDOOR.md` FD-01 flipped ⬜ → ✅; the "Agent front door" dimension flips 🚧 → ✅.

## Phase 3 — Surface the recent features

*Doc-only; overlaps GH-11 Asks 2–5 (same edits satisfy both).*

- [ ] **FD-07** — add cross-repo recipes to `skills/relay-xyz/SKILL.md` (+ `relay-automation/QUICKSTART.md`), **distinguishing the two tools** (parallel session Q1): one-shot foreign-repo **read** → `CONSULT_ROOT=<repo> relay-automation/consult.sh --models codex` (the right tool — Path A would snapshot the *harness* HEAD, not the target, so the files under review aren't even in the worktree); turn-based foreign-repo **relay** (edits/rounds) → `relay-automation/relay-drive.sh --target-root <repo>`. *Observable:* `grep -rlE 'target-root|CONSULT_ROOT' skills/relay-xyz/SKILL.md` returns ≥1 hit.
- [ ] **FD-08** — add a "make the skill discoverable: `bash skills/relay-xyz/install.sh`" note where a newcomer sees it (`README.md` or `ROUTER.md`). *Observable:* `grep -rl install.sh README.md ROUTER.md` returns ≥1 hit.
- [ ] **FD-09** — update `AGENTS.md` "One skill ships here / `skill/xyz`" to the real `skills/` inventory. *Observable:* `grep -c 'One skill ships here' AGENTS.md` returns `0`.
- [ ] **FD-10** — add an "agent users: run un-sandboxed (keychain + outbound egress)" callout to `README.md`. *Observable:* `grep -ci sandbox README.md` returns ≥1.

### QA checklist — Phase 3

- [ ] `--target-root` *and* `CONSULT_ROOT` are discoverable from at least one prose doc, not only `--help` — and the recipe says which is for reads vs relays.
- [ ] `install.sh` is reachable from the front door — the skill chicken-and-egg is gone.
- [ ] `AGENTS.md`'s skill claim matches reality (`ls skills/` vs the doc).
- [ ] `FRONTDOOR.md` FD-07…FD-10 flipped ⬜ → ✅.
- [ ] If these landed via GH-11, its ledger line + `GH-11` doc reflect Asks 2–5 done.

## Phase 4 — relay-xyz adherence (parallel Opus-Max session)

*That session's core finding: the relay-xyz infra is **complete** — the friction is **agent-adherence**
(agents skim SKILL.md and skip `find-harness.sh`). The breadcrumb instinct is right, but it has to live
in a channel Claude Code auto-loads and be a portable pointer, never a cached path. Doc-only; highest
leverage first.*

- [ ] **FD-11 (highest leverage, ~free)** — hoist `bash skills/relay-xyz/find-harness.sh --check` to the **first imperative line of the SKILL.md body**, as a hard gate: *"ALWAYS run this first — never claim the harness is missing without running it."* (Today the load-bearing command sits deep under ~50 lines of "when to use" prose; a skimming agent never reaches it.) *Observable:* `find-harness.sh --check` appears within the first ~25 lines of `skills/relay-xyz/SKILL.md`.
- [ ] **FD-12 (per-repo persistence — right shape only)** — document in SKILL.md that a per-repo breadcrumb must use a channel Claude Code **auto-loads** — the target repo's **memory** (seeded on first successful run) or its **`CLAUDE.md` by skill name** — and be a **portable pointer** ("run `find-harness.sh`" / the skill name), **never a cached absolute path or a bare root file** (not auto-loaded, so the skimming agent skips it too; machine-specific; a stale cached path is *worse* than none; cross-repo blast radius; Costly to reverse). *Observable:* SKILL.md carries a "per-repo persistence" note, and **no** root pointer file is auto-installed into any target repo.

### QA checklist — Phase 4

- [ ] A skimming agent meets `find-harness.sh --check` as the **first** actionable SKILL.md step (read the top ~10 body lines and confirm).
- [ ] SKILL.md's cross-repo guidance distinguishes **one-shot read → `CONSULT_ROOT` + `consult.sh`** from **turn-based relay → `relay-drive.sh --target-root`** (the parallel Q1: Path A is the wrong tool for a read).
- [ ] No bare pointer-file is auto-installed into any target repo; the persistence pattern is memory / `CLAUDE.md`-by-name only (portable pointer, no absolute path).
- [ ] `FRONTDOOR.md` FD-11/FD-12 added + flipped ⬜ → ✅; the "relay-xyz adherence" dimension flips to ✅.
