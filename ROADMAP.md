---
title: Combined Roadmap — Cost-Observed Marathon Loops + Adversarial Hardening
status: Active
created: 2026-06-16
updated: 2026-07-08
branch: main
supersedes: PROJECT/2-WORKING/ROADMAP-COMBINED.md (promoted to canonical 2026-06-17); folds in the former standalone ROADMAP.md (adversarial-hardening track, now Part B)
synthesizes:
  - PROJECT/1-INBOX/LOOPS.md
  - PROJECT/4-MISC/COST-OBSERVABILITY-PLAN.md
  - PROJECT/1-INBOX/MARATHON.md
goal: >
  Canonical pointer/ledger index for the repo's work — queued intake, projects in progress,
  completed, attempted, and deferred — linking to the canonical PROJECT/** docs that own the
  execution detail. This is an index, not a plan body.
---

<!-- PDDA ROADMAP CONTRACT — this file is a POINTER/LEDGER, not a plan body.
     Allowed: queued intake / projects in progress / completed / attempted / deferred + links to PROJECT/** docs.
     NOT allowed: phase checklists, build steps, deep execution notes — put those in the project doc.
     Carve-out: a SHORT exception note is OK only when omitting it would hide an operationally critical fact.
     Coverage rule: every PROJECT/2-WORKING doc must be reflected here by a pointer (or opt out with roadmap_exempt: true).
     Enforced by `utils/pdda/pdda.sh roadmap` + `utils/pdda/pdda.sh roadmap-coverage` (deterministic) + `utils/pdda/pdda.sh doc-ready` ROADMAP rubric (LLM). -->

# Combined Roadmap: Cost-Observed Marathon Loops + Adversarial Hardening

> **Pointer/ledger only — not a plan body.** Execution detail (phase checklists, build steps, QA
> gates, deep notes) lives in the linked `PROJECT/**` docs; keep it there. See the contract banner above.

Three tracks, sequenced independently:

- **Part A — Marathon:** cost observability (done) → headless multi-phase chaining (done) → real-monolith dogfood (active)
- **Part B — Adversarial Hardening:** epoch fencing (done) → chaos suite → cross-repo E2E → reference deploy
- **Part C — Autonomous Self-Improvement:** the gated LOOPS.md endgame

## Status

| What was just completed | What's next |
|---|---|
| **2026-07-04 marathon Wave 2+3, reviewed and merged (PR #125, `d9db49d`):** **GH-55** auto-includes covering tests/helpers in preflight allowlists, **GH-54** forbids in-turn execution of fs-touching tests, and **GH-48** generalized `marathon-plan`'s zone model with a real rebalance-OS live check (#54/#55 closed; #48 stays open pending an operator closure call). Targeted gates are green (`test/swarm-preflight.sh` 75/75, `test/marathon-plan.sh` 57/57); the full `validate.sh` rerun isn't blocked by this wave's files — non-green runs traced to pre-existing `oracle-guard.sh`/`improve-loop-qa.sh` flakiness, not `relay-self-sufficiency.sh`. Independent review before merge filed two non-blocking follow-ups: **GH-126**, **GH-127**. Also closed 2026-07-04: **#113**/**#114** (already fixed in code, left open past landing) and **GH-112** retitled to reflect its spike-to-full-rewrite scope creep (PR #121, now targeting a new `development` branch, not `main`). With Plan B and the blend plan both fully shipped, authored **[Marathon Plan C](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — the dogfood-reliability cluster (#106, #107, #108+#126+#127 bundled, #116 Bug B, #117, #124) — then **fired and shipped it the same day**: Wave 1's 5 parallel lanes (`742c230`, `691848c`, `53c8dce`, `b4e73df`, `6daaff5`) plus the **#107 kernel track** Opus-serial (`524d345`, 31/31, + [decisions record](decisions/2026-07-04-containment-ignore-toolcache.md)), integration commit `d5a1681`, full `validate.sh` exit 0. All 7 commits are ancestors of `main`; all 6 capture docs + the plan are in `3-COMPLETED`. *(The post-merge sweep moved the docs but left this cell and the per-item entries advertising Plan C as unfired — corrected 2026-07-08.)* | **Still open:** **GH-61 Tier 2** (needs operator `macos-latest`/`ubuntu-latest` runner decision). **Operator-driven:** rebalance-OS cross-repo harness dogfood beyond the queue-planner proof; **GH-48** closure call. **Part B:** G2 dup-token determinism (next adversarial-hardening slice). **Queue / parked:** GH-30 Phases 2–5 (writer wiring + risk-4 containment), #94 re-verify-vs-`5972ef4` installer repro. |

### Immediate next-up (ordered)

1. ✅ **Done — GH-51 [1-kernel] fixed (2026-06-30).** `rtl_init` now collapses a same-repo `--target-root` to the caller's root (no-op), so `--target-root .` no longer reverts the relay file off-lane. Decision record + regression test; `validate.sh` 69/69. The remaining GH-51 items ([2]/[3]/[5]) are minor. → [#51](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/51)
2. **Re-open the rebalance-OS cross-repo harness dogfood — now UNBLOCKED** (GH-51 [1-kernel] landed). Converts the #14 fix into a real external-repo proof: drive a scoped rebalance-OS lane via `swarm-preflight --target-root <clone> → marathon-drive`. The highest-value momentum test now that `--target-root` is containment-correct.
3. **[GH-48-QUEUE-PLAN-CROSS-REPO-ZONES.md](PROJECT/3-COMPLETED/GH-48-QUEUE-PLAN-CROSS-REPO-ZONES.md) built 2026-07-04; live rebalance validation corrected one assumption.** The foreign helper lane now classifies correctly as `signed-helper`, but the real 3-lane rebalance queue still stays one wave because only one lane touches that capped zone. Useful proof for the planner; not itself the full cross-repo harness dogfood.
4. **Part B Phase-2 remainder: G2 dup-token determinism** (`test/chaos-dup-token.sh`) — the next adversarial-hardening slice now that **R2 (#52) + G4 are done**. Kernel epoch-fence territory → Opus + `decisions/` record.
5. ✅ **Done — Part C real-agent run PROVEN (2026-06-30, operator GO).** `improve-loop` drove a live `marathon-drive` (**codex builder + agy reviewer**) as `--build-cmd`: baseline 4 → live build → `KEEP` oracle held → champion ACCEPTed (metric 3) → halted on the iteration cap, with provenance. Every safety invariant fired against a real build. The run surfaced two harness defects (neither a loop bug): **#58** (`--builder claude` not on PATH headless — use codex/agy) and **#59** (allowlisted artifact in an untracked dir → spurious off-lane exit 6). → [#50](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/50)

> **Active scheduling overlay:** re-run `utils/marathon-plan.sh` to regenerate after today's ledger update. Last generated: [MARATHON-PLAN-2026-07-02.md](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-02.md) (2026-07-02 — stale; the 2026-07-03 wave shipped ~10 items that were in the held list). Derived from this ledger; the ledger stays source of truth.

## Model assignment (heuristic)

Mechanical / pattern-following work → **Sonnet High**; trust-critical kernel-correctness reasoning
(epoch-fencing kernel, dup-token determinism) → **Opus**. Full build-track table:
[MARATHON-HARNESS.md → Model assignment](PROJECT/3-COMPLETED/MARATHON-HARNESS.md#model-assignment-build-track-guidance).

> **Operational note (carve-out — operationally critical):** Gemini CLI retired 2026-06-19; **agy**
> (Antigravity CLI) is the permanent cross-model lane. **Run agy turns sandbox-OFF** (it exits 0 with
> empty output when its backend is blocked) and an agy lane is **cost-blind** (no token output).
> Detail: [MARATHON-HARNESS.md → Operational note](PROJECT/3-COMPLETED/MARATHON-HARNESS.md#operational-note--cross-model-lane).

## Ledger

### Ad-hoc detours
- **GH-173 B3 + GH-178 A1/A4 — three bounded patches, one PR** 🆕 **2026-07-08, [PR #184](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/pull/184)** — **B3**: found the actual uncited-"[Pass]" source is `new-relay.sh`'s scaffolded Reviewer template (not `consult.sh`'s PREAMBLE or `relay-turn-lib.sh`'s generic role_note); added a citation requirement there + a mechanical `rtl_check_uncited_findings()` downgrade in `relay-turn-lib.sh`. **A1**: `consult.sh`'s advisor `case` dispatch is now a data table (`ADV_NAMES`/`ADV_RUNFNS`); "add vendor N+1" recipe in `relay-automation/README.md`. **A4** (deliberately scoped down, not the full firsthand-vs-asserted taxonomy): reused A2's stdout+transcript+sidecar mechanism to stamp `NO FIRSTHAND VERIFICATION CITED` on any advisor answer with zero citations anywhere. All three carry regression tests; targeted suites green. See CHANGELOG `[Unreleased]` and both PDDA docs' Validation tables. → [GH-173-JEDI-WRIGHT-FEEDBACK.md](PROJECT/2-WORKING/GH-173-JEDI-WRIGHT-FEEDBACK.md) · [GH-178-EPISTEMIC-RECONCILIATION-HARDENING.md](PROJECT/2-WORKING/GH-178-EPISTEMIC-RECONCILIATION-HARDENING.md) · [#173](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/173) · [#178](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/178)
- **`/open-router` skill — OpenRouter model-name resolve discoverability** 🆕 **2026-07-08** — `resolve-model-alias.sh` (GH-120) already resolves a colloquial name to a canonical slug locally, but a live session probed `aider --list-models` + curled the live catalog for "GLM 5.2" instead of using it. Added doc pointers (`aider-turn.sh`, `AGENTS.md`) + a new `skills/open-router/SKILL.md` so the alias table is checked first; no behavior change. See CHANGELOG `[Unreleased]`.
- **GH-183 · GH-178 B1's isolation-breach detector false-positives on legitimate agy turns** 🆕 **found 2026-07-08 reviewing PR #182** — the post-hoc `$ROOT`-citation check fails a fully in-bounds agy turn whenever its response narrates the mandatory `TICK_REPO_ROOT="$ROOT"` tick command (ordinary LLM narration); shipped in PR #182 as-is per operator call rather than block the merge. Fix direction: exclude the known tick-command line from the scan, same pattern as the existing `[trace]`-line filter. → [GH-178-EPISTEMIC-RECONCILIATION-HARDENING.md](PROJECT/2-WORKING/GH-178-EPISTEMIC-RECONCILIATION-HARDENING.md) · [#183](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/183)

### Queue / parked intake
- **GH-305 — 4-agent swarm validation test** (2026-07-24) - spike: one instrumented 4-agent concurrent-swarm run on a large repo (`LTVera-Pandas`/`sleuth-app`) with a codebase-memory dependency-aware lane preflight, added observability (overlap-rejection counts, per-lane wall-clock, JSON run summary, graph-cut-vs-path-cut control arm), and a fault-injection phase (manual reap, no watchdog today). Captured via /idea. Issue [#305](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/305). -> [PROJECT/1-INBOX/GH-305-4AGENT-SWARM-TEST.md](PROJECT/1-INBOX/GH-305-4AGENT-SWARM-TEST.md)
- **GH-296 · relay-drive.sh EPERM on tick lock in a same-repo vendored .xyz reviewing an artifact outside .xyz/** 🆕 **captured 2026-07-23 via /file-xyz-bug from `pdda`; root-caused + fixed same day on branch `gh-296-relay-drive-tick-eperm`** — **not a fresh instance of #263's bug: a regression via incomplete port.** #263's fix genuinely landed in the Bash `codex-turn.sh` (commit `8c361a0`), but this repo later flipped its default turn-shim runtime to Python (GH-264, `af7bb4d`) — *after* #263 shipped — and `utils/py/codex-turn.py` (the file that actually executes by default now) never received the #263/GH-36 fix at all, and separately mis-resolved its own `root` (defaulted to the harness's own directory instead of falling back to the CWD's git toplevel like Bash does). Fixed: `resolve_turn_root()` helper in `rtl.py` + `codex-turn.py`'s `--add-dir` built off `tick_repo_root`. **Follow-up folded in (same session, not a separate issue):** `agy-turn.py`/`claude-turn.py` shared the identical `root` defect (their own pre-launch `tick claim --paths` computation escaped the repo root with `../..`), plus a second, independent GH-51-class symlink physical/logical-path bug found while testing the fix (`git rev-parse --show-toplevel`'s physical-path form vs. a caller's unresolved macOS `/var`→`/private/var` path) — both fixed in `rtl.claim_paths_for_turn`. 5 new/extended regression cases across `test/{codex,agy,claude}-turn.sh`, each verified to fail pre-fix / pass post-fix via `git stash`. `relay-pkg.tar.gz` rebuilt (drifted from the `test/*.sh` edits). Also related to open #272 (same TICK_REPO_ROOT-in-vendored-lane root-cause family; this session's `root`-default fix may also resolve it — unverified, flagged for a human to confirm separately). Workaround while unfixed: `consult.sh` with `CONSULT_ROOT`. → [GH-296-RELAY-DRIVE-TICK-EPERM.md](PROJECT/1-INBOX/GH-296-RELAY-DRIVE-TICK-EPERM.md) · [#296](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/296)
- **GH-281 · Sentinel / Debug Flywheel — opt-in debug capture (public) + private triage overlay** 🆕 **captured + triaged + Codex-relay adjudicated 2026-07-22** — turns the harness's runtime failure/lesson signal into a capture→triage→plan→execute→PR→review pipeline, split into a public opt-in `XYZ_DEBUG_LOG` capture-only tier (one gitignored `debug.log`, zero network) and a private, unbundled Gemma triage overlay. Verdict: accept direction, gate execution. A `/relay-xyz` Codex review (Changes requested, 2 Blockers + 3 Shoulds, all accepted) corrected two holes: the Tier-2 issue→PR loop is operator experimentation *outside this repo's delivery scope* (reopening needs DO-NOT-BUILD's canonical amendment path, not a capture note), and the human gate is hardened (no auto-clear of `ratings_provisional`; explicit per-run attestation; canonical PDDA selection gates only on `risk`). Tier-1 Stage-0 is the *proposed* first slice, ready only after its own confirmed ratings + a green §1.7 run. Umbrella — do not fire as one lane. cx/risk/eff 4/4/4 (provisional, route-to-human). → [GH-281-SENTINEL-DEBUG-FLYWHEEL.md](PROJECT/1-INBOX/GH-281-SENTINEL-DEBUG-FLYWHEEL.md) · [#281](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/281)
  - **↳ Tier-1 Stage-0 carve (2-WORKING, swarm-preflight `ready` 2026-07-22)** — the headlessly-buildable slice: 3 standalone zero-network scripts (`harvest-findings.sh`, `finding-new.sh`, `hooks/sentinel-network-guard.sh`) + 2 covering suites + the `debug.log` gitignore line + `validate.sh` test-registration. Driver Stage-0 hooks (`marathon-drive.sh`) and integration acceptance are **orchestrator-only** (self-edit hazard), not in the builder lane. cx/risk/eff 2/2/2. **SHIPPED** — built (Codex+Agy marathon), a 3-round Codex relay closed 2 blockers, merged #285; §1.3 driver hooks applied + integration-tested + Agy-QA-approved. → [GH-281-SENTINEL-TIER1-STAGE0.md](PROJECT/2-WORKING/GH-281-SENTINEL-TIER1-STAGE0.md)
  - **↳ Tier-2 overlay (2-WORKING, built + Agy-QA-approved 2026-07-22)** — the full triage overlay (reader → Gemma classify → draft PDDA doc → file issue → nightly batch → PR-emit → adversarial red-team → morning report) shipped as **transparent in-repo reference code**, with every call-home path (ollama/`gh`/`git push`/marathon-fire) gated behind a gitignored `runtime.env`. **Inert by default** — proven by `test/sentinel-overlay.sh` (zero network/LLM/GitHub with no config). Operator opts in locally to activate. cx/risk/eff 4/4/3 (provisional). → [GH-281-SENTINEL-TIER2-OVERLAY.md](PROJECT/2-WORKING/GH-281-SENTINEL-TIER2-OVERLAY.md)
- **GH-280 · Investigate Aider+Qwen3.8-Max reliability vs Aider+Claude-Sonnet-5 control ($5 OpenRouter budget)** 🔬 **Phase 1+2 run 2026-07-23 — 80 trials, root cause narrowed, not fully isolated.** Corrected baseline: 6/7 (~86%) real historical failure rate. 62/62 clean across edit-format, file-count, prompt-complexity, repo-scale, and an OpenRouter `qwen3.7-max`-vs-`sonnet-5` control (10/10 pass each, $0.40 of $5 spent) — rules out Qwen-inherent-unreliability and Aider-edit-format-mismatch alone. Only real `git worktree add --detach` reproduced it: 15/18 pass (16.7% fail, matching production's empty-file signature) — points at Aider's own behavior under detached-HEAD worktree mechanics, not Qwen, not this repo's harness (`relay-turn-lib.sh` copyback logic read in full, confirmed sound). **Update 2026-07-23 (second session):** with real GH-161 log persistence finally in place, a real Qwen-builder exercise caught and read an actual failure transcript — the "no tracked changes" symptom is Aider's `whole` edit format silently discarding a genuinely correct edit because Qwen emits unified-diff hunks instead; forcing `AIDER_FLAGS=--edit-format diff` took a real production task from 0/3 real edits to a 90%-complete, substantively correct implementation in round 1. Root cause resolved: **H2 (edit-format mismatch) was real all along** — earlier synthetic tests were too small/simple to trigger it. cx/risk/eff 3/2/3. → [GH-280-AIDER-QWEN-VS-SONNET-INVESTIGATION.md](PROJECT/1-INBOX/GH-280-AIDER-QWEN-VS-SONNET-INVESTIGATION.md) · [#280](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/280)
- **GH-279 · aider-qwen marathon trial — consolidated run issues** **captured 2026-07-22; SHIPPED 2026-07-24** — re-grounded all 6 original items against the live repo before firing: items 1/2 resolved via #280, item 3 tracked separately as #278, items 5 and 6b confirmed not applicable. Fired via `swarm-preflight → marathon-drive` (Codex builder, agy reviewer): fixed **(4)** zero-byte/unchanged artifacts passing as "appeared" in `marathon-drive.sh`/`marathon_drive.py` (reused the existing `requires_test_delta()` non-empty+git-delta pattern) and **(6a)** the aider-turn API key leaking into `ps` argv on the `AIDER_OPENAI_API_BASE` seam (moved to `OPENAI_API_KEY` env). First turn's off-allowlist edits (a stray `CHANGELOG.md` entry, an unrelated concurrently-open scratch file) were correctly reverted by containment; closed out manually rather than risk a third automated invocation colliding with the operator's own concurrent edits — 63/63 + 137/137 targeted tests, full `validate.sh` green (only pre-existing environmental reds). Agy's own review didn't run (auth expired); reviewed by the orchestrating session instead, disclosed in the doc/CHANGELOG rather than treated as equivalent. This closes out the GH-279/GH-280 marathon. → [GH-279-AIDER-QWEN-MARATHON-TRIAL-FINDINGS.md](PROJECT/3-COMPLETED/GH-279-AIDER-QWEN-MARATHON-TRIAL-FINDINGS.md) · [#279](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/279)
- **GH-276 · Weekly risk-control reconciliation — keeping GH-275 on the ball** 🆕 **captured 2026-07-22** — recurring, comment-based evidence review that reconciles the live `development` branch, GitHub state, PDDA docs, containment/parity incidents, vendor drift, and recovery controls without becoming a second plan or auto-firing work. → [GH-276-WEEKLY-RISK-RECONCILIATION.md](PROJECT/1-INBOX/GH-276-WEEKLY-RISK-RECONCILIATION.md) · [#276](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/276)
- **GH-275 · Long-Term Risk Remediation — balanced, evidence-led safety program** 🆕 **captured and reconciled 2026-07-22** — umbrella residual-risk register, rewritten as a six-phase actionable checklist after a timed rescan of `development`; reuses existing issues, treats shipped work as controls, caps committed work at three proof-sized lanes, and delegates weekly drift checks to GH-276. → [GH-275-LONG-TERM-RISK.md](PROJECT/1-INBOX/GH-275-LONG-TERM-RISK.md) · [#275](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/275)
- **GH-274 · marathon-drive: re-invoking a phase whose tick token is already done clobbers RELAY.md's Approved record instead of detecting a satisfied lane** 🆕 **found live 2026-07-21 during GH-273 Phase 0's real fire** — the relay succeeded (Codex builder, agy reviewer, Approved), the `bash validate.sh` pre-advance gate flaked on an unrelated pre-existing test, and retrying via `marathon-drive.sh` re-rendered `RELAY.md` back to `STATUS: Open` and failed on the already-`done` tick token (can't reopen), discarding the accurate Approved record — recovered manually via `git revert`. Fix direction: extend the existing GH-207 satisfied-lane detection to also cover "gate failed after an already-terminal relay," not just its current mid-relay reroute case. Not blocking — a distinct `--phase-id` per fire avoids the collision entirely (confirmed live during GH-273 Phase 1's fire), but `swarm-preflight`'s suggested command never varies it, so any caller copying that verbatim across repeat fires hits this. cx/risk/eff 3/2/2. → [GH-274-MARATHON-DRIVE-DONE-TOKEN-RETRY-CLOBBER.md](PROJECT/2-WORKING/GH-274-MARATHON-DRIVE-DONE-TOKEN-RETRY-CLOBBER.md) · [#274](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/274)
- **GH-272 · Driven relay turn's tick release resolves wrong TICK_REPO_ROOT in a vendored same-repo lane** 🆕 **captured 2026-07-21 via /file-xyz-bug from `sleuth-app`; root-caused + fixed 2026-07-23 via GH-296/PR #297, NOT this doc's own contract** — traced to `RelayTurnLib._run_rtl()` (`utils/py/rtl.py`) feeding `codex-turn.py`/`agy-turn.py`/`claude-turn.py`'s buggy `root` default into every bridged `relay-turn-lib.sh` call (incl. the turn-prompt text and the GH-67 backstop release) as `TICK_REPO_ROOT`. Confirmed via a live A/B repro with `xyz-vendor.sh`: pre-fix baked `TICK_REPO_ROOT="<target>/.xyz"` into the prompt and the backstop release itself, matching this issue's exact symptom; post-fix (PR #297) resolves both. This doc's own drafted contract targeted `relay-turn-lib.sh`'s `rtl_tick_bin()` — the wrong file — and is marked superseded/do-not-fire. cx/risk/eff 3/2/2 (provisional, now moot). → [GH-272-TICK-REPO-ROOT-VENDORED-MISMATCH.md](PROJECT/2-WORKING/GH-272-TICK-REPO-ROOT-VENDORED-MISMATCH.md) · [#272](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/272) · [#297](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/pull/297)
- **GH-284 · marathon: open a non-merging PR with closeout notes after a successful run** 🆕 **captured 2026-07-23 (/10days sweep)** — after a marathon run completes successfully, open a non-merging PR (opt-in `--closeout-pr` flag) carrying deterministic notes built from the plan name/phase count/tick events, reusing `marathon-closeout.sh`'s existing PR-creation code behind a new `--open-only` flag. Fully spec'd by the issue itself (exact seam, acceptance criteria, explicit non-goals: no merge, no force-push, no branch creation). cx/risk/eff 2/2/2 (provisional). → [GH-284-MARATHON-CLOSEOUT-PR.md](PROJECT/2-WORKING/GH-284-MARATHON-CLOSEOUT-PR.md) · [#284](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/284)
- **GH-153 · Cost observability: Codex per-turn token capture (parseCodexStats)** 🆕 **captured 2026-07-23 (/10days sweep)** — was gated on #151 (Phase 4 discovery spike), now CLOSED; its recorded finding says the Codex capture gate check passes and `parseCodexStats` can mirror the existing `parseGeminiStats` almost exactly. Confirmed unimplemented in `src/cost.js`; `codex-turn.sh` needs `--json` wired into its `codex exec` invocation. Full spec already written in `PROJECT/1-INBOX/COST-OBSERVABILITY-PLAN.md` Phase 7. cx/risk/eff 2/1/2 (provisional). → [GH-153-CODEX-TOKEN-CAPTURE.md](PROJECT/2-WORKING/GH-153-CODEX-TOKEN-CAPTURE.md) · [#153](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/153)
- **GH-268 · Beta onboarding & build-quality test report — remediation plan (re: #123)** 🆕 **plan drafted 2026-07-22** — beta tester (Matthew Taylor) found 2 blocking onboarding issues (Quickstart fails on a clean clone; a named skill has no installer), 4 fix-before-broader-beta README/sandbox items, 2 follow-up process gaps (manual relay handoff; the review loop checks the diff not the file it lands in — an independent audit found 20 issues incl. 1 critical in code a relay approved), and 1 untested item (cross-model Codex/agy lane never ran — blocked on #232, now closed). Single plan doc, 4 phases, no per-finding issue split per operator instruction. cx/risk/eff 4/3/5 (provisional). → [GH-268-BETA-ONBOARDING-BUILD-QUALITY-REMEDIATION.md](PROJECT/2-WORKING/GH-268-BETA-ONBOARDING-BUILD-QUALITY-REMEDIATION.md) · [#268](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/268)
- **GH-261 · marathon-drive: reconcile the Bash/Python disjoint-failure union (last Phase-1 gate for the XYZ_PYTHON flip)** ✅ **SHIPPED — closed 2026-07-22 (PR #271, merge 2a2da17)** — split out of #255; Python side already DONE (112/0 under XYZ_PYTHON=1, PR #262). Bash side (`test/marathon-drive.sh` exit 6 where 4/0 expected) fixed via direct RTL_TRACE instrumentation, not the issue's original hypothesis: real causes were (1) a bidirectional physical/logical symlink-form mismatch between `RTL_ROOT` and an absolute `RTL_ALLOW` entry, and (2) `rtl_check()` missing the collapsed-directory transcript-log exemption GH-266 already gave `rtl_worktree_end`. The issue's own "inherited TICK_REPO_ROOT" hypothesis was tested directly and confirmed real but NOT the actual gating cause. `test/marathon-drive.sh` 112/112 under both runtimes, confirmed on a real ubuntu:latest container. cx/risk/eff 4/5/3. → [GH-261-MARATHON-DRIVE-BASH-CONTAINMENT-VENDORED-CHAIN.md](PROJECT/3-COMPLETED/GH-261-MARATHON-DRIVE-BASH-CONTAINMENT-VENDORED-CHAIN.md) · [#261](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/261)
- **GH-266 · rtl_worktree_end doesn't exempt relay-system/ (its own transcript dir) — false containment violation discards a fully in-scope turn** ✅ **SHIPPED — closed 2026-07-22 (PR #271, merge 2a2da17)** — the worktree-isolation containment path (`rtl_worktree_end`) had no exemption for the harness's own `relay-system/` transcript directory, unlike `.tick/` (explicitly exempted) and unlike the non-worktree `rtl_check()` path (exempts `$RTL_LOG_REL` directly). A fully in-scope Codex build turn was discarded every time the isolated worktree's own transcript log landed under `relay-system/`. Adjacent to #30 (XYZ_ARCHIVE_ROOT redirect — complementary, not a substitute) and #160 (closed, different false-positive mechanism). Shared an edit surface with #261 (`relay-turn-lib.sh`) — fixed together. cx/risk/eff 2/4/2. → [GH-266-WORKTREE-RELAYSYSTEM-CONTAINMENT.md](PROJECT/3-COMPLETED/GH-266-WORKTREE-RELAYSYSTEM-CONTAINMENT.md) · [#266](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/266)
- **GH-263 · codex-turn.sh isolation=0 path can't reach the parent-root .tick lock in vendored installs** ✅ **SHIPPED — closed 2026-07-22 (PR #271, merge 2a2da17)** — the GH-36 `--add-dir "$TICK_REPO_ROOT/.tick"` fix was only in the `RELAY_WORKTREE_ISOLATION=1` branch of `codex-turn.sh`; the `isolation=0` opt-out assumed Codex CWD == ROOT, but a vendored run driven from `cd $HARNESS` (= `.xyz`) sandboxed Codex to `.xyz` → `EPERM` on the token claim before the turn started. Follow-up to #36; adjacent to [[GH-234]]. cx/risk/eff 2/3/2. → [GH-263-CODEX-ISOLATION0-TICK-EPERM.md](PROJECT/3-COMPLETED/GH-263-CODEX-ISOLATION0-TICK-EPERM.md) · [#263](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/263)
- **GH-258 · vendor-stack skill — one-step XYZ harness + PDDA governance install into a target repo** ✅ **SHIPPED — closed 2026-07-21** (commit `cb01fae`, merged PR #259) — new `skills/vendor-stack/` orchestrates the two independent installers: vendors XYZ into `<target>/.xyz/` (`xyz-vendor.sh`), then optionally installs PDDA (pdda `install.sh`, observe mode) via a device-agnostic `find-pdda.sh` resolver. Prompt + resolver live at the skill layer, not baked into `xyz-vendor.sh` (which ships in every vendored `.xyz/` and must stay non-interactive). Each tool keeps its own upgrade registry. Files on disk + smoke-tested; symlink `install.sh` not yet run. cx/risk/eff 2/1/2. → [GH-258-VENDOR-STACK-SKILL.md](PROJECT/3-COMPLETED/GH-258-VENDOR-STACK-SKILL.md) · [#258](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/258)
- **GH-251 · OpenRouter/aider reviewer seam doesn't persist its review (builder-only in practice)** ✅ **SHIPPED — closed 2026-07-21** (commit `cf7a123`, merged PR #256; NOTE: live-OpenRouter-model verification remains an outstanding operator step, not claimed done) — found during a live multi-turn `/relay-xyz` GLM 5.2 QA of the 2026-07-19 marathon: aider produces a correct review in-transcript but its relay-file append is lost through repomap-wandering + worktree containment, so `--review-once` correctly scores it a stall (a live confirmation of GH-245, not a classifier bug). Fix: a review-mode / transcript-salvage for `aider-turn.sh`, or document builder-only and route reviews to codex/agy. cx/risk/eff 2/2/2. → [GH-251-OPENROUTER-AIDER-REVIEWER-SEAM.md](PROJECT/3-COMPLETED/GH-251-OPENROUTER-AIDER-REVIEWER-SEAM.md) · [#251](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/251)
- **GH-199 · swe-diagram: add a font picker to the generated diagram HTML (default + Indie Flower)** ✅ **SHIPPED — closed 2026-07-21** (commit `1e08154`) — no font picker exists in the generated diagram HTML today (0 matches for `Indie Flower` in `utils/swe-diagram/assets/template.html`); an orphaned branch `origin/gh-199-swe-diagram-font-picker` (`322ecbe`, agy-reviewed) already implements it but was never merged — operator may prefer merging that over a rebuild. cx/risk/eff 1/1/2. → [GH-199-SWE-DIAGRAM-FONT-PICKER.md](PROJECT/3-COMPLETED/GH-199-SWE-DIAGRAM-FONT-PICKER.md) · [#199](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/199)
- **GH-232 · validate.sh: ~12 tests fail on ubuntu-latest CI runner (environment incompatibilities, first exposed by GH-230's CI step)** ✅ **SHIPPED — closed 2026-07-22 (PR #271, merge 2a2da17)** — re-diagnosed against a real ubuntu:latest container across 4 rounds: portable `sed -i` (3b59d75), `hq_sanitize`'s GNU `tr` range-ambiguity corruption (899302a), missing `CODEX_BIN`/`--builder` pins + shallow-clone `fetch-depth` (0abb6aa), macOS AppleDouble tar junk in `make-pkg.sh` (70640ca). Confirmed green on real CI (run 29865944751, `tier1` SUCCESS). cx/risk/eff 3/2/3. → [GH-232-VALIDATE-UBUNTU-CI-FAILURES.md](PROJECT/3-COMPLETED/GH-232-VALIDATE-UBUNTU-CI-FAILURES.md) · [#232](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/232)
- **GH-234 · find-harness.sh --env exports TICK_REPO_ROOT one directory too deep (found during GH-177 wipe investigation, never filed)** ✅ **SHIPPED — closed 2026-07-21** (commit `125f184`, merged PR #252) — `find-harness.sh:178` exports `TICK_REPO_ROOT` as the vendored `.xyz` harness path instead of the actual repo root that `bin/tick` and tick-consuming shims expect (confirmed present today). cx/risk/eff 2/2/2. → [GH-234-FIND-HARNESS-TICK-ROOT-TOO-DEEP.md](PROJECT/3-COMPLETED/GH-234-FIND-HARNESS-TICK-ROOT-TOO-DEEP.md) · [#234](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/234)
- **GH-236 · Worktree isolation under $TMPDIR breaks codex turns in /tmp-rooted environments (and escalates as a false timeout)** ✅ **SHIPPED — closed 2026-07-21** (commit `36d9e62`, merged PR #252) — `relay-turn-lib.sh`'s isolation worktree is created under `${TMPDIR:-/tmp}/rtl-wt.XXXXXX`, which breaks codex turns in /tmp-rooted environments and surfaces as a false timeout instead of the real cause. cx/risk/eff 2/3/2. → [GH-236-WORKTREE-TMPDIR-CODEX-BREAK.md](PROJECT/3-COMPLETED/GH-236-WORKTREE-TMPDIR-CODEX-BREAK.md) · [#236](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/236)
- **GH-242 · agy S10: off-prompt-nonempty repro — deferred from #155, needs a controlled trigger** ✅ **SHIPPED — closed 2026-07-21** (commit `9c052f4`) — agy's off-prompt-nonempty (S10) condition, split out of #155, has no controlled repro and no regression coverage in `test/agy-turn.sh`; spike/discovery item, may come back marathon-unready from preflight. cx/risk/eff 2/3/3. → [GH-242-AGY-S10-OFFPROMPT-REPRO.md](PROJECT/3-COMPLETED/GH-242-AGY-S10-OFFPROMPT-REPRO.md) · [#242](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/242)
- **GH-247 · marathon-triage: bare utils/ paths break in vendored .xyz/ installs** ✅ **SHIPPED — closed 2026-07-21** (commit `95c3e12`, merged PR #252) — `skills/marathon-triage/SKILL.md` hardcodes bare `utils/swarm-preflight.sh`/`utils/marathon-plan.sh` paths that don't resolve in a vendored `.xyz/` install; fix resolves `utils/` relative to the harness root. Shares an edit surface with #250 — serialize, do not run concurrently. cx/risk/eff 1/1/1. → [GH-247-MARATHON-TRIAGE-VENDORED-PATHS.md](PROJECT/3-COMPLETED/GH-247-MARATHON-TRIAGE-VENDORED-PATHS.md) · [#247](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/247)
- **GH-249 · Marathon gate enforces 'existing tests pass', not new-test presence — brief acceptance criteria are advisory prose** ✅ **SHIPPED — closed 2026-07-21** (commit `c1b3b6d`; Python port commit `4418122`) — `relay-automation/marathon-drive.sh`'s pre-advance gate only checks existing tests pass, never that a lane added a test for its fix; add an optional `requires_test` contract field + test-delta check. cx/risk/eff 3/3/3. → [GH-249-MARATHON-GATE-NEW-TEST-PRESENCE.md](PROJECT/3-COMPLETED/GH-249-MARATHON-GATE-NEW-TEST-PRESENCE.md) · [#249](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/249)
- **GH-250 · marathon-triage: emit a default recommendation per item, not symmetric options** ✅ **SHIPPED — closed 2026-07-21** (commit `9bf1b56`) — `skills/marathon-triage/SKILL.md`'s report step lists symmetric options with no default call; add a `RECOMMEND:`/`BECAUSE:`/`UNLESS:` framing per item. Shares an edit surface with #247 — serialize, do not run concurrently. cx/risk/eff 1/1/2. → [GH-250-MARATHON-TRIAGE-DEFAULT-RECOMMENDATION.md](PROJECT/3-COMPLETED/GH-250-MARATHON-TRIAGE-DEFAULT-RECOMMENDATION.md) · [#250](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/250)
- **GH-245 · `--target-root` review turn cannot report, and `relay-drive` misclassifies the outcome both ways** ✅ **SHIPPED — closed 2026-07-21** (commit `3783665`, merged PR #246) — a cross-repo review turn is structurally unable to produce a review: `--target-root` bases the worktree on the *target* repo while `--relay-file` resolves against the *harness* repo, so with the documented review-turn setting `ALLOW_PATHS=""` the only writable path isn't in the worktree. Codex finished the review (~88k tokens), hit `patch rejected: writing outside of the project`, released the token cleanly and lost everything but a one-sentence transcript summary — on the path SKILL.md presents as the normal case. The amplifier is defect 2: `--review-once` classifies on **token state alone**, so within one hour it scored an empty turn as a successful review (exit 5; `## Log` empty, `NEXT:` unflipped, shim had already printed "no tracked changes") and a complete 6-finding review as a `genuine stall` (exit 3; file written, `NEXT:` flipped, token merely left claimed). Both times the distinguishing evidence was present and unused. Same family as [#236](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/236). Not blocking — workaround is `xyz-vendor.sh <target-repo>` then drop `--target-root`. **Promoted to `2-WORKING` 2026-07-19 via /10days sweep with an auto-drafted contract.** cx/risk/eff 2/4/2. → [GH-245-TARGET-ROOT-REVIEW-UNWRITABLE.md](PROJECT/3-COMPLETED/GH-245-TARGET-ROOT-REVIEW-UNWRITABLE.md) · [#245](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/245)
- **GH-241 · `MARATHON.example.yaml` understates sequencing and `depends_on`'s scalar-only shape** ✅ **SHIPPED — closed 2026-07-21** (commits `353d4c0`/`500dd87`, merged PR #256) — the only worked example a plan author has leaves two things unstated that together produce a plan which parses cleanly, reads correctly, and aborts on first run. (1) Nothing in the example says phases run **one at a time** and halt on first failure — and since every phase in it except `p1` carries a `depends_on`, the natural inference is that phases *without* one run concurrently. A real 4-phase plan shipped with a header claiming "3 lanes, then 1" on the back of a sound disjoint-write-set analysis; the write-set work was load-bearing, the concurrency conclusion was fiction. (2) `depends_on: [phase-id]` parses as the **literal string** and fails at `bin/marathon-yaml:102` as an unknown-phase lookup, pointing the author at their phase ids rather than the field's shape. Both caught by `--dry-run` at zero cost, which is also the fix's fourth option: recommend it in the example header. cx/risk/eff 1/2/1. → [GH-241-MARATHON-EXAMPLE-SEQUENCING.md](PROJECT/3-COMPLETED/GH-241-MARATHON-EXAMPLE-SEQUENCING.md) · [#241](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/241)
- **GH-238 · marathon-drive: pre-advance gate defaults to `bash validate.sh` — halts AFTER approval in any consuming repo without it** ✅ **SHIPPED — closed 2026-07-21** (commit `d999c36`, merged PR #243; Python port commit `b180ace`) — external field report from a vendored `.xyz/` consumer (`Hypercart-Dev-Tools/rebalance-OS`), all claims verified against source: `marathon-drive.sh:376` defaults the gate to `bash $ROOT/validate.sh` and never existence-checks it, and `:751` runs it only *after* `relay approved` — so a consumer without `validate.sh` pays a full builder turn **plus** a full reviewer turn before exit 5. Wider than filed: the gate is also probed on the exit-7 timeout (`:662`) and exit-3 stall (`:779`) recovery paths, so those degrade too. `MARATHON.example.yaml` never mentions the gate at all. Fix: fail fast at plan load (resolve + probe before turn 1) and document the default in the example; explicitly **not** silently skipping the gate, and **not** a mode-dependent default. Distinct from #170/#232 (those are `validate.sh`'s own tests failing here). cx/risk/eff 2/2/2. → [GH-238-MARATHON-PREADVANCE-GATE-DEFAULT.md](PROJECT/3-COMPLETED/GH-238-MARATHON-PREADVANCE-GATE-DEFAULT.md) · [#238](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/238)
- **GH-239 · swarm-preflight: no contract example ships — consumers hit exit 3 with nothing to copy, so the gate gets bypassed** ✅ **SHIPPED — closed 2026-07-21** (merged PR #243) — same reporter, same day, same root cause (defaults/onboarding assume the consumer looks like this repo). The contract schema lives **only** in `utils/swarm-preflight.sh:24-36`'s header comment; `MARATHON.example.yaml` is the sole shipped worked example and has no contract equivalent. Worse than filed: a dozen-plus real filled-in contracts exist in `PROJECT/**/GH-*.md`, but `PROJECT/**` isn't part of a vendored `.xyz/` install — every working example is structurally invisible to the audience that needs one. Consequence is behavioural, not cosmetic: consumers skip `swarm-preflight.sh` and hand-author `MARATHON.yaml` because *that* format has an on-ramp, so the freshness/fix-required/collision gates get routinely bypassed. Fix: ship `relay-automation/CONTRACT.example.md` (per-field annotated, `fix_probes` polarity called out explicitly — inverting it yields a STALE exit-4 *false completion* signal) + print the minimal skeleton on exit 3. Deferring `--emit-contract-skeleton`. cx/risk/eff 1/1/2. → [GH-239-PREFLIGHT-CONTRACT-EXAMPLE.md](PROJECT/3-COMPLETED/GH-239-PREFLIGHT-CONTRACT-EXAMPLE.md) · [#239](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/239)
- **PDDA sweep 2026-07-18 · 4 shipped docs swept to `3-COMPLETED`, 6 stale marathon status words corrected** 🆕 **2026-07-18, branch `docsweep/2026-07-18`** — GH-151/152/155/230 were sitting in `2-WORKING` with terminal status in their own frontmatter and `CLOSED/COMPLETED` issues; moved to `3-COMPLETED`. Six marathon plans filed under `3-COMPLETED`/`4-MISC` still read `status: Active (2-WORKING)` (`07-01/02/03/05/06`, `4-MISC/07-07`); corrected. Both classes are instances of the #224 drift backlog, found by a manual marathon-file audit rather than by the #189 checks. Caveat carried forward: GH-155's own doc says sub-item **S10 stays open** despite the issue being closed. → [GH-224-PDDA-DRIFT-BACKLOG-RECONCILE.md](PROJECT/3-COMPLETED/GH-224-PDDA-DRIFT-BACKLOG-RECONCILE.md) · [#224](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/224)
- **GH-235 · Consult A4 provenance v0: prompt-trace classifier for cited claims (FIRSTHAND vs ECHOED)** ✅ **SHIPPED — closed 2026-07-21** (Bash commits `1e87a1f`/`e1a7b61`; Python port commit `19830e4`) — closes the shipped A4 slice's blind spot (a prompt-echoed citation looks identical to a firsthand one). A parallel Codex+agy consult (`relay-system/2026-07-17/a4-scope-181325/`) converged independently on **v0** (2-category: FIRSTHAND vs ECHOED on already-cited claims) over the 4-category taxonomy, both citing Principle 7 + the reuse tie-breaker. v0: persist `PROMPT_TEXT`, sibling predicate to `rtl_has_uncited_claim()`, per-advisor `PROVENANCE.txt` sidecar, existing `NO FIRSTHAND` stamp unchanged. Defers INFERENCE/UNSUPPORTED split + reconciliation backstop. Coordinates with #226. cx/risk/eff 2/2/2. → [GH-235-CONSULT-A4-PROVENANCE-V0.md](PROJECT/3-COMPLETED/GH-235-CONSULT-A4-PROVENANCE-V0.md) · [#235](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/235)
- **GH-230 · Front-door onboarding drift: undocumented npm install + package.json drift** ✅ **SHIPPED 2026-07-17; closed** — read-only `/front-door` audit found a genuinely fresh clone following README's own Quickstart verbatim hits `Cannot find module 'acorn'` (real `package.json` deps, gitignored `node_modules/`, no doc mentions `npm install`, and CI's `tier1` job never runs `./validate.sh` so the gap is invisible to CI). Also cleaned up: `package.json`'s stale `"name": "lane-169"`, garbled beta-banner `"description"`, and a deliberately-failing `npm test` stub. No leaked secrets found. cx/risk/eff 1/1/2. → [GH-230-FRONT-DOOR-ONBOARDING-DRIFT.md](PROJECT/2-WORKING/GH-230-FRONT-DOOR-ONBOARDING-DRIFT.md) · [#230](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/230)
- **GH-177 · mktemp-into-destructive-EXIT-trap recurrence — `test/hq-hardening.sh` (+2 siblings) rm -rf'd the entire repo a 2nd time** ✅ **SHIPPED 2026-07-17** (`e7fd117`) — closed 2026-07-08 without the code fix ever landing; the unguarded `TMP="$(cd "$(mktemp -d)" && pwd -P)"; trap 'rm -rf "$TMP"' EXIT` idiom was still live in `test/hq-hardening.sh`/`hq-promote.sh`/`hq-locator.sh` and fired again when `validate.sh` ran under a sandboxed shell (`mktemp -d` fails silently, `cd ""` succeeds and stays at cwd = repo root, the EXIT trap deletes it). Recovered clean via `git init` + `fetch` + `read-tree`/`checkout-index` from `origin/development` (no upstream loss) + a Time Machine assist for the working tree. Fixed all 3 files with the guard the original issue already specified, plus a new static-audit regression test (`test/mktemp-trap-guard.sh`) wired into `validate.sh`, verified (via disposable scratch files) to fail on the historical pattern and pass clean (191 files, 0 findings) post-fix. cx/risk/eff 1/3/1. → [GH-177-MKTEMP-TRAP-REPO-WIPE.md](PROJECT/3-COMPLETED/GH-177-MKTEMP-TRAP-REPO-WIPE.md) · [#177](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/177)
- **GH-226 · Full provenance follow-up should coordinate with the already-reworked consult/relay summary surface** ✅ **SHIPPED (coordination/decision scope only) — closed 2026-07-21** (merged PR #257; the two-lane implementation itself is a SEPARATE, still-held effort — see PROJECT/2-WORKING/MARATHON-PLAN-2026-07-20-M-GH226-PROVENANCE-IMPL.md, Plan M) — Jedi Wright flagged a real coordination gap in Slack: GH-211 already changed the operator-facing TLDR/category layer, GH-178 intentionally shipped only a narrow provenance slice, and a future "full provenance" pass will likely rework the same human-facing surface twice unless the two are designed together. Ask: inventory the operator-facing summary surfaces GH-211 touched and the provenance surfaces GH-178 touched, then decide whether the next pass stays one coordinated issue or splits by repo/surface. cx/risk/eff 2/2/2. → [GH-226-PROVENANCE-SUMMARY-SURFACE-COORDINATION.md](PROJECT/3-COMPLETED/GH-226-PROVENANCE-SUMMARY-SURFACE-COORDINATION.md) · [#226](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/226)
- **GH-225 · 10days/marathon guardrail: `isolation:"worktree"` lanes can branch from a stale historical commit, not the marathon branch** ✅ **SHIPPED — closed 2026-07-21** (commit `cf608da`, merged PR #227) — found live during the GH-174/215/222/189 marathon fire the same day: all 4 parallel lanes' isolation worktrees branched from stale historical commits (e.g. `788a5c6`, `e8acdc5`), not the marathon branch named as `target.ref`; caught only by manually checking `git log` before merging, and recovered via cherry-pick instead of a full merge (which would have silently reintroduced superseded history). Fix: doc-only guardrail in `skills/10days/SKILL.md` Step 7 — verify each lane's worktree base against the marathon branch start commit before merging, cherry-pick instead of merge when it fails. cx/risk/eff 1/2/1. → [GH-225-ISOLATION-WORKTREE-STALE-BASE-GUARDRAIL.md](PROJECT/3-COMPLETED/GH-225-ISOLATION-WORKTREE-STALE-BASE-GUARDRAIL.md) · [#225](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/225)
- **GH-224 · PDDA drift backlog: 45 stale 3-COMPLETED status words + 15 ROADMAP/issue-state mismatches** ✅ **SHIPPED — closed 2026-07-21** (commit `19468be`) — GH-189's two new checks (`pdda-check-roadmap-issue-state`, 3-COMPLETED scan) surfaced real, reproducible drift when run against this repo's own docs/ROADMAP on the then-unmerged `marathon/gh174-215-222-189-2026-07-17`: 15 ROADMAP ledger entries reading ✅/SHIPPED against a still-OPEN issue (or vice versa) and 45 `3-COMPLETED` docs with non-terminal frontmatter status. Doc-only reconciliation, no safe bulk regex — per-item eyeball against real `gh issue view` state. cx/risk/eff 2/1/3. → [GH-224-PDDA-DRIFT-BACKLOG-RECONCILE.md](PROJECT/3-COMPLETED/GH-224-PDDA-DRIFT-BACKLOG-RECONCILE.md) · [#224](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/224)
- **GH-223 · utils/py/consult.py missing GH-178 A4 citation-stamp parity** ✅ **SHIPPED — closed 2026-07-21** (commit `54972e9`, merged PR #228) — unmasked while fixing GH-215 (consult.py SINGLE-MODEL stamping): `XYZ_PYTHON=1 bash test/consult.sh` gets past test 3 now but fails at test 12 — `consult.py` never got `relay-automation/consult.sh:310-336`'s "NO FIRSTHAND VERIFICATION CITED" stamping. Direct port, no design work; existing test 12 already exercises it. cx/risk/eff 2/1/2. → [GH-223-CONSULT-PY-CITATION-STAMP-PARITY.md](PROJECT/3-COMPLETED/GH-223-CONSULT-PY-CITATION-STAMP-PARITY.md) · [#223](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/223)
- **GH-222 · Marathon end-of-session hygiene: no cost summary in `marathon-drive.sh`, no worktree cleanup in `/10days`** ✅ **SHIPPED 2026-07-17 via a marathon lane (worktree-isolated Sonnet subagent)** — added `xyz_marathon_cost_summary()` to `marathon-drive.sh`, mirroring `relay-drive.sh`'s GH-152 pattern verbatim, EXIT-trap wired; `skills/10days/SKILL.md` Step 7 now removes each lane's worktree after merge, Step 8 reports cleanup failures. `bash validate.sh` 113/114 (only pre-existing `acorn-extract.sh`). cx/risk/eff 2/1/2. → [GH-222-MARATHON-SESSION-HYGIENE.md](PROJECT/3-COMPLETED/GH-222-MARATHON-SESSION-HYGIENE.md) · [#222](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/222)
- **GH-189 · PDDA: sweeping a doc to 3-COMPLETED silences the only issue-state check watching it** ✅ **SHIPPED 2026-07-17 via a marathon lane (worktree-isolated Sonnet subagent)** — added `check_roadmap_issue_state()` (ROADMAP entry vs. issue state, both directions) + extended `check_issue_doc_sync()` to also scan `3-COMPLETED` via a new `pdda_list_completed_docs()` helper, both warn-only/offline-degrading. 7 new `test/pdda-roadmap-coverage.sh` assertions (10/10 pass). Run against this repo's own real docs: genuinely found 48 stale `3-COMPLETED` status words and 15 real ROADMAP/issue-state mismatches — not false positives (spot-checked #211: ledger says SHIPPED, issue still OPEN). `bash validate.sh` 113/114 (only pre-existing environmental reds). cx/risk/eff 2/2/2. → [GH-189-PDDA-COMPLETED-STATE-BLINDSPOT.md](PROJECT/3-COMPLETED/GH-189-PDDA-COMPLETED-STATE-BLINDSPOT.md) · [#189](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/189)
- **GH-198 Bug 1 · `rtl_enforce`'s file-scoped commit had no pathspec (pre-existing staged content could ride into a relay commit)** ✅ **built 2026-07-16** — `relay-turn-lib.sh`'s commit step staged each allowlisted path independently but committed with no pathspec, so pre-existing staged content silently rode along (reproduced live 2026-07-10/11). Fixed by scoping the commit to the allowlist, cross-checked against what's actually staged (a naive scope reintroduced the GH-29 batched-pathspec-abort bug, caught by `test/relay-target-root-newfile.sh` regressing during the fix). Kernel path (`relay-turn-lib.sh`) — implemented directly, not via the marathon below. New `test/relay-commit-pathspec.sh` (9/9). GH-198 Bug 2 (uncommitted-artifact review UX gap) remains open. → [#198](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/198)
- **GH-213/209/203 · 3-lane marathon, all Approved** ✅ **built 2026-07-16, `validate.sh` green (only pre-existing #208 red)** — all three non-kernel bugs from the last-7-days issue sweep. **GH-213**: `find-harness.sh --check` gains a case-collision detector (`test/find-harness.sh` 20/20). **GH-209**: scope narrowed from the issue's full architectural ask (risks breaking GH-206's zero-config goal) to a static audit (`test/marathon-root-audit.sh` 13/13, new) — the builder turn found and fixed the actual leak: `MARATHON_LANE_NS` bleeding from a live `marathon-drive.sh` process into a nested `test/marathon.sh` run, which had caused a gate false-negative earlier in this same marathon. **GH-203**: scope narrowed to a non-destructive `swarm-preflight.sh` stale-`.git/index.lock` warning (`test/swarm-preflight.sh` 94/94, +7). One more recovery: agy's gh209 review tripped the known, already-tracked GH-183 isolation-breach false positive; content independently re-verified and committed by hand. → [GH-213-CASE-COLLISION-LANDMINE.md](PROJECT/3-COMPLETED/GH-213-CASE-COLLISION-LANDMINE.md) · [GH-209-MARATHON-ROOT-LEAK-AUDIT.md](PROJECT/3-COMPLETED/GH-209-MARATHON-ROOT-LEAK-AUDIT.md) · [GH-203-STALE-INDEX-LOCK-PREFLIGHT.md](PROJECT/3-COMPLETED/GH-203-STALE-INDEX-LOCK-PREFLIGHT.md) · [#213](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/213) · [#209](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/209) · [#203](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/203)
- **GH-212 · Make marathon builder-default (no billed CLI) and plan-location (PROJECT/2-WORKING) explicit, enforced defaults** ✅ **built 2026-07-15, `validate.sh` green (only pre-existing #208 red) — PR pending** — `marathon.sh`/`marathon-drive.sh` hardcode `BUILDER="claude"` (a billed headless CLI subprocess), disagreeing with `swarm-preflight.sh`'s own suggested `--builder codex`; separately, `marathon-plans` appears 0 times in any vendored doc so agents pattern-match a downstream repo's stray top-level plan folder instead of `PROJECT/2-WORKING/`. Operator chose the simple default-swap (`BUILDER` → `codex`, `--builder claude` becomes a documented billed opt-in) over a new `--builder self` control-flow mode. Ask: default swap in both scripts + the `XYZ_PYTHON=1` port, a `marathon.sh --plan` guard that refuses a plan outside `PROJECT/2-WORKING/` (exempt: harness-owned examples, or `MARATHON_ALLOW_PLAN_OUTSIDE_WORKING=1`), both conventions documented in `GUIDING-PRINCIPLES.md`. cx/risk/eff 3/2/4. → [GH-212-MARATHON-BUILDER-PLAN-DEFAULTS.md](PROJECT/3-COMPLETED/GH-212-MARATHON-BUILDER-PLAN-DEFAULTS.md) · [#212](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/212)
- **GH-211 · Consult and Relay — compact TLDR + sorted-category output** ✅ **built 2026-07-15** — `relay`'s and `consult`'s chat-facing reports (not the relay log file or consult transcripts) now open with a 1–2 sentence TLDR and, for non-trivial findings, close with a sorted category list: relay reuses its own `[Blocker]`/`[Should]`/`[Nit]`/`[Pass]` grades (new "Reporting to the human" section, `giant-brains-claude-skills` `eb9271b`, committed not pushed — operator's call, that repo is shared across every project symlinking it), consult adds a Blocking/Worth-doing-optional/Skip triage under its existing disagree-first synthesis (`skills/consult/SKILL.md`, this repo). Verdict semantics, disagree-first ordering, and the log/transcript file formats are unchanged — this only reshapes the chat reply. cx/risk/eff 1/1/2. → [GH-211-CONSULT-RELAY-TLDR-SUMMARIES.md](PROJECT/1-INBOX/GH-211-CONSULT-RELAY-TLDR-SUMMARIES.md) · [#211](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/211)
- **GH-207 · Marathon retry/resume brittle against pre-existing lane state (cross-marathon lane-id collision + no-progress HALT on a done lane)** ✅ **built 2026-07-15 on `marathon/gh-205-turn-timeout-false-halt-2026-07-15` (agy Approved, gate 162 green) — PR pending** — `.tick/attempts/<lane>` + `phases/<id>/RELAY.md` are keyed by bare plan-local ids in a shared per-repo namespace, so a new marathon's `p1` inherits a 9-day-old stale attempt and PARKs at the cap before its builder runs; a lane whose artifact is already built + gate-green escalates `no-progress` (exit 3) with no way to advance the `depends_on` chain; plus spurious 0-line `dependency.drift` on harness-owned files. Ask: namespace lane state by marathon name, idempotent re-render commit, an already-satisfied lane path (green gate + reviewer approval → advance). cx/risk/eff 4/3/4. → [GH-207-MARATHON-RETRY-RESUME-BRITTLE.md](PROJECT/3-COMPLETED/GH-207-MARATHON-RETRY-RESUME-BRITTLE.md) · [#207](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/207)
- **GH-206 · Marathon: vendored .xyz/ conflates harness-home with repo-root — won't run without MARATHON_ROOT + bin overrides** ✅ **built 2026-07-15 on `marathon/gh-205-turn-timeout-false-halt-2026-07-15` (agy Approved, gate green) — PR pending; follow-up #209 open** — `marathon.sh:37-40` derives one `ROOT` used as both harness home (`bin/tick`, `bin/marathon-yaml`) and repo root (briefs, `TICK_REPO_ROOT`); in a vendored `.xyz/` those differ and no single value works — a plain invocation needs a 4-variable override set (reproduced in sleuth-app 2026-07-15). Ask: split into `MARATHON_HOME` (script-relative) + `MARATHON_ROOT` (`git rev-parse --show-toplevel`), each overridable, mirroring consult's `CONSULT_ROOT`. cx/risk/eff 3/3/3. → [GH-206-VENDORED-MARATHON-ROOT-SPLIT.md](PROJECT/3-COMPLETED/GH-206-VENDORED-MARATHON-ROOT-SPLIT.md) · [#206](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/206)
- **GH-205 · Marathon: 300s per-turn cap (RELAY_TURN_TIMEOUT_S) too short for real code+test builds → false HALT** ✅ **built 2026-07-15 on `marathon/gh-205-turn-timeout-false-halt-2026-07-15` (agy Approved, gate 144/144) — PR pending; the lane live-reproduced its own bug mid-run** — a builder turn that already wrote + committed its artifact with a green gate still gets killed at `codex-turn.sh:131`'s 300s default (exit 7) and HALTs the whole chain, shipping the artifact unreviewed (observed live, sleuth-app GH-367 marathon 2026-07-15). Ask: raise the default (600–900s), add per-lane `turn_timeout_s:` to MARATHON.yaml, treat exit-7-with-artifact-present+gate-green as complete-pending-review, document the env var (currently only a shim comment). cx/risk/eff 3/3/3. → [GH-205-TURN-TIMEOUT-FALSE-HALT.md](PROJECT/3-COMPLETED/GH-205-TURN-TIMEOUT-FALSE-HALT.md) · [#205](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/205)
- **GH-197 · Spike: two-axis (disclosure/steering) diagnostic tag pass on AGENTS.md** ✅ **Phase 0 complete 2026-07-10, Codex-reviewed and Approved after 3 rounds — verdict: predicted disclosure-heavy skew does not hold for AGENTS.md's 8 principles** — tagged all 8 current AGENTS.md Operating Principles (disclosure/steering/both); split is steering-heavy or balanced (6 steering-leaning vs. 4 disclosure-leaning), not disclosure-heavy as `AGENT-MD-PYDANTIC.md`'s Phase 0 predicted. Codex review ([relay](PROJECT/1-INBOX/GH-197-AGENTS-MD-TWO-AXIS-SPIKE.md), 3 rounds) caught and fixed two overreaches in the original write-up: the first draft claimed a strict file-level split (`ROUTER.md` = pure disclosure, `AGENTS.md` = pure steering), but `ROUTER.md` also carries steering-shaped verify gates and `AGENTS.md` opens with disclosure-shaped routing pointers — narrowed to "ROUTER leans disclosure, AGENTS leans steering," and "no further work needed"/repo-wide-sufficiency claims were retracted as unproven by a two-file read. One candidate missing-steering-rule flagged (a standing "stop and ask when ambiguous" rule lives only in system-level instructions, not in either repo-owned file) but not actioned. cx/risk/eff 1/1/1. → [GH-197-AGENTS-MD-TWO-AXIS-SPIKE.md](PROJECT/1-INBOX/GH-197-AGENTS-MD-TWO-AXIS-SPIKE.md) · [#197](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/197)
- **GH-191 · ATE: generalize harness runner beyond Aider (pluggable command/flags/classifier)** 🆕 **captured 2026-07-09, rated — not urgent, deferred backlog** — `utils/ate/`'s variation-test runner is described generically but hardcoded to Aider: `run_aider()`'s binary name and flags (`--edit-format`/`--map-tokens`/`--no-auto-commits`/etc.) are literal, `build_variations()`'s grid schema is Aider-specific (`edit_formats`/`map_tokens`/`auto_commits`), `CLASSIFY_PROMPT` names the Aider/OpenRouter/GLM-5.2 pipeline directly, and `install.sh` hard-requires the `aider` CLI on `PATH`. Ask: replace with a `variations.yaml`-declared command template (safe argv substitution, not a shell string), arbitrary grid keys, a parameterized classifier prompt, and a conditional install check — Aider stays the default/reference config, no regression. **Promoted to `2-WORKING` 2026-07-19 via /10days sweep with an auto-drafted contract.** cx/risk/eff 3/2/3. → [GH-191-ATE-GENERALIZE-HARNESS.md](PROJECT/2-WORKING/GH-191-ATE-GENERALIZE-HARNESS.md) · [#191](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/191)
- **GH-173 · Jedi Wright beta feedback: agy worktree grounding + reconciliation-layer hardening** **captured 2026-07-07, Phase 2 dogfood-mining pass 2026-07-07 night** — fast-path (Consult+Relay) beta trip report triaged into 4 verified bugs + 4 architecture gaps + 2 doc notes, all clustered on the epistemic/reconciliation layer. Phase 1 (#175) firing now via an Antigravity agent in an isolated worktree. Tonight's task: mined today's own Codex+agy consult + aider/GLM-5.2 relay transcripts (fresh field evidence, same category as Jedi's report) back into the Validation table — found **B2**'s exact warning-emission site + a live false-positive (`relay-drive.sh:226-240` vs. `relay-turn-lib.sh`'s seeding step), a live unstamped **A2** degrade (codex timeout, no mechanical marker), a live hedged-but-wrong advisor claim for **A4**, a **B1** non-repro (agy fine today — suggests intermittency), and **B3** partial counter-evidence (both advisors cited unprompted today). Likely splits into per-cluster `GH-*` issues on promotion; ratings provisional. cx/risk/eff 4/3/4. → [GH-173-JEDI-WRIGHT-FEEDBACK.md](PROJECT/2-WORKING/GH-173-JEDI-WRIGHT-FEEDBACK.md) · [#173](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/173)
- **GH-175 · #173 feedback Phase-1 low-fruit slice: bring-up + README supply-chain docs + Codex preflight attestation** ✅ **SHIPPED 2026-07-08 (PR #179, `39729a0`); closed 2026-07-08** — the contained, no-reporter-dependency slice of #173: **B4** headless bring-up doc notes (macOS Documents prompt, `agy -p` vs TTY), **D2** README supply-chain notes (symlinked skills, agy self-update), **A3** parse Codex's exec preamble (model/provider/sandbox) into the consult panel record — confirmed live in `README.md` + `relay-automation/consult.sh`. Additive/doc-only; disjoint write-set from #159/#168/#169. Parent [#173](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/173); design-heavy items stay parked there. cx/risk/eff 2/1/2. → [GH-173-JEDI-WRIGHT-FEEDBACK.md](PROJECT/2-WORKING/GH-173-JEDI-WRIGHT-FEEDBACK.md) · [Plan E](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-07-E-BUILD.md) · [#175](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/175)
- **GH-178 · #173 feedback split: epistemic/reconciliation-layer hardening (agy grounding, stale warning, advisor pluggability, degraded-panel stamp, verdict provenance)** — updated 2026-07-08 — all 5 items now have a shipped fix: **B1** agy worktree grounding (isolation-breach transcript scan, PR #181; caveat #183), **B2** stale HEAD-visibility warning (PR #181), **A1** advisor pluggability (data-table dispatch, this pass), **A2** degraded-panel stamp (PR #181), **A4** verdict provenance (this pass — deliberately a scoped "no citation anywhere" slice, NOT the full firsthand-vs-asserted taxonomy, which stays future-scoped). Parent [#173](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/173). See the ad-hoc detour entry above for today's B3/A1/A4 PR. cx/risk/eff 4/3/5. → [GH-178-EPISTEMIC-RECONCILIATION-HARDENING.md](PROJECT/2-WORKING/GH-178-EPISTEMIC-RECONCILIATION-HARDENING.md) · [#178](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/178)
- **GH-172 · vendored harness root-semantics audit before Python-default cutover** ✅ **SHIPPED — 4-phase marathon merged 2026-07-17 ([PR #214](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/pull/214), codex builder, agy reviewer, `validate.sh` green except pre-existing #208); #172 closed** — comprehensive Bash/Python audit of harness-install root vs coordination root vs target/work root in vendored `.xyz` runs. Found and fixed real gaps in `relay-automation/consult.sh` and `utils/py/consult.py` (stale tick-binary fallback) and `utils/py/relay_drive.py` (uncommitted-relay-file warning parity); every other scoped entry point verified clean. Cutover recommendation: safe to cut a stable Bash branch now; **not yet safe** to switch `main` to Python-default — `utils/py/consult.py` still lacks the Bash degraded-panel `SINGLE-MODEL — NOT RECONCILED` stamping, keeping `XYZ_PYTHON=1 bash test/consult.sh` red — filed as [#215](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/215), see its own entry below. cx/risk/eff 4/4/4. → [GH-172-VENDORED-ROOT-AUDIT.md](PROJECT/3-COMPLETED/GH-172-VENDORED-ROOT-AUDIT.md) · [GH-172-BASH-AUDIT-FINDINGS.md](PROJECT/3-COMPLETED/GH-172-BASH-AUDIT-FINDINGS.md) · [GH-172-PYTHON-AUDIT-FINDINGS.md](PROJECT/3-COMPLETED/GH-172-PYTHON-AUDIT-FINDINGS.md) · [GH-172-CUTOVER-RECOMMENDATION.md](PROJECT/3-COMPLETED/GH-172-CUTOVER-RECOMMENDATION.md) · [#172](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/172)
- **Marathon Plan F · validate.sh's pre-existing failing tests + parity gaps (GH-170, GH-174, GH-215, GH-208, GH-154)** 🟢 **updated 2026-07-17 — Lanes 12-13 SHIPPED (see their own entries below); Lanes 1-9 (#170) STALE; Lanes 10-11 still queued, not yet fired** — originally 9 lanes from `validate.sh` triage (`analyze.sh`, `cost.sh`, `watchdog-relay.sh`, `deep-research.sh`, `relay-token-collision.sh`, `new-relay.sh`, `find-harness.sh`, `transcript-audit.sh`, `marathon-plan.sh`) plus Lane 10 (#174). While building Swarm Preflight Contracts for this plan 2026-07-17: **all 9 Lane 1-9 tests now pass** (`swarm-preflight.sh --gh-issue 170` → STALE/exit 4; independently re-verified, incl. 5x repeats for the two flaky lanes; full `validate.sh` confirms only pre-existing `#208` red at that time) — root cause of the flip unconfirmed, #170 commented recommending closure, not closed unilaterally. **Lane 10** re-scoped to test-only: its code fix landed as a side effect of GH-172 Phase 0 (commit `7e9e683`), only the dedicated `XYZ_PYTHON=1` regression test remains. **Lane 11** (#215): GH-172's cutover-recommendation follow-up (`utils/py/consult.py` degraded-panel parity). **Lanes 12-13 added 2026-07-17** during a recent-issues (last 10 days) sweep: Lane 12 (#208) is the exact `worktree-isolation.sh` flaky race this doc's own Status table names as the last red gate; Lane 13 (#154) is another Bash/Python-port parity gap (marathon-plan.sh's GH-48 zone model missing from the JS port). Contracts added to all source docs, `swarm-preflight.sh --dry-run` confirmed ready (exit 0) for #174, #215, #208, #154; a combined 4-phase marathon (Lanes 10/11 excluded from this fire, Lanes 12-13 + Plan G's 2 lanes included) fired 2026-07-17. → [MARATHON-PLAN-2026-07-07-F-VALIDATE-FIXES.md](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-07-F-VALIDATE-FIXES.md)
- **GH-174 · agy-turn.py never got GH-171's claim-before-launch guard (XYZ_PYTHON=1 agy turns still exposed)** ✅ **SHIPPED 2026-07-17 via a marathon lane (worktree-isolated Sonnet subagent)** — the source fix had already landed via GH-172 Phase 0; added the missing regression test, case `(18a)` in `test/marathon-drive.sh`, proving the agy-leg claim event lands under `XYZ_PYTHON=1`. `bash test/marathon-drive.sh` 106/106, zero regressions. cx/risk/eff 1/1/1. → [GH-174-AGY-PY-CLAIM-GUARD.md](PROJECT/3-COMPLETED/GH-174-AGY-PY-CLAIM-GUARD.md) · [#174](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/174)
- **GH-215 · utils/py/consult.py missing Bash degraded-panel SINGLE-MODEL stamping** ✅ **SHIPPED 2026-07-17 via a marathon lane (worktree-isolated Sonnet subagent)** — ported the `SINGLE-MODEL — NOT RECONCILED` stamping from `consult.sh` into `consult.py` verbatim, 2 new `test/consult.sh` cases, gate `bash test/consult.sh` 50/50 green. **Found in the process:** `XYZ_PYTHON=1 bash test/consult.sh` is still red for an unrelated, pre-existing reason this bug was masking (GH-178 A4's citation stamping was never ported to Python either) — tracked as [#223](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/223), not fixed here. cx/risk/eff 2/1/2. → [GH-215-CONSULT-PY-DEGRADED-PANEL-PARITY.md](PROJECT/3-COMPLETED/GH-215-CONSULT-PY-DEGRADED-PANEL-PARITY.md) · [#215](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/215)
- **GH-208 · worktree-isolation.sh: flaky moved-ROOT-HEAD preserve-case race (GH-13/#14 guard)** ✅ **SHIPPED 2026-07-17 via the GH208-154-149-198 marathon (codex builder, agy reviewer, Approved)** — 9 local runs of `test/worktree-isolation.sh` at HEAD had produced 8 failures (case 6, `rc=6`); root cause turned out NOT to be a `relay-turn-lib.sh` race (that logic was correct) but a **test-fixture timing bug**: case (5)'s deliberate async write hadn't landed before case (6)'s cleanup ran. Fixed with a 2s wait in the fixture, no source-code change. Verified 8/8 clean repeated runs. Full `validate.sh`: 113/114 (only the separately-fixed `relay-pkg-freshness.sh` staleness). cx/risk/eff 2/2/2. → [GH-208-WORKTREE-ISOLATION-RACE.md](PROJECT/3-COMPLETED/GH-208-WORKTREE-ISOLATION-RACE.md) · [#208](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/208)
- **GH-154 · port-drift: marathon-plan shell heredoc vs. Python-layer port diverged (missing GH-48 zone model)** ✅ **SHIPPED 2026-07-17 via the GH208-154-149-198 marathon (Approved)** — ported `compileZoneConfig`/`QP_ZONES_CONFIG`/`QUEUE_PLAN_ZONES_FILE` into `utils/py/_marathon_plan_node.js`; also wired `utils/marathon-plan.sh`'s `XYZ_PYTHON=1` dispatcher to translate `--zones-config` into `QUEUE_PLAN_ZONES_FILE` so the flag survives the shell→Python→Node handoff. 2 new parity assertions in `test/marathon-plan.sh` (explicit-zones dry-run + rendered doc, shell vs. `XYZ_PYTHON=1`). Full `bash test/marathon-plan.sh`: 60/60 (up from 58). GH-110 P3a is now unblocked. cx/risk/eff 2/2/3. → [GH-154-MARATHON-PLAN-PORT-PARITY.md](PROJECT/3-COMPLETED/GH-154-MARATHON-PLAN-PORT-PARITY.md) · [#154](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/154)
- **Marathon Plan G · marathon/relay driver hardening (GH-149, GH-198)** ✅ **both lanes SHIPPED 2026-07-17 (codex builder, agy reviewer, both Approved)** — two small, independent driver-script bugs, neither fitting Plan F's theme: Lane 1 (#149) fixed, Lane 2 (#198 Bug 2) fixed — see their own entries below. → [MARATHON-PLAN-2026-07-17-G-DRIVER-HARDENING.md](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-17-G-DRIVER-HARDENING.md)
- **GH-149 · marathon-drive --require-clean self-trips on its own .relay-driver.lock inside a linked worktree** ✅ **SHIPPED 2026-07-17 via the GH208-154-149-198 marathon (Approved)** — `marathon-drive.sh` now resolves the driver lock via `git rev-parse --git-common-dir "$ROOT"` when `$ROOT/.git` is a file (linked worktree), landing it in the real `.git/` dir outside the worktree's own `git status --porcelain` view (falls back to the original hidden-lock behavior for a vendored `.xyz/` copy). New regression case in `test/marathon-drive.sh`. Full `bash test/marathon-drive.sh`: 105/105. cx/risk/eff 2/2/2. → [GH-149-REQUIRE-CLEAN-SELFTRIP.md](PROJECT/3-COMPLETED/GH-149-REQUIRE-CLEAN-SELFTRIP.md) · [#149](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/149)
- **GH-198 · relay-drive.sh headless turn: file-scoped commit ignores pathspec + uncommitted-artifact review fails opaquely** ✅ **Bug 2 SHIPPED 2026-07-17 via the GH208-154-149-198 marathon (Approved); Bug 1 already fixed separately (commit `bee1abf`)** — `relay-drive.sh` gained `preflight_setup_artifact_paths()`, called before each turn dispatch: scans the relay file's `Setup` section, fails fast with `artifact path not found in worktree: <path>` if a referenced artifact is missing, instead of failing opaquely mid-turn. 2 new cases in `test/relay-artifact-file.sh`. Full suite: 13/13 (up from 11). cx/risk/eff 1/1/2. → [GH-198-RELAY-DRIVE-ARTIFACT-PREFLIGHT.md](PROJECT/3-COMPLETED/GH-198-RELAY-DRIVE-ARTIFACT-PREFLIGHT.md) · [#198](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/198)
- **Marathon Plan H · cross-repo live marathon status query (GH-218)** 🆕 **captured 2026-07-17 via `/idea`, promoted to 2-WORKING, 2-phase marathon built** — lets the operator/Claude ask "what marathons are running right now" across every XYZ-vendored repo, returning repo + marathon/lane + in-flight task + claimant. Composes entirely existing primitives (`hq_known_repos`/registry.tsv for repo discovery, `tick project`'s own derived `STATE.md` for live claim state, `rollup.sh`'s existing embed mechanism for Obsidian) — explicitly rejects a per-repo MCP server as unjustified overkill (everything needed is a local file/process read on this machine). Phase 1: new `utils/hq/marathon-live.sh`. Phase 2: small `rollup.sh` hook to embed its report. cx/risk/eff 3/1/2. → [MARATHON-PLAN-2026-07-17-H-LIVE-MARATHON-STATUS.md](PROJECT/2-WORKING/MARATHON-PLAN-2026-07-17-H-LIVE-MARATHON-STATUS.md)
- **GH-218 · Cross-repo live marathon status query (repo + lane + in-flight task), no per-repo MCP servers** ✅ **SHIPPED — closed 2026-07-21** (commit `d919e92`, merged PR #256) — see Marathon Plan H above for the full scope; capture doc carries the Phase 1/2 checklists and Swarm Preflight Contract. cx/risk/eff 3/1/2. → [GH-218-CROSS-REPO-LIVE-MARATHON-STATUS.md](PROJECT/3-COMPLETED/GH-218-CROSS-REPO-LIVE-MARATHON-STATUS.md) · [#218](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/218)
- **GH-171 · codex-turn.sh's claim-before-launch never fires in vendored .xyz consumer repos — no-progress persists after GH-165** ✅ **SHIPPED 2026-07-07 (merge commit `625d26f`); #171 closed** — found live re-firing sleuth-app's stalled marathon after syncing in GH-165: `_tickbin` was blindly built as `$TICK_REPO_ROOT/bin/tick`, which doesn't exist in a vendored install (real binary is `.xyz/bin/tick`) — the `-x` check silently failed, so GH-165's whole claim block was skipped with no error, exactly reproducing the pre-GH-165 symptom. Fixed on `fix/gh-171-chain-root-cause` (commit `b00ee48`), merged to main via `625d26f`: new `rtl_tick_bin()` resolver (TICK_BIN override → real path under the pinned root → harness-local fallback), preserves an inherited `TICK_REPO_ROOT` instead of clobbering it, extends the same claim-before-launch guard to `agy-turn.sh`. New regression test reproduces the exact vendored-consumer shape end-to-end (`test/marathon-drive.sh`, 5 new cases); independently verified twice — once pre-merge and again post-merge (not trusting the commit message either time): `marathon-drive.sh` 60/0, `codex-turn.sh` 37/0, `relay-turn-handoff.sh` 2/0, `agy-turn.sh` 33/0, `aider-turn.sh` 36/0, `claude-turn.sh` 30/0, `XYZ_PYTHON=1 codex-turn.sh` 37/0 — zero regressions. Review surfaced one parity gap, split out as #174. Still needs: re-verify sleuth-app's actual GH-349 marathon lane now that the fix is live. → [#171](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/171)
- **GH-170 · validate.sh: 9 pre-existing failing tests** ✅ **SHIPPED — closed 2026-07-21** (all 9 tests pass; no fixing commit identified, root cause of the flip unconfirmed but effect confirmed via live re-run) — triage doc authored 2026-07-07 with per-item findings + fix direction; while building this plan's Swarm Preflight Contract, `swarm-preflight.sh --gh-issue 170` returned STALE (exit 4). Independently re-verified: all 9 tests pass, incl. 5x repeated runs of the two confirmed-flaky lanes; full `validate.sh` confirms only pre-existing `#208` red repo-wide. Root cause of the flip unconfirmed — commented on the issue with full evidence recommending closure. cx/risk/eff 3/2/4. → [GH-170-VALIDATE-FAILING-TESTS.md](PROJECT/3-COMPLETED/GH-170-VALIDATE-FAILING-TESTS.md) · [#170](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/170)
- **Marathon Plan E · acorn-integration + aider-turn.sh + hq-resolve bugfix cluster (GH-159, GH-168, GH-169, GH-175, GH-186)** ✅ **all 5 lanes complete; updated 2026-07-09** — #159/#168/#169/#175 shipped via PR #179 (merge `39729a0`), #159/#169/#175 closed on GitHub. **#186 built + Approved 2026-07-09** on local branch `marathon/gh-186-aider-vendor-version-drift-2026-07-09` (not yet pushed/PR'd) via a codex-builder/agy-reviewer marathon relay — surfaced and fixed a real secondary bug along the way (`ALLOW_PATHS` env leakage in `test/aider-turn.sh` Case 12); `bash validate.sh` green except 2 confirmed pre-existing/unrelated failures. Both review rounds hit a detector false positive (filed as **#187**, second instance of GH-183's pattern) requiring manual, transcript-verified commit recovery. → [MARATHON-PLAN-2026-07-07-E-BUILD.md](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-07-E-BUILD.md) · [PR #179](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/pull/179)
- **GH-186 · aider-turn.sh: --add-gitignore-files removal (775380c) unverified against vendored installs on older aider — risk of silently reopening GH-168** ✅ **Built + Approved 2026-07-09 (local branch, not yet merged)** — captured 2026-07-08 reviewing `775380c`; fixed via runtime `aider --help` flag-support detection instead of hardcoding either flag state, so vendored installs on any aider version get correct behavior. `test/aider-turn.sh` extended to 44/44 passing. cx/risk/eff 2/2/2. → [GH-186-AIDER-VENDOR-VERSION-DRIFT.md](PROJECT/3-COMPLETED/GH-186-AIDER-VENDOR-VERSION-DRIFT.md) · [#186](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/186)
- **GH-187 · agy isolation-breach detector: second false-positive trigger (markdown citation link), confirmed live-blocking a real marathon lane** ✅ **closed 2026-07-10** — captured 2026-07-08/09, found live firing GH-186 twice (both review rounds) — the GH-178 B1 post-hoc `$ROOT`-citation scan (`agy-turn.sh:234-242`) fired on agy's own end-of-turn markdown citation links (`[file](file:///…/xyz-3-agents-swarm/…)`), not an actual containment violation; confirmed both times by reading the raw transcript before manually recovering. GH-183's proposed fix (exclude the tick-command line) doesn't cover this trigger shape — needs a broader fix (verify from filesystem state, not transcript text, or strip citational contexts before scanning). → [#187](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/187)
- **GH-169 · Acorn integration (first pass): vendor acorn+acorn-walk as a lightweight JS symbol/call-site extractor for GH-156** ✅ **SHIPPED 2026-07-08 (PR #179, `39729a0`); closed 2026-07-08** — off GH-163's license/smoke-test verdict; infra-only, additive, not GH-156's full scorer. `src/acorn-extract.js` + `acorn`/`acorn-walk` deps confirmed live in `main`. cx/risk/eff 2/1/2. → [GH-169-ACORN-FIRST-PASS-INTEGRATION.md](PROJECT/3-COMPLETED/GH-169-ACORN-FIRST-PASS-INTEGRATION.md) · [#169](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/169)
- **GH-168 · aider-turn.sh: --no-gitignore doesn't enable reading gitignored files — missing --add-gitignore-files silently skips relay threads in gitignored dirs** ✅ **shipped ad-hoc 2026-07-08 (`775380c`, on `claude/gh-173-178-beta-patches-ygfgc5`, unmerged); #168 closed** — captured 2026-07-07, found live via a `.xyz`-vendored relay run (pdda repo); one-line flag fix + regression test. Follow-up found reviewing the fix itself: the flag was later confirmed obsolete/removed upstream and dropped again, verified only against one local aider version — tracked as **GH-186**. cx/risk/eff 1/1/1. → [GH-168-AIDER-TURN-GITIGNORE-BUG.md](PROJECT/3-COMPLETED/GH-168-AIDER-TURN-GITIGNORE-BUG.md) · [#168](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/168)
- **GH-165 · codex-turn: Codex can edit+commit without first owning the relay token, leaving `no-progress` stalls even after GH-67's release backstop** ✅ **SHIPPED 2026-07-07; closed** — Codex shim (`relay-automation/codex-turn.sh` + `utils/py/codex-turn.py`) now claims the exact `RELAY_TASK`, asserts `claimer == codex` before launch, and pings the token; fails fast (exit 5) before any mutation if ownership is missing. Both `bash test/codex-turn.sh` and `XYZ_PYTHON=1 bash test/codex-turn.sh` green with new no-tick and unowned-token regression cases. `validate.sh` reached 101/104 — the remaining 3 reds (`worktree-isolation.sh` moved-ROOT-HEAD case, `test_python_layer.py` missing `pytest` in this environment) are pre-existing and unrelated to this fix, not filed as their own issues yet. → [GH-165-CODEX-TOKEN-OWNERSHIP.md](PROJECT/3-COMPLETED/GH-165-CODEX-TOKEN-OWNERSHIP.md) · [#165](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/165)
- **Marathon Plan D · explore-and-plan cluster (GH-161..164)** ✅ **all four lanes complete 2026-07-07** — GH-161/162/164 went beyond their Phase 0 explore scope and shipped Phase 1 code (PR #167, closed); GH-163 completed its Phase 0 deliverable as designed (verdict: neither sibling repo has reusable AST infra — see its own entry below). → [MARATHON-PLAN-2026-07-07-D-EXPLORE-IDEAS.md](PROJECT/2-WORKING/MARATHON-PLAN-2026-07-07-D-EXPLORE-IDEAS.md)
- **GH-163 · Review wp-code-check / WP-DB-Toolkit for existing fast AST tooling reusable for swarmability** ✅ **Phase 0 complete 2026-07-07 — verdict: not applicable as-is** — both sibling repos opened and read; neither ships reusable general-purpose AST/call-graph infra for GH-156's stated need. Verdict fed into GH-156's Phase 0 scoring-contract doc: budget for building fresh parsing infra rather than assuming reuse. Addendum the same day found a third-party candidate instead (`acorn`+`acorn-walk`, MIT, smoke-tested) — usable as-is for the JS side; vendoring tracked separately as GH-169. cx/risk/eff 1/1/1. → [GH-163-WP-SIBLING-AST-REVIEW.md](PROJECT/1-INBOX/GH-163-WP-SIBLING-AST-REVIEW.md) · [#163](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/163)
- **GH-159 · hq-lib.sh: `hq_repo_resolve` reports ambiguous when the same path appears twice as a candidate** ✅ **SHIPPED 2026-07-08 (PR #179, `39729a0`); closed 2026-07-08** — `hq.sh resolve sleuth-app` returned `CANDIDATES=<path>,<path>` (the identical path twice, from two discovery sources) and bailed out `RESOLVED_VIA=ambiguous` / empty `REPO_PATH` instead of deduping to the one real match. Fixed in `hq_xyz_lookup` (`utils/hq/hq-lib.sh`) via a `seen_coords` dedup before the `n>1` collision check, confirmed live in `main`. cx/risk/eff 2/1/2. → [GH-159-HQ-REPO-RESOLVE-DEDUPE.md](PROJECT/3-COMPLETED/GH-159-HQ-REPO-RESOLVE-DEDUPE.md) · [#159](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/159)
- **GH-156 · Prelight: swarmability scoring using codebase-memory-mcp graph signals** 🆕 **captured 2026-07-06; mirrored into 2-WORKING the same day per operator request** — track a planner-side "swarmability" score that turns graph signals (`search_graph`, `trace_path`, `query_graph`) into an operator-readable verdict about whether a task can be split safely. The active plan keeps the first seam advisory and default-off: task-scoped impact expansion, hard serial gates for shared writable surfaces / hotspots / kernel-core overlap, then candidate-lane suggestions with confidence and reasons. The goal is better lane selection without touching the coordination kernel in the first slice. cx/risk/eff 3/2/3. → [GH-156-SWARMABILITY-PRELIGHT.md](PROJECT/1-INBOX/GH-156-SWARMABILITY-PRELIGHT.md) · [#156](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/156)
- **GH-157 · Peer research: competitive analysis dashboard for XYZ multi-agent coordination** ⏸️ **captured 2026-07-06 · paused research intake** — preserve the competitive-analysis dashboard as GitHub-backed research input rather than an active build plan. Current content compares XYZ against MiMo Code, Traycer, and Omnigent; maps benchmark options; and identifies roadmap gaps (memory, benchmarking, multiplayer, MCP, error memory). Intentionally paused in `1-INBOX` until someone chooses to convert the findings into benchmark work, messaging, or product follow-ups. cx/risk/eff 2/1/2. → [GH-157-PEER-RESEARCH.md](PROJECT/1-INBOX/GH-157-PEER-RESEARCH.md) · [#157](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/157)
- **GH-147 · LM Studio local-LLM lane for consult, relay, and swarm** 🟢 **Phase 0-2 SHIPPED, Phase 3 next** — the spike proved raw LM Studio transport against `http://127.0.0.1:1234` with model `agents-a1` and narrowed the first seam to **Aider bridge first**. **Phase 2 SHIPPED 2026-07-17 by `/10days`** (`160c0fe`) — threaded the same base-URL/model/dummy-key contract Phase 1 proved in `consult.sh` through the production Aider relay turn shim (`aider-turn.sh` + `utils/py/aider-turn.py`), byte-identical default OpenRouter path confirmed, `test/aider-turn.sh` 55/55. **Phase 3** explicit swarm-preflight / marathon lane assignment, **Phase 4** hardening + live proof remain. Defaults stay opt-in. cx/risk/eff 4/3/4. → [GH-147-LM-STUDIO.md](PROJECT/2-WORKING/GH-147-LM-STUDIO.md) · [#147](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/147)
- **GH-141 · Containment: rtl_enforce's pre-turn dirty snapshot can't see a concurrent peer session's edit that lands mid-turn, reverting it as off-lane** 🆕 **captured 2026-07-05** — `rtl_before()` snapshots `git status --porcelain` once at turn start so `rtl_enforce` can exempt pre-existing ambient WIP; it can't see an independent session's edit landing *during* a turn's execution window, so `rtl_check()` reverts it directly in `$RTL_ROOT` as if the agent made an off-lane edit itself. Live incident: a review-only agy turn reverted a concurrent Claude Code session's in-progress edit to `PROJECT/1-INBOX/PEER-RESEARCH.md`; recovered by hand (content still visible in scrollback), not recoverable in general. Real but narrow — needs a driven turn AND a second live session hand-editing the same clone simultaneously; same family as the already-guarded concurrent-commits race, but for uncommitted edits. Found reviewing **#140**. **Investigated 2026-07-17 by `/10days` — no fix shipped, deliberately.** No code-only signal can distinguish a peer session's concurrent edit from the agent's own off-lane self-escape (both produce identical porcelain diffs), and `rtl_enforce`'s revert is the documented backstop for a real, already-exploited self-escape vector (GH-22) — a naive fix would silently disable that protection. Two non-detection follow-ups recorded for an operator decision: a recoverable backup-before-revert, and documenting the don't-hand-edit-a-live-clone constraint. Still open, still not marathon-ready. cx/risk/eff 2/2/2 (provisional). → [GH-141-CONCURRENT-PEER-EDIT-RACE.md](PROJECT/2-WORKING/GH-141-CONCURRENT-PEER-EDIT-RACE.md) · [#141](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/141)
- **GH-151 · Cost observability Phase 4 — coverage-truth spike** ✅ **SHIPPED 2026-07-17 by `/10days`** (`7c77e4f`) — findings: Codex `exec --json` emits a real usage object (feasible for #153, gated on wiring `--json` into `codex-turn.sh`); agy confirmed structurally cost-blind (no usage surface anywhere probed); Gemini relay-lane has zero live turn-shim callers, recommended for **retire**, not reconnect; claude-turn.sh metering confirmed wired by code read. Gates Phase 5-7 (#152, #153). cx/risk/eff 2/1/2 (provisional). → [GH-151-COST-OBS-COVERAGE-SPIKE.md](PROJECT/2-WORKING/GH-151-COST-OBS-COVERAGE-SPIKE.md) · [#151](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/151)
- **GH-152 · Cost observability Phase 6 — auto-surface cost at end of driven runs** ✅ **SHIPPED 2026-07-17 by `/10days`** (`89d823a`) — `relay-drive.sh` now emits the `tick analyze --format human` cost block at end-of-run (floor/coverage markers preserved verbatim, no recomputation) behind a `RELAY_COST_SUMMARY` env toggle, wired through the existing lock's `EXIT` trap without altering the driven run's real exit code. `test/relay-review-once.sh` 5/5. `marathon-drive.sh`'s own top-level summary is a natural follow-on, not required here. cx/risk/eff 2/2/2 (provisional). → [GH-152-COST-AUTOSURFACE.md](PROJECT/2-WORKING/GH-152-COST-AUTOSURFACE.md) · [#152](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/152)
- **GH-155 · agy hardening remainder — S2/S5/S8 regression tests** ✅ **SHIPPED 2026-07-17 by `/10days`** (`5f37954`) — 3 new regression cases in `test/agy-turn.sh` (S2 role/model adherence, S5 distraction, S8 cost-blindness), 54/54 own tests (41 pre-existing + 13 new), full `validate.sh` 113/114 (only pre-existing unrelated `acorn-extract.sh` red). S10 (off-prompt-nonempty repro) stays open per #142's own note that it "could not re-trigger on demand." cx/risk/eff 2/1/2 (provisional). → [GH-155-AGY-HARDENING-REMAINDER.md](PROJECT/2-WORKING/GH-155-AGY-HARDENING-REMAINDER.md) · [#155](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/155)
- **GH-150 · marathon-plan: docOf() picks wrong doc → false "unrated"** ✅ **SHIPPED 2026-07-06 (`ec76b4e`); #150 closed** — `docOf(item, gh)` now prefers a `GH-<item.gh>-*.md` link (2-WORKING first, then any dir) ahead of the `mds[0]` fallback, so an entry citing a completed `MARATHON-PLAN-*.md` before its `→ GH-<n>` pointer no longer reads ratings from that unrelated doc. Built via marathon (codex build → agy Approved, gate `test/marathon-plan.sh` 58/0); verified on main + #109 no longer mis-flagged unrated. cx/risk/eff 2/1/2. → [GH-150-MARATHON-PLAN-DOCOF-POINTER.md](PROJECT/3-COMPLETED/GH-150-MARATHON-PLAN-DOCOF-POINTER.md) · [#150](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/150)
- **GH-133 · flaky: relay-dep-drift.sh case 4 "turn still committed" fails ~1-in-3, reproducible on untouched main** ✅ **SHIPPED 2026-07-06 (`f28330c`); #133 closed** — root cause confirmed SIGPIPE: case 4's `git log --oneline | grep -q 'RELAY-T'` closed the pipe early. Fixed by capturing the log then matching with `[[ "$turn_log" == *RELAY-T* ]]` (no pipe). Built via marathon in an isolated worktree (codex build → agy Approved, gate 12/0), verified flake-free 6/6 + 8/8; full `validate.sh` green. cx/risk/eff 2/1/2. → [GH-133-RELAY-DEP-DRIFT-FLAKE.md](PROJECT/3-COMPLETED/GH-133-RELAY-DEP-DRIFT-FLAKE.md) · [#133](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/133)
- **GH-94 · Installer self-extracting heredoc mangles `!`→`\!` when materialized programmatically** ✅ **SHIPPED 2026-07-06; #94 closed** — re-verify spike: faithful verbatim materialization of §4 (lines 98–1333) runs **clean** (11 files, `tick` runs, 0 `\!`) → the artifact is correct. Bug confirmed real but **materialization-method-specific**: only an upstream `!`-escaping (`bash -c`/history-expansion) layer corrupts `!==`→`\!==` before it reaches the quoted heredoc. Shipped the proportionate fix — a materialization guard in `SKILL.md` §4/§4b (write the raw block to a file, never through a `!`-escaping wrapper). npx/release-tarball **deferred** (disproportionate at this severity; `package.json` intentionally absent). cx/risk/eff 2/2/2. → [GH-94-INSTALLER-HEREDOC-ESCAPE.md](PROJECT/3-COMPLETED/GH-94-INSTALLER-HEREDOC-ESCAPE.md) · [#94](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/94)
- **GH-129 · deep-research: add Perplexity grounded search (Sonar) via OpenRouter as the second backend** ✅ **SHIPPED 2026-07-04 (PR #130, `79395ee`); #129 closed · agy relay review PASS (zero findings) · live-verified pre-merge** — the follow-up phase GH-87's doc explicitly reserved: `--provider agy|openrouter` flag (default `agy`, byte-identical default path) + `runOpenRouter` via OpenRouter's chat-completions API (the repo's established gateway — GH-77/118/120, `OPENROUTER_API_KEY`), model `perplexity/sonar` (env-overridable), `--search-context-size` → Perplexity's native `web_search_options.search_context_size`, citations normalized annotations/`citations[]`/URL-scan, fail-closed typed errors incl. `missing_api_key`. `test/deep-research.sh` 45/45, `validate.sh` exit 0, one real OpenRouter→Sonar smoke (4.5s, 15 citations). **Write-set overlaps #124 — its lane is now unblocked, fires post-merge.** cx/risk/eff 2/2/2. → [GH-129-DEEP-RESEARCH-PERPLEXITY-OPENROUTER.md](PROJECT/3-COMPLETED/GH-129-DEEP-RESEARCH-PERPLEXITY-OPENROUTER.md) · [#129](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/129)
- **GH-126 · swarm-preflight: GH-55 covering-test inference uses naive substring match — false-positive risk in ALLOW_PATHS** ✅ **SHIPPED 2026-07-04 (`691848c`, bundled into the [#108 lane](PROJECT/3-COMPLETED/GH-108-SWARM-PREFLIGHT-HARDENING-BUNDLE.md) — same file)** — `expand-artifacts.mjs`'s covering-test detection (`raw.includes(artifact)`) didn't verify the artifact path was an actual reference (source/exec/require), only that it appeared anywhere in the file text — a false positive would widen the builder's `ALLOW_PATHS` beyond the intended covering test. Fixed by a genuine-reference check. Found reviewing PR #125. → [#126](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/126)
- **GH-127 · swarm-preflight: GH-54 fs-touching test detector misses bare `>` redirects** ✅ **SHIPPED 2026-07-04 (`691848c`, bundled into the [#108 lane](PROJECT/3-COMPLETED/GH-108-SWARM-PREFLIGHT-HARDENING-BUNDLE.md) — same file)** — the fs-touching regex caught `cat >`, `printf...>`, `>>`, `mktemp`, etc., but not a bare single `>` (e.g. `echo x > file`), a common and equally filesystem-touching pattern; a test using only that form got the weaker self-verify guidance instead of GH-54's "do not run in-turn" rule. Fixed by bare-`>` detection. Found reviewing PR #125. → [#127](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/127)
- **GH-106 · codex-turn: default `CODEX_FLAGS` hangs on approvals in headless runs** ✅ **SHIPPED 2026-07-04 (`742c230`, 31/31) · part of [Marathon Plan C](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — the default `-s workspace-write` sandbox still gated some actions behind an interactive approval prompt; in a headless run nothing answers it, so the turn hung to the `RELAY_TURN_TIMEOUT_S` cap (exit 7), burning a lane attempt. Fixed: the default is now a non-interactive-safe autonomy level, still fully overridable via `CODEX_FLAGS` as before. Sibling of #107/#108 (same dogfood run). cx/risk/eff 2/2/2. → [GH-106-CODEX-FLAGS-HEADLESS-HANG.md](PROJECT/3-COMPLETED/GH-106-CODEX-FLAGS-HEADLESS-HANG.md) · [#106](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/106)
- **GH-107 · Containment reverts a complete, passing turn on an off-lane tool-cache dir (`.codebase-memory/`)** ✅ **SHIPPED 2026-07-04 (`524d345`) — KERNEL zone, built Opus-serial as specified · part of [Marathon Plan C](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — `rtl_worktree_end`'s off-lane loop discarded a turn's entire work when the builder's own tooling wrote an untracked cache dir the target repo never gitignored (reproduced live: a 442-line, 67/67-tested, otherwise-correct build discarded anyway). Sibling of #54 — same mechanism, different trigger (tool cache vs test fixtures). Fixed: `rtl_is_containment_ignored()` (`relay-turn-lib.sh:345-364`) checked in that loop before the off-lane fallthrough — root-anchored built-in list (`.codebase-memory`, `.aider*`, `node_modules/.cache`) extended by the comma-separated `CONTAINMENT_IGNORE` env var, **empty by default so default behavior is byte-for-byte unchanged**. Kernel-required [decisions record](decisions/2026-07-04-containment-ignore-toolcache.md) written alongside; `test/worktree-isolation.sh` 31/31 incl. a control asserting a non-built-in path *without* the env still exits 6; Python-layer inheritance asserted via `rtl.py`. cx/risk/eff 3/4/2. → [GH-107-CONTAINMENT-OFFLANE-TOOLCACHE.md](PROJECT/3-COMPLETED/GH-107-CONTAINMENT-OFFLANE-TOOLCACHE.md) · [#107](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/107)
- **GH-108 · `--pre-advance-cmd 'npm test -- <name>'` runs the whole suite, unrelated flakes fail a green lane** ✅ **SHIPPED 2026-07-04 (`691848c`, 85/85) — bundle also closed #126/#127 (same file) · part of [Marathon Plan C](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — `swarm-preflight.sh` passed a contract's `gate` field verbatim as `--pre-advance-cmd`; whether `npm test -- <name>` actually filters depends on the target repo's own test script, and often doesn't (reproduced live: an unrelated `EADDRINUSE` flake failed an otherwise 67/67-green lane). Fixed (Level 1, documented-caveat): when `GATE_CMD` heuristically looks like a filtered-runner invocation, the packet emits an explicit scoping caveat rather than attempting cross-runner auto-detection. Landed in one lane with **#126** (GH-55 covering-test substring-match tightening) and **#127** (GH-54 bare-`>` fs-touching gap) since all three touch `utils/swarm-preflight.sh`. cx/risk/eff 3/2/3. → [GH-108-SWARM-PREFLIGHT-HARDENING-BUNDLE.md](PROJECT/3-COMPLETED/GH-108-SWARM-PREFLIGHT-HARDENING-BUNDLE.md) · [#108](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/108) · [#126](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/126) · [#127](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/127)
- **GH-124 · deep-research.mjs shipped un-run against real `agy` — add a real-`agy` smoke test + runaway-grounding guard** ✅ **SHIPPED 2026-07-04 (`6daaff5`, 45/45 + self-skipping live test) · part of [Marathon Plan C](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — GH-87's adapter hung on *every* real `agy` call (stub-only tests hid it); two bugs fixed post-merge (`91f17f2` missing `--dangerously-skip-permissions`; `74cd553` `execFile` silently ignores `stdio` → agy stdin never EOF'd → switched to `spawn`). Now works (~27–52s, real citations, `test/deep-research.sh` 23/23). Hardening follow-up: an **opt-in real-`agy` smoke test** (network + `agy` on PATH, self-skips like `relay-self-sufficiency.sh`) so a stub can't again hide a real-backend break; a guard against `--search-context-size high` runaway grounding; revisit the default 120s timeout. Surfaced dogfooding #111. cx/risk/eff 2/1/2. → [GH-124-DEEP-RESEARCH-REAL-AGY-HARDENING.md](PROJECT/3-COMPLETED/GH-124-DEEP-RESEARCH-REAL-AGY-HARDENING.md) · [#124](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/124)
- **GH-118 · Make Aider edit formats more forgiving for OpenRouter models** ✅ **closed** — captured 2026-07-03 · rated · live-confirmed — Aider natively uses a 'whole' or 'udiff' edit format depending on the model, but unknown OpenRouter models (like GLM-5.2) often default to formats they don't strictly follow (e.g. outputting standard diffs instead of 'whole' file), causing Aider to fail with 'no tracked changes'. **2026-07-03 live tests confirmed the diagnosis and the fix on two models:** GLM-5.2 (first attempt: no edit emitted at all, just chat; retry with `AIDER_FLAGS=--edit-format diff` produced valid SEARCH/REPLACE) and Nemotron Ultra 3 free tier (same 'whole' default — zero nvidia/nemotron entries anywhere in Aider's model-settings.yml or heuristic chain — failed by emitting a raw unified-diff hunk, reproducing the original bug report almost exactly). Revised fix: document `AIDER_FLAGS=--edit-format diff` (already a passthrough) rather than adding a new `AIDER_EDIT_FORMAT` var; maintain a running known-model compat note. cx/risk/eff 2/1/2. → [GH-118-AIDER-OPENROUTER-FORMAT.md](PROJECT/1-INBOX/GH-118-AIDER-OPENROUTER-FORMAT.md) · [#118](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/118)
- **GH-119 · aider-turn.sh: reviewer can auto-add and edit out-of-scope tracked files under --yes-always; all-or-nothing containment discards the valid in-lane edit too** ✅ **SHIPPED 2026-07-03 (`93e2366`); closed 2026-07-04 · sibling of #54/#107** — surfaced live while testing GH-118's fix: GLM-5.2, acting as Reviewer, found a real bug and emitted a valid SEARCH/REPLACE for the relay file, but also emitted an edit for `marathon-drive.sh` (never added to the chat) which Aider's `--yes-always` auto-confirmed adding. The off-lane guard correctly caught it but discarded the *entire* turn, including the valid relay-file review. Same containment mechanism as #54/#107, different (deliberate, role-violating) trigger. Fixed: pre-seeds the diff's changed files as `--read` for review-only turns (`ALLOW_PATHS=""`) so they're structurally unwritable regardless of `--yes-always`; new `test/aider-turn.sh` case; independently re-verified via two reverse-dogfood reviews (24-file scale, zero scope-creep both times). cx/risk/eff 2/2/2. → [GH-119-AIDER-REVIEWER-SCOPE-CREEP.md](PROJECT/3-COMPLETED/GH-119-AIDER-REVIEWER-SCOPE-CREEP.md) · [#119](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/119)
- **GH-120 · Build a fuzzy-match OpenRouter model-name lookup table (alias → canonical slug)** ✅ **SHIPPED 2026-07-03 (`17e2681`); closed** — resolving colloquial model names ("GLM 5.2", "Nemotron Ultra 3") to canonical OpenRouter slugs currently required a live API query every time. Shipped: `relay-automation/openrouter-model-aliases.yml` + `resolve-model-alias.sh` (4-tier fuzzy match) + `test/model-alias.sh` (10/10), wired into `validate.sh`, documented. Independently re-verified via a GLM 5.2 reverse-dogfood review, which also caught and fixed a stale README claim (`1642304`). cx/risk/eff 1/1/2. → [GH-120-OPENROUTER-MODEL-ALIAS-TABLE.md](PROJECT/3-COMPLETED/GH-120-OPENROUTER-MODEL-ALIAS-TABLE.md) · [#120](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/120)
- **GH-117 · fix(marathon-drive): --dry-run must probe builder/reviewer binary before mutating tick state** ✅ **SHIPPED 2026-07-04 (`b4e73df`, 55/55) · part of [Marathon Plan C](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — `marathon-drive.sh` didn't probe builder/reviewer binary availability before seeding tick state, so missing-binary errors fired after the tick task was already seeded and spent (no recovery without a fresh relay-task id). Fixed: binary probe after arg parsing, before any tick mutation — catches missing `claude`/`agy`/`codex` on both `--dry-run` and live runs. (Integration commit `d5a1681` then stubbed `CLAUDE_BIN`/`AGY_BIN` in `driver-lock.sh`/`xyz-harness-hooks.sh`, which this probe correctly rejected for their unresolvable default builder.) Found in a live 3-phase marathon run. cx/risk/eff 2/1/2. → [GH-117-MARATHON-DRIVE-BINARY-PREFLIGHT.md](PROJECT/3-COMPLETED/GH-117-MARATHON-DRIVE-BINARY-PREFLIGHT.md) · [#117](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/117)
- **GH-116 · fix(tick): misleading 'break' error on open tasks + marathon retry flag** ✅ **SHIPPED — Bug A (`bb9138b`), Bug B 2026-07-04 (`53c8dce`, 17/17) · part of [Marathon Plan C](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — Bug A (`tick break` on an `open` task said "only the claiming agent can mutate it" — misleading; real problem was the task not being `claimed` at all) landed in `src/scope.js`, confirmed live. Bug B: `marathon.sh` had no retry flag, so recovering a spent task meant manually renaming the phase id in YAML. Fixed: `--retry <phase-id>` passes a suffixed `--relay-task` to that phase's `marathon-drive.sh` call (which already supported the override natively — no change needed there). Found in live run. cx/risk/eff 2/1/2 (Bug B only). → [GH-116-MARATHON-RETRY-FLAG.md](PROJECT/3-COMPLETED/GH-116-MARATHON-RETRY-FLAG.md) · [#116](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/116)
- **GH-114 · chore: remove deprecated gemini-turn.sh and scrub dead GEMINI references** ✅ **SHIPPED 2026-07-03; closed 2026-07-04** — `gemini-turn.sh` marked DEPRECATED 2026-06-19; Gemini CLI phased out. Delete the shim, remove its README row and `GEMINI_AGENT` comment from `marathon-agent.sh`. `agy-turn.sh` is the live permanent replacement. **✅ Follow-up gap found + fixed 2026-07-03:** the original `2b5f8a3` fix deleted `relay-automation/gemini-turn.sh` and dropped `gemini` from `marathon-drive.sh`'s help/validation, but left 5 test files still hardcoding `--reviewer gemini` / `RELAY_AGENT=gemini` / a direct reference to the deleted shim (`test/gemini-turn.sh`, `test/relay-turn-timeout.sh`, `test/marathon-yaml.sh`, `test/marathon-drive.sh`, `test/xyz-harness-hooks.sh`) — surfaced when today's GH-118/119/120 marathon ran the full suite (6 failures, all traced to this one root cause via zero-diff confirmation against pre-marathon HEAD). Fixed: deleted the dangling `test/gemini-turn.sh` (and its now-invalid `validate.sh` entry), swapped the other 4 files' gemini usage to `agy` (discovering along the way that `agy-turn.sh`'s auth pre-flight + empty-log guard needed dedicated stubs, unlike codex/claude). `validate.sh`: **90/90**. cx/risk/eff 1/1/1. → [#114](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/114)
- **GH-113 · fix(marathon-yaml): validator rejects agy reviewer — blocks multi-phase YAML plans** ✅ **SHIPPED 2026-07-03; closed 2026-07-04** — `marathon-yaml.js:114` hard-codes `codex|gemini` allowlist; `MARATHON.example.yaml` documents and uses `reviewer: agy`; `marathon-drive.sh` already routes `agy*`→`AGY_AGENT`. Blocking bug: any multi-phase YAML plan with an agy reviewer phase fails at parse time. Single-phase `marathon-drive.sh --reviewer agy` runs are unaffected. One-line regex fix. **✅ Follow-up gap found + fixed 2026-07-03:** `test/marathon-yaml.sh`'s reviewer-rejection assertion hardcoded the pre-agy error string (`"must start with codex or gemini"`), which went stale the moment the actual message changed to `"must start with codex, gemini, or agy"` when agy was added — a false failure, not a real regression. Relaxed the assertion to check the `"must start with codex"` substring (resilient to future prefix-list changes) instead of hardcoding the full message. Note: `gemini` remains a genuinely accepted reviewer prefix in `src/marathon-yaml.js` itself (out of scope for this test-only fix). cx/risk/eff 1/1/1. → [#113](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/113)
- **GH-112 · Progressive Python port — boundary decision + opt-in Python layer** 🐍 **spike SHIPPED · port LANDED as opt-in 2026-07-04 (`gh112-python-optin`)** — spike answered all three questions ([decision record](decisions/2026-07-04-python-port-boundary.md): `relay-turn-lib.sh` stays Bash permanently; Option A discrete Python CLIs; zero test-suite changes needed). The full port first arrived as PR #121 (Python-by-default, `development`-targeted, conflicting) — **not merged**; rebuilt file-level on a clean branch off post-#134/#135 `main`: 13 modules under `utils/py/` + pytest layer, with the toggle **inverted** — Bash stays the default in all 11 entry scripts, `XYZ_PYTHON=1` opts into the Python route (same CLI/exit-code contract). `validate.sh` 100/100 green default-mode; toggle routing 22/22; pytest 8/8. **#134 parity lane SHIPPED 2026-07-05 (marathon-drive; codex→agy Approved):** ported GH-106/GH-117/GH-108 into the `utils/py/` twins + 4 parity tests (incl. a GH-107 inheritance assertion via `rtl.py`), and wired `pytest` into `validate.sh` (102/102). **Issue #112 CLOSED 2026-07-05** — port scope done; promotion of Python to the DEFAULT is intentionally deferred (Bash stays default per the decision record; a fresh issue can track any future default-flip). ✅ cx/risk/eff 2/2/2. → [GH-112-PYTHON-134-PARITY.md](PROJECT/3-COMPLETED/GH-112-PYTHON-134-PARITY.md) · [GH-112-PYTHON-PORT-SPIKE.md](PROJECT/3-COMPLETED/GH-112-PYTHON-PORT-SPIKE.md) · [#112](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/112)
- **GH-109 · Gemini 3.1 Deep Think audit — watchdog process leak, tmp collision, DRY turn scripts, Python inline extraction** 🆕 **captured 2026-07-03 · rated · P1 SHIPPED 2026-07-04** — 4 of 5 findings actioned: orphaned `sleep` grandchild leak in `consult.sh` watchdog (resource bug, `aceffe3`), `/tmp` permission collision in `relay-xyz-guard.sh` (trivial `$UID` fix, `ed2f9a1`), inline Python heredoc extraction to discrete utils (scoped-down Item 5), and DRY turn-script audit (scoped-down Item 1 — extend relay-turn-lib.sh, not a new dispatcher). Item 4 (retire scanner for Gitleaks/TruffleHog) declined — no-external-dep constraint. Phase 1 (the two resource/collision bugs) shipped via [MARATHON-PLAN-2026-07-04.md](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-04.md) Lanes B/C; issue stays open for the remaining scoped-down items. cx/risk/eff 3/3/3. → [GH-109-GEMINI-FEEDBACK.md](PROJECT/1-INBOX/GH-109-GEMINI-FEEDBACK.md) · [#109](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/109)
- **GH-110 · Fable 5 Max audit — shellcheck + vendor integrity + strict-mode hardening** 🆕 **captured 2026-07-03 · rated · P1 SHIPPED 2026-07-04** — Fable 5 Max ran shellcheck (63 findings, almost all cosmetic), executed all 85 tests in a clean Linux container, and diffed the vendor tarball. Five valid findings: broken test assertion in `xyz-vendor.sh:140`, stale `relay-pkg.tar.gz` (safety core — also the GH-104 remaining follow-up), 8/85 vendored-copy test failures, 788-line JS heredoc invisible to the Tier-1 check gate, and undocumented strict-mode convention. Phased: P1 quick fixes (~1 hr) shipped via [MARATHON-PLAN-2026-07-04.md](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-04.md) Lane A. **P2a SHIPPED 2026-07-06 (`073857a`)** — rebuilt the stale tarball via `make-pkg.sh` + added `test/relay-pkg-freshness.sh` (cmps every packaged file vs live, fails on drift; verified it catches injected drift) wired into `validate.sh`. Built via marathon (codex → agy Approved; gate flaked on unrelated `xyz-harness-hooks.sh`, #110 change verified clean on main: freshness 3/0, skill-extract 4/0, path-integrity 2/0). **P2b SHIPPED 2026-07-17 by `/10days`** (`49c7b8d`) — roadmap-dashboard skip-guard + new top-level `run-tests.sh` entry point. P3a tracked separately as #154. cx/risk/eff 3/2/3. → [GH-110-SHELLCHECK-VENDOR-FIXES.md](PROJECT/2-WORKING/GH-110-SHELLCHECK-VENDOR-FIXES.md) · [#110](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/110)
- **GH-87 · Deep Research mode — provider-agnostic grounded search seam (Agy Gemini Search first adapter)** ✅ **Phase 1 MERGED 2026-07-04 (PR #122)** — the new Aider-capable harness needs a first-class grounded-search tool without hard-wiring the whole system to one vendor. Local framing intentionally keeps the **search-provider seam provider-agnostic** while taking **Agy Gemini Search** as the first backend (via the Agy CLI), with Perplexity as a follow-up phase. Shipped: `relay-automation/deep-research.mjs` (zero-dep Node adapter, runs `agy -p` in a throwaway tmpdir, fail-closed typed errors, normalized `{answer, citations, query, provider, model, raw}`) + `test/deep-research.sh` (21 assertions), wired into `validate.sh`. Reviewed via automated agy relay (Approved) + operator code review. Phase-1 limitation: `--temperature`/`--max-tokens` recorded in `raw.config` but not yet passed to `agy`. **Post-merge (2026-07-04): 2 real-`agy` hangs fixed** — the adapter had shipped un-run against real `agy` (stub-only tests): `--dangerously-skip-permissions` (`91f17f2`) + `execFile`→`spawn` so agy's stdin EOFs (`74cd553`); now returns ~27–52s with real citations, `test/deep-research.sh` 23/23. Hardening tracked in **#124**; first real use posted 4 grounded searches to #111. cx/risk/eff 3/2/3. → [GH-87-DEEP-RESEARCH-MODE.md](PROJECT/3-COMPLETED/GH-87-DEEP-RESEARCH-MODE.md) · [#87](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/87)
- **GH-61 · CI GitHub Actions (Tier 1 lint/doc-hygiene + Tier 2 validate.sh)** ✅ **Tier 1 SHIPPED 2026-07-02 (`d9b8a14`, marathon dogfood — codex + agy Approved); Tier 2 held for the operator runner decision** — `.github/workflows/ci.yml` (single `ubuntu-latest` job: `shellcheck -S error` + `bash -n` + `node --check` + settings-JSON validate + `pdda.sh run`) + a dependency-free `test/ci-workflow.sh` wired into `validate.sh` (**80/80**). The run reproduced **#59** live (allowlisted artifact in a new untracked dir → spurious off-lane exit 6). **Tier 2 remaining:** running `./validate.sh` inside CI carries an unresolved `macos-latest` (fast, ~10× minutes) vs `ubuntu-latest` + portability/skip-gating decision — reserved for the operator; #61 stays open for it. no CI today; add Actions to catch the ~80% (bash logic + doc/path drift). **Tier 1** (cheap, always-green, no auth): `shellcheck` + `bash -n` on all `*.sh`, `node --check`/JSON-validate, `utils/pdda/pdda.sh run` full-mode. **Tier 2** (the real gate): `./validate.sh` 69-test suite — needs a portability + live-agent skip-gating pass, and a runner decision (`macos-latest` fast/~10× minutes vs `ubuntu` cheap/needs the pass) left open; don't make it *required* until reliably green. Marathon-sequenceable: Tier 1 = independent quick-win lane; Tier 2 depends on the skip-gating sub-task. → [GH-61-CI-GITHUB-ACTIONS.md](PROJECT/1-INBOX/GH-61-CI-GITHUB-ACTIONS.md) · brief: [gh-61-ci-tier1-brief.md](PROJECT/2-WORKING/briefs/gh-61-ci-tier1-brief.md) · [#61](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/61)
- **GH-41 · `task.done` not terminal vs higher-epoch reclaim (silent token resurrection)** 🐞 ✅ **SHIPPED 2026-07-03 (Plan A lane 1 — PR #99)** — latent kernel gap (GH-40 canary #1): in `src/project.js` `foldWithMeta` a completed task was silently resurrected by a later higher-epoch `task.claimed` (`done`→`claimed`, 0 rejections). **Option A (terminality-seal)** landed (`/consult` Codex + agy unanimous; [decisions/2026-07-02-terminality-seal.md](decisions/2026-07-02-terminality-seal.md)): a terminal is now authorized against `ownerAsOf(terminal.ts)` (not the global winner), and once sealed every later claim is rejected as the new distinct reason `claim-after-terminal` — never a `done`→`claimed` flip. Canary **inverted** (`verify-fixture.sh`: `done 1 claim-after-terminal`; control `done 0`); all projection/tick/chaos suites green, no epoch-fence regression. Option B deferred. cx/risk/eff 4/4/3. → [GH-41-DONE-NOT-TERMINAL.md](PROJECT/3-COMPLETED/GH-41-DONE-NOT-TERMINAL.md) · [#41](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/41)
- **GH-23 · Cursor CLI lane (3rd cross-model worker)** ⏸️ **parked-by-operator 2026-07-02** — add Cursor CLI as a Path-A worker (net-new `relay-automation/cursor-turn.sh` mirroring `codex-turn.sh`/`agy-turn.sh`). Assessed lane-shaped (2/2/2, shim) BUT (a) no capture doc yet and (b) only buildable if Cursor CLI has a headless print + auto-approve mode (unverified). Operator's call 2026-07-02: **skip/park** — Codex + agy already give cross-model coverage; revisit if a 3rd lane is wanted. → [#23](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/23)
- **GH-30 · optional centralized transcript archive** ✅ **ALL PHASES SHIPPED 2026-07-03 (Plan A lane 5 complete)** — optional `XYZ_ARCHIVE_ROOT` redirects relay/consult/marathon transcripts out of each foreign repo's `relay-system/` into one namespaced archive; default unset = byte-for-byte current. **Phases 1–2:** single resolver `rtl_transcript_root` + `rtl_repo_slug` in `relay-turn-lib.sh` (Model-A validation: absolute + exists + git repo, else HARD ERROR), and all writers derive their transcript base from it. **Phase 3 (Model A, risk-4 core):** `rtl_init` flags `RTL_ARCHIVE_MODE` when the relay file's git repo ≠ `RTL_ROOT`, and `rtl_enforce` commits the **transcript into the archive** via an isolated `git -C` pathspec commit while the **code artifact + `.tick` token stay on the target** — target tree free of `relay-system/`, no transcript commit in target history, and the isolated archive commit can never orphan a concurrent peer commit (GH-13 guard target-only, holds when token-tree ≠ transcript-tree). **Phase 4:** `extract-relay-telemetry.sh` reads the resolver, aggregating across all `<repo-slug>/` dirs. **Phase 5:** `new-relay.sh` wired; `CONSUMING.md` + `README.md` document the contract; CHANGELOG bet recorded. New `test/archive-commit.sh` (16) + `test/archive-telemetry.sh` (3) → `validate.sh` green. cx/risk/eff 3/4/3. Kernel lane (`relay-turn-lib.sh`). → [GH-30-CENTRALIZED-TRANSCRIPT-ARCHIVE.md](PROJECT/3-COMPLETED/GH-30-CENTRALIZED-TRANSCRIPT-ARCHIVE.md) · [#30](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/30)
- **Marathon dogfood · rebalance-OS parallel build queue (cross-repo, `--target-root`)** 🆕 **captured 2026-06-28** — **orchestrate this from Claude Code in THIS repo** as a real cross-repo dogfood: drive the rebalance-OS build queue (`PROJECT/4-MISC/QUEUE-2026-06-27.md` in that repo) via `swarm-preflight → marathon-drive --target-root <rebalance-OS clone>`. It has **3 collision-free lanes**: **SWIFT-APP** (Focus5Float VS Code focus-if-open Phase 1, claim `macOS/Apps/Focus5Float/Sources/**`), **BACKEND** (Unified Refresh/Restart v1 — signed Reminders helper + pulse server, claim `scripts/**`), **SPIKE** (Focus 5 App Store Phase 0, new `macOS/Apps/Focus5Native/**`). Disjoint write-globs ⇒ XYZ-safe concurrently. Per-lane gates differ (`swift build`+`FOCUS5_SELFTEST=1` / `pytest`+live `/api/refresh` / spike build); the *verify-and-archive* items finish on an **operator GUI litmus** (no CLI agent can satisfy a LaunchServices GUI check). Do not touch rebalance-OS outside an explicit, scoped lane run. **Status 2026-06-29: Lane A (Focus5Float VS Code focus-if-open) shipped INLINE — built by hand, NOT via the harness, since the marathon builder lane was then blocked by #14 — and pushed to rebalance `feat/apple-reminders-write`. Lanes B (refresh/restart v1) + C (App Store spike) handed to Agy (its app, direct in rebalance); the archive sweep is operator GUI-litmus. #14 is now fixed. **Status 2026-07-01: UNBLOCKED — GH-51 [1-kernel] closed 2026-06-30; `--target-root` containment normalization is fixed. Deferral condition met. Fire one scoped lane as the cross-repo proof.**
- **GH-48 · generalize marathon-plan's zone model for cross-repo pre-pre-flight** 🔨 **built and merged via PR #125 (`d9db49d`) 2026-07-04; issue stays OPEN — closure call pending** — `marathon-plan` now loads zone rules from config (`--zones-config`, `QUEUE_PLAN_ZONES_FILE`, root-local file, built-in default), generalizing the old hardcoded xyz-only `KERNEL_PATHS`/`SHIM_RE` classifier while keeping xyz's own output byte-identical by default. New coverage locks foreign-zone names, `pathRegexCaseInsensitive`, `escalateOrchestratorOnly`, config precedence, and fail-loud behavior. The rebalance-OS live check proved the important cross-repo fix — BACKEND reclassifies as `signed-helper` — but also corrected the earlier doc assumption: a single helper-writing lane does **not** split into its own wave under `maxPerWave: 1`. → [GH-48-QUEUE-PLAN-CROSS-REPO-ZONES.md](PROJECT/3-COMPLETED/GH-48-QUEUE-PLAN-CROSS-REPO-ZONES.md) · [#48](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/48)
- **Dueling/relay · commit-signal advance (file-driven mode)** 🟡 — **built + tested 2026-06-26** (agy review pending): `poll.sh --turn-source file` reads whose-turn from the relay `NEXT:` field, making the tick token **optional** — exactly what the first cross-repo dueling run needed when the Maintainer Claude worked straight off the relay file and never joined `tick`. Optional `--peer-commit-repo`/`--peer-commit-match` add the "advance on the peer's fix commit" signal (idle → run-runner only once a matching commit lands). `relay_field` now tolerates `**bold**` markdown keys; STATUS terminal + artifact-scope-clean still apply. 7 new assertions in [test/poll-driver.sh](test/poll-driver.sh); `validate.sh` **48/48**. → [poll.sh](relay-automation/poll.sh) · [Field findings](PROJECT/4-MISC/AUTOMATED-RELAY.md#field-findings--first-cross-repo-dueling-run-2026-06-26)
- **Dueling/relay · token resilience for a non-participating peer + multi-round reuse** 🟡 — **core addressed by `--turn-source file`** (token-optional ⇒ no `parked suspect` stall and no `spent/reserved — not claimable` mid-run, the two lock failures from 2026-06-26). Residual (only when you DO use the tick token): a non-watchdog `reap`+`claim` reconcile path (still manual) and token-reuse semantics that survive N `release --to peer` round-trips. Lower priority now that file-source is the recommended dueling path. → [AUTOMATED-RELAY.md → Field findings](PROJECT/4-MISC/AUTOMATED-RELAY.md#field-findings--first-cross-repo-dueling-run-2026-06-26)
- **Gate design · convergence gates that pin a *worse* code shape** 🐞 — the KWFS `#70` gate statically extracts a literal `return array(...)` from *each* of three formatter methods and asserts equality, which makes the issue's intended fix (one canonical method + delegate/delete the duplicates) **fail the gate** — the Maintainer was forced to keep three duplicated-but-identical literals, divergence the gate calls "converged." Generalizes: static-extraction needle gates can pin a less-maintainable shape than the refactor they exist to enforce. Fix direction: assert runtime output-key equality (or canonical + delegation), not three literals. → [AUTOMATED-RELAY.md → Field findings](PROJECT/4-MISC/AUTOMATED-RELAY.md#field-findings--first-cross-repo-dueling-run-2026-06-26)
- **Orchestration · in-loop gate verification must run sandbox-off** 🟡 — an orchestrator/watcher re-running a target gate (`bash tests/run.sh`) under the Claude Bash sandbox **false-fails** (PHP can't create lock files → bogus "syntax errors"); the peer's un-sandboxed run is authoritative. Handle: in-loop gate self-checks run sandbox-off, or trust the peer's run + spot-confirm sandbox-off (memory: `codex-cli-needs-sandbox-disabled` family). → [AUTOMATED-RELAY.md → Field findings](PROJECT/4-MISC/AUTOMATED-RELAY.md#field-findings--first-cross-repo-dueling-run-2026-06-26)
- **GH-142 · Tooling · agy reliability testing** 🟢 **Phase 1 characterized + Phase 2 S9 SHIPPED 2026-07-06 (`dcc1505`); S2/S5/S8 + Phase 3 open** — S1–S10 catalog filled (live agy 1.0.16); containment scenarios contained. **S9/F8 fixed**: both shims (bash + Python parity) pre-validate `AGY_MODEL` against `agy models` and exit 5 loudly instead of silently falling back to default (marathon: codex → agy Approved; `test/agy-turn.sh` 33/0, 6 new S9 cases). Remaining: S2 (role adherence), S5 (distraction), S8 (cost-blindness), the intermittent-S10 controlled repro, and a behavioral test for the Python S9 path (bash covered). cx/risk/eff 3/2/3. → [GH-142-AGY-RELIABILITY-TESTING.md](PROJECT/2-WORKING/GH-142-AGY-RELIABILITY-TESTING.md) · [#142](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/142)
- **GH-143 · Tooling · front-door onboarding health** 🟢 **Phase 4 SHIPPED 2026-07-06 (`67068da`); issue open for FD-09/FD-10** — **FD-11** (hoisted `find-harness.sh --check` to SKILL.md line 21 as a hard gate) + **FD-12** (new "Per-repo persistence" section) both landed and flipped ✅ in `PROJECT/4-MISC/FRONTDOOR.md`. Remaining front-door items: **FD-09** (AGENTS.md skill-inventory drift) + **FD-10** (README un-sandboxed callout). cx/risk/eff 1/1/1. → [GH-143-FRONT-DOOR-ONBOARDING.md](PROJECT/2-WORKING/GH-143-FRONT-DOOR-ONBOARDING.md) · [#143](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/143)
- **GH-144 · PDDA · feedback-synthesis direction** 🟢 **Phase 1 SHIPPED 2026-07-06 (`52f4521`); issue open for Phases 2–5** — synthesized the three June 23 external feedback notes (Perplexity/ChatGPT/Gemini) into two canonical governance docs: `PROJECT/CONSTITUTION.md` (policy of record — PDDA's lane + 5 non-negotiables) + `PROJECT/DO-NOT-BUILD.md` (anti-scope list), both linked from `ROUTER.md`'s Role split. Direction: keep PDDA a *thin repo-governance + safety layer*. **Remaining:** Phase 2 (contract/mode enforcement hardening); Phases 3–5 (artifact ergonomics, the Perplexity-only evidence bridge, integrations) deferred/decision-gated. cx/risk/eff 3/2/3. → [GH-144-PDDA-FEEDBACK-SYNTHESIS.md](PROJECT/4-MISC/GH-144-PDDA-FEEDBACK-SYNTHESIS.md) · relay [pdda-feedback-synthesis.md](relay-system/2026-06-23/pdda-feedback-synthesis.md) · [#144](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/144)

### In progress

- **GH-295 · Add Pi (pi.dev) as a new supported headless builder harness, alongside Codex/agy** 🟡 **all 4 phases done 2026-07-24 — ready for operator review/merge** — surfaced while closing out GH-280 (Aider+Qwen). Shipped `pi-turn.sh` + `pi-turn.py` (39-case test suite green, README documented) as a new selectable builder lane (not a codex/agy replacement), with genuine per-call `tick cost` capture — a real gap-closer vs. agy's cost-blind lane. Phase 4 validated Pi against the actual production direct-Alibaba `qwen3.8-max-preview` endpoint end-to-end (Pi has first-class native support for it, `qwen-token-plan` provider, zero extra code needed): real scratch-repo dry run committed cleanly, exit 0, production repo confirmed untouched throughout. cx/risk/eff 3/2/3. → [GH-295-PI-BUILDER-INTEGRATION.md](PROJECT/2-WORKING/GH-295-PI-BUILDER-INTEGRATION.md) · [#295](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/295)
- **GH-240 · PDDA-compatible marathon triage and cleanup skills** ✅ **SHIPPED 2026-07-18** — `marathon-triage` is now repo-owned and remains read-only/plan-only; new `marathon-cleanup` reconciles canonical docs, terminal frontmatter/status, GitHub issue and PR state, reachable commits, verification, and CHANGELOG evidence before proposing any move. It distinguishes a completed marathon slice from a fully completed issue, requires every lane to verify before archiving the parent, and defaults to confirmation-gated mutation. Both Claude entries symlink to this clone through idempotent installers. Skill validators, ShellCheck, file-scoped PDDA checks, and `./validate.sh` 115/115 passed. cx/risk/eff 2/2/2. → [GH-240-MARATHON-SKILLS.md](PROJECT/3-COMPLETED/GH-240-MARATHON-SKILLS.md) · [#240](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/240)
- **GH-201 · swe-diagram: add Git history lane visualization** ✅ **SHIPPED — closed 2026-07-21** (commit `6df5bfc`, merged PR #244) — deterministic branch lanes, a local Git-to-JSON generator (`utils/swe-diagram/scripts/git-history-to-json.js`), focused and independent fixtures, skill guidance, and a real newest-20-commits example now live under `ARCHITECTURE/`. The exact real window has one branch cut and no merge commit; synthetic and forward fixtures prove non-fast-forward merge rendering. cx/risk/eff 3/1/3. → [GH-201-SWE-DIAGRAM-GIT-LANES.md](PROJECT/3-COMPLETED/GH-201-SWE-DIAGRAM-GIT-LANES.md) · [#201](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/201)
- **GH-221 · Explicitly document Claude CLI is NOT a default builder — Claude Code is orchestrator/reviewer only** ✅ **SHIPPED 2026-07-17** — some Claude Code sessions had drifted into treating the Claude CLI as a peer marathon/relay builder; the code-level default was already `codex` (GH-212), but the only existing doc caveat was cost-framed, not role-framed, and the vendored `skills/relay-xyz/SKILL.md` (what a vendored `.xyz/` install's own sessions actually read) had zero mention of a role split at all. Fixed: added an explicit statement to `AGENTS.md`'s "Repo-specific rails" and to `skills/relay-xyz/SKILL.md` (vendored, so it reaches every `.xyz/` install on next re-vendor) — Claude Code is the orchestrator/reviewer, Agy/Codex CLI are the builders, Claude CLI is an explicit user opt-in only. Docs-only; no script behavior changed. `pdda.sh run` clean. cx/risk/eff 1/1/1. → [GH-221-BUILDER-ORCHESTRATOR-ROLE-SPLIT.md](PROJECT/3-COMPLETED/GH-221-BUILDER-ORCHESTRATOR-ROLE-SPLIT.md) · [#221](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/221)
- **GH-156 · Prelight swarmability scoring using codebase-memory-mcp graph signals** 🟡 **planning doc opened 2026-07-06; Phase 0 scoring contract next** — active work item to define a task-scoped, advisory "swarmability" score before any runtime integration. Current shape: resolve impacted symbols/files, expand graph impact, hard-gate shared writable surfaces and hotspot/core overlap, then emit `serial|parallel-safe|parallel-with-caveats|insufficient-confidence` plus candidate lanes and reasons. The first slice stays planner-only and default-off; no scheduling behavior or containment policy changes in Phase 0/1. cx/risk/eff 3/2/3. → [GH-156-SWARMABILITY-PRELIGHT.md](PROJECT/4-MISC/GH-156-SWARMABILITY-PRELIGHT.md) · [#156](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/156)
- **GH-299 · Trust-centered alternative system diagram layout** 🟡 **`trust-clustered` layout implemented + scored 5/5 on acceptance criteria 2026-07-23; final report/handoff next** — baselined the real spec under all 3 existing `utils/swe-diagram` layouts first (none cleared all 5 readability criteria — hub-ring is structurally disqualified since group swimlanes are unsupported in that mode), then implemented a new spec-driven `trust-clustered` mode: 5 columns derived from each node's existing `trust` field (orchestration spine, turn-shim cluster, untrusted periphery, shared state, off-live-path governance/audit), containment core emphasized as a hub, plus a generic edge-label-overlap fix applied to all layouts. Additive — ships as `ARCHITECTURE/system-diagram-trust-clustered.{json,html}`; default layered output untouched. `test/swe-diagram.sh` 42/42; `./validate.sh` clean except 2 pre-existing unrelated environmental reds. cx/risk/eff 3/1/3. → [GH-299-DIAGRAM-TRUST-LAYOUT.md](PROJECT/2-WORKING/GH-299-DIAGRAM-TRUST-LAYOUT.md) · [#299](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/299)

- **GH-148 · Python-layer swarm_preflight: inferred-path check lacked GH-137 sanitizer** ✅ **SHIPPED 2026-07-05 (`36983fb`); closed** — an independent `/relay-xyz` review (codex in the **reviewer** seat, deliberately the opposite of the role it held as builder of the original commit — GUIDING-PRINCIPLES §12) of `cd4c215` (GH-112's #134 Python-parity port) found a real containment gap: `utils/py/swarm_preflight.py`'s inferred test/helper walk only checked `.startswith("test/")`, which a `test/../...` string satisfies despite resolving outside the test tree — reopening the exact bug GH-137 closed in Bash (`sanitizeInferredPath`, which also rejects generated `__pycache__`/`.pyc` paths). No live exposure (`XYZ_PYTHON` defaults off) but a real silent gap. Fixed: ported `sanitize_inferred_path()`/`is_generated_inferred_path()` 1:1 from the Bash function, gated at both widening call sites; new regression tests mirror the Bash T35b fixture plus direct unit coverage of the named bypass string. Independently re-verified: codex's own Round-2 confirmatory pass re-ran the tests itself and Approved. `pytest test/test_python_layer.py` 15/15. cx/risk/eff 2/3/1. → [relay-system/2026-07-05/gh112-134-parity-codex-review.md](relay-system/2026-07-05/gh112-134-parity-codex-review.md) · [#148](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/148)
- **GH-146 · swe-diagram: add hub & ring (radial) layout option** ✅ **SHIPPED 2026-07-05 (PR #145, merge `365a1d6`); closed** — a GLM-5.2 chat brainstorm proposed an optional radial "hub & ring" layout for event-driven architectures (a central broker/gateway communicating radially with independent services) as an alternative to the existing left-to-right layered layout. Landed on the same branch/PR as #145 (the GLM-5.2 relay review): optional `layout: "hub-ring"` spec field, hub auto-picked by highest edge degree (or explicit `spec.hub`), remaining nodes split into evenly-sized concentric rings, straight rectangle-clipped edges (the layered layout's flow-direction bezier doesn't apply radially). `SKILL.md` documents when to use each layout. Verified via headless-Chrome screenshots on a synthetic spec and this repo's own real architecture (`relay-drive.sh` auto-picked as hub). cx/risk/eff 2/1/2. → [DIAGRAM-HUB.html](PROJECT/3-COMPLETED/DIAGRAM-HUB.html) · [#146](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/146)
- **GH-138 · HQ hardening: add `promote` command + fix marathon-plan detection glob** ✅ **SHIPPED (verified live on `main` 2026-07-06); #138 closed as STALE** — both gaps already landed and verified: (1) `hq promote --gh-issue N <project>` exists (hq.sh:504); (2) the glob was broadened `MARATHON-PLAN-*.md`→`MARATHON-*.md` (hq-lib.sh:288). `test/hq-promote.sh` (8/0) covers the new command + the `MARATHON-<date>.md` detection regression and is wired into `validate.sh`. cx/risk/eff 2/2/2. → [GH-138-HQ-PROMOTE-AND-MARATHON-GLOB.md](PROJECT/3-COMPLETED/GH-138-HQ-PROMOTE-AND-MARATHON-GLOB.md) · [#138](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/138)
- **GH-137 · swarm-preflight: covering-test inference can inject `test/..` (repo root) + generated files into ALLOW_PATHS** ✅ **SHIPPED + closed 2026-07-05 (built by a marathon-drive lane; found dogfooding the GH-112 parity marathon)** — swarm-preflight's embedded covering-test/helper inference ([utils/swarm-preflight.sh](utils/swarm-preflight.sh) ~L286–366) could emit a path normalizing to the repo root (`test/..` passed `existsSync`) or a generated file (`__pycache__/*.pyc`) into the suggested `artifacts`/ALLOW_PATHS — a GUIDING-PRINCIPLES §3 containment footgun. Fixed: new `sanitizeInferredPath` drops any inferred path with a `..` segment, resolving outside the repo `test/` tree, or generated (`__pycache__`/`.pyc`), applied to both inferred tests and the helper walk; declared `artifacts[]` pass through untouched (narrowing only). `test/swarm-preflight.sh` regression (T35b); `GH-137` marker. Built codex→agy **Approved**, gate 87/0, `validate.sh` 102/102. cx/risk/eff 2/2/2. → [GH-137-SWARM-PREFLIGHT-INFERENCE-CONTAINMENT.md](PROJECT/3-COMPLETED/GH-137-SWARM-PREFLIGHT-INFERENCE-CONTAINMENT.md) · [#137](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/137)
- **GH-132 · HQ resolution/dispatch hardening (Codex-review follow-ups)** 🟡 **all 4 fixes built + regression-tested on `worktree-hq-gh132-hardening` → PR** — a headless Codex review of the shipped HQ feature (thread on `main`, `4158bd0`; *Changes requested*) found 4 untested edge cases, now closed: **(B1)** basename-only resolution could pick the wrong repo → `hq_xyz_lookup` preserves `owner/repo` identity, disambiguates a collision by real git slug, else returns AMBIGUOUS (`rc=2`); **(B2)** a stale XYZ path could arm `fire` at a dead target → stale rows skipped (`[ -d ]`), not counted as installs; **(S3)** `park --title` wrote unescaped YAML + exited 0 → `hq_yaml_dq` escaping + `park --create` fails hard on a bad frontmatter check; **(S4)** `resolve '*'` matched via `find` → glob tokens refused. New `test/hq-hardening.sh` 11/11 (each reproduces the old failure first); full HQ suite 90 checks green; shellcheck + pdda clean. cx/risk/eff 3/2/2. → [GH-132-HQ-RESOLUTION-HARDENING.md](PROJECT/3-COMPLETED/GH-132-HQ-RESOLUTION-HARDENING.md) · [#132](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/132)
- **Marathon Plan B Waves 1–3** ✅ **fired 2026-07-04; only #94 remains parked** — Wave 1 shipped GH-92, GH-93, GH-96 (Seam #2 only), GH-89, GH-86; Wave 2 shipped GH-55 + GH-48; Wave 3 shipped GH-54 (all three merged via PR #125, `d9db49d`). The shared-file zones serialized exactly as planned (`#89 → #55`, `#86 → #48`) and the cross-zone brief lane ran last, alone. `validate.sh` reruns weren't blocked by the wave's touched files — the one non-green rerun was independently traced to pre-existing, order-dependent flakiness in `oracle-guard.sh`/`improve-loop-qa.sh` (not the `relay-self-sufficiency.sh` gate originally blamed). Independent review before merge filed two non-blocking follow-ups: **#126** (GH-55's covering-test substring match), **#127** (GH-54's fs-touching regex misses bare `>`). See [MARATHON-PLAN-2026-07-03-B-PARALLEL.md](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-03-B-PARALLEL.md) for the collision map and the still-parked `#94` gate.
- **GH-92 · poll.sh: relay-pointer parser rejects whole-line bold (`**NEXT: x**`) → silent turn-1 deadlock** 🔴 ✅ **SHIPPED 2026-07-04 (`aabc04d`, `dcbdadd`); closed** — `relay_field` (`relay-automation/poll.sh:152`) stripped leading markdown around a pointer key/value but not trailing, so `**NEXT: claude-reb**` parsed to `claude-reb**`, failed the `--claude-agents` membership check, and the poller silently nudged cross-model forever (a real duel stalled ~90 min). Fixed: extended the sed pipeline to strip trailing `` [`*]+ `` and widened the leading-strip to also cover a leading backtick; added a `WARNING (GH-92)` diagnostic. 6 new `test/poll-driver.sh` cases (37/37). Caught + fixed centrally: the packaged `relay-pkg.tar.gz` snapshot went stale against the fix, regenerated via `make-pkg.sh`. cx/risk/eff 1/1/1. → [GH-92-POLL-BOLD-POINTER-DEADLOCK.md](PROJECT/3-COMPLETED/GH-92-POLL-BOLD-POINTER-DEADLOCK.md) · [#92](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/92)
- **GH-93 · tick analyze: concurrency % spans the whole event log, not the run → reports 0% for a ~51% run** ✅ **SHIPPED 2026-07-04 (`f86d8cf`); closed** — `analyze()` (`src/analyze.js:435-442`) derived the run window from the whole `.tick/events/` log span, not the actual run, so stale events from a prior marathon collapsed the printed concurrency toward 0% even on a genuinely concurrent run. Fixed: defaults to the work-bounded window (first `task.claimed` → last `task.done`, matching SKILL.md's existing guidance); whole-log span kept as a separate informational field. New GH-93 regression fixture in `test/analyze.sh` (18/18; the actual test harness — the doc's original `test/analyze.js` name didn't exist). cx/risk/eff 1/2/2. → [GH-93-ANALYZE-CONCURRENCY-SPAN.md](PROJECT/3-COMPLETED/GH-93-ANALYZE-CONCURRENCY-SPAN.md) · [#93](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/93)
- **GH-96 · XYZ⇄Rebalance integration (XYZ side): xyz-sync check + emit contract** ✅ **Seams #2 + #1 SHIPPED; issue stays open for Seam #3** — mirrors rebalance-OS#102. Seam #2 (`21d9d79`, 2026-07-04): `xyz-sync check [<install_dir>|--all]` diffing each registered install's recorded `tick_version`/`source_commit` against the harness's currently-shipped values, warns (never auto-pulls) on drift. Seam #1 (`f1e5223`, 2026-07-06): `XYZ.json` schema + emit-cadence documented in `relay-automation/README.md`; a `XYZ.heartbeat.json` companion (`utils/telemetry/write-xyz-heartbeat.sh`, atomic overwrite) written at phase/round-start and cleared on every terminal path, so a hung/dead run goes stale instead of presenting the last completed record as current. **Dogfood found a real containment-kernel bug along the way**: the first marathon attempt built+approved cleanly but then crashed `marathon-drive.sh` itself and wiped the local working tree + `.git` internals (origin unaffected, recovered via re-clone) — root cause was `rtl_worktree_end`'s non-atomic `cp -R` copyback corrupting the live-executing `marathon-drive.sh`/`relay-drive.sh` mid-script when they were the lane's own artifacts. Fixed as its own kernel patch, **GH-140** (`f64a051`): atomic temp-file+rename copyback. Re-fired the identical lane on a temp branch with close monitoring — clean end-to-end this time, confirming the fix; fast-forward merged to `main`. Seam #3 (tick-lane consume, explicitly gated behind Seam #1 proving out) remains, not built here. Reviewing the GH-140 fix itself then surfaced a second, structurally different hazard (a concurrent peer session's edit reverted as off-lane) — filed separately as **#141**, not fixed here. cx/risk/eff 2/1/2 (Seam #2), 3/1/3 (Seam #1). → [GH-96-XYZ-REBALANCE-SYNC-CHECK.md](PROJECT/3-COMPLETED/GH-96-XYZ-REBALANCE-SYNC-CHECK.md) · [GH-96-XYZ-JSON-EMIT-CONTRACT-HEARTBEAT.md](PROJECT/3-COMPLETED/GH-96-XYZ-JSON-EMIT-CONTRACT-HEARTBEAT.md) · [GH-140-CONTAINMENT-ATOMIC-COPYBACK.md](PROJECT/3-COMPLETED/GH-140-CONTAINMENT-ATOMIC-COPYBACK.md) · [#96](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/96) · [#140](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/140) · [#141](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/141)
- **GH-89 · swarm-preflight: no ready path for greenfield (new-file) lanes** ✅ **SHIPPED 2026-07-04 (`e6f418c`); closed** — the GH-39 A2 artifact-existence check required every `artifacts[]` path to already exist at `target.ref`, so a pure new-file build (reproduced live on GH-87 and GH-88's real contracts) always read `NOT-READY`. Fixed: opt-in `artifacts_new: string[]` field exempting listed paths from the existence check, gated on a matching `fix_probes` `path_absent` entry on the same path (proves genuine unbuilt-ness, can't be used to dodge the check) — checked post-merge so a multi-issue bundle can split the declaration and its probe across sibling docs. Live regression against GH-87's/GH-88's actual contracts confirms both now read READY. `test/swarm-preflight.sh` 65/65. cx/risk/eff 2/1/2. → [GH-89-SWARM-PREFLIGHT-GREENFIELD.md](PROJECT/3-COMPLETED/GH-89-SWARM-PREFLIGHT-GREENFIELD.md) · [#89](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/89)
- **GH-86 · marathon-plan — surface PR-review lanes so they don't silently drop** ✅ **SHIPPED 2026-07-04 (`2ce409b`); closed** — `marathon-plan.sh` generated only build lanes; PR-review lanes lived only in a manual `PR-REVIEW-QUEUE-<date>.md` overlay nothing surfaced, so two review lanes (PR #79/#81) were silently never run (caught 2026-07-02). Fixed (Level 1 only): the plan now prints a "Review lanes (run via relay-xyz)" section naming the overlay + its lanes when today's overlay exists — verified zero output diff when it's absent. 2 new `test/marathon-plan.sh` scenarios (46/46). cx/risk/eff 2/1/2. → [GH-86-SURFACE-REVIEW-LANES.md](PROJECT/3-COMPLETED/GH-86-SURFACE-REVIEW-LANES.md) · [#86](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/86)
- **Part B — Adversarial hardening** ⚠️ — Phase 1 (epoch fencing) shipped; Phase 2 chaos-suite *detection* partials landed; Phases 2–4 are the active "adversarially proven → commercially viable" frontier. Immediate next-up: promote exactly one proof-sized Phase-2 slice into a contract-backed lane (important because Part B only keeps momentum if it advances in small, verifiable proofs instead of reopening the whole frontier at once). → [ADVERSARIAL-HARDENING.md](PROJECT/2-WORKING/ADVERSARIAL-HARDENING.md)
- **Tooling · relay-to-issue skill** ✅ **SHIPPED + VERIFIED END-TO-END 2026-07-05; closed** — a post-relay skill that distills a closed `/relay` thread into ONE checklist-style GitHub issue, filed in the repo the relay was *about* (cross-repo aware; dedup-stamped; auto-posts via `gh`). `skills/relay-to-issue/` (SKILL + `relay-to-issue.sh` + `install.sh`); the full `resolve → file → provenance-stamp → dedup` loop proven live against a real closed relay — a live `gh issue create` posted #139, and a re-run correctly reported `ALREADY_FILED` (dedup holds). Nothing outstanding. **Correction (2026-07-07):** this line was stale (still marked 🟡, pointing at a `2-WORKING` path that no longer exists) — the doc had already moved to `3-COMPLETED`, and the generated [MARATHON-PLAN-2026-07-07.md](PROJECT/4-MISC/MARATHON-PLAN-2026-07-07.md) was surfacing this as a live Wave 1 item off that drift. Re-run `utils/marathon-plan.sh` to clear the ghost lane once this line is corrected. → [RELAY-TO-ISSUE-SKILL.md](PROJECT/3-COMPLETED/RELAY-TO-ISSUE-SKILL.md)

### Completed
- **GH-300 · swe-diagram: search input touches/overflows the font-picker when typing — two distinct bugs, same symptom family** ✅ **SHIPPED 2026-07-23** — **(1)** `type="search"` never reset `-webkit-appearance`, letting WebKit's native cancel-button decoration override the input's explicit 180px width; fixed, but not independently re-verified in Safari (no Safari automation in this environment). **(2)** found on operator re-report: `positionPicker()` only ran on load/font-change/resize, never on the search box's own `input` event, so the custom clear button appearing (growing the right-anchored `.swe-search` box leftward) left the picker stranded — a window resize incidentally fixed it by recomputing from the now-current width, which is exactly the behavior reported. Fixed by dispatching a `swe-search-resize` event from `renderer.js`'s search handlers; **verified quantitatively** (headless-Chrome bounding-rect measurement: gap went from a healthy 8px to a -22px overlap after typing, now stays a constant 8px with no resize needed) — this fix is plain DOM logic, not WebKit-specific, so the Chrome verification is directly conclusive. `test/swe-diagram.sh` 42/42 throughout. → [GH-300-SWE-DIAGRAM-SEARCH-INPUT-OVERFLOW.md](PROJECT/3-COMPLETED/GH-300-SWE-DIAGRAM-SEARCH-INPUT-OVERFLOW.md) · [#300](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/300)
- **GH-273 · Automate the marathon pre-flight/post-flight ceremony** ✅ **SHIPPED 2026-07-21; closed** — all 5 phases fired via `swarm-preflight → marathon-drive` (Codex builder/agy reviewer): skill-nudge `UserPromptSubmit` hook, `/pre-marathon` + `/post-marathon` slash commands, `.claude/loose-ends-sequence.md`, `relay-automation/marathon-closeout.sh`, and a symmetric `--post-approve-cmd` flag in both `marathon-drive.sh` and `marathon_drive.py`. Full `validate.sh` green after every phase, including two genuine (non-flaky) Phase 4 fixes (a test missing `MARATHON_ROOT=`, a correctly-flagged `eval` needing a baseline entry). A real harness bug found along the way (same-phase-id retry clobbers the Approved record) was split out and filed separately as [#274](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/274) — not blocking, not part of this scope. cx/risk/eff 3/2/5. → [GH-273-MARATHON-CLOSEOUT-AUTOMATION.md](PROJECT/3-COMPLETED/GH-273-MARATHON-CLOSEOUT-AUTOMATION.md) · [#273](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/273)
- **GH-200 · swe-diagram: add top-down layered layout** ✅ **SHIPPED 2026-07-14** — deterministic top-to-bottom rank flow, vertical typed edges, group swimlanes, focused tests, skill guidance, and a third 31-node/43-edge self-contained example under `ARCHITECTURE/`. Focused gate 19/19; headless-Chrome and isolated forward tests passed. cx/risk/eff 2/1/2. → [GH-200-SWE-DIAGRAM-TOP-DOWN.md](PROJECT/3-COMPLETED/GH-200-SWE-DIAGRAM-TOP-DOWN.md) · [#200](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/200)

> Older shipped items live in [PROJECT/4-MISC/ROADMAP-COMPLETED-ARCHIVE.md](PROJECT/4-MISC/ROADMAP-COMPLETED-ARCHIVE.md) (57 entries) — moved out to keep this ledger lean (PDDA). Items below remain here because their capture doc is still in `PROJECT/1-INBOX/`.

- **GH-78 · optional hourly doc-preflight (contract-enforcing auto-edits + telemetry)** ✅ **SHIPPED 2026-07-03 (`115713f`, PR #81 — agy Approved via relay-xyz review 2026-07-02; rebased onto main, churn conflicts resolved to main, `validate.sh` green)** — opt-in `utils/telemetry/preflight-docs.sh`: when `PDDA_LLM_BIN` is set it runs an LLM doc-readiness pass that may apply contract-enforcing auto-edits, gated by a deterministic safety valve (rejects edits that worsen checks or exceed 30 lines) with JSON telemetry; a no-op that never blocks when unset. Thorough offline test coverage in `test/preflight-docs.sh` (14/14). → [GH-78-DOC-PREFLIGHT-AUTOMATION.md](PROJECT/1-INBOX/GH-78-DOC-PREFLIGHT-AUTOMATION.md) · [#81](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/pull/81) · [#78](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/78)
- **GH-45 · QUEUE 'must-complete' commitment contract + anti-rabbit-hole safeguard** 🐞 ✅ **SHIPPED 2026-07-03 (PR #91 — marathon; cross-model review applied)** — make a wave/lane a *commitment contract* so a session can't silently abandon the plan to deep-dive one item (the GH-39 drift). v1: AGENTS.md re-anchor+park paragraph + a per-lane attempt cap (`.tick/attempts/<lane>`, default 2, `--force` override) in `marathon-drive.sh` + mirror in `relay-drive.sh` + `test/lane-attempt-cap.sh`; cross-model review added reset-on-success + `--force` passthrough through `marathon.sh`. Phases 3–4 deferred. cx/risk/eff 3/3/3. → [GH-45-QUEUE-COMMITMENT-CONTRACT.md](PROJECT/1-INBOX/GH-45-QUEUE-COMMITMENT-CONTRACT.md) · [#91](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/pull/91) · [#45](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/45)
- **GH-58 · marathon `--builder claude` fails headless (`exec: claude: not found`)** 🐞 ✅ **SHIPPED 2026-07-02 (marathon — agy build, codex Approved, gate 30/30; merged `82fbf0e`)** — `relay-automation/claude-turn.sh` does `CLAUDE_BIN=${CLAUDE_BIN:-claude}` then `exec`s with no PATH discovery + no fail-fast; headless it dies with a raw `exec: not found`. Fix (shim, not kernel): resolve `CLAUDE_BIN` via PATH then `~/.claude/local/claude`, else fail fast with a clear message + distinct exit code; new `test/claude-turn.sh`. cx/risk/eff 2/2/2. Build with a codex/agy builder (not `--builder claude`). → [GH-58-CLAUDE-TURN-PATH-DISCOVERY.md](PROJECT/1-INBOX/GH-58-CLAUDE-TURN-PATH-DISCOVERY.md) · [#58](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/58)
- **GH-85 · marathon-plan `undocumented-partial-completion` false-positives on edit-existing-file lanes** 🐞 ✅ **SHIPPED 2026-07-02 (marathon — agy build, codex Approved, gate 36/36; merged `82fbf0e`)** — the planner's `some-artifacts-exist` + `changelog-mentions-it` signals misfire for a normal fix that edits existing files (artifact exists because it's *edited*, not built) + is documented in CHANGELOG, so genuinely-0%-built lanes (GH-45, GH-30) get held as `partial`. Fix: require a NEW/created artifact for the signal, and/or drop the changelog signal. Keep GH-44-style true positives. cx/risk/eff 2/2/2. → [GH-85-MARATHON-PLAN-PARTIAL-FALSE-POSITIVE.md](PROJECT/1-INBOX/GH-85-MARATHON-PLAN-PARTIAL-FALSE-POSITIVE.md) · [#85](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/85)
- **GH-84 · test/runner-loop.sh non-hermetic — runner.sh dirty-guard checks the real repo** 🐞 ✅ **SHIPPED 2026-07-02 (marathon — agy build, codex Approved, gate 7/7; merged `82fbf0e`)** — `relay-automation/runner.sh:4` resolves `ROOT_DIR` from its own script location (the real repo), not `TICK_REPO_ROOT`, so its dirty-tree artifact guard checks the real working tree; `test/runner-loop.sh` uses `README.md` as the artifact path, so a dirty real README false-fails the test (`runner: artifact paths have unstaged changes`). Surfaced by GH-83. Live relay use is correct — this is a test-hermeticity leak (GH-74/GH-44 class). Fix: additive `RUNNER_ROOT_DIR` override (default unchanged), test binds it to `$A` + a dirty-file regression case. cx/risk/eff 1/1/1, independent zone. → [GH-84-RUNNER-LOOP-NONHERMETIC.md](PROJECT/1-INBOX/GH-84-RUNNER-LOOP-NONHERMETIC.md) · [#84](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/84)
- **GH-83 · README onboarding UX — single operator path + de-brittle stale test count** ✅ **SHIPPED 2026-07-02 (`347b127`)** — cross-model `/consult` (Codex + agy) reviewed `README.md` cold as a new operator; both graded the same issues **[Blocker]**: no single "do this first" (four starting moves at once), humans sent to the agent-facing `ROUTER.md` first, insider-heavy opener, and a **stale `47/47`** count (actually `80/80` — a freshness bug). Fix: rewrite the top into a self-sufficient human landing page (plain-language value prop → one Step 1 `./validate.sh` → path branch → 4-line glossary), scope ROUTER as agent-facing, direct auth pointer, de-brittle the count, move install/kernel below concepts. Docs-only, trim + reorder (least-code). → [GH-83-README-ONBOARDING-UX.md](PROJECT/1-INBOX/GH-83-README-ONBOARDING-UX.md) · [#83](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/83)
- **GH-59 · relay-turn-lib: allowlisted artifact in an untracked dir trips spurious off-lane (exit 6)** ✅ **SHIPPED 2026-07-02 (`ac6dacb`, marathon lane — codex + agy Approved, gate 23/0)** — `rtl_in_allow` now allows a git-collapsed untracked-dir entry (`dir/`) when it is a **true ancestor** of a concrete allowlisted **file** entry (`greenfield/` ⊂ `greenfield/output.txt`), never a bare prefix (`green/` still fails) — generalizing the `.relay-artifacts` exemption; case-insensitive variant mirrored, bash-3.2. **Retires the stub-dir workaround** for greenfield-in-a-new-dir marathon lanes (the GH-61 case). [decision](decisions/2026-07-02-offlane-untracked-dir.md) records the contract + 3 invariants; `test/worktree-isolation.sh` enforces all three (greenfield-fixed / off-lane-still-rejected / no copy-back widening). `validate.sh` **80/80**. → [GH-59-OFFLANE-UNTRACKED-DIR.md](PROJECT/1-INBOX/GH-59-OFFLANE-UNTRACKED-DIR.md) · [#59](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/59)
- **GH-56 · marathon-drive: reconcile/fresh-id a leaked tick token before re-seed** ✅ **SHIPPED 2026-07-02 (`d679fc4`, marathon lane — codex + agy Approved)** — `marathon-drive.sh` now runs `reconcile_relay_task()` before seeding: **dies on a live claim** (never reaps it), clears a stale `open` handoff routed to builder/reviewer, else proceeds — so re-running a phase-id after an aborted run no longer fails `task ... is open`. Regression coverage in `test/marathon-drive.sh` (45 checks). Split from #51 §5. `validate.sh` **80/80**. Surfaced a self-hosting caveat: a lane editing `marathon-drive.sh` itself corrupts the running script mid-run via copy-back (transient; committed file valid). → [GH-56-MARATHON-LEAKED-TOKEN-RECONCILE.md](PROJECT/1-INBOX/GH-56-MARATHON-LEAKED-TOKEN-RECONCILE.md) · [#56](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/56)
- **GH-75 · XYZ.json — final completion telemetry at relay/swarm/marathon session end** ✅ **SHIPPED 2026-07-02** — every relay/marathon/swarm harness now appends a durable, newest-first completion record to a gitignored `XYZ.json` at the harness repo root (schema: `{harness, sessionId, health, title, description, updatedAt}`). Shared `utils/telemetry/health-lib.sh` factors GH-24's STATUS/VERDICT→health mapping out of `extract-relay-telemetry.sh` (extractor output byte-identical — no GH-24 regression); shared `utils/telemetry/append-xyz-completion.sh` does a locked (GH-72 `mkdir` advisory lock — no lost update) + atomic (temp-file + `os.replace` — no corruption/partial write) read-modify-write-prepend. Wired into `relay-drive.sh`'s terminal exits (green / orange×2 / red), `marathon-drive.sh`'s own per-run hook (gated by `XYZ_HARNESS_CONTEXT`; `harness:"swarm"` when swarm-preflight's generated invocation self-propagates the tag, else `"marathon"`; silent under `marathon-phase`), and `marathon.sh`'s single whole-run record — so a `marathon.sh` N-phase run emits exactly one record, never N/N+1. New `test/xyz-completion.sh` (writer + 16-way concurrency + corrupt-file self-heal + health-lib table) and `test/xyz-harness-hooks.sh` (all harness terminal paths + nesting), plus a swarm-tag assertion in `test/swarm-preflight.sh`. `validate.sh` green. → [GH-75-XYZ-JSON-COMPLETION-TELEMETRY.md](PROJECT/1-INBOX/GH-75-XYZ-JSON-COMPLETION-TELEMETRY.md) · [#75](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/75)
- **GH-69 · Marathon branch suggestion + agent confirmation prompt** ✅ **SHIPPED 2026-07-01** — marathon builds commit several times (build → gate → review) but the pipeline had no branch-cutting step; partial/failed work landed on whatever was checked out. Three-stage fix: (1) `marathon-plan.sh` emits a deterministic `suggested_branch: marathon/<slug>-<date>` per active wave lane (read-only, no git writes); (2) `swarm-preflight.sh` checks real branch existence via `git show-ref` and emits `branch_ready`/`skip_branch_prompt` in the packet (JSON + `packet.md` + text report); (3) the orchestrating-agent contract ("ask the operator before proceeding when `branch_ready: false`, unless the carve-out applies") is documented inline in `swarm-preflight.sh`'s header and self-stated in every packet's "Suggested branch" line — a driving agent doesn't need to recompute it. Carve-out (`risk: 1` + independent-zone artifacts) verified to skip the prompt; kernel-zone artifacts and non-risk-1 items correctly do NOT skip it. No branch is ever auto-created. `test/marathon-plan.sh` 34/34, `test/swarm-preflight.sh` 44/44, `validate.sh` 77/77. → [GH-69-MARATHON-BRANCH-PROMPT.md](PROJECT/1-INBOX/GH-69-MARATHON-BRANCH-PROMPT.md) · [#69](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/69)
- **GH-71 · Code Structure & Implementation Upgrade** ✅ **Phases 1–2 SHIPPED 2026-07-01 (`6a7b6ea`, `fa37c58`)** — plan to upgrade codebase maturity, re-scoped into two safe/reversible initial phases + two deferred rewrites. **Phase 1 (root-dir cleanup):** moved `4X4.md`, `FRONTDOOR.md`, `snapshot.md`, `CODEX.md`, `BACKLOG.md` into `PROJECT/4-MISC/`; canonical front door (`README.md`/`ROUTER.md`/`AGENTS.md`/`ROADMAP.md`/`CHANGELOG.md`/`CLAUDE.md`/`LICENSE.md`) untouched at root; fixed the moved files' now-two-levels-deep internal links + the two live ROADMAP.md cross-references; left historical/archival references (`CHANGELOG.md`, `relay-system/**`, `AUDIT/**`, `decisions/**`, `PROJECT/3-COMPLETED/**`) untouched as point-in-time records. **Phase 2 (JS kernel API boundaries):** JSDoc `@param`/`@returns`/`@throws` on every exported function across the 12 `src/*.js` files (the event-log↔projection API surface) + `utils/checkjs.sh` wiring up the exact Tier 1 lint GH-61 proposed (`node --check`, dependency-free — no `tsc`/TypeScript added) plus a deterministic JSDoc-coverage gate so the annotations stay enforced, not a one-time pass. `test/checkjs.sh` (6 checks) in `validate.sh`. **Deferred (unchanged, still behind a concrete trigger — not a letter grade):** Phase 3 `poll.sh`→Node + Phase 4 `relay-drive.sh`→Node; hold until an actual maintenance incident or a specific Bash-caused bug class justifies rewriting the most safety-critical, best-dogfooded components. `validate.sh` 77/77; `pdda.sh run` clean. → [GH-71-UPGRADE-CODE-STRUCTURE.md](PROJECT/1-INBOX/GH-71-UPGRADE-CODE-STRUCTURE.md) · [#71](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/71)
- **GH-70 · relay-xyz: per-repo harness for concurrent automated relays** ✅ **Phases 1–2 SHIPPED 2026-07-01 (`e8d9999`)** — the per-clone global driver lock (GH-42) serializes all relays sharing one harness; per-repo concurrency comes from each repo having its own vendored `.xyz/` (GH-49). **Phase 1 (doc):** SKILL.md "Concurrent relays across repos" section — `xyz-vendor.sh` (not `install.sh`) is the path to per-repo isolation; "When to use" names the case. **Phase 2 (additive):** `find-harness.sh --check` warns when a foreign repo has no local `.xyz/` (using the centralized harness + shared lock) and points at `xyz-vendor.sh`; flags a held lock; fail-open (always exit 0). `test/find-harness.sh` (8 checks); `validate.sh` 76/76. **Phase 3 deferred** (`install.sh --with-harness`) — revisit when a real user finds `xyz-vendor.sh` too indirect. → [GH-70-CONCURRENT-RELAY-LOCAL-HARNESS.md](PROJECT/1-INBOX/GH-70-CONCURRENT-RELAY-LOCAL-HARNESS.md) · [#70](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/70)
- **GH-66 · Session/transcript-log audit for stale-instruction drift** ✅ **SHIPPED 2026-07-01 (`ff2ea44`, marathon Wave 2 Lane B)** — `utils/transcript-audit.sh`: periodic, READ-ONLY audit over a transcript dir (default `AUDIT/relay-automation-transcripts/`). Emits a structured, greppable report of three drift classes — `stale-ref` (deprecated/`.bak`/missing repo paths), `repeat-explore` (same dir explored ≥ threshold across turns), `unbounded-stall` (transcript with no terminal exit/STATUS/DECISION marker). Findings are a report, not a gate; never writes/moves/deletes a transcript. `test/transcript-audit.sh` (7 checks incl. a read-only checksum/mtime guarantee) in `validate.sh`. Ran clean against the real 37-file corpus (7 unbounded-stalls). Non-goals honored: not a transcript store (GH-30), no auto-apply. → [GH-66-SESSION-LOG-AUDIT.md](PROJECT/1-INBOX/GH-66-SESSION-LOG-AUDIT.md) · [#66](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/66)
- **GH-64 · Security-scanning guardrail in the review chain** ✅ **Tool + test SHIPPED 2026-07-01 (`a0cc84e`, marathon Wave 2 Lane A); ACTIVE BLOCKING GATE wired 2026-07-01** — `relay-automation/hooks/security-scan.sh`: self-contained, no-network static scanner for shell scripts; fail-loud (`SECURITY: <file>:<line> [rule]` to stderr, non-zero exit), never auto-fixes. Rules: eval-of-variable, `curl`/`wget`-piped-to-shell, AWS keys, PEM private-key headers, GitHub PATs, Slack tokens, literal credential assignments. **Active-gate follow-on (this session):** a hand-maintained, checked-in baseline (`relay-automation/hooks/security-scan-baseline.txt`, exact `file\trule\tline-text` triples, matched by content not line number) suppresses the known-legitimate findings (reviewed dispatcher `eval`s in `poll.sh`/`relay-drive.sh`/`marathon-drive.sh`/`improve-loop.sh`, doc-comment prose, the scanner's own test fixtures) WITHOUT hiding them — every baselined finding still prints (labeled `SECURITY (baselined)`), per GUIDING-PRINCIPLES.md #8; a genuinely new finding still fails loud. New `--no-baseline` (raw scan) and `--tsv` (machine-parseable rows for authoring baseline entries) flags. `test/security-scan.sh` now 28 checks including a real-repo-against-the-checked-in-baseline assertion — that assertion IS the blocking gate, already wired into `validate.sh` (no separate CI step needed). Confirmed NO real secret leak in any of the 45 baselined findings. → [GH-64-SECURITY-SCANNING-GUARDRAIL.md](PROJECT/1-INBOX/GH-64-SECURITY-SCANNING-GUARDRAIL.md) · [#64](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/64)
- **GH-68 · Cross-agent dependency conflict detection** ✅ **Phase 1 SHIPPED 2026-07-01 (`94558c5`)** — warn-only, inject-only `dependency.drift` signal per [decision](decisions/2026-07-01-cross-agent-dep-conflict.md): new event verb (`src/events.js` + `tick drift`, best-effort so it can't fail a turn); post-commit detection in `relay-turn-lib.sh` when a landed turn changes the containment kernel / `src/project.js` / `src/events.js`; `rtl_drift_brief` (per-agent `.tick` watermark, capped) read by both shims into the next turn brief; `src/project.js` skips the verb in the fold (no phantom task). **Costly** kernel schema extension. Regression: `test/relay-dep-drift.sh` (12 checks); `validate.sh` 72/72 exit 0. **Phase 2** (blocking gate) is an explicit deferred follow-on — only if field data shows the warn-only posture misses a real conflict. → [GH-68-CROSS-AGENT-DEP-CONFLICT.md](PROJECT/1-INBOX/GH-68-CROSS-AGENT-DEP-CONFLICT.md) · [#68](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/68)
- **GH-22 · agy worktree isolation silent data loss** ✅ 2026-06-26 (#22 closed) — fixed the exit-0 silent data loss where, under `RELAY_WORKTREE_ISOLATION=1`, agy edited the relay file via the absolute ROOT path but `rtl_worktree_end` copied the stale empty worktree copy back over ROOT. Relay file path is now CWD-relative so agy writes into the worktree; fix validated live during the Phase 6 dogfood (agy reviewed under isolation). → [GH-22-AGY-WORKTREE-ISOLATION-DATA-LOSS.md](PROJECT/1-INBOX/GH-22-AGY-WORKTREE-ISOLATION-DATA-LOSS.md) · [#22](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/22)
### Deferred · vision

- **Part A · Phase 4 — M6 / M7** 🔲 deferred — cross-phase context injection + state projection, until a phase genuinely needs them. → [MARATHON-HARNESS.md → Deferred](PROJECT/3-COMPLETED/MARATHON-HARNESS.md#deferred--m6--m7)

---

*Detail for every entry lives in its linked `PROJECT/**` doc. Part B gaps also map to `PROJECT/4-MISC/4X4.md`; any
event-schema change gets a decision record under `decisions/` before it lands.*
