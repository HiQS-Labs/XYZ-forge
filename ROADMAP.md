---
title: Combined Roadmap — Cost-Observed Marathon Loops + Adversarial Hardening
status: Active
created: 2026-06-16
updated: 2026-07-03
branch: main
supersedes: PROJECT/2-WORKING/ROADMAP-COMBINED.md (promoted to canonical 2026-06-17); folds in the former standalone ROADMAP.md (adversarial-hardening track, now Part B)
synthesizes:
  - PROJECT/1-INBOX/LOOPS.md
  - PROJECT/4-MISC/COST-OBSERVABILITY-PLAN.md
  - PROJECT/1-INBOX/MARATHON.md
goal: >
  Canonical pointer/ledger index for the repo's work — queued intake, projects in progress,
  completed, attempted, and deferred — linking to the canonical PROJECT/** docs that own the
  execution detail. This is an index, not a plan body.
---

<!-- PDDA ROADMAP CONTRACT — this file is a POINTER/LEDGER, not a plan body.
     Allowed: queued intake / projects in progress / completed / attempted / deferred + links to PROJECT/** docs.
     NOT allowed: phase checklists, build steps, deep execution notes — put those in the project doc.
     Carve-out: a SHORT exception note is OK only when omitting it would hide an operationally critical fact.
     Coverage rule: every PROJECT/2-WORKING doc must be reflected here by a pointer (or opt out with roadmap_exempt: true).
     Enforced by `utils/pdda/pdda.sh roadmap` + `utils/pdda/pdda.sh roadmap-coverage` (deterministic) + `utils/pdda/pdda.sh doc-ready` ROADMAP rubric (LLM). -->

# Combined Roadmap: Cost-Observed Marathon Loops + Adversarial Hardening

> **Pointer/ledger only — not a plan body.** Execution detail (phase checklists, build steps, QA
> gates, deep notes) lives in the linked `PROJECT/**` docs; keep it there. See the contract banner above.

Three tracks, sequenced independently:

- **Part A — Marathon:** cost observability (done) → headless multi-phase chaining (done) → real-monolith dogfood (active)
- **Part B — Adversarial Hardening:** epoch fencing (done) → chaos suite → cross-repo E2E → reference deploy
- **Part C — Autonomous Self-Improvement:** the gated LOOPS.md endgame

## Status

| What was just completed | What's next |
|---|---|
| **2026-07-04 marathon Wave 2+3, reviewed and merged (PR #125, `d9db49d`):** **GH-55** auto-includes covering tests/helpers in preflight allowlists, **GH-54** forbids in-turn execution of fs-touching tests, and **GH-48** generalized `marathon-plan`'s zone model with a real rebalance-OS live check (#54/#55 closed; #48 stays open pending an operator closure call). Targeted gates are green (`test/swarm-preflight.sh` 75/75, `test/marathon-plan.sh` 57/57); the full `validate.sh` rerun isn't blocked by this wave's files — non-green runs traced to pre-existing `oracle-guard.sh`/`improve-loop-qa.sh` flakiness, not `relay-self-sufficiency.sh`. Independent review before merge filed two non-blocking follow-ups: **GH-126**, **GH-127**. Also closed 2026-07-04: **#113**/**#114** (already fixed in code, left open past landing) and **GH-112** retitled to reflect its spike-to-full-rewrite scope creep (PR #121, now targeting a new `development` branch, not `main`). With Plan B and the blend plan both fully shipped, authored **[Marathon Plan C](PROJECT/2-WORKING/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — the dogfood-reliability cluster (#106, #107, #108+#126+#127 bundled, #116 Bug B, #117, #124), rated and doc'd, not yet fired. | **Ready to fire:** Marathon Plan C's Wave 1 (#106‖#108‖#116‖#117‖#124, parallel) + its kernel track (#107, Opus-serial). **Still open:** **GH-61 Tier 2** (needs operator `macos-latest`/`ubuntu-latest` runner decision). **Operator-driven:** rebalance-OS cross-repo harness dogfood beyond the queue-planner proof; **GH-48** closure call. **Part B:** G2 dup-token determinism (next adversarial-hardening slice). **Queue / parked:** GH-30 Phases 2–5 (writer wiring + risk-4 containment), #94 re-verify-vs-`5972ef4` installer repro. |

### Immediate next-up (ordered)

1. ✅ **Done — GH-51 [1-kernel] fixed (2026-06-30).** `rtl_init` now collapses a same-repo `--target-root` to the caller's root (no-op), so `--target-root .` no longer reverts the relay file off-lane. Decision record + regression test; `validate.sh` 69/69. The remaining GH-51 items ([2]/[3]/[5]) are minor. → [#51](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/51)
2. **Re-open the rebalance-OS cross-repo harness dogfood — now UNBLOCKED** (GH-51 [1-kernel] landed). Converts the #14 fix into a real external-repo proof: drive a scoped rebalance-OS lane via `swarm-preflight --target-root <clone> → marathon-drive`. The highest-value momentum test now that `--target-root` is containment-correct.
3. **[GH-48-QUEUE-PLAN-CROSS-REPO-ZONES.md](PROJECT/2-WORKING/GH-48-QUEUE-PLAN-CROSS-REPO-ZONES.md) built 2026-07-04; live rebalance validation corrected one assumption.** The foreign helper lane now classifies correctly as `signed-helper`, but the real 3-lane rebalance queue still stays one wave because only one lane touches that capped zone. Useful proof for the planner; not itself the full cross-repo harness dogfood.
4. **Part B Phase-2 remainder: G2 dup-token determinism** (`test/chaos-dup-token.sh`) — the next adversarial-hardening slice now that **R2 (#52) + G4 are done**. Kernel epoch-fence territory → Opus + `decisions/` record.
5. ✅ **Done — Part C real-agent run PROVEN (2026-06-30, operator GO).** `improve-loop` drove a live `marathon-drive` (**codex builder + agy reviewer**) as `--build-cmd`: baseline 4 → live build → `KEEP` oracle held → champion ACCEPTed (metric 3) → halted on the iteration cap, with provenance. Every safety invariant fired against a real build. The run surfaced two harness defects (neither a loop bug): **#58** (`--builder claude` not on PATH headless — use codex/agy) and **#59** (allowlisted artifact in an untracked dir → spurious off-lane exit 6). → [#50](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/50)

> **Active scheduling overlay:** re-run `utils/marathon-plan.sh` to regenerate after today's ledger update. Last generated: [MARATHON-PLAN-2026-07-02.md](PROJECT/2-WORKING/MARATHON-PLAN-2026-07-02.md) (2026-07-02 — stale; the 2026-07-03 wave shipped ~10 items that were in the held list). Derived from this ledger; the ledger stays source of truth.

## Model assignment (heuristic)

Mechanical / pattern-following work → **Sonnet High**; trust-critical kernel-correctness reasoning
(epoch-fencing kernel, dup-token determinism) → **Opus**. Full build-track table:
[MARATHON-HARNESS.md → Model assignment](PROJECT/3-COMPLETED/MARATHON-HARNESS.md#model-assignment-build-track-guidance).

> **Operational note (carve-out — operationally critical):** Gemini CLI retired 2026-06-19; **agy**
> (Antigravity CLI) is the permanent cross-model lane. **Run agy turns sandbox-OFF** (it exits 0 with
> empty output when its backend is blocked) and an agy lane is **cost-blind** (no token output).
> Detail: [MARATHON-HARNESS.md → Operational note](PROJECT/3-COMPLETED/MARATHON-HARNESS.md#operational-note--cross-model-lane).

## Ledger

### Queue / parked intake
- **GH-129 · deep-research: add Perplexity grounded search (Sonar) via OpenRouter as the second backend** ✅ **SHIPPED 2026-07-04 (PR #130, `79395ee`); #129 closed · agy relay review PASS (zero findings) · live-verified pre-merge** — the follow-up phase GH-87's doc explicitly reserved: `--provider agy|openrouter` flag (default `agy`, byte-identical default path) + `runOpenRouter` via OpenRouter's chat-completions API (the repo's established gateway — GH-77/118/120, `OPENROUTER_API_KEY`), model `perplexity/sonar` (env-overridable), `--search-context-size` → Perplexity's native `web_search_options.search_context_size`, citations normalized annotations/`citations[]`/URL-scan, fail-closed typed errors incl. `missing_api_key`. `test/deep-research.sh` 45/45, `validate.sh` exit 0, one real OpenRouter→Sonar smoke (4.5s, 15 citations). **Write-set overlaps #124 — its lane is now unblocked, fires post-merge.** cx/risk/eff 2/2/2. → [GH-129-DEEP-RESEARCH-PERPLEXITY-OPENROUTER.md](PROJECT/3-COMPLETED/GH-129-DEEP-RESEARCH-PERPLEXITY-OPENROUTER.md) · [#129](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/129)
- **GH-126 · swarm-preflight: GH-55 covering-test inference uses naive substring match — false-positive risk in ALLOW_PATHS** 🆕 **captured 2026-07-04** — `expand-artifacts.mjs`'s covering-test detection (`raw.includes(artifact)`) doesn't verify the artifact path is an actual reference (source/exec/require), only that it appears anywhere in the file text — a false positive would widen the builder's `ALLOW_PATHS` beyond the intended covering test. Low likelihood today (paths are mostly long/specific); tighten before this generalizes to shorter/foreign paths. Found reviewing PR #125. → [#126](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/126)
- **GH-127 · swarm-preflight: GH-54 fs-touching test detector misses bare `>` redirects** 🆕 **captured 2026-07-04** — the fs-touching regex catches `cat >`, `printf...>`, `>>`, `mktemp`, etc., but not a bare single `>` (e.g. `echo x > file`), a common and equally filesystem-touching pattern. A test using only that form would get the weaker self-verify guidance instead of GH-54's "do not run in-turn" rule. Already flagged as a known v1 limitation in the GH-54 doc's own "what's next"; this names the specific gap. Found reviewing PR #125. → [#127](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/127)
- **GH-106 · codex-turn: default `CODEX_FLAGS` hangs on approvals in headless runs** 🆕 **captured 2026-07-04 · rated · part of [Marathon Plan C](PROJECT/2-WORKING/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — the default `-s workspace-write` sandbox still gates some actions behind an interactive approval prompt; in a headless run nothing answers it, so the turn hangs to the `RELAY_TURN_TIMEOUT_S` cap (exit 7), burning a lane attempt. Fix: change the default to a non-interactive-safe autonomy level (`-c approval_policy=never` or the header comment's own documented `--dangerously-bypass-approvals-and-sandbox` escape hatch), fully overridable via `CODEX_FLAGS` as today. Sibling of #107/#108 (same dogfood run). cx/risk/eff 2/2/2. → [GH-106-CODEX-FLAGS-HEADLESS-HANG.md](PROJECT/2-WORKING/GH-106-CODEX-FLAGS-HEADLESS-HANG.md) · [#106](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/106)
- **GH-107 · Containment reverts a complete, passing turn on an off-lane tool-cache dir (`.codebase-memory/`)** 🆕 **captured 2026-07-04 · rated — KERNEL zone, Opus-serial · part of [Marathon Plan C](PROJECT/2-WORKING/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — `rtl_worktree_end`'s off-lane loop (`relay-turn-lib.sh:343-380`) discards a turn's entire work when the builder's own tooling writes an untracked cache dir the target repo never gitignored (reproduced live: a 442-line, 67/67-tested, otherwise-correct build discarded anyway). Sibling of #54 — same mechanism, different trigger (tool cache vs test fixtures). Fix: opt-in `CONTAINMENT_IGNORE` exemption list (default-off, byte-identical behavior unless set) checked in the same loop. Touches the containment core — Opus-serial + `decisions/` record, not a parallel Sonnet lane. cx/risk/eff 3/4/2. → [GH-107-CONTAINMENT-OFFLANE-TOOLCACHE.md](PROJECT/2-WORKING/GH-107-CONTAINMENT-OFFLANE-TOOLCACHE.md) · [#107](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/107)
- **GH-108 · `--pre-advance-cmd 'npm test -- <name>'` runs the whole suite, unrelated flakes fail a green lane** 🆕 **captured 2026-07-04 · rated · bundled with #126/#127 (same file) · part of [Marathon Plan C](PROJECT/2-WORKING/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — `swarm-preflight.sh` passes a contract's `gate` field verbatim as `--pre-advance-cmd`; whether `npm test -- <name>` actually filters depends on the target repo's own test script, and often doesn't (reproduced live: an unrelated `EADDRINUSE` flake failed an otherwise 67/67-green lane). Fix (Level 1, documented-caveat): when `GATE_CMD` heuristically looks like a filtered-runner invocation, emit an explicit scoping caveat in the packet rather than attempting cross-runner auto-detection. Bundled in the same lane as **#126** (GH-55 covering-test substring-match tightening) and **#127** (GH-54 bare-`>` fs-touching gap) since all three touch `utils/swarm-preflight.sh`. cx/risk/eff 3/2/3. → [GH-108-SWARM-PREFLIGHT-HARDENING-BUNDLE.md](PROJECT/2-WORKING/GH-108-SWARM-PREFLIGHT-HARDENING-BUNDLE.md) · [#108](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/108) · [#126](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/126) · [#127](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/127)
- **GH-124 · deep-research.mjs shipped un-run against real `agy` — add a real-`agy` smoke test + runaway-grounding guard** 🆕 **captured 2026-07-04 · rated · part of [Marathon Plan C](PROJECT/2-WORKING/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — GH-87's adapter hung on *every* real `agy` call (stub-only tests hid it); two bugs fixed post-merge (`91f17f2` missing `--dangerously-skip-permissions`; `74cd553` `execFile` silently ignores `stdio` → agy stdin never EOF'd → switched to `spawn`). Now works (~27–52s, real citations, `test/deep-research.sh` 23/23). Hardening follow-up: an **opt-in real-`agy` smoke test** (network + `agy` on PATH, self-skips like `relay-self-sufficiency.sh`) so a stub can't again hide a real-backend break; a guard against `--search-context-size high` runaway grounding; revisit the default 120s timeout. Surfaced dogfooding #111. cx/risk/eff 2/1/2. → [GH-124-DEEP-RESEARCH-REAL-AGY-HARDENING.md](PROJECT/2-WORKING/GH-124-DEEP-RESEARCH-REAL-AGY-HARDENING.md) · [#124](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/124)
- **GH-118 · Make Aider edit formats more forgiving for OpenRouter models** 🆕 **captured 2026-07-03 · rated · live-confirmed** — Aider natively uses a 'whole' or 'udiff' edit format depending on the model, but unknown OpenRouter models (like GLM-5.2) often default to formats they don't strictly follow (e.g. outputting standard diffs instead of 'whole' file), causing Aider to fail with 'no tracked changes'. **2026-07-03 live tests confirmed the diagnosis and the fix on two models:** GLM-5.2 (first attempt: no edit emitted at all, just chat; retry with `AIDER_FLAGS=--edit-format diff` produced valid SEARCH/REPLACE) and Nemotron Ultra 3 free tier (same 'whole' default — zero nvidia/nemotron entries anywhere in Aider's model-settings.yml or heuristic chain — failed by emitting a raw unified-diff hunk, reproducing the original bug report almost exactly). Revised fix: document `AIDER_FLAGS=--edit-format diff` (already a passthrough) rather than adding a new `AIDER_EDIT_FORMAT` var; maintain a running known-model compat note. cx/risk/eff 2/1/2. → [GH-118-AIDER-OPENROUTER-FORMAT.md](PROJECT/1-INBOX/GH-118-AIDER-OPENROUTER-FORMAT.md) · [#118](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/118)
- **GH-119 · aider-turn.sh: reviewer can auto-add and edit out-of-scope tracked files under --yes-always; all-or-nothing containment discards the valid in-lane edit too** ✅ **SHIPPED 2026-07-03 (`93e2366`); closed 2026-07-04 · sibling of #54/#107** — surfaced live while testing GH-118's fix: GLM-5.2, acting as Reviewer, found a real bug and emitted a valid SEARCH/REPLACE for the relay file, but also emitted an edit for `marathon-drive.sh` (never added to the chat) which Aider's `--yes-always` auto-confirmed adding. The off-lane guard correctly caught it but discarded the *entire* turn, including the valid relay-file review. Same containment mechanism as #54/#107, different (deliberate, role-violating) trigger. Fixed: pre-seeds the diff's changed files as `--read` for review-only turns (`ALLOW_PATHS=""`) so they're structurally unwritable regardless of `--yes-always`; new `test/aider-turn.sh` case; independently re-verified via two reverse-dogfood reviews (24-file scale, zero scope-creep both times). cx/risk/eff 2/2/2. → [GH-119-AIDER-REVIEWER-SCOPE-CREEP.md](PROJECT/3-COMPLETED/GH-119-AIDER-REVIEWER-SCOPE-CREEP.md) · [#119](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/119)
- **GH-120 · Build a fuzzy-match OpenRouter model-name lookup table (alias → canonical slug)** ✅ **SHIPPED 2026-07-03 (`17e2681`); closed** — resolving colloquial model names ("GLM 5.2", "Nemotron Ultra 3") to canonical OpenRouter slugs currently required a live API query every time. Shipped: `relay-automation/openrouter-model-aliases.yml` + `resolve-model-alias.sh` (4-tier fuzzy match) + `test/model-alias.sh` (10/10), wired into `validate.sh`, documented. Independently re-verified via a GLM 5.2 reverse-dogfood review, which also caught and fixed a stale README claim (`1642304`). cx/risk/eff 1/1/2. → [GH-120-OPENROUTER-MODEL-ALIAS-TABLE.md](PROJECT/3-COMPLETED/GH-120-OPENROUTER-MODEL-ALIAS-TABLE.md) · [#120](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/120)
- **GH-117 · fix(marathon-drive): --dry-run must probe builder/reviewer binary before mutating tick state** 🆕 **captured 2026-07-03 · rated · part of [Marathon Plan C](PROJECT/2-WORKING/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — `marathon-drive.sh` doesn't probe builder/reviewer binary availability before seeding tick state. Missing binary errors fire after the tick task is already seeded and spent (no recovery without a fresh relay-task id). Fix: binary probe after arg parsing, before any tick mutation — catches missing `claude`/`agy`/`codex` on both `--dry-run` and live runs. Found in a live 3-phase marathon run. cx/risk/eff 2/1/2. → [GH-117-MARATHON-DRIVE-BINARY-PREFLIGHT.md](PROJECT/2-WORKING/GH-117-MARATHON-DRIVE-BINARY-PREFLIGHT.md) · [#117](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/117)
- **GH-116 · fix(tick): misleading 'break' error on open tasks + marathon retry flag** 🆕 **Bug A ✅ SHIPPED (`bb9138b`); Bug B captured 2026-07-04 · rated · part of [Marathon Plan C](PROJECT/2-WORKING/MARATHON-PLAN-2026-07-04-C-RELIABILITY.md)** — Bug A (`tick break` on an `open` task said "only the claiming agent can mutate it" — misleading; real problem was the task not being `claimed` at all) already landed in `src/scope.js`, confirmed live. **Remaining (Bug B):** `marathon.sh` has no retry flag — recovering a spent task requires manually renaming the phase id in YAML. Fix: `--retry <phase-id>` passes a suffixed `--relay-task` to that phase's `marathon-drive.sh` call (which already supports the override natively — no change needed there). Found in live run. cx/risk/eff 2/1/2 (Bug B only). → [GH-116-MARATHON-RETRY-FLAG.md](PROJECT/2-WORKING/GH-116-MARATHON-RETRY-FLAG.md) · [#116](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/116)
- **GH-114 · chore: remove deprecated gemini-turn.sh and scrub dead GEMINI references** ✅ **SHIPPED 2026-07-03; closed 2026-07-04** — `gemini-turn.sh` marked DEPRECATED 2026-06-19; Gemini CLI phased out. Delete the shim, remove its README row and `GEMINI_AGENT` comment from `marathon-agent.sh`. `agy-turn.sh` is the live permanent replacement. **✅ Follow-up gap found + fixed 2026-07-03:** the original `2b5f8a3` fix deleted `relay-automation/gemini-turn.sh` and dropped `gemini` from `marathon-drive.sh`'s help/validation, but left 5 test files still hardcoding `--reviewer gemini` / `RELAY_AGENT=gemini` / a direct reference to the deleted shim (`test/gemini-turn.sh`, `test/relay-turn-timeout.sh`, `test/marathon-yaml.sh`, `test/marathon-drive.sh`, `test/xyz-harness-hooks.sh`) — surfaced when today's GH-118/119/120 marathon ran the full suite (6 failures, all traced to this one root cause via zero-diff confirmation against pre-marathon HEAD). Fixed: deleted the dangling `test/gemini-turn.sh` (and its now-invalid `validate.sh` entry), swapped the other 4 files' gemini usage to `agy` (discovering along the way that `agy-turn.sh`'s auth pre-flight + empty-log guard needed dedicated stubs, unlike codex/claude). `validate.sh`: **90/90**. cx/risk/eff 1/1/1. → [#114](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/114)
- **GH-113 · fix(marathon-yaml): validator rejects agy reviewer — blocks multi-phase YAML plans** ✅ **SHIPPED 2026-07-03; closed 2026-07-04** — `marathon-yaml.js:114` hard-codes `codex|gemini` allowlist; `MARATHON.example.yaml` documents and uses `reviewer: agy`; `marathon-drive.sh` already routes `agy*`→`AGY_AGENT`. Blocking bug: any multi-phase YAML plan with an agy reviewer phase fails at parse time. Single-phase `marathon-drive.sh --reviewer agy` runs are unaffected. One-line regex fix. **✅ Follow-up gap found + fixed 2026-07-03:** `test/marathon-yaml.sh`'s reviewer-rejection assertion hardcoded the pre-agy error string (`"must start with codex or gemini"`), which went stale the moment the actual message changed to `"must start with codex, gemini, or agy"` when agy was added — a false failure, not a real regression. Relaxed the assertion to check the `"must start with codex"` substring (resilient to future prefix-list changes) instead of hardcoding the full message. Note: `gemini` remains a genuinely accepted reviewer prefix in `src/marathon-yaml.js` itself (out of scope for this test-only fix). cx/risk/eff 1/1/1. → [#113](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/113)
- **GH-112 · Progressive Python port — boundary decision + opt-in Python layer** 🐍 **spike SHIPPED · port LANDED as opt-in 2026-07-04 (`gh112-python-optin`)** — spike answered all three questions ([decision record](decisions/2026-07-04-python-port-boundary.md): `relay-turn-lib.sh` stays Bash permanently; Option A discrete Python CLIs; zero test-suite changes needed). The full port first arrived as PR #121 (Python-by-default, `development`-targeted, conflicting) — **not merged**; rebuilt file-level on a clean branch off post-#134/#135 `main`: 13 modules under `utils/py/` + pytest layer, with the toggle **inverted** — Bash stays the default in all 11 entry scripts, `XYZ_PYTHON=1` opts into the Python route (same CLI/exit-code contract). `validate.sh` 100/100 green default-mode; toggle routing 22/22; pytest 8/8. **Follow-up lane (marathon-ready):** close the #134 drift in the opt-in Python layer — port GH-106/GH-117/GH-108 into the `utils/py/` twins + parity tests (GH-107 containment is inherited via `rtl.py`; GH-116 has no twin). Light-medium; queued so a future default-flip doesn't inherit the debt. cx/risk/eff 2/2/2. → [GH-112-PYTHON-134-PARITY.md](PROJECT/2-WORKING/GH-112-PYTHON-134-PARITY.md) · [GH-112-PYTHON-PORT-SPIKE.md](PROJECT/3-COMPLETED/GH-112-PYTHON-PORT-SPIKE.md) · [#112](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/112)
- **GH-109 · Gemini 3.1 Deep Think audit — watchdog process leak, tmp collision, DRY turn scripts, Python inline extraction** 🆕 **captured 2026-07-03 · rated · P1 SHIPPED 2026-07-04** — 4 of 5 findings actioned: orphaned `sleep` grandchild leak in `consult.sh` watchdog (resource bug, `aceffe3`), `/tmp` permission collision in `relay-xyz-guard.sh` (trivial `$UID` fix, `ed2f9a1`), inline Python heredoc extraction to discrete utils (scoped-down Item 5), and DRY turn-script audit (scoped-down Item 1 — extend relay-turn-lib.sh, not a new dispatcher). Item 4 (retire scanner for Gitleaks/TruffleHog) declined — no-external-dep constraint. Phase 1 (the two resource/collision bugs) shipped via [MARATHON-PLAN-2026-07-04.md](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-04.md) Lanes B/C; issue stays open for the remaining scoped-down items. cx/risk/eff 3/3/3. → [GH-109-GEMINI-FEEDBACK.md](PROJECT/1-INBOX/GH-109-GEMINI-FEEDBACK.md) · [#109](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/109)
- **GH-110 · Fable 5 Max audit — shellcheck + vendor integrity + strict-mode hardening** 🆕 **captured 2026-07-03 · rated · P1 SHIPPED 2026-07-04** — Fable 5 Max ran shellcheck (63 findings, almost all cosmetic), executed all 85 tests in a clean Linux container, and diffed the vendor tarball. Five valid findings: broken test assertion in `xyz-vendor.sh:140`, stale `relay-pkg.tar.gz` (safety core — also the GH-104 remaining follow-up), 8/85 vendored-copy test failures, 788-line JS heredoc invisible to the Tier-1 check gate, and undocumented strict-mode convention. Phased: P1 quick fixes (~1 hr) — `.DS_Store` exclusion in `xyz-vendor.sh` + empty `skills/swe/` removal — shipped via [MARATHON-PLAN-2026-07-04.md](PROJECT/3-COMPLETED/MARATHON-PLAN-2026-07-04.md) Lane A; P2 vendor integrity (~2–3 hrs) and P3 code quality (~1 day) remain, issue stays open for those. cx/risk/eff 3/2/3. → [GH-110-SHELLCHECK-VENDOR-FIXES.md](PROJECT/1-INBOX/GH-110-SHELLCHECK-VENDOR-FIXES.md) · [#110](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/110)
- **GH-87 · Deep Research mode — provider-agnostic grounded search seam (Agy Gemini Search first adapter)** ✅ **Phase 1 MERGED 2026-07-04 (PR #122)** — the new Aider-capable harness needs a first-class grounded-search tool without hard-wiring the whole system to one vendor. Local framing intentionally keeps the **search-provider seam provider-agnostic** while taking **Agy Gemini Search** as the first backend (via the Agy CLI), with Perplexity as a follow-up phase. Shipped: `relay-automation/deep-research.mjs` (zero-dep Node adapter, runs `agy -p` in a throwaway tmpdir, fail-closed typed errors, normalized `{answer, citations, query, provider, model, raw}`) + `test/deep-research.sh` (21 assertions), wired into `validate.sh`. Reviewed via automated agy relay (Approved) + operator code review. Phase-1 limitation: `--temperature`/`--max-tokens` recorded in `raw.config` but not yet passed to `agy`. **Post-merge (2026-07-04): 2 real-`agy` hangs fixed** — the adapter had shipped un-run against real `agy` (stub-only tests): `--dangerously-skip-permissions` (`91f17f2`) + `execFile`→`spawn` so agy's stdin EOFs (`74cd553`); now returns ~27–52s with real citations, `test/deep-research.sh` 23/23. Hardening tracked in **#124**; first real use posted 4 grounded searches to #111. cx/risk/eff 3/2/3. → [GH-87-DEEP-RESEARCH-MODE.md](PROJECT/3-COMPLETED/GH-87-DEEP-RESEARCH-MODE.md) · [#87](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/87)
- **GH-61 · CI GitHub Actions (Tier 1 lint/doc-hygiene + Tier 2 validate.sh)** ✅ **Tier 1 SHIPPED 2026-07-02 (`d9b8a14`, marathon dogfood — codex + agy Approved); Tier 2 held for the operator runner decision** — `.github/workflows/ci.yml` (single `ubuntu-latest` job: `shellcheck -S error` + `bash -n` + `node --check` + settings-JSON validate + `pdda.sh run`) + a dependency-free `test/ci-workflow.sh` wired into `validate.sh` (**80/80**). The run reproduced **#59** live (allowlisted artifact in a new untracked dir → spurious off-lane exit 6). **Tier 2 remaining:** running `./validate.sh` inside CI carries an unresolved `macos-latest` (fast, ~10× minutes) vs `ubuntu-latest` + portability/skip-gating decision — reserved for the operator; #61 stays open for it. no CI today; add Actions to catch the ~80% (bash logic + doc/path drift). **Tier 1** (cheap, always-green, no auth): `shellcheck` + `bash -n` on all `*.sh`, `node --check`/JSON-validate, `utils/pdda/pdda.sh run` full-mode. **Tier 2** (the real gate): `./validate.sh` 69-test suite — needs a portability + live-agent skip-gating pass, and a runner decision (`macos-latest` fast/~10× minutes vs `ubuntu` cheap/needs the pass) left open; don't make it *required* until reliably green. Marathon-sequenceable: Tier 1 = independent quick-win lane; Tier 2 depends on the skip-gating sub-task. → [GH-61-CI-GITHUB-ACTIONS.md](PROJECT/1-INBOX/GH-61-CI-GITHUB-ACTIONS.md) · brief: [gh-61-ci-tier1-brief.md](PROJECT/2-WORKING/briefs/gh-61-ci-tier1-brief.md) · [#61](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/61)
- **GH-41 · `task.done` not terminal vs higher-epoch reclaim (silent token resurrection)** 🐞 ✅ **SHIPPED 2026-07-03 (Plan A lane 1 — PR #99)** — latent kernel gap (GH-40 canary #1): in `src/project.js` `foldWithMeta` a completed task was silently resurrected by a later higher-epoch `task.claimed` (`done`→`claimed`, 0 rejections). **Option A (terminality-seal)** landed (`/consult` Codex + agy unanimous; [decisions/2026-07-02-terminality-seal.md](decisions/2026-07-02-terminality-seal.md)): a terminal is now authorized against `ownerAsOf(terminal.ts)` (not the global winner), and once sealed every later claim is rejected as the new distinct reason `claim-after-terminal` — never a `done`→`claimed` flip. Canary **inverted** (`verify-fixture.sh`: `done 1 claim-after-terminal`; control `done 0`); all projection/tick/chaos suites green, no epoch-fence regression. Option B deferred. cx/risk/eff 4/4/3. → [GH-41-DONE-NOT-TERMINAL.md](PROJECT/3-COMPLETED/GH-41-DONE-NOT-TERMINAL.md) · [#41](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/41)
- **GH-23 · Cursor CLI lane (3rd cross-model worker)** ⏸️ **parked-by-operator 2026-07-02** — add Cursor CLI as a Path-A worker (net-new `relay-automation/cursor-turn.sh` mirroring `codex-turn.sh`/`agy-turn.sh`). Assessed lane-shaped (2/2/2, shim) BUT (a) no capture doc yet and (b) only buildable if Cursor CLI has a headless print + auto-approve mode (unverified). Operator's call 2026-07-02: **skip/park** — Codex + agy already give cross-model coverage; revisit if a 3rd lane is wanted. → [#23](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/23)
- **GH-30 · optional centralized transcript archive** ✅ **ALL PHASES SHIPPED 2026-07-03 (Plan A lane 5 complete)** — optional `XYZ_ARCHIVE_ROOT` redirects relay/consult/marathon transcripts out of each foreign repo's `relay-system/` into one namespaced archive; default unset = byte-for-byte current. **Phases 1–2:** single resolver `rtl_transcript_root` + `rtl_repo_slug` in `relay-turn-lib.sh` (Model-A validation: absolute + exists + git repo, else HARD ERROR), and all writers derive their transcript base from it. **Phase 3 (Model A, risk-4 core):** `rtl_init` flags `RTL_ARCHIVE_MODE` when the relay file's git repo ≠ `RTL_ROOT`, and `rtl_enforce` commits the **transcript into the archive** via an isolated `git -C` pathspec commit while the **code artifact + `.tick` token stay on the target** — target tree free of `relay-system/`, no transcript commit in target history, and the isolated archive commit can never orphan a concurrent peer commit (GH-13 guard target-only, holds when token-tree ≠ transcript-tree). **Phase 4:** `extract-relay-telemetry.sh` reads the resolver, aggregating across all `<repo-slug>/` dirs. **Phase 5:** `new-relay.sh` wired; `CONSUMING.md` + `README.md` document the contract; CHANGELOG bet recorded. New `test/archive-commit.sh` (16) + `test/archive-telemetry.sh` (3) → `validate.sh` green. cx/risk/eff 3/4/3. Kernel lane (`relay-turn-lib.sh`). → [GH-30-CENTRALIZED-TRANSCRIPT-ARCHIVE.md](PROJECT/3-COMPLETED/GH-30-CENTRALIZED-TRANSCRIPT-ARCHIVE.md) · [#30](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/30)
- **Marathon dogfood · rebalance-OS parallel build queue (cross-repo, `--target-root`)** 🆕 **captured 2026-06-28** — **orchestrate this from Claude Code in THIS repo** as a real cross-repo dogfood: drive the rebalance-OS build queue (`PROJECT/4-MISC/QUEUE-2026-06-27.md` in that repo) via `swarm-preflight → marathon-drive --target-root <rebalance-OS clone>`. It has **3 collision-free lanes**: **SWIFT-APP** (Focus5Float VS Code focus-if-open Phase 1, claim `macOS/Apps/Focus5Float/Sources/**`), **BACKEND** (Unified Refresh/Restart v1 — signed Reminders helper + pulse server, claim `scripts/**`), **SPIKE** (Focus 5 App Store Phase 0, new `macOS/Apps/Focus5Native/**`). Disjoint write-globs ⇒ XYZ-safe concurrently. Per-lane gates differ (`swift build`+`FOCUS5_SELFTEST=1` / `pytest`+live `/api/refresh` / spike build); the *verify-and-archive* items finish on an **operator GUI litmus** (no CLI agent can satisfy a LaunchServices GUI check). Do not touch rebalance-OS outside an explicit, scoped lane run. **Status 2026-06-29: Lane A (Focus5Float VS Code focus-if-open) shipped INLINE — built by hand, NOT via the harness, since the marathon builder lane was then blocked by #14 — and pushed to rebalance `feat/apple-reminders-write`. Lanes B (refresh/restart v1) + C (App Store spike) handed to Agy (its app, direct in rebalance); the archive sweep is operator GUI-litmus. #14 is now fixed. **Status 2026-07-01: UNBLOCKED — GH-51 [1-kernel] closed 2026-06-30; `--target-root` containment normalization is fixed. Deferral condition met. Fire one scoped lane as the cross-repo proof.**
- **GH-48 · generalize marathon-plan's zone model for cross-repo pre-pre-flight** 🔨 **built and merged via PR #125 (`d9db49d`) 2026-07-04; issue stays OPEN — closure call pending** — `marathon-plan` now loads zone rules from config (`--zones-config`, `QUEUE_PLAN_ZONES_FILE`, root-local file, built-in default), generalizing the old hardcoded xyz-only `KERNEL_PATHS`/`SHIM_RE` classifier while keeping xyz's own output byte-identical by default. New coverage locks foreign-zone names, `pathRegexCaseInsensitive`, `escalateOrchestratorOnly`, config precedence, and fail-loud behavior. The rebalance-OS live check proved the important cross-repo fix — BACKEND reclassifies as `signed-helper` — but also corrected the earlier doc assumption: a single helper-writing lane does **not** split into its own wave under `maxPerWave: 1`. → [GH-48-QUEUE-PLAN-CROSS-REPO-ZONES.md](PROJECT/2-WORKING/GH-48-QUEUE-PLAN-CROSS-REPO-ZONES.md) · [#48](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/48)
- **Dueling/relay · commit-signal advance (file-driven mode)** 🟡 — **built + tested 2026-06-26** (agy review pending): `poll.sh --turn-source file` reads whose-turn from the relay `NEXT:` field, making the tick token **optional** — exactly what the first cross-repo dueling run needed when the Maintainer Claude worked straight off the relay file and never joined `tick`. Optional `--peer-commit-repo`/`--peer-commit-match` add the "advance on the peer's fix commit" signal (idle → run-runner only once a matching commit lands). `relay_field` now tolerates `**bold**` markdown keys; STATUS terminal + artifact-scope-clean still apply. 7 new assertions in [test/poll-driver.sh](test/poll-driver.sh); `validate.sh` **48/48**. → [poll.sh](relay-automation/poll.sh) · [Field findings](PROJECT/4-MISC/AUTOMATED-RELAY.md#field-findings--first-cross-repo-dueling-run-2026-06-26)
- **Dueling/relay · token resilience for a non-participating peer + multi-round reuse** 🟡 — **core addressed by `--turn-source file`** (token-optional ⇒ no `parked suspect` stall and no `spent/reserved — not claimable` mid-run, the two lock failures from 2026-06-26). Residual (only when you DO use the tick token): a non-watchdog `reap`+`claim` reconcile path (still manual) and token-reuse semantics that survive N `release --to peer` round-trips. Lower priority now that file-source is the recommended dueling path. → [AUTOMATED-RELAY.md → Field findings](PROJECT/4-MISC/AUTOMATED-RELAY.md#field-findings--first-cross-repo-dueling-run-2026-06-26)
- **Gate design · convergence gates that pin a *worse* code shape** 🐞 — the KWFS `#70` gate statically extracts a literal `return array(...)` from *each* of three formatter methods and asserts equality, which makes the issue's intended fix (one canonical method + delegate/delete the duplicates) **fail the gate** — the Maintainer was forced to keep three duplicated-but-identical literals, divergence the gate calls "converged." Generalizes: static-extraction needle gates can pin a less-maintainable shape than the refactor they exist to enforce. Fix direction: assert runtime output-key equality (or canonical + delegation), not three literals. → [AUTOMATED-RELAY.md → Field findings](PROJECT/4-MISC/AUTOMATED-RELAY.md#field-findings--first-cross-repo-dueling-run-2026-06-26)
- **Orchestration · in-loop gate verification must run sandbox-off** 🟡 — an orchestrator/watcher re-running a target gate (`bash tests/run.sh`) under the Claude Bash sandbox **false-fails** (PHP can't create lock files → bogus "syntax errors"); the peer's un-sandboxed run is authoritative. Handle: in-loop gate self-checks run sandbox-off, or trust the peer's run + spot-confirm sandbox-off (memory: `codex-cli-needs-sandbox-disabled` family). → [AUTOMATED-RELAY.md → Field findings](PROJECT/4-MISC/AUTOMATED-RELAY.md#field-findings--first-cross-repo-dueling-run-2026-06-26)
- **Tooling · agy reliability testing** ⏸️ parked — proposal + 3 dogfoods this session: agy **clean as a reviewer** (×2), **scope-sensitive as a builder** (failed a kernel-spanning task → F4/F6/F7 contained; succeeded on a small bounded one). Resume to run the S1–S10 matrix. → [AGY-RELIABILITY-TESTING.md](PROJECT/1-INBOX/AGY-RELIABILITY-TESTING.md)
- **Tooling · front-door onboarding health** 🟡 parked — read-only audit shipped → [FRONTDOOR.md](PROJECT/4-MISC/FRONTDOOR.md) (continuous deterministic dashboard; 10 findings, re-runnable checks) + a phased remediation plan. Verdict ⚠️ Bumpy: clone-to-working works (`validate.sh` 36/36, secrets clean), but stale test counts (3 docs) + 2 dead README links + a phantom-path `CLAUDE.md` + undocumented `--target-root`/`install.sh` remain. Phases 1–3 queued (doc-only). → [FRONT-DOOR/2026-06-22.md](PROJECT/1-INBOX/FRONT-DOOR/2026-06-22.md)
- **PDDA · feedback-synthesis direction** 🟡 parked — **proposal (1-INBOX), agy-reviewed 2026-06-23**: reduces the three June 23 external feedback notes (Perplexity/ChatGPT/Gemini) to one direction — keep PDDA a *thin repo-governance + safety layer*. Near-term scope = Phases 1–2 (constitution/positioning + contract/mode hardening); Phases 3–5 (artifact ergonomics, the Perplexity-only evidence bridge, integrations) deferred. Relay-reviewed by agy: 1 Blocker + 3 Should applied → **Approved**. Awaiting promotion decision to `2-WORKING`. → [PDDA-FEEDBACK-SYNTHESIS-PLAN.md](PROJECT/1-INBOX/PDDA/PDDA-FEEDBACK-SYNTHESIS-PLAN.md) · relay [pdda-feedback-synthesis.md](relay-system/2026-06-23/pdda-feedback-synthesis.md)

### In progress

- **GH-132 · HQ resolution/dispatch hardening (Codex-review follow-ups)** 🟡 **all 4 fixes built + regression-tested on `worktree-hq-gh132-hardening` → PR** — a headless Codex review of the shipped HQ feature (thread on `main`, `4158bd0`; *Changes requested*) found 4 untested edge cases, now closed: **(B1)** basename-only resolution could pick the wrong repo → `hq_xyz_lookup` preserves `owner/repo` identity, disambiguates a collision by real git slug, else returns AMBIGUOUS (`rc=2`); **(B2)** a stale XYZ path could arm `fire` at a dead target → stale rows skipped (`[ -d ]`), not counted as installs; **(S3)** `park --title` wrote unescaped YAML + exited 0 → `hq_yaml_dq` escaping + `park --create` fails hard on a bad frontmatter check; **(S4)** `resolve '*'` matched via `find` → glob tokens refused. New `test/hq-hardening.sh` 11/11 (each reproduces the old failure first); full HQ suite 90 checks green; shellcheck + pdda clean. cx/risk/eff 3/2/2. → [GH-132-HQ-RESOLUTION-HARDENING.md](PROJECT/2-WORKING/GH-132-HQ-RESOLUTION-HARDENING.md) · [#132](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/132)
- **Marathon Plan B Waves 1–3** ✅ **fired 2026-07-04; only #94 remains parked** — Wave 1 shipped GH-92, GH-93, GH-96 (Seam #2 only), GH-89, GH-86; Wave 2 shipped GH-55 + GH-48; Wave 3 shipped GH-54 (all three merged via PR #125, `d9db49d`). The shared-file zones serialized exactly as planned (`#89 → #55`, `#86 → #48`) and the cross-zone brief lane ran last, alone. `validate.sh` reruns weren't blocked by the wave's touched files — the one non-green rerun was independently traced to pre-existing, order-dependent flakiness in `oracle-guard.sh`/`improve-loop-qa.sh` (not the `relay-self-sufficiency.sh` gate originally blamed). Independent review before merge filed two non-blocking follow-ups: **#126** (GH-55's covering-test substring match), **#127** (GH-54's fs-touching regex misses bare `>`). See [MARATHON-PLAN-2026-07-03-B-PARALLEL.md](PROJECT/2-WORKING/MARATHON-PLAN-2026-07-03-B-PARALLEL.md) for the collision map and the still-parked `#94` gate.
- **GH-92 · poll.sh: relay-pointer parser rejects whole-line bold (`**NEXT: x**`) → silent turn-1 deadlock** 🔴 ✅ **SHIPPED 2026-07-04 (`aabc04d`, `dcbdadd`); closed** — `relay_field` (`relay-automation/poll.sh:152`) stripped leading markdown around a pointer key/value but not trailing, so `**NEXT: claude-reb**` parsed to `claude-reb**`, failed the `--claude-agents` membership check, and the poller silently nudged cross-model forever (a real duel stalled ~90 min). Fixed: extended the sed pipeline to strip trailing `` [`*]+ `` and widened the leading-strip to also cover a leading backtick; added a `WARNING (GH-92)` diagnostic. 6 new `test/poll-driver.sh` cases (37/37). Caught + fixed centrally: the packaged `relay-pkg.tar.gz` snapshot went stale against the fix, regenerated via `make-pkg.sh`. cx/risk/eff 1/1/1. → [GH-92-POLL-BOLD-POINTER-DEADLOCK.md](PROJECT/3-COMPLETED/GH-92-POLL-BOLD-POINTER-DEADLOCK.md) · [#92](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/92)
- **GH-93 · tick analyze: concurrency % spans the whole event log, not the run → reports 0% for a ~51% run** ✅ **SHIPPED 2026-07-04 (`f86d8cf`); closed** — `analyze()` (`src/analyze.js:435-442`) derived the run window from the whole `.tick/events/` log span, not the actual run, so stale events from a prior marathon collapsed the printed concurrency toward 0% even on a genuinely concurrent run. Fixed: defaults to the work-bounded window (first `task.claimed` → last `task.done`, matching SKILL.md's existing guidance); whole-log span kept as a separate informational field. New GH-93 regression fixture in `test/analyze.sh` (18/18; the actual test harness — the doc's original `test/analyze.js` name didn't exist). cx/risk/eff 1/2/2. → [GH-93-ANALYZE-CONCURRENCY-SPAN.md](PROJECT/3-COMPLETED/GH-93-ANALYZE-CONCURRENCY-SPAN.md) · [#93](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/93)
- **GH-96 · XYZ⇄Rebalance integration (XYZ side): xyz-sync check** ✅ **Seam #2 SHIPPED 2026-07-04 (`21d9d79`); issue stays open for Seams #1/#3** — mirrors rebalance-OS#102; the issue's own "ship first" seam. Added `xyz-sync check [<install_dir>|--all]` diffing each registered install's recorded `tick_version`/`source_commit` against the harness's currently-shipped values, warns (never auto-pulls) on drift — the substrate rebalance's Phase-0 collector was blocked on. Seams #1 (XYZ.json emit contract) and #3 (tick-lane consume, explicitly gated) remain separate, not built here; corrected Marathon Plan B's write-set guess (dropped `src/take.js` — zero relevance to Seam #2). New `test/xyz-sync-check.sh` (12/12). cx/risk/eff 2/1/2. → [GH-96-XYZ-REBALANCE-SYNC-CHECK.md](PROJECT/3-COMPLETED/GH-96-XYZ-REBALANCE-SYNC-CHECK.md) · [#96](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/96)
- **GH-89 · swarm-preflight: no ready path for greenfield (new-file) lanes** ✅ **SHIPPED 2026-07-04 (`e6f418c`); closed** — the GH-39 A2 artifact-existence check required every `artifacts[]` path to already exist at `target.ref`, so a pure new-file build (reproduced live on GH-87 and GH-88's real contracts) always read `NOT-READY`. Fixed: opt-in `artifacts_new: string[]` field exempting listed paths from the existence check, gated on a matching `fix_probes` `path_absent` entry on the same path (proves genuine unbuilt-ness, can't be used to dodge the check) — checked post-merge so a multi-issue bundle can split the declaration and its probe across sibling docs. Live regression against GH-87's/GH-88's actual contracts confirms both now read READY. `test/swarm-preflight.sh` 65/65. cx/risk/eff 2/1/2. → [GH-89-SWARM-PREFLIGHT-GREENFIELD.md](PROJECT/3-COMPLETED/GH-89-SWARM-PREFLIGHT-GREENFIELD.md) · [#89](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/89)
- **GH-86 · marathon-plan — surface PR-review lanes so they don't silently drop** ✅ **SHIPPED 2026-07-04 (`2ce409b`); closed** — `marathon-plan.sh` generated only build lanes; PR-review lanes lived only in a manual `PR-REVIEW-QUEUE-<date>.md` overlay nothing surfaced, so two review lanes (PR #79/#81) were silently never run (caught 2026-07-02). Fixed (Level 1 only): the plan now prints a "Review lanes (run via relay-xyz)" section naming the overlay + its lanes when today's overlay exists — verified zero output diff when it's absent. 2 new `test/marathon-plan.sh` scenarios (46/46). cx/risk/eff 2/1/2. → [GH-86-SURFACE-REVIEW-LANES.md](PROJECT/3-COMPLETED/GH-86-SURFACE-REVIEW-LANES.md) · [#86](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/86)
- **Part B — Adversarial hardening** ⚠️ — Phase 1 (epoch fencing) shipped; Phase 2 chaos-suite *detection* partials landed; Phases 2–4 are the active "adversarially proven → commercially viable" frontier. Immediate next-up: promote exactly one proof-sized Phase-2 slice into a contract-backed lane (important because Part B only keeps momentum if it advances in small, verifiable proofs instead of reopening the whole frontier at once). → [ADVERSARIAL-HARDENING.md](PROJECT/2-WORKING/ADVERSARIAL-HARDENING.md)
- **Tooling · relay-to-issue skill** 🟡 — **shipped 2026-06-22**: a post-relay skill that distills a closed `/relay` thread into ONE checklist-style GitHub issue, filed in the repo the relay was *about* (cross-repo aware; dedup-stamped; auto-posts via `gh`). `skills/relay-to-issue/` (SKILL + `relay-to-issue.sh` + `install.sh`); `resolve` smoke-tested green. Remaining: operator `install.sh` + one un-sandboxed live `gh issue create` to confirm posting E2E. → [RELAY-TO-ISSUE-SKILL.md](PROJECT/2-WORKING/RELAY-TO-ISSUE-SKILL.md)

### Completed
- **GH-128 · HQ — multi-repo command center: registry-resolved project card + PDDA-compliant intake/dispatch** ✅ **SHIPPED 2026-07-04, merged via PR #131 (`2bcd36c`); closed** — Phases 0–4 all built & verified. Phase 0 (registry discovery) + Phase 1 (read-only resolver/card) + Phase 2 (`park` issue-first intake writer, preview-by-default; proven live filing rebalance-OS #109/#110) + **Phase 1.x fuzzy** (`hq_norm`/`hq_candidates`: loose names like "rebalanceOS" resolve; ambiguous names return rc=2 + `CANDIDATES` instead of guessing) + **Phase 3 dispatch** (`queue [--create]` appends a non-destructive HQ-queued lane; `fire --gh-issue N --risk R` is a **gated prepare-and-hand-off** — Tier A + `risk<3`, emits the `swarm-preflight` command, never driving the harness itself) + **Phase 4** (`hq next` Rebalance-priority board + the **user-level locator** `skills/hq/find-hq.sh` + `skills/hq/install.sh`, so `/hq` runs from a session in **any** repo — symlink-safe, no hardcoded path). Tests: `hq.sh` 24/24 · `hq-park.sh` 23/23 · `hq-dispatch.sh` 18/18 · `hq-next.sh` 6/6 · `hq-locator.sh` 8/8, all in `validate.sh`; shellcheck + PDDA clean. **The one non-goal — live execution of a `fire` marathon — is held by design** (the operator drives via the relay-xyz skill; GUIDING-PRINCIPLES §8). cx/risk/eff 3/2/3. → [GH-128-HQ-COMMAND-CENTER.md](PROJECT/3-COMPLETED/GH-128-HQ-COMMAND-CENTER.md) · [#128](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/128)
- **Tooling · PDDA runtime consolidation (upgrade)** ✅ **2026-06-30 (doc relocated to 3-COMPLETED 2026-07-04 — was sitting in 2-WORKING past its own recorded completion)** — migrated this repo off its old split PDDA (`utils/pdda-run.sh` + 11 flat `pdda-check-*.sh`) onto the upstream single-dispatcher runtime (`utils/pdda/pdda.sh`, installed by `pdda/install.sh`). Adopted the upstream **integer `1`–`5`** triage-ratings contract (was `low|medium|high`): rewrote all 25 rated docs (`low→2/med→3/high→4`, order-preserving), updated `marathon-plan.sh` + its test; **dropped** `pdda-check-ratings.sh` (its enforcement now folded into `pdda.sh frontmatter` + `marathon-plan`'s unrated detection). Rewired hooks/gate-test/operator-docs; removed 12 flat files; kept repo-specific tooling. Gates green: `pdda.sh run` full-mode 0 errors, `test/marathon-plan.sh` 31/31, `validate.sh` 0. → [decision](decisions/2026-06-30-pdda-runtime-consolidation.md) · [PDDA-RUNTIME-CONSOLIDATION-MIGRATION.md](PROJECT/3-COMPLETED/PDDA-RUNTIME-CONSOLIDATION-MIGRATION.md)
- **GH-55 · swarm-preflight: auto-include a changed artifact's covering tests in the builder allowlist** ✅ **SHIPPED 2026-07-04, merged via PR #125 (`d9db49d`); closed** — `swarm-preflight.sh` now derives an effective builder allowlist from contract `artifacts[]` plus explicit covering `test/**` references and recursively sourced local test helpers, preserving deterministic order and de-duping duplicates before writing the packet and `marathon-invocation.txt`. New `test/swarm-preflight.sh` fixtures T31/T32 lock the covering-test/helper pull-in and no-dup behavior (75/75 overall). Independent review before merge found the covering-test match is a plain substring check, not a true reference check — filed as **[#126](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/126)**. Repo-wide `validate.sh` rerun wasn't blocked by this lane's files — the non-green run traced to pre-existing `oracle-guard.sh`/`improve-loop-qa.sh` flakiness. → [GH-55-AUTO-INCLUDE-COVERING-TESTS.md](PROJECT/3-COMPLETED/GH-55-AUTO-INCLUDE-COVERING-TESTS.md) · [#55](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/55)
- **GH-54 · marathon brief: forbid in-turn execution of filesystem-touching tests** ✅ **SHIPPED 2026-07-04, merged via PR #125 (`d9db49d`); closed** — when a lane's allowlist includes fs-touching `test/*.sh`, `swarm-preflight.sh` now emits a stronger scope-lock rule ("read as specs; do not run in-turn") and `marathon-plan.sh` mirrors the same warning in its generated "How to fire a lane" guidance. Locked by `test/swarm-preflight.sh` T33 and `test/marathon-plan.sh` N(c). Independent review before merge found the fs-touching regex misses a bare `>` redirect (only `cat >`/`printf...>`/`>>` are caught) — filed as **[#127](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/127)**. Repo-wide `validate.sh` rerun wasn't blocked by this lane's files — the non-green run traced to pre-existing `oracle-guard.sh`/`improve-loop-qa.sh` flakiness. → [GH-54-FORBID-FS-TOUCHING-TESTS.md](PROJECT/3-COMPLETED/GH-54-FORBID-FS-TOUCHING-TESTS.md) · [#54](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/54)
- **GH-104 · Full-library portability + full-mirror vendor install** ✅ **SHIPPED 2026-07-03 (`5972ef4` + portability fix `a17ef13`)** — two-part close: **(1)** original full-mirror (`5972ef4`) replaced the drift-prone `make-pkg.sh` manifest with a complete directory mirror (`relay-automation/ bin/ src/ test/ skills/`); **(2)** portability fix (`a17ef13`) closed three remaining gaps: `utils/` added to `VENDOR_DIRS` (ships `swarm-preflight.sh`, `marathon-plan.sh`, `telemetry/`); tool-root separated from target-root in all three entrypoints (`swarm-preflight.sh`, `marathon-plan.sh`, `marathon-drive.sh` — `_here_parent`/`_xyz_harness` detection sets `ROOT` = target repo while `TICK_BIN`/`XYZ_APPEND_BIN` stay anchored to `.xyz/`); generated-doc paths (`packet.md` invocation, marathon-plan "How to fire a lane") now emit `.xyz/`-prefixed commands for vendored installs. `test/xyz-vendor.sh` gains `utils/` mirror check + collision-safety fixture (target `src/`/`utils/`/`bin/` untouched). Remaining separate follow-up: `install.sh`/`relay-pkg.tar.gz` still on stale manifest. → [#104](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/104)
- **GH-5 · Contract-seam warning for coupled lanes** ✅ **SHIPPED 2026-07-03 (Plan A lane 4)** — the one thing that made two lanes actually independent in the real run was a coordinator-pinned shared contract (both coded TO the contract, not to each other's source), but nothing detected coupling — operators found it only when an agent stalled on another lane. The wave-packer defers only on EXACT write-set collision, so two lanes writing disjoint files under a common dir (e.g. `src/schema/producer.js` ‖ `src/schema/consumer.js`) co-wave and look independent. Now `marathon-plan.sh` detects the seam (`sharedSpine`: deepest shared dir of ≥2 segments; top-level-only sharing is not a seam) and emits a **"Contract seams — pin a contract"** section naming each coupled pair + the shared dir + the fix (pin a `CONTRACT.md`, point each lane prompt at it) — making the "pin the contract" step first-class (fix 1) + the coupled-lane warning (fix 2); deferred-coupled-work (fix 3) is the existing Held/flagged section. Advisory (never re-waves). Pairs with GH-4 work-stealing (a stolen coupled task would stall). `test/marathon-plan.sh` scenario K (+3: seam flagged, disjoint-subtree not, pin-a-contract stated); 39/39. Auto-scaffold + import-graph inference deferred. cx/risk/eff 3/2/2. → [GH-5-CONTRACT-SEAM-WARNING.md](PROJECT/3-COMPLETED/GH-5-CONTRACT-SEAM-WARNING.md) · [#5](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/5)
- **GH-4 · Lane imbalance + no work-stealing undercuts the bars** ✅ **SHIPPED 2026-07-03 (Plan A lane 3)** — the success bar was `concurrency% AND each agent ≥2 done`, but a clean 2-lane/2-agent split gives 1 done each by construction → a flawless run failed. Fixed all three ways: **(1)** `tick analyze` emits a **VERDICT** (pass/fail/incomplete) gating on concurrency% + zero-parked + all-lanes-done + zero-collisions, **NOT** per-agent done count (`computeVerdict`; `TICK_CONCURRENCY_TARGET_PCT` finite-tunable, default 50); **(2)** work-stealing needed no new mechanism — `tick take` already pulls a collision-free task from any free lane, and removing the per-agent bar makes the resulting imbalance a *win* (a fast agent fills idle tail-time); **(3)** a **lane-balance** idle-tail line + `computeCollisions` (0 overlapping concurrent claims — verifies the containment invariant). `test/workstealing-verdict.sh` (6, end-to-end steal→drain→PASS on a 2-vs-1 split) + `test/analyze.sh` (+2, FAIL on an undone/broken lane); `validate.sh` 87/87. Runner auto-`take`-loop + marathon exit-gate deferred (non_goals). cx/risk/eff 3/2/3. → [GH-4-WORKSTEALING-VERDICT.md](PROJECT/3-COMPLETED/GH-4-WORKSTEALING-VERDICT.md) · [#4](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/4)
- **GH-3 · Parked-claim threshold false-positive for autonomous agents** ✅ **SHIPPED 2026-07-03 (Plan A lane 2)** — `tick analyze`'s parked-claim detector (`src/analyze.js`) flagged a fully-active 90%-concurrency run as parked: its liveness signal was **`task.heartbeat`-only** and the fixed 10-min threshold is human-paced, but an autonomous subagent in one long atomic tool call has no yield point to `tick ping`. Fixed events-only (no git dependency): **(2)** liveness is now **any `task.*` event from the agent** (scope_changed/commented/claim/ping/done all prove it's alive), and **(1)** the threshold is operator-tunable via **`TICK_PARKED_THRESHOLD_MS`** (default unchanged, 10 min). `parked_suspects` shape/semantics unchanged → safe for `watchdog.sh` reap + `poll.sh`. `test/heartbeat.sh` +3 (scope_changed=liveness; env override raises+suppresses); chaos-midturn-kill/watchdog-liveness green. git-mtime (fix 3) + DISQUALIFIES→WARN (fix 4) deferred. cx/risk/eff 2/2/2. → [GH-3-PARKED-CLAIM-FALSE-POSITIVE.md](PROJECT/3-COMPLETED/GH-3-PARKED-CLAIM-FALSE-POSITIVE.md) · [#3](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/3)
- **GH-88 · Cross-repo marathon monitor v1.0 (read-only fzf TUI)** ✅ **SHIPPED 2026-07-03 (marathon Wave 1 — Sonnet build, Opus-reviewed)** — stateless read-only viewer joining existing primitives (`~/.config/xyz/registry.tsv` col 5 · `.git/relay-driver.lock`/`.relay-driver.lock` + `kill -0 pid` · newest `.tick/events/*marathon*`): `relay-automation/marathon-{ls,detail,tui}.sh` (fzf TUI) print one row/repo with derived states LIVE/STALE/IDLE/GONE, writing no new state. Review caught + fixed 2 real bugs the parallel build masked: the registry **header row was parsed as data** (→ spurious GONE) and the test's `${TMPDIR}/…` produced `//` paths that broke exact-match assertions. `test/marathon-monitor.sh` **13/13** (both lock-path cases + all 4 states). Launching stays manual (v1.1). → [GH-88-CROSS-REPO-MARATHON-MONITOR.md](PROJECT/3-COMPLETED/GH-88-CROSS-REPO-MARATHON-MONITOR.md) · [#88](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/88)
- **GH-63 · Signal triage stage for inbound signals before GH-*.md capture** ✅ **SHIPPED 2026-07-03 (marathon Wave 1 — Sonnet build)** — deterministic pre-capture classifier `utils/signal-triage.sh`: first-match, source-keyed rules tag any inbound signal (validate-fail / security-scan / relay-exit6 / canary-reject → **bug**; watchdog-parked / pdda-sync / dependency-drift → **drift**; manual → **enhancement**; dedupe / informational → **noise**), dedupe-first, emits an inspectable JSON note (`{signal_id, source, category, severity, evidence, …}`, matched-rule id in `evidence`) to `relay-system/triage/<id>.json` — no `.tick/` writes. `test/signal-triage.sh` **44/44**. (ROUTER.md hint deferred as a small follow-up.) → [GH-63-SIGNAL-TRIAGE-STAGE.md](PROJECT/3-COMPLETED/GH-63-SIGNAL-TRIAGE-STAGE.md) · [#63](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/63)
- **GH-44 · scratch-repo `.git` fall-through can pollute the parent repo (RCA)** ✅ **SHIPPED 2026-07-03 (marathon Wave 1 — Sonnet build)** — factored the scratch-repo hardening out of the fixtures into a shared `test/_scratch-repo.sh` helper (`make_scratch_repo <dir>`: `GIT_CEILING_DIRECTORIES` cap + `git init` + hard `.git`-exists assertion so a failed/blocked init can never fall through to the parent repo, + local identity). Both canary fixtures (`canary-peer-orphan`, `canary-reviewer-overstep`) now source it instead of duplicating the guard inline; both green in `validate.sh`. (AGENTS.md rail deferred — that file is under concurrent edit.) → [GH-44-SCRATCH-REPO-GIT-FALLTHROUGH.md](PROJECT/3-COMPLETED/GH-44-SCRATCH-REPO-GIT-FALLTHROUGH.md) · [#44](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/44)
- **GH-77 · Aider ↔ OpenRouter turn-taker lane (discrete from Codex)** ✅ **SHIPPED 2026-07-03 (PR #79 — codex-reviewed via relay-xyz; token-ownership [Blocker] fixed on merge; rebased, `validate.sh` green)** — new `relay-automation/aider-turn.sh` drives Aider against OpenRouter behind the shared `relay-turn-lib.sh` containment core (discrete from Codex). The shim owns the tick token ops (Aider can't run shell mid-turn) and — per the codex review [Blocker] — **asserts `claimer == self` via `tick info` before launching Aider, failing the turn (exit 5) if the claim didn't stick**, so a turn can never commit with the token open under the old owner. Additive routing in `marathon-agent.sh` (+`consult.sh` advisor); `--no-auto-commits`. **Live E2E (2026-07-03, real OpenRouter turn) surfaced + fixed a containment bug** — Aider's own `.aider.*` history files leaked into the tree and tripped the off-allowlist guard (exit 6 on every real turn); the shim now redirects them out via `--chat-history-file`/`--input-history-file`/`--llm-history-file`. `test/aider-turn.sh` **32/32** (+ unowned-token guard + aux-file redirect) + `test/consult.sh` (+2). → [GH-77-AIDER-OPENROUTER-LANE.md](PROJECT/3-COMPLETED/GH-77-AIDER-OPENROUTER-LANE.md) · [#79](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/pull/79) · [#77](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/77)
- **GH-78 · optional hourly doc-preflight (contract-enforcing auto-edits + telemetry)** ✅ **SHIPPED 2026-07-03 (`115713f`, PR #81 — agy Approved via relay-xyz review 2026-07-02; rebased onto main, churn conflicts resolved to main, `validate.sh` green)** — opt-in `utils/telemetry/preflight-docs.sh`: when `PDDA_LLM_BIN` is set it runs an LLM doc-readiness pass that may apply contract-enforcing auto-edits, gated by a deterministic safety valve (rejects edits that worsen checks or exceed 30 lines) with JSON telemetry; a no-op that never blocks when unset. Thorough offline test coverage in `test/preflight-docs.sh` (14/14). → [GH-78-DOC-PREFLIGHT-AUTOMATION.md](PROJECT/1-INBOX/GH-78-DOC-PREFLIGHT-AUTOMATION.md) · [#81](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/pull/81) · [#78](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/78)
- **GH-45 · QUEUE 'must-complete' commitment contract + anti-rabbit-hole safeguard** 🐞 ✅ **SHIPPED 2026-07-03 (PR #91 — marathon; cross-model review applied)** — make a wave/lane a *commitment contract* so a session can't silently abandon the plan to deep-dive one item (the GH-39 drift). v1: AGENTS.md re-anchor+park paragraph + a per-lane attempt cap (`.tick/attempts/<lane>`, default 2, `--force` override) in `marathon-drive.sh` + mirror in `relay-drive.sh` + `test/lane-attempt-cap.sh`; cross-model review added reset-on-success + `--force` passthrough through `marathon.sh`. Phases 3–4 deferred. cx/risk/eff 3/3/3. → [GH-45-QUEUE-COMMITMENT-CONTRACT.md](PROJECT/1-INBOX/GH-45-QUEUE-COMMITMENT-CONTRACT.md) · [#91](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/pull/91) · [#45](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/45)
- **GH-58 · marathon `--builder claude` fails headless (`exec: claude: not found`)** 🐞 ✅ **SHIPPED 2026-07-02 (marathon — agy build, codex Approved, gate 30/30; merged `82fbf0e`)** — `relay-automation/claude-turn.sh` does `CLAUDE_BIN=${CLAUDE_BIN:-claude}` then `exec`s with no PATH discovery + no fail-fast; headless it dies with a raw `exec: not found`. Fix (shim, not kernel): resolve `CLAUDE_BIN` via PATH then `~/.claude/local/claude`, else fail fast with a clear message + distinct exit code; new `test/claude-turn.sh`. cx/risk/eff 2/2/2. Build with a codex/agy builder (not `--builder claude`). → [GH-58-CLAUDE-TURN-PATH-DISCOVERY.md](PROJECT/1-INBOX/GH-58-CLAUDE-TURN-PATH-DISCOVERY.md) · [#58](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/58)
- **GH-85 · marathon-plan `undocumented-partial-completion` false-positives on edit-existing-file lanes** 🐞 ✅ **SHIPPED 2026-07-02 (marathon — agy build, codex Approved, gate 36/36; merged `82fbf0e`)** — the planner's `some-artifacts-exist` + `changelog-mentions-it` signals misfire for a normal fix that edits existing files (artifact exists because it's *edited*, not built) + is documented in CHANGELOG, so genuinely-0%-built lanes (GH-45, GH-30) get held as `partial`. Fix: require a NEW/created artifact for the signal, and/or drop the changelog signal. Keep GH-44-style true positives. cx/risk/eff 2/2/2. → [GH-85-MARATHON-PLAN-PARTIAL-FALSE-POSITIVE.md](PROJECT/1-INBOX/GH-85-MARATHON-PLAN-PARTIAL-FALSE-POSITIVE.md) · [#85](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/85)
- **GH-84 · test/runner-loop.sh non-hermetic — runner.sh dirty-guard checks the real repo** 🐞 ✅ **SHIPPED 2026-07-02 (marathon — agy build, codex Approved, gate 7/7; merged `82fbf0e`)** — `relay-automation/runner.sh:4` resolves `ROOT_DIR` from its own script location (the real repo), not `TICK_REPO_ROOT`, so its dirty-tree artifact guard checks the real working tree; `test/runner-loop.sh` uses `README.md` as the artifact path, so a dirty real README false-fails the test (`runner: artifact paths have unstaged changes`). Surfaced by GH-83. Live relay use is correct — this is a test-hermeticity leak (GH-74/GH-44 class). Fix: additive `RUNNER_ROOT_DIR` override (default unchanged), test binds it to `$A` + a dirty-file regression case. cx/risk/eff 1/1/1, independent zone. → [GH-84-RUNNER-LOOP-NONHERMETIC.md](PROJECT/1-INBOX/GH-84-RUNNER-LOOP-NONHERMETIC.md) · [#84](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/84)
- **GH-83 · README onboarding UX — single operator path + de-brittle stale test count** ✅ **SHIPPED 2026-07-02 (`347b127`)** — cross-model `/consult` (Codex + agy) reviewed `README.md` cold as a new operator; both graded the same issues **[Blocker]**: no single "do this first" (four starting moves at once), humans sent to the agent-facing `ROUTER.md` first, insider-heavy opener, and a **stale `47/47`** count (actually `80/80` — a freshness bug). Fix: rewrite the top into a self-sufficient human landing page (plain-language value prop → one Step 1 `./validate.sh` → path branch → 4-line glossary), scope ROUTER as agent-facing, direct auth pointer, de-brittle the count, move install/kernel below concepts. Docs-only, trim + reorder (least-code). → [GH-83-README-ONBOARDING-UX.md](PROJECT/1-INBOX/GH-83-README-ONBOARDING-UX.md) · [#83](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/83)
- **GH-59 · relay-turn-lib: allowlisted artifact in an untracked dir trips spurious off-lane (exit 6)** ✅ **SHIPPED 2026-07-02 (`ac6dacb`, marathon lane — codex + agy Approved, gate 23/0)** — `rtl_in_allow` now allows a git-collapsed untracked-dir entry (`dir/`) when it is a **true ancestor** of a concrete allowlisted **file** entry (`greenfield/` ⊂ `greenfield/output.txt`), never a bare prefix (`green/` still fails) — generalizing the `.relay-artifacts` exemption; case-insensitive variant mirrored, bash-3.2. **Retires the stub-dir workaround** for greenfield-in-a-new-dir marathon lanes (the GH-61 case). [decision](decisions/2026-07-02-offlane-untracked-dir.md) records the contract + 3 invariants; `test/worktree-isolation.sh` enforces all three (greenfield-fixed / off-lane-still-rejected / no copy-back widening). `validate.sh` **80/80**. → [GH-59-OFFLANE-UNTRACKED-DIR.md](PROJECT/1-INBOX/GH-59-OFFLANE-UNTRACKED-DIR.md) · [#59](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/59)
- **GH-56 · marathon-drive: reconcile/fresh-id a leaked tick token before re-seed** ✅ **SHIPPED 2026-07-02 (`d679fc4`, marathon lane — codex + agy Approved)** — `marathon-drive.sh` now runs `reconcile_relay_task()` before seeding: **dies on a live claim** (never reaps it), clears a stale `open` handoff routed to builder/reviewer, else proceeds — so re-running a phase-id after an aborted run no longer fails `task ... is open`. Regression coverage in `test/marathon-drive.sh` (45 checks). Split from #51 §5. `validate.sh` **80/80**. Surfaced a self-hosting caveat: a lane editing `marathon-drive.sh` itself corrupts the running script mid-run via copy-back (transient; committed file valid). → [GH-56-MARATHON-LEAKED-TOKEN-RECONCILE.md](PROJECT/1-INBOX/GH-56-MARATHON-LEAKED-TOKEN-RECONCILE.md) · [#56](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/56)
- **GH-75 · XYZ.json — final completion telemetry at relay/swarm/marathon session end** ✅ **SHIPPED 2026-07-02** — every relay/marathon/swarm harness now appends a durable, newest-first completion record to a gitignored `XYZ.json` at the harness repo root (schema: `{harness, sessionId, health, title, description, updatedAt}`). Shared `utils/telemetry/health-lib.sh` factors GH-24's STATUS/VERDICT→health mapping out of `extract-relay-telemetry.sh` (extractor output byte-identical — no GH-24 regression); shared `utils/telemetry/append-xyz-completion.sh` does a locked (GH-72 `mkdir` advisory lock — no lost update) + atomic (temp-file + `os.replace` — no corruption/partial write) read-modify-write-prepend. Wired into `relay-drive.sh`'s terminal exits (green / orange×2 / red), `marathon-drive.sh`'s own per-run hook (gated by `XYZ_HARNESS_CONTEXT`; `harness:"swarm"` when swarm-preflight's generated invocation self-propagates the tag, else `"marathon"`; silent under `marathon-phase`), and `marathon.sh`'s single whole-run record — so a `marathon.sh` N-phase run emits exactly one record, never N/N+1. New `test/xyz-completion.sh` (writer + 16-way concurrency + corrupt-file self-heal + health-lib table) and `test/xyz-harness-hooks.sh` (all harness terminal paths + nesting), plus a swarm-tag assertion in `test/swarm-preflight.sh`. `validate.sh` green. → [GH-75-XYZ-JSON-COMPLETION-TELEMETRY.md](PROJECT/1-INBOX/GH-75-XYZ-JSON-COMPLETION-TELEMETRY.md) · [#75](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/75)
- **GH-69 · Marathon branch suggestion + agent confirmation prompt** ✅ **SHIPPED 2026-07-01** — marathon builds commit several times (build → gate → review) but the pipeline had no branch-cutting step; partial/failed work landed on whatever was checked out. Three-stage fix: (1) `marathon-plan.sh` emits a deterministic `suggested_branch: marathon/<slug>-<date>` per active wave lane (read-only, no git writes); (2) `swarm-preflight.sh` checks real branch existence via `git show-ref` and emits `branch_ready`/`skip_branch_prompt` in the packet (JSON + `packet.md` + text report); (3) the orchestrating-agent contract ("ask the operator before proceeding when `branch_ready: false`, unless the carve-out applies") is documented inline in `swarm-preflight.sh`'s header and self-stated in every packet's "Suggested branch" line — a driving agent doesn't need to recompute it. Carve-out (`risk: 1` + independent-zone artifacts) verified to skip the prompt; kernel-zone artifacts and non-risk-1 items correctly do NOT skip it. No branch is ever auto-created. `test/marathon-plan.sh` 34/34, `test/swarm-preflight.sh` 44/44, `validate.sh` 77/77. → [GH-69-MARATHON-BRANCH-PROMPT.md](PROJECT/1-INBOX/GH-69-MARATHON-BRANCH-PROMPT.md) · [#69](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/69)
- **GH-71 · Code Structure & Implementation Upgrade** ✅ **Phases 1–2 SHIPPED 2026-07-01 (`6a7b6ea`, `fa37c58`)** — plan to upgrade codebase maturity, re-scoped into two safe/reversible initial phases + two deferred rewrites. **Phase 1 (root-dir cleanup):** moved `4X4.md`, `FRONTDOOR.md`, `snapshot.md`, `CODEX.md`, `BACKLOG.md` into `PROJECT/4-MISC/`; canonical front door (`README.md`/`ROUTER.md`/`AGENTS.md`/`ROADMAP.md`/`CHANGELOG.md`/`CLAUDE.md`/`LICENSE.md`) untouched at root; fixed the moved files' now-two-levels-deep internal links + the two live ROADMAP.md cross-references; left historical/archival references (`CHANGELOG.md`, `relay-system/**`, `AUDIT/**`, `decisions/**`, `PROJECT/3-COMPLETED/**`) untouched as point-in-time records. **Phase 2 (JS kernel API boundaries):** JSDoc `@param`/`@returns`/`@throws` on every exported function across the 12 `src/*.js` files (the event-log↔projection API surface) + `utils/checkjs.sh` wiring up the exact Tier 1 lint GH-61 proposed (`node --check`, dependency-free — no `tsc`/TypeScript added) plus a deterministic JSDoc-coverage gate so the annotations stay enforced, not a one-time pass. `test/checkjs.sh` (6 checks) in `validate.sh`. **Deferred (unchanged, still behind a concrete trigger — not a letter grade):** Phase 3 `poll.sh`→Node + Phase 4 `relay-drive.sh`→Node; hold until an actual maintenance incident or a specific Bash-caused bug class justifies rewriting the most safety-critical, best-dogfooded components. `validate.sh` 77/77; `pdda.sh run` clean. → [GH-71-UPGRADE-CODE-STRUCTURE.md](PROJECT/1-INBOX/GH-71-UPGRADE-CODE-STRUCTURE.md) · [#71](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/71)
- **GH-74 · test/xyz-vendor.sh not hermetic under a git worktree** ✅ **CLOSED 2026-07-01 (`b144a23`)** — root cause turned out to differ from the issue's original theory: not the `~/.claude/skills` symlink, but a canonicalization mismatch — `test/xyz-vendor.sh` computed `ROOT` via plain `cd && pwd` (logical) while `find-harness.sh` resolves its own location via `cd -P` (physical). A worktree under a macOS symlinked tmp path (`/tmp`/`/var` → `/private/...`) made the two diverge as strings for the identical directory, false-failing the "no-.xyz default resolution" assertion. One-line test-only fix (`cd -P`); no `find-harness.sh` runtime change. Verified 30/30 in the real clone AND from a `git worktree` at an arbitrary path; `validate.sh` 76/76. → [#74](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/74)
- **GH-70 · relay-xyz: per-repo harness for concurrent automated relays** ✅ **Phases 1–2 SHIPPED 2026-07-01 (`e8d9999`)** — the per-clone global driver lock (GH-42) serializes all relays sharing one harness; per-repo concurrency comes from each repo having its own vendored `.xyz/` (GH-49). **Phase 1 (doc):** SKILL.md "Concurrent relays across repos" section — `xyz-vendor.sh` (not `install.sh`) is the path to per-repo isolation; "When to use" names the case. **Phase 2 (additive):** `find-harness.sh --check` warns when a foreign repo has no local `.xyz/` (using the centralized harness + shared lock) and points at `xyz-vendor.sh`; flags a held lock; fail-open (always exit 0). `test/find-harness.sh` (8 checks); `validate.sh` 76/76. **Phase 3 deferred** (`install.sh --with-harness`) — revisit when a real user finds `xyz-vendor.sh` too indirect. → [GH-70-CONCURRENT-RELAY-LOCAL-HARNESS.md](PROJECT/1-INBOX/GH-70-CONCURRENT-RELAY-LOCAL-HARNESS.md) · [#70](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/70)
- **GH-72 · Registry/projection write-lock** ✅ **CLOSED 2026-07-01 (`24d5869`→`56433d6`)** — the `mkdir` advisory lock, hardened through a `/consult` + a 4-round headless codex relay review that found **FIVE** real defects, all fixed + verified: (1) empty-pid TOCTOU, (2) retry/fail-open-under-contention (fast retry + wall-clock deadline), (3) unlocked bootstrap truncation (header now created under the lock), (4) fail-open wrote unlocked → now **skips** (best-effort), (5) projection ran inside the registry lock → now published after release. Ownership-checked release/cleanup. New `test/registry-lock-concurrency.sh` (16 concurrent → 16/16, stable + under CPU load; projection-enabled 16/16 ×3). `validate.sh` 75/75. codex final verdict **Approved**. A textbook dogfood — the harness review caught 4 issues the reasoning-only consult missed. → [#72](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/72)
- **GH-73 · Docs pin fixed /tmp transcript log paths** ✅ **CLOSED 2026-07-01 (`d427a90`, concurrent dogfood Lane A / agy)** — every fixed `/tmp` transcript-log example in `SKILL.md` + `README.md` switched to per-PID `-$$.log` + a note that fixed paths break concurrent same-machine runs (shims were already per-PID; only docs lagged). Closed after a cross-model `/consult` (codex + agy both CLOSE). → [#73](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/73)
- **GH-66 · Session/transcript-log audit for stale-instruction drift** ✅ **SHIPPED 2026-07-01 (`ff2ea44`, marathon Wave 2 Lane B)** — `utils/transcript-audit.sh`: periodic, READ-ONLY audit over a transcript dir (default `AUDIT/relay-automation-transcripts/`). Emits a structured, greppable report of three drift classes — `stale-ref` (deprecated/`.bak`/missing repo paths), `repeat-explore` (same dir explored ≥ threshold across turns), `unbounded-stall` (transcript with no terminal exit/STATUS/DECISION marker). Findings are a report, not a gate; never writes/moves/deletes a transcript. `test/transcript-audit.sh` (7 checks incl. a read-only checksum/mtime guarantee) in `validate.sh`. Ran clean against the real 37-file corpus (7 unbounded-stalls). Non-goals honored: not a transcript store (GH-30), no auto-apply. → [GH-66-SESSION-LOG-AUDIT.md](PROJECT/1-INBOX/GH-66-SESSION-LOG-AUDIT.md) · [#66](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/66)
- **GH-64 · Security-scanning guardrail in the review chain** ✅ **Tool + test SHIPPED 2026-07-01 (`a0cc84e`, marathon Wave 2 Lane A); ACTIVE BLOCKING GATE wired 2026-07-01** — `relay-automation/hooks/security-scan.sh`: self-contained, no-network static scanner for shell scripts; fail-loud (`SECURITY: <file>:<line> [rule]` to stderr, non-zero exit), never auto-fixes. Rules: eval-of-variable, `curl`/`wget`-piped-to-shell, AWS keys, PEM private-key headers, GitHub PATs, Slack tokens, literal credential assignments. **Active-gate follow-on (this session):** a hand-maintained, checked-in baseline (`relay-automation/hooks/security-scan-baseline.txt`, exact `file\trule\tline-text` triples, matched by content not line number) suppresses the known-legitimate findings (reviewed dispatcher `eval`s in `poll.sh`/`relay-drive.sh`/`marathon-drive.sh`/`improve-loop.sh`, doc-comment prose, the scanner's own test fixtures) WITHOUT hiding them — every baselined finding still prints (labeled `SECURITY (baselined)`), per GUIDING-PRINCIPLES.md #8; a genuinely new finding still fails loud. New `--no-baseline` (raw scan) and `--tsv` (machine-parseable rows for authoring baseline entries) flags. `test/security-scan.sh` now 28 checks including a real-repo-against-the-checked-in-baseline assertion — that assertion IS the blocking gate, already wired into `validate.sh` (no separate CI step needed). Confirmed NO real secret leak in any of the 45 baselined findings. → [GH-64-SECURITY-SCANNING-GUARDRAIL.md](PROJECT/1-INBOX/GH-64-SECURITY-SCANNING-GUARDRAIL.md) · [#64](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/64)
- **GH-67 · tick-release missing from relay-turn-lib.sh (both shims)** ✅ **SHIPPED 2026-07-01 (`102cc74`)** — `rtl_enforce` now authoritatively hands off / closes the RELAY-TURN token after its file-scoped commit: relay `STATUS` Approved|Closed → `tick done`, else `tick release --to <RELAY_PEER>`. Fixes the deadlock where both worker shims left the token `open` with no handoff (codex stalls AND agy Approved turns), recoverable only by manual `tick log task.done`. Idempotent + ownership-guarded (a mis-fire is a WARN, never a turn failure); contained to the shared kernel. Regression: `test/relay-turn-handoff.sh` (both branches); `validate.sh` exit 0. → [#67](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/67)
- **GH-68 · Cross-agent dependency conflict detection** ✅ **Phase 1 SHIPPED 2026-07-01 (`94558c5`)** — warn-only, inject-only `dependency.drift` signal per [decision](decisions/2026-07-01-cross-agent-dep-conflict.md): new event verb (`src/events.js` + `tick drift`, best-effort so it can't fail a turn); post-commit detection in `relay-turn-lib.sh` when a landed turn changes the containment kernel / `src/project.js` / `src/events.js`; `rtl_drift_brief` (per-agent `.tick` watermark, capped) read by both shims into the next turn brief; `src/project.js` skips the verb in the fold (no phantom task). **Costly** kernel schema extension. Regression: `test/relay-dep-drift.sh` (12 checks); `validate.sh` 72/72 exit 0. **Phase 2** (blocking gate) is an explicit deferred follow-on — only if field data shows the warn-only posture misses a real conflict. → [GH-68-CROSS-AGENT-DEP-CONFLICT.md](PROJECT/1-INBOX/GH-68-CROSS-AGENT-DEP-CONFLICT.md) · [#68](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/68)
- **GH-51 · marathon/swarm-preflight harness defects (GH-37 dogfood)** ✅ **CLOSED 2026-06-30** — root cause (#1 `--target-root .` off-lane false-positive) + budget-scaling (#4) shipped (`19b8736`, `validate.sh` 69/69); cross-repo dogfood unblocked. 3 minor remainders split out: **#54** (brief: forbid in-turn fs-touching tests), **#55** (swarm-preflight: auto-include a changed artifact's tests), **#56** (marathon-drive: reconcile/fresh-id a leaked tick token). → [#51](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/51)
- **Part C · autonomous self-improvement loop (#50)** ✅ **MERGED to `main` 2026-06-30 (PR #53)** — the LOOPS.md capstone: a champion/challenger hill-climb that measurably improves an artifact against a scalar, behind the GH-40 oracle, until a stop condition fires. 6 tested primitives (`measure`/`loop-stop`/`oracle-guard`/`champion`/`heldout-check`/`loop-cost`) composed by `improve-loop.sh`; QA-checklist gates + a real-target dogfood (shrank a real file **6→2 lines** under a real oracle, plateau-halted, no-regress); additive (zero kernel change). Codex code-review (3 Blockers fixed). Merged + full suite **69/69**. The bet ([decision](decisions/2026-06-29-self-improvement-loop.md), Costly): 5 invariants make unattended optimization safe. **Live-agent run PROVEN 2026-06-30** (operator GO): `improve-loop` drove a live codex+agy `marathon-drive` build — oracle held, champion accepted (4→3), halted; all invariants fired live. Surfaced harness defects #58/#59 (neither a loop bug). → [AUTONOMOUS-SELF-IMPROVEMENT-LOOP.md](PROJECT/3-COMPLETED/AUTONOMOUS-SELF-IMPROVEMENT-LOOP.md) · [#50](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/50)
- **GH-40 · double-blind Reviewer canary spike (Part C prerequisite)** ✅ **Phases 1+2 complete (4 canaries pass double-blind) + Phase 3 scaffolding shipped 2026-06-29 (PR #47)** — gating prerequisite for the deferred Part C self-improvement loop. **Phase 1 (Gamma):** [gamma-poison](test/fixtures/gamma-poison/) — one-char `src/paths.js` "cleanup" silently drops a test; blind Reviewer **REJECT**ed. **Phase 2 (3/3):** [token-reuse](test/fixtures/canary-token-reuse/) (surfaced **#41**), [peer-orphan](test/fixtures/canary-peer-orphan/), [reviewer-overstep](test/fixtures/canary-reviewer-overstep/) — derived from real artifacts, never hand-authored. **Phase 3 scaffolding (no loop):** `relay-automation/proposals-sink.sh` (formats Reviewer findings into a sign-off checklist; **refuses to write any rule/operator doc**) + `test/phase3-signoff-guard.sh` (17 assertions); 3 Codex code-QA rounds. Surfaced #41 + #44. → [GH-40-DOUBLE-BLIND-REVIEWER.md](PROJECT/3-COMPLETED/GH-40-DOUBLE-BLIND-REVIEWER.md) · [#40](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/40)
- **GH-49 · optional vendored local copy of harness scripts (WIP-decoupled fallback)** ✅ 2026-06-30 (#49) — opt-in vendored mode: `relay-automation/xyz-vendor.sh` snapshots the curated harness (16-file `relay-pkg` manifest awk-parsed from `make-pkg.sh` + `bin/tick` + `src/*.js`) into a **git-ignored `.xyz/`** in a foreign repo, pinned + version-stamped, registered in the GH-62 registry. `find-harness.sh` prefers `.xyz/` (env → `.xyz/` → current-repo → script-relative; default path byte-identical) with a **warn-loudly-continue** staleness banner (never blocks). `xyz-sync.sh` list/update/delete (dry-run unless `--yes`; closes the GH-62 follow-on). SessionStart reminder hook. **Built swarm-produced** (codex authored Phases 1/2+3/4, agy reviewed → Approved) with every edit **independently execution-verified** (GUIDING #12); `test/xyz-vendor.sh` 28 assertions in `validate.sh` (**70/70**). **Level-2 dogfood self-hosted**: the vendored `.xyz/` harness drove agy to review a foreign-repo artifact and commit file-scoped with no live clone — surfacing + fixing **2 containment-kernel gaps** (driver-lock assumed `.git/`; GH-51 collapse rooted at the `.xyz/` subdir not the toplevel; both byte-identical for a normal clone). → [decision](decisions/2026-06-30-vendored-harness-locator.md) · [GH-49-VENDORED-LOCAL-COPY.md](PROJECT/3-COMPLETED/GH-49-VENDORED-LOCAL-COPY.md) · relays [p1](relay-system/2026-06-30/gh49-phase1-vendor.md)/[p2+3](relay-system/2026-06-30/gh49-phase23-locator.md)/[p4](relay-system/2026-06-30/gh49-phase4-xyzsync.md) · [#49](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/49)
- **GH-62 · XYZ install registry (call-home to remember install locations)** ✅ 2026-06-30 (#62) — borrowed PDDA's install→call-home pattern: new `install.sh` materializes the runtime (`bin/tick`+`src/*.js`) into a target dir, then registers the install in a machine-local `~/.config/xyz/registry.tsv` (key=`install_dir`, latest-wins; cols utc/`tick_version`/`source_commit`/`coordinated_repo`; best-effort git-pulse projection; never committed). Same fail-open register step mirrored into the `/xyz` SKILL self-extract (lockstep). Surfaces the `bin/tick`(0.2.0)↔SKILL-embed(0.1.0) drift per-install. **Independently QA'd via automated agy `/relay-xyz` (2 rounds → Approved)** — 3 hardening fixes folded in (`cd "-"` sentinel guard, registry-init fail-open, tmp-leak cleanup). `./validate.sh` 69/69. Follow-ons (out of scope): `xyz-sync` push tool + the embed-drift fix. → [GH-62-XYZ-INSTALL-REGISTRY.md](PROJECT/3-COMPLETED/GH-62-XYZ-INSTALL-REGISTRY.md) · relay [gh62-install-registry-qa.md](relay-system/2026-06-30/gh62-install-registry-qa.md) · [#62](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/62)
- **GH-52 · Part B / R2 — auto-reap authority** ✅ 2026-06-30 (#52 closed) — flipped `watchdog.sh --allow-reap` from a print-only stub to a **real, scoped, idempotent, exactly-once** `tick reap <agent> --task <task>` of each confirmed parked suspect (re-offers the orphaned token; never reaps a live claim; epoch-safe via Phase-1 fencing; best-effort with the escalation as the guarantee). Authority = the `--allow-reap` grant; evidence = `parked_suspects[]` membership (gap past threshold). Closes the recovery half of G1. Decision record [decisions/2026-06-30-auto-reap-authority.md](decisions/2026-06-30-auto-reap-authority.md). Tests: `chaos-midturn-kill` 15/15 (real reap → release → peer reclaims → idempotent 2nd pass → live claim untouched), `watchdog-liveness` 7/7, `watchdog-relay` 6/6; merged, suite **69/69**. → [GH-52-AUTO-REAP-AUTHORITY.md](PROJECT/3-COMPLETED/GH-52-AUTO-REAP-AUTHORITY.md) · [#52](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/52)
- **GH-11 · relay-xyz cross-repo targeting** ✅ 2026-06-30 (#11 closed) — Ask 1 (the `--target-root` flag + `relay-turn-lib.sh` kernel wiring routing worktree/allowlist/commit via `RELAY_TARGET_ROOT`, `test/relay-target-root.sh` 7/7) shipped 2026-06-21; **Asks 2–5 swept 2026-06-30** into `skills/relay-xyz/SKILL.md` (a cross-repo subsection documenting `--target-root` + `CONSULT_ROOT` with foreign-repo examples, the `$TMPDIR` absolute-path warning, and the find-harness-solves-the-inverse note). `skill-extract` + `path-integrity` green. → [GH-11-CROSS-REPO-TARGETING.md](PROJECT/3-COMPLETED/GH-11-CROSS-REPO-TARGETING.md) · [#11](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/11)
- **Tooling · relay-xyz durability (discovery shakedown)** ✅ 2026-06-30 — discovery audit via the shakedown lens (locator proven green; symlink-only discovery → `skills/relay-xyz/install.sh`) + drive-layer fixes from a sibling headless run (space-safe `--agent-cmd` dispatch; worktree isolation default-ON for driven runs); dangling `consult`/`wpcc` symlinks repaired. Shipped + regression-tested 2026-06-21. **Optional follow-ups deferred (non-blocking):** role-vs-model assertion + per-run `RELAY-TURN` id — open a fresh issue if pursued. → [RELAY-XYZ-DISCOVERY-SHAKEDOWN.md](PROJECT/3-COMPLETED/RELAY-XYZ-DISCOVERY-SHAKEDOWN.md)
- **GH-19 · relay-xyz skill-skip durability** ✅ 2026-06-30 (#19 closed) — deterministic `PreToolUse` guard ([relay-automation/hooks/relay-xyz-guard.sh](relay-automation/hooks/relay-xyz-guard.sh), wired in `.claude/settings.json`) blocks executing a `relay-automation/` driver before the relay-xyz skill is loaded; proof-of-load from a `Skill` invocation or `find-harness.sh`; session-scoped, fail-open, high-precision (reads / `test/*.sh` / unrelated commands pass). ROUTER rail + `test/relay-xyz-skill-guard.sh` 11/11 (in `validate.sh`). **Organic catch operator-verified 2026-06-30:** a fresh session that skipped the skill was blocked driving `relay-drive.sh`, self-corrected by loading relay-xyz, and retried — the exact block→load→retry flow. → [GH-19-RELAY-XYZ-SKILL-GUARD.md](PROJECT/3-COMPLETED/GH-19-RELAY-XYZ-SKILL-GUARD.md) · [#19](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/19)
- **GH-24 · relay telemetry extractor** ✅ 2026-06-30 (#24 closed) — on-demand ETL `utils/telemetry/extract-relay-telemetry.sh` reads `relay-system/<date>/*.md` for a `--from`/`--to` range and emits `relay-system/combined/aggregated-FROM-to-TO.json` in the focus5float stoplight schema `{ health, title, description, updatedAt }` (health from STATUS:/VERDICT:). **Validated 2026-06-30** against live `relay-system/` (06-27→06-30): 14 records, valid JSON, exact schema, histogram 8 green / 4 orange / 2 red, mapping correct. Output-only by design (no relay mutation, no `validate.sh` integration). → [GH-24-RELAY-TELEMETRY-EXTRACTOR.md](PROJECT/3-COMPLETED/GH-24-RELAY-TELEMETRY-EXTRACTOR.md) · [#24](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/24)
- **Tooling · marathon planner (pre-pre-flight)** ✅ 2026-06-30 — `utils/marathon-plan.sh` (formerly `queue-plan.sh`) reviews this ledger, validates each item (already-closed / already-landed / undocumented-partial / drift / unrated — deterministic, flag-for-human), ranks survivors by the PDDA `complexity`/`risk`/`effort` ratings, and writes a sequenced collision-aware `MARATHON-PLAN-YYYY-MM-DD.md` (the stage before `swarm-preflight`). Ratings contract codified in `PROJECT/PDDA.md` (integer `1`–`5`), validated by `utils/pdda/pdda.sh frontmatter`. `test/marathon-plan.sh` 31/31; in `validate.sh`. **The final gating step — confirm the provisional ratings backfill + refresh the overlay — completed 2026-06-30** (all 7 provisional ratings resolved → 0 remaining; `MARATHON-PLAN-2026-06-30.md` regenerated, Wave 1 = #52). **Renamed `queue-plan`→`marathon-plan` 2026-06-30** for label clarity (output `QUEUE-*.md`→`MARATHON-PLAN-*.md`). → [MARATHON-PLANNER.md](PROJECT/3-COMPLETED/MARATHON-PLANNER.md)
- **Tooling · relay containment-guard hardening (#13/#14)** ✅ 2026-06-29 (#13 + #14 closed) — hardened `relay-turn-lib.sh` so a headless turn can't destroy work: the commit-bypass guard must not orphan a concurrent peer commit (#13) and the turn agent must not self-commit mid-turn (#14). **Root cause:** the GH-13 "preserve a moved ROOT HEAD" branch was dead code in the shims — `RTL_WT_USED` was lost in `rtl_worktree_begin`'s `wt="$(…)"` subshell, so every worktree turn with a moved ROOT HEAD reset+exit-6'd and discarded the build (the 2× codex Phase-4 marathon failures). Fixed by persisting `RTL_WT_USED` in `rtl_worktree_end` (`c992e68` + `939b871`); shim-level regression added. **Confirmed end-to-end by TWO marathons — GH-46 Phase-4 AND GH-37** (this session): worktree turns with concurrent ROOT commits ran with no reset+fail; `validate.sh` 60/60. Optional prevent-level `git` PATH-shadow deferred (non-blocking defense-in-depth). → [RELAY-CONTAINMENT-HARDENING.md](PROJECT/3-COMPLETED/RELAY-CONTAINMENT-HARDENING.md)
- **GH-37 · agy consult lane hangs on expired auth** ✅ 2026-06-29 (#37 closed) — **shipped via marathon dogfood** (Codex builder + agy reviewer → Approved, `validate.sh` 60/60). A fast `agy whoami` auth pre-flight now lives in both `agy-turn.sh` and `consult.sh`: an expired/failed session exits in seconds (shim exit 5; consult degrades to Codex-only) naming the `agy login` remedy, instead of the 300s interactive-prompt hang. New `agy-turn` auth-fail test (exit 5, no commit) added; all exit-6 containment assertions preserved. **The run surfaced 5 harness defects** (the build was correct on turn 1; every blocker was a swarm-preflight/marathon gap — timeout sizing, artifact-set scope, leaked-token reuse, self-verify-trips-containment, and the `--target-root .` relay-file off-lane false-positive); filed separately. → [GH-37-AGY-CONSULT-AUTH-HANG.md](PROJECT/3-COMPLETED/GH-37-AGY-CONSULT-AUTH-HANG.md) · [#37](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/37)
- **GH-33 · leverage the built-in /loop skill to drive relays** ✅ 2026-06-29 (#33 closed) — **Phases 0–4 shipped.** Phase 0 chose a **thin wrapper** (`relay-loop.sh`) over a `relay-drive.sh` mode. Phase 1 `poll.sh --emit-delay` → deadline-clamped `DELAY:` (additive, default-off). Phase 2 `relay-loop.sh` adaptive cadence (`NEXT-POLL:` for `/loop` dynamic tick; `--sleep-loop` pure-bash self-pace) + dynamic-mode recipe in the `relay-xyz` SKILL, reschedule kept pluggable. Phase 3 `relay-loop.sh --background` (detached dispatch + pidfile single-turn lock; `poll.sh` left a pure oracle). **Phase 4 (the marquee — Path A/B cross-model unification) landed via the GH-46 marathon dogfood** (`--cross-model-cmd` launches the shim on `nudge-cross-model`, else degrades to nudge); `test/relay-loop.sh` 15/15, `validate.sh` green. Distinct from #31/#32. **Phase 5 (let `/loop` own lifecycle) deferred as optional polish; Phase 6 docs done.** → [GH-33-LOOP-SKILL-INTEGRATION.md](PROJECT/3-COMPLETED/GH-33-LOOP-SKILL-INTEGRATION.md) · [#33](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/33)
- **GH-46 · Phase 4 swarm-preflight contract + cross-model hands-free marathon dogfood** ✅ 2026-06-29 (#46 closed) — split out of #33 so the *runnable* Phase 4 contract got its own issue + attention. Carried the **authoritative** Swarm Preflight Contract (unify Path A/B: `relay-loop.sh --background` launches the cross-model shim on `nudge-cross-model` via `--cross-model-cmd`; degrades to a human nudge when the CLI is absent; containment byte-identical to Path A). **SHIPPED 2026-06-29 via the marathon dogfood** (codex builder + agy reviewer): after 2 failures from #14 (codex self-commit → reset), the #14 fix landed and the **3rd run succeeded — codex built Phase 4, agy approved** — the **end-to-end #14 confirmation** that the codex builder lane no longer resets+fails. (The marathon's pre-advance gate tripped on a non-hermetic `driver-lock.sh` test inheriting `RELAY_DRIVER_LOCKED` — a test-env bug, fixed same day, `validate.sh` 56/56.) → [GH-46-PHASE4-SWARM-PREFLIGHT.md](PROJECT/3-COMPLETED/GH-46-PHASE4-SWARM-PREFLIGHT.md) · [#46](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/46)
- **GH-42 · concurrent relay/marathon on one clone resets ROOT HEAD** ✅ 2026-06-28 (#42 closed) — fixed by adding a repo-level directory lock (`.git/relay-driver.lock`) inside `marathon-drive.sh` and `relay-drive.sh`. **Follow-up gaps found 2026-06-29: ✅ stale-lock self-heal FIXED** — the lock now records the holder PID and reclaims it when the holder is dead (`kill -0`), so a crashed/killed driver no longer blocks every later run; a live holder still refuses (`test/driver-lock.sh` 4/4). (Residual, lower-priority: the lock guards driver-vs-driver only — a *manual* concurrent committer can still move HEAD mid-run; the #13/#14 worktree-preserve fix now contains that case for worktree turns.) → [GH-42-CONCURRENT-RELAY-ROOT-HEAD.md](PROJECT/3-COMPLETED/GH-42-CONCURRENT-RELAY-ROOT-HEAD.md) · [#42](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/42)
- **GH-43 · marathon headless-build robustness (timeout/leaked-token/self-gate)** ✅ 2026-06-28 (#43 closed) — fixed gap 2 (leaked tick claims) by extending `tick reap` to accept a `--task <id>` filter and automatically invoking it in `marathon-drive.sh` before token seeding. This ensures any stalled runs of the same phase don't permanently exhaust the agent's claim capacity. → [GH-43-MARATHON-ROBUSTNESS.md](PROJECT/3-COMPLETED/GH-43-MARATHON-ROBUSTNESS.md) · [#43](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/43)
- **GH-39 · swarm preflight gaps (validation + scope-locked brief)** ✅ 2026-06-28 (#39 closed) — agy-authored gaps expanded from the GH-36 dogfood. **Slice 1 (validation, agy VERDICT PASS):** A2 artifact-paths-exist, A1 gate-resolves (flag-aware), A3 lane-CLI advisory. **Slice 2 (B6 scope-locked brief):** `swarm-preflight` packet now inlines the acceptance checklist + an edit-only/no-gate/no-wander scope-lock + a size-based `RELAY_TURN_TIMEOUT_S` rec (#43-1); `rtl_turn_prompt` forbids self-running the full gate (#43-3). `swarm-preflight` 31/31, `codex-turn` 30/30, `validate.sh` 55/55. A4 dropped as superseded by PR #38; A5/B7/#43-2 parked. → [GH-39-SWARM-PREFLIGHT-GAPS.md](PROJECT/3-COMPLETED/GH-39-SWARM-PREFLIGHT-GAPS.md) · [#39](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/39)
- **GH-27 · ROADMAP dashboard renderer** ✅ 2026-06-28 (#27 closed) — dependency-free `utils/roadmap-dashboard.sh` reads the canonical `ROADMAP.md` and writes one static [ROADMAP-DASHBOARD.md](ROADMAP-DASHBOARD.md) + a deterministic `--check` refresh test (`validate.sh` `roadmap-dashboard` 4/4). Renderer shipped + in the suite; auto-wiring into the doc-update flow is an optional follow-up. → [GH-27-ROADMAP-DASHBOARD.md](PROJECT/3-COMPLETED/GH-27-ROADMAP-DASHBOARD.md) · brief: [gh-27-roadmap-dashboard-brief.md](PROJECT/3-COMPLETED/gh-27-roadmap-dashboard-brief.md) · [#27](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/27)
- **GH-16 · same-device cross-repo swarm readiness (umbrella)** ✅ 2026-06-28 (#16 closed) — the epic to drive a multi-lane swarm against an external target repo on macOS without the harness reverting its own output. Its core gaps shipped via the children (GH-11 `--target-root`, GH-18 friction, GH-22 isolation, GH-29 new-file commit, #17 case-sensitivity); residual child issues remain tracked individually (#13/#14 closed 2026-06-29; #11 open — doc-only Asks 2–5; #3, #4, #5 open). Doc reconciled to `3-COMPLETED` 2026-06-29. → [GH-16-CROSS-REPO-SWARM.md](PROJECT/3-COMPLETED/GH-16-CROSS-REPO-SWARM.md) · [#16](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/16)
- **GH-36 · headless Codex isolated-turn friction (`.tick` sandbox)** ✅ 2026-06-28 (#36 closed) — **first real marathon dogfood since GH-29**. Under worktree isolation Codex's default `-s workspace-write` can't write the shared `.tick/` lock (outside its sandbox) → `tick claim` EPERM → deadlock. Fix: pass `--add-dir "$ROOT/.tick"` to codex exec when isolation is on (default-off unchanged). Built by the Codex builder via `swarm-preflight → marathon-drive`: **v1 reproduced the bug live + containment held** (off-lane edits reverted, escalated, tree clean); **v2 (bypass flag + scope-locked brief) built the fix**; **agy cross-model review → Approved** (4× [Pass]). Regression: `test/codex-turn.sh` +2 (29/29); `validate.sh` 54/54. Dogfood findings → candidate **GH-38** (preflight brief should inline acceptance criteria). → [GH-36-HEADLESS-CODEX-TICK-SANDBOX.md](PROJECT/3-COMPLETED/GH-36-HEADLESS-CODEX-TICK-SANDBOX.md) · [#36](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/36)
- **GH-29 · cross-repo (`--target-root`) build doesn't commit NEW untracked files** ✅ 2026-06-27 (#29 closed) — the first-wave dogfood blocker. A builder turn that *creates* files in a foreign target reported `no tracked changes` and committed nothing (even modified tracked files dropped), because the file-scoped commit batched `git add -- "${RTL_ALLOW[@]}"` — and `git add` aborts the **whole** batch on the first non-matching pathspec (an allowlist entry the turn was permitted to create but didn't). Fix: stage each allowlisted path independently (`git add -A -- <path>` per path, `|| true`). Regression test [relay-target-root-newfile.sh](test/relay-target-root-newfile.sh) (6 assertions) reproduces the batch-abort + asserts new+modified files commit cross-repo; `validate.sh` **54/54**. GH-22 sibling (same-repo copyback). → [GH-29-CROSS-REPO-NEWFILE-COMMIT.md](PROJECT/3-COMPLETED/GH-29-CROSS-REPO-NEWFILE-COMMIT.md) · [#29](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/29)
- **Part A · Phase 6 — real-substrate dogfood (graduation test)** ✅ **FIRED + GRADUATED 2026-06-26**: the marathon ran end-to-end for the first time. **WPCC `ts-type-suppression`** built by codex, reviewed by agy → **Approved**, gate passed, scanner detects **3/3** suppression directives on the fixture; committed to WP-Code-Check `marathon-dogfood/ts-type-suppression` (`3e22f97`, first slice of WPCC #129). Run history: v1 escalated on an off-allowlist `PATTERN-LIBRARY.md` (containment held), v2/v3 surfaced the cross-repo new-file commit gap (**GH-29**, still open) → recovered by hand-commit. Pipeline `swarm-preflight → marathon-drive → codex build → gate → agy review` proven; GH-22 fix validated live (agy reviewed under isolation). → [MARATHON-DOGFOOD-2026-06-25-WPCC-TS-TYPE-SUPPRESSION.md](PROJECT/4-MISC/MARATHON-DOGFOOD-2026-06-25-WPCC-TS-TYPE-SUPPRESSION.md) · brief: [wpcc-ts-type-suppression-brief.md](PROJECT/4-MISC/wpcc-ts-type-suppression-brief.md) · transcript: [marathon-wpcc-095945.md](relay-system/2026-06-26/marathon-wpcc-095945.md) · retired substrates: [Sleuth](PROJECT/4-MISC/MARATHON-DOGFOOD-2026-06-24-SLEUTH-NEARMISS-2LITE.md) ([brief](PROJECT/4-MISC/sleuth-near-miss-2lite-brief.md)) · [WPCC-old](PROJECT/4-MISC/MARATHON-DOGFOOD-2026-06-18-WPCC-PHASE2.md)
- **GH-31 · cross-repo external-artifact review flow** ✅ 2026-06-27 (#31 closed, merged #34) — first-class `--artifact-file` path (copy the artifact into the worktree, expose it read-only to the reviewer, closing #15) so reviewing another repo's PR/diff doesn't require embedding a large diff into the relay md; plus a fence-safe embed fallback and a `relay-automation/new-relay.sh` scaffolder. → [GH-31-CROSS-REPO-ARTIFACT-REVIEW.md](PROJECT/3-COMPLETED/GH-31-CROSS-REPO-ARTIFACT-REVIEW.md) · [#31](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/31)
- **GH-32 · single-turn review ergonomics** ✅ 2026-06-27 (#32 closed) — warn when an **untracked** relay file is invisible to an isolated worktree; `--review-once` gives a completed non-approval turn a **distinct exit 5** (separate from the genuine-stall exit 3 and the Escalated exit 4); documented the `tick info` token-inspect verb. Sibling of #22. → [GH-32-SINGLE-TURN-REVIEW-ERGONOMICS.md](PROJECT/3-COMPLETED/GH-32-SINGLE-TURN-REVIEW-ERGONOMICS.md) · [#32](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/32)
- **GH-25 · swarm preflight planner** ✅ 2026-06-26 (#25 closed) — `utils/swarm-preflight.sh`: one durable intake/preflight entrypoint that turns either a `PROJECT/2-WORKING` plan doc or a bundle of GitHub issues into a marathon-ready run packet (deterministic candidate/freshness checks, "fix still required" validation, remediation-readiness gate, Codex-vs-agy lane assignment), reusing the existing marathon/relay runtime instead of a second control plane. agy relay review passed → merged. → [GH-25-SWARM-PREFLIGHT-PLANNER.md](PROJECT/3-COMPLETED/GH-25-SWARM-PREFLIGHT-PLANNER.md) · [#25](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/25)
- **GH-22 · agy worktree isolation silent data loss** ✅ 2026-06-26 (#22 closed) — fixed the exit-0 silent data loss where, under `RELAY_WORKTREE_ISOLATION=1`, agy edited the relay file via the absolute ROOT path but `rtl_worktree_end` copied the stale empty worktree copy back over ROOT. Relay file path is now CWD-relative so agy writes into the worktree; fix validated live during the Phase 6 dogfood (agy reviewed under isolation). → [GH-22-AGY-WORKTREE-ISOLATION-DATA-LOSS.md](PROJECT/1-INBOX/GH-22-AGY-WORKTREE-ISOLATION-DATA-LOSS.md) · [#22](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/22)
- **GH-20 · agy first-class footing in live relay docs** ✅ 2026-06-25 — doc-only parity pass shipped (`4ff8e4c`): the live relay entry points (root [README.md](README.md), [relay-automation/README.md](relay-automation/README.md), both relay skills, harness-locator labels, front-door dashboard, automated-relay hub) now present **Codex and agy as co-equal Path-A workers** wherever the runtime supports both, while keeping the real auth/sandbox/cost-blindness asymmetries explicit. `test/codex-turn.sh` 27/27, `test/agy-turn.sh` 22/22, `validate.sh` 45/45. Issue [#20](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/20) closed; doc archived. → [GH-20-AGY-FIRST-CLASS-FOOTING.md](PROJECT/3-COMPLETED/GH-20-AGY-FIRST-CLASS-FOOTING.md)
- **GH-18 · cross-repo driven-relay friction** ✅ 2026-06-24, agy-approved — field-validated punch-list from a real cross-repo Codex review (thread/artifact in `rebalance-OS`, harness here via `--target-root`). Phase 0 verification reproduced #1/#2/#5 as real code bugs; #3 (foreign `.tick`) found **largely stale** for driven runs (mitigated by [codex-turn.sh:57](relay-automation/codex-turn.sh#L57)) → doc-only; #4 doc-only. Phase 2 code (`7709abc`): **#2** repo-relative `--relay-file` resolved under `--target-root`, **#1b** token-collision hints in `bin/tick` + `relay-drive.sh` (default unchanged), **#5** `STATUS: Escalated` now terminal-by-design (exit 4, not the stall's exit 3) — 3 new tests, **`validate.sh` 41→44/44**; agy headless review **Approved** (3×[Pass], confirmed #5 doesn't mask a true stall). Child of GH-16; issue #18 closed, doc archived. → [GH-18-CROSS-REPO-RELAY-FRICTION.md](PROJECT/3-COMPLETED/GH-18-CROSS-REPO-RELAY-FRICTION.md) · [#18](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/18)
- **GH-21 · relay quality gate — independent post-generation validator** ✅ 2026-06-25 — three phases shipped: (1) `bin/validate-relay-block` hooked into `bin/tick release`/`done` + `exit 8`; (2) `test/relay-self-sufficiency.sh` self-sufficiency test for the TAKE YOUR TURN block; (3) `--consult-verify` flag on `relay-drive.sh` wiring `consult.sh` (codex + gemini) as an independent post-turn challenger. All 46/46 validate.sh. Process finding: agy cannot complete code-write relay turns in headless mode (see GH-22 backlog candidate). → [GH-21-RELAY-QUALITY-GATE.md](PROJECT/3-COMPLETED/GH-21-RELAY-QUALITY-GATE.md) · [#21](https://github.com/Claude-AI-Tools-Ventura-County/xyz-3-agents-swarm/issues/21)
- **Part A · Phase 1 — Cost observability foundation** ✅ 2026-06-16 — deterministic token / wall-clock / human-minute capture in `tick analyze`. → [COST-OBSERVABILITY-PLAN.md](PROJECT/4-MISC/COST-OBSERVABILITY-PLAN.md)
- **Part A · Phases 2–4 — Marathon harness build** ✅ 2026-06-17/18 — dispatcher + headless single-phase loop + autonomous-builder containment + multi-phase `MARATHON.yaml` chaining (M6/M7 deferred). → [MARATHON-HARNESS.md](PROJECT/3-COMPLETED/MARATHON-HARNESS.md)
- **Part A · Phase 5 — Cross-system cost comparison** ✅ 2026-06-16 — xyz vs relay, every cell from `tick analyze --format json`. → [COST-COMPARISON.md](PROJECT/4-MISC/COST-COMPARISON.md)
- **Part B · Phase 1 — Epoch fencing & stale-writer prevention** ✅ 2026-06-18 — monotonic per-task epoch fences zombie writers in the projection kernel. → [ADVERSARIAL-HARDENING.md](PROJECT/2-WORKING/ADVERSARIAL-HARDENING.md#phase-1--epoch-fencing--stale-writer-prevention-r1--g3) · [decision record](decisions/2026-06-18-epoch-fencing.md)
- **Tooling · Automated /relay loop** ✅ 2026-06-15 — Producer↔Reviewer relay that runs hands-free (all-Claude) or one-line-nudge (cross-model), self-heals on stalls (watchdog), and terminates on `Approved`; shipped + packaged as a sibling skill (Phases 1–5, `validate.sh` 20/20). Kept in `2-WORKING` as a completion hub. → [AUTOMATED-RELAY.md](PROJECT/4-MISC/AUTOMATED-RELAY.md)
- **Tooling · relay-xyz install hygiene** ✅ 2026-06-22 — both dangling user-skill symlinks repaired (operator-signed-off): `consult` → `skills/consult` (was the singular-`skill/` typo) and `wpcc` → `…/wp-code-check/skills/wpcc` (the clone is now present at that path; symlink target was already correct). Both resolve to their `SKILL.md` ✓. → [RELAY-XYZ-DISCOVERY-SHAKEDOWN.md](PROJECT/3-COMPLETED/RELAY-XYZ-DISCOVERY-SHAKEDOWN.md)

### Deferred · vision

- **Part A · Phase 4 — M6 / M7** 🔲 deferred — cross-phase context injection + state projection, until a phase genuinely needs them. → [MARATHON-HARNESS.md → Deferred](PROJECT/3-COMPLETED/MARATHON-HARNESS.md#deferred--m6--m7)

---

*Detail for every entry lives in its linked `PROJECT/**` doc. Part B gaps also map to `PROJECT/4-MISC/4X4.md`; any
event-schema change gets a decision record under `decisions/` before it lands.*
