# Parked from GH-299 Gen 4 build (2026-09-04)

- [Nit, agy QA relay] `test/gh155-phase3-repro-builder.sh:81` leaks the caller's env (e.g. `RELAY_FILE`) into the synthesized reproducer when run by an active relay agent, changing `deepseek-turn.py` behaviour. Pre-existing Gen 3 suite, out of GH-299 scope. Fix: `env -i` / explicit allowlist when the suite executes the emitted repro.
- `utils/ci-route.sh` error output is not usage-shaped → every fuzz mutant classifies as Tier-1 *anomaly*. Either add a `usage:` line to ci-route's unknown-verb path or teach `classify_stderr` its shape; decide after the soak triage.
- runtime:parity: 6/12 mutants diverge between `relay-automation/codex-turn.sh` default and `XYZ_PYTHON=0`. Needs its own issue after the soak quantifies it.
- `gen4_campaign` host-identity check flags the OPERATOR's own commits during a run as a host violation (observed when Phase 5 was committed mid-run). Correct but noisy; consider distinguishing "HEAD moved by a commit on the same branch" from "identity corrupted".
