# Parked 2026-09-25 — GH-831 session

- [ ] **`roadmap update --accepted-start` records a second `in_flight` transition.**
  - **Finding.** The documented two-step start writes two transitions: first `roadmap update --section "In
    progress"`, then `roadmap update --gid <rmi> --accepted-start`. The second is an `in_flight` work event
    with `"accepted_start": true` and `"transition": true`, a second in-flight transition, not an acceptance
    record on the first.
  - **Evidence.** `releases.sql` `work_events` for GH-831 (02:29:59Z and 02:30:00Z) and GH-822 (18:08:48Z
    and 18:08:58Z on 2026-09-25) each show two `in_flight` rows.
  - **Why.** CodeRabbit on PR #832 points to the duplicate guard in `utils/py/releases_app.py`: it checks
    `status_label == "in-progress"`, which the ordinary section move does not set.
  - **Outside scope** of GH-831: it is ledger application code, and PR #832 changes rules and skill text only.
  - **Next check.** Does any consumer double-count `in_flight` (the work-state projection, the labels
    connector, radar)? If yes, promote it issue-first. The fix would be in `releases_app.py`'s
    accepted-start guard, verified with the existing `gh549-work-events.sh`/`gh605-work-state.sh` suites (no
    new tests, GH-831).
