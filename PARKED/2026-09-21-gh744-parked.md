# Parked — GH-744 skills frequency-tier session (2026-09-21/22)

Dropped from the GH-744 task as out of scope; filed here so they stop resurfacing. None blocks PR #747.

1. **codex-cli 0.154.0-alpha.6.2 (ChatGPT.app-bundled binary) cannot run tool calls.** Every shell
   call fails with `failed to decode code-mode IPC frame: missing field code_mode_host_duration_ns`
   (the command actually executes; the result frame cannot be decoded). `features.code_mode_host=false`
   removes the tool entirely. Relay-xyz's default Codex reviewer is therefore unavailable on this
   machine until the CLI is updated or downgraded; agy took both GH-744 relays.
2. **`gh549-work-events.sh` (105/20) and `gh605-work-state.sh` (27/1) are red on this macOS host on
   pristine `origin/development` 9c384240** while hosted CI is green on the same SHA. gh549 reports
   `XYZ_WORK_CONNECTORS_REGISTRY is active — running non-vendored connector(s)`; gh605 fails
   `test_wal_header_without_sidecars_refuses_before_open_for_schema_seven_and_eight`. Environment, not
   the tree — but a local full gate cannot go 409/409 here until they are triaged.
3. **agy reviewer turns twice broke the relay thread structure** on an all-PASS review: round 1
   replaced the `## Log` heading with its block (shim exit 8, terminal reverted), round 2 folded a
   blank line above the `### System` block (relay-drive `review-body-rewritten`, exit 4). Both are
   the guard working; the cost was two extra rounds of a three-round cap for a review that passed
   on the first. A pre-turn whitespace normalisation of the thread, or a tolerant byte compare for
   blank-line-only diffs above the reviewer's block, would remove the false trip.
4. **`relay-drive --review-once` graded the round-1 plan review as a stall (exit 3)** although the
   block landed and was committed — the GH-720 heading-regex misgrade, already tracked.
5. **Standalone AgentChorus repo is not on this machine**, so `sync-to-standalone.sh --check` could
   not run; the manifest's 14 sources were verified file-by-file instead.
