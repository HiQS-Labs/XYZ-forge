# Parked — 2026-09-21 (GH-730 final QA)

- `ci-local.sh:425-427` comment says `--probe` does not exercise the suite; suite execution is gated only by `FAST` (`ci-local.sh:397-405`), `PROBE` only suppresses the evidence record (`ci-local.sh:435`). Pre-existing doc mismatch, Codex round-2 Nit. Reword to "probe runs are non-qualifying, not suite-free".
- Primary clone still carries `skills/skills-sync-trinity/scripts/__pycache__/` (zero tracked files) — harmless now; `rm -rf skills/skills-sync-trinity` when convenient.
